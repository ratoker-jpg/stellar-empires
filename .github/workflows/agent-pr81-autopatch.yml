name: Agent PR81 native Veyra autopatch

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch-and-validate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/pr81-native-veyra
          fetch-depth: 0
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Apply native Veyra patch
        run: python tools/agent_pr81_native_veyra.py
      - name: Install dependencies
        run: npm ci
      - name: Validate
        run: npm run check
      - name: Remove temporary automation
        run: rm tools/agent_pr81_native_veyra.py .github/workflows/agent-pr81-autopatch.yml
      - name: Commit implementation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: add native Veyra mechanical catalog"
          git push origin HEAD:agent/pr81-native-veyra
