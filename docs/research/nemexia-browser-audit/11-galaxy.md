# 11 — Galaxy

Historical Help describes a Universe containing Galaxies, Systems, Suns, Planet fields, and Team Planets. It gives historical topology caps/counts, but this audit deliberately stores them as Help-only taxonomy in `data/galaxy-entities.json`; it does not assert the current Horus universe follows those values.

`CONFIRMED_UI`: the main navigation links to Galaxy using the active coordinate’s galaxy and solar values. The second server opened a Galaxy page with editable galaxy/solar fields, Search, and Advanced Search. No entities were rendered in the captured system’s semantic view.

System types, entity cards, probes, attacks, and five requested system-state screenshots remain `UNKNOWN`. Safe continuation: record only already-visible systems; do not send fleets, probe, or attack.

`CONFIRMED_UI`: the Advanced Search landing page supports name search across Player, Planet, Alliance, and Team Planet, and exposes a second points/coordinates search mode. No search term was entered or submitted; result contracts and filters beyond these categories remain `UNKNOWN`.
