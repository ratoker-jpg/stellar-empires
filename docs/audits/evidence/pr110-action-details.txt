PR110 focused action files
index.html
src/assets/aegisVerticalSliceAssets.ts
src/assets/artTokens.ts
src/assets/completeMechanicalAssetManifest.ts
src/assets/factionAtlasSelection.ts
src/assets/factionMechanicalAssets.ts
src/assets/galaxyFleetRuntimeAssets.test.ts
src/assets/galaxyFleetRuntimeAssets.ts
src/assets/generated/runtimeAssetManifest.generated.ts
src/assets/generated/spaceMapAssetManifest.generated.ts
src/assets/planetIndustryRuntimeAssets.test.ts
src/assets/planetIndustryRuntimeAssets.ts
src/assets/runtimeMechanicalAssets.ts
src/game/scenes/BootScene.ts
src/game/scenes/SpaceMapScene.ts
src/main.ts
src/runtime/BotAutomationController.ts
src/simulation/bots/economyPlanner.ts
src/simulation/bots/fleetMissionPlanner.ts
src/simulation/bots/memory.ts
src/simulation/bots/perception.ts
src/simulation/bots/profiles.ts
src/simulation/bots/researchProductionPlanner.ts
src/simulation/bots/scheduler.ts
src/simulation/bots/threatRecoveryPlanner.ts
src/simulation/colonization/colonization.ts
src/simulation/combat/combatProfiles.ts
src/simulation/combat/debris.ts
src/simulation/combat/fleetDoctrine.ts
src/simulation/combat/resolveAttackMission.ts
src/simulation/combat/resolveBattle.ts
src/simulation/combat/shipAbilities.ts
src/simulation/combat/types.ts
src/simulation/command/commandDoctrine.ts
src/simulation/command/commanderShips.ts
src/simulation/command/types.ts
src/simulation/createInitialGameState.ts
src/simulation/defense/planetaryDefense.ts
src/simulation/executeGameCommand.ts
src/simulation/factions/completeCatalogTargets.ts
src/simulation/factions/factionMechanicalCatalogRegistry.ts
src/simulation/factions/factionMechanicalRoles.ts
src/simulation/factions/factionResearchEffects.ts
src/simulation/factions/mechanicalIds.ts
src/simulation/factions/synodMechanicalCatalog.ts
src/simulation/factions/veyraMechanicalCatalog.ts
src/simulation/fleets/fleetCalculations.ts
src/simulation/fleets/fleetCommands.ts
src/simulation/fleets/fleetDoctrineCommands.ts
src/simulation/fleets/flightCalculations.ts
src/simulation/fleets/flightCommands.ts
src/simulation/fleets/types.ts
src/simulation/galaxy/intelligenceView.ts
src/simulation/history/stateHistory.ts
src/simulation/intelligence/intelligenceState.ts
src/simulation/intelligence/resolveScout.ts
src/simulation/intelligence/types.ts
src/simulation/logistics/routes.ts
src/simulation/logistics/types.ts
src/simulation/market/market.ts
src/simulation/planet/aegisBuildingCatalog.ts
src/simulation/planet/buildingAliases.ts
src/simulation/planet/specialization.ts
src/simulation/planet/specializationCommands.ts
src/simulation/pve/expeditionFleetCommands.ts
src/simulation/pve/expeditions.ts
src/simulation/pve/neutralForces.ts
src/simulation/pve/pveBalance.ts
src/simulation/pve/spaceObjects.ts
src/simulation/reducer.ts
src/simulation/replay.ts
src/simulation/reports/missionReports.ts
src/simulation/research/aegisResearchCatalog.ts
src/simulation/research/completeResearchCatalog.ts
src/simulation/research/progression.ts
src/simulation/research/researchAliases.ts
src/simulation/research/researchCommands.ts
src/simulation/research/types.ts
src/simulation/types.ts
src/simulation/units/catalog.ts
src/simulation/units/completeCommanderShipCatalog.ts
src/simulation/units/completeShipCatalog.ts
src/simulation/units/productionCommands.ts
src/simulation/units/types.ts
src/simulation/upgrades/shipUpgrades.ts
src/simulation/validation/fullGameValidation.ts
src/storage/migrateGameState.ts
src/storage/migrateGameStateV13.ts
src/storage/migrateGameStateV14.ts
src/storage/migrateLegacySynodAliases.ts
src/storage/migrateLegacyVeyraAliases.ts
src/storage/saveFormat.ts
src/styles/accessibilityRuntime.css
src/styles/commandDoctrine.css
src/styles/commandRanking.css
src/styles/expeditions.css
src/styles/factionTheme.css
src/styles/factions.css
src/styles/fleetDoctrine.css
src/styles/galaxyIntel.css
src/styles/globalHud.css
src/styles/main.css
src/styles/missionReports.css
src/styles/missions.css
src/styles/planet.css
src/styles/planetZones.css
src/styles/spaceObjects.css
src/ui/accessibilityRuntime.ts
src/ui/commandDoctrineScreen.ts
src/ui/commandRanking.test.ts
src/ui/commandRanking.ts
src/ui/commandRankingScreen.ts
src/ui/empireOverview.ts
src/ui/empireOverviewViewModel.ts
src/ui/expeditionPanel.ts
src/ui/factionShellIdentity.test.ts
src/ui/factionShellIdentity.ts
src/ui/fleetComposerViewModel.ts
src/ui/fleetDoctrineScreen.ts
src/ui/fleetMissionEvents.test.ts
src/ui/fleetMissionEvents.ts
src/ui/galaxyIntelPanel.ts
src/ui/logisticsRoutesPanel.ts
src/ui/marketPanel.ts
src/ui/militaryZoneViewModel.ts
src/ui/missionReportsPanel.ts
src/ui/missionScreen.ts
src/ui/operationsWorkspace.test.ts
src/ui/operationsWorkspace.ts
src/ui/planetDevelopmentControls.ts
src/ui/planetScreen.ts
src/ui/productionScreen.ts
src/ui/researchScreen.ts
src/ui/shipUpgradesScreen.ts
src/ui/spaceObjectsPanel.ts
tests/assets/assetPipelineConfig.test.ts
tests/assets/completeMechanicalAssetManifest.test.ts
tests/assets/runtimeMechanicalAssets.test.ts
tests/fixtures/gameStateV13Fixture.ts
tests/simulation/botEconomyPlanner.test.ts
tests/simulation/botFleetMissionPlanner.test.ts
tests/simulation/botPerception.test.ts
tests/simulation/botResearchProductionPlanner.test.ts
tests/simulation/botScheduler.test.ts
tests/simulation/botThreatRecoveryPlanner.test.ts
tests/simulation/buildingCatalogValidation.test.ts
tests/simulation/buildingQueue.test.ts
tests/simulation/colonization.test.ts
tests/simulation/combat.test.ts
tests/simulation/commandDoctrine.test.ts
tests/simulation/completeBuildingCatalog.test.ts
tests/simulation/completeCommanderShipCatalog.test.ts
tests/simulation/completeShipCatalog.test.ts
tests/simulation/debris.test.ts
tests/simulation/economy.test.ts
tests/simulation/expeditions.test.ts
tests/simulation/factionMechanicalCatalogRegistry.test.ts
tests/simulation/fleetDoctrine.test.ts
tests/simulation/flightLifecycle.test.ts
tests/simulation/galaxyIntelligenceView.test.ts
tests/simulation/intelligence.test.ts
tests/simulation/logisticsRoutes.test.ts
tests/simulation/market.test.ts
tests/simulation/neutralPirateBases.test.ts
tests/simulation/planetSpecialization.test.ts
tests/simulation/planetaryDefense.test.ts
tests/simulation/pveBalanceAndReports.test.ts
tests/simulation/reducer.test.ts
tests/simulation/researchCatalog.test.ts
tests/simulation/researchQueue.test.ts
tests/simulation/shipUpgrades.test.ts
tests/simulation/spaceObjects.test.ts
tests/simulation/stateHistoryRetention.test.ts
tests/simulation/synodNativeCatalog.test.ts
tests/simulation/unitCatalog.test.ts
tests/simulation/unitProduction.test.ts
tests/simulation/veyraNativeCatalog.test.ts
tests/simulation/worldEvents.test.ts
tests/storage/autosave.test.ts
tests/storage/migrateGameStateV14.test.ts
tests/storage/saveFormat.test.ts
tests/ui/empireOverviewViewModel.test.ts
tests/ui/fleetComposerViewModel.test.ts
tests/ui/militaryZoneViewModel.test.ts
tests/ui/planetViewModel.test.ts
tests/ui/spaceMapViewModel.test.ts

=== REFERENCES ===
index.html:49:            <img class="commander-emblem" src="./assets/factions/aegis/emblem.svg" alt="" />
index.html:88:        <aside class="command-panel" aria-label="Контекстная панель">
index.html:91:            <div class="commander-row">
index.html:93:                class="commander-emblem context-emblem"
index.html:235:                <section class="operation-card economy-report">
src/assets/aegisVerticalSliceAssets.ts:8:  | 'commander'
src/assets/aegisVerticalSliceAssets.ts:38:    id: 'building.aegis.command',
src/assets/artTokens.ts:22:    command: '#E7A847',
src/assets/completeMechanicalAssetManifest.ts:10:import { COMPLETE_COMMANDER_SHIP_CATALOG } from '../simulation/units/completeCommanderShipCatalog';
src/assets/completeMechanicalAssetManifest.ts:146:  'commander.shared.annihilator': 'commander-ship.annihilator.png',
src/assets/completeMechanicalAssetManifest.ts:147:  'commander.shared.corsair': 'commander-ship.corsair.png',
src/assets/completeMechanicalAssetManifest.ts:148:  'commander.shared.regenerator': 'commander-ship.reanimator.png',
src/assets/completeMechanicalAssetManifest.ts:149:  'commander.shared.viper': 'commander-ship.viper.png',
src/assets/completeMechanicalAssetManifest.ts:150:  'commander.shared.scorpion': 'commander-ship.scorpion.png',
src/assets/completeMechanicalAssetManifest.ts:151:  'commander.shared.phantom': 'commander-ship.phantom.png',
src/assets/completeMechanicalAssetManifest.ts:152:  'commander.shared.hunter': 'commander-ship.hunter.png',
src/assets/completeMechanicalAssetManifest.ts:153:  'commander.shared.typhoon': 'commander-ship.typhoon.png',
src/assets/completeMechanicalAssetManifest.ts:154:  'commander.shared.executor': 'commander-ship.executioner.png',
src/assets/completeMechanicalAssetManifest.ts:155:  'commander.shared.juggernaut': 'commander-ship.juggernaut.png',
src/assets/completeMechanicalAssetManifest.ts:156:  'commander.shared.argo': 'commander-ship.argo.png',
src/assets/completeMechanicalAssetManifest.ts:157:  'commander.shared.judge': 'commander-ship.judge.png',
src/assets/completeMechanicalAssetManifest.ts:158:  'commander.shared.polias': 'commander-ship.polias.png',
src/assets/completeMechanicalAssetManifest.ts:167:        category: 'commander' as const,
src/assets/completeMechanicalAssetManifest.ts:195:    command: 'building.aegis.command',
src/assets/completeMechanicalAssetManifest.ts:205:    command: 'building.synod.concord-nexus',
src/assets/completeMechanicalAssetManifest.ts:215:    command: 'building.veyra.swarm-heart',
src/assets/completeMechanicalAssetManifest.ts:288:                : assets.command;
src/assets/completeMechanicalAssetManifest.ts:344:export function validateCompleteMechanicalAssetManifest(
src/assets/factionAtlasSelection.ts:15:    case 'commander':
src/assets/factionMechanicalAssets.ts:123:    seed('technology.synod.chorus-command', 'Хоровое командование', 'Координация'),
src/assets/galaxyFleetRuntimeAssets.test.ts:5:  getFleetShipArtUrl,
src/assets/galaxyFleetRuntimeAssets.test.ts:6:  getFleetShipPresentationRole,
src/assets/galaxyFleetRuntimeAssets.test.ts:8:} from './galaxyFleetRuntimeAssets';
src/assets/galaxyFleetRuntimeAssets.test.ts:10:describe('galaxy and fleet runtime assets', () => {
src/assets/galaxyFleetRuntimeAssets.test.ts:18:    expect(getFleetShipArtUrl('synod', 'ship.aegis.cargo')).toContain(
src/assets/galaxyFleetRuntimeAssets.test.ts:21:    expect(getFleetShipArtUrl('veyra', 'ship.aegis.recycler')).toContain(
src/assets/galaxyFleetRuntimeAssets.test.ts:24:    expect(getFleetShipArtUrl('synod', 'ship.synod.titan')).toContain(
src/assets/galaxyFleetRuntimeAssets.test.ts:38:    expect(getFleetShipPresentationRole('ship.aegis.spy-probe')).toBe('scout');
src/assets/galaxyFleetRuntimeAssets.test.ts:39:    expect(getFleetShipPresentationRole('ship.aegis.scout')).toBe('fighter');
src/assets/galaxyFleetRuntimeAssets.test.ts:40:    expect(getFleetShipPresentationRole('ship.aegis.colonizer')).toBe('colony');
src/assets/galaxyFleetRuntimeAssets.test.ts:41:    expect(getFleetShipPresentationRole('ship.aegis.unknown')).toBe('fighter');
src/assets/galaxyFleetRuntimeAssets.ts:11:export type FleetShipPresentationRole =
src/assets/galaxyFleetRuntimeAssets.ts:126:  Record<FactionId, Readonly<Record<FleetShipPresentationRole, string>>>
src/assets/galaxyFleetRuntimeAssets.ts:234:export function getFleetShipPresentationRole(unitId: string): FleetShipPresentationRole {
src/assets/galaxyFleetRuntimeAssets.ts:245:export function getFleetShipArtUrl(factionId: FactionId, unitId: string): string {
src/assets/galaxyFleetRuntimeAssets.ts:250:  return SHIP_ART_BY_FACTION[factionId][getFleetShipPresentationRole(unitId)];
src/assets/generated/runtimeAssetManifest.generated.ts:80:  "commander.shared.annihilator": { outputPath: "public/assets/generated/catalog/commanders/shared/annihilator.webp", width: 512, height: 512 },
src/assets/generated/runtimeAssetManifest.generated.ts:81:  "commander.shared.argo": { outputPath: "public/assets/generated/catalog/commanders/shared/argo.webp", width: 512, height: 512 },
src/assets/generated/runtimeAssetManifest.generated.ts:82:  "commander.shared.corsair": { outputPath: "public/assets/generated/catalog/commanders/shared/corsair.webp", width: 512, height: 512 },
src/assets/generated/runtimeAssetManifest.generated.ts:83:  "commander.shared.executor": { outputPath: "public/assets/generated/catalog/commanders/shared/executor.webp", width: 512, height: 512 },
src/assets/generated/runtimeAssetManifest.generated.ts:84:  "commander.shared.hunter": { outputPath: "public/assets/generated/catalog/commanders/shared/hunter.webp", width: 512, height: 512 },
src/assets/generated/runtimeAssetManifest.generated.ts:85:  "commander.shared.judge": { outputPath: "public/assets/generated/catalog/commanders/shared/judge.webp", width: 512, height: 512 },
src/assets/generated/runtimeAssetManifest.generated.ts:86:  "commander.shared.juggernaut": { outputPath: "public/assets/generated/catalog/commanders/shared/juggernaut.webp", width: 512, height: 512 },
src/assets/generated/runtimeAssetManifest.generated.ts:87:  "commander.shared.phantom": { outputPath: "public/assets/generated/catalog/commanders/shared/phantom.webp", width: 512, height: 512 },
src/assets/generated/runtimeAssetManifest.generated.ts:88:  "commander.shared.polias": { outputPath: "public/assets/generated/catalog/commanders/shared/polias.webp", width: 512, height: 512 },
src/assets/generated/runtimeAssetManifest.generated.ts:89:  "commander.shared.regenerator": { outputPath: "public/assets/generated/catalog/commanders/shared/regenerator.webp", width: 512, height: 512 },
src/assets/generated/runtimeAssetManifest.generated.ts:90:  "commander.shared.scorpion": { outputPath: "public/assets/generated/catalog/commanders/shared/scorpion.webp", width: 512, height: 512 },
src/assets/generated/runtimeAssetManifest.generated.ts:91:  "commander.shared.typhoon": { outputPath: "public/assets/generated/catalog/commanders/shared/typhoon.webp", width: 512, height: 512 },
src/assets/generated/runtimeAssetManifest.generated.ts:92:  "commander.shared.viper": { outputPath: "public/assets/generated/catalog/commanders/shared/viper.webp", width: 512, height: 512 },
src/assets/generated/runtimeAssetManifest.generated.ts:181:  "ui.mission.sun-attack": { outputPath: "public/assets/generated/universe/markers/sun-attack.webp", width: 128, height: 128 },
src/assets/generated/runtimeAssetManifest.generated.ts:182:  "ui.mission.sun-support": { outputPath: "public/assets/generated/universe/markers/sun-support.webp", width: 128, height: 128 },
src/assets/generated/spaceMapAssetManifest.generated.ts:27:  "ui.mission.sun-attack": { textureKey: "space.ui.mission.sun-attack", family: "marker", viewGroup: "solar-system", outputPath: "public/assets/generated/universe/markers/sun-attack.webp", width: 128, height: 128, bytes: 6234, decodedBytes: 65536, sha256: "bcf085f98319d1b138e1c30ff962f04cc55a4a67f1da7e195939ee5d331c271a" },
src/assets/generated/spaceMapAssetManifest.generated.ts:28:  "ui.mission.sun-support": { textureKey: "space.ui.mission.sun-support", family: "marker", viewGroup: "solar-system", outputPath: "public/assets/generated/universe/markers/sun-support.webp", width: 128, height: 128, bytes: 6782, decodedBytes: 65536, sha256: "79f22f27509af97441320a0dc4523f2f70f4bb4853241b114af769c617806017" },
src/assets/generated/spaceMapAssetManifest.generated.ts:142:  marker: ["ui.mission.sun-attack", "ui.mission.sun-support"],
src/assets/planetIndustryRuntimeAssets.test.ts:25:    expect(getBuildingPresentationRole('building.aegis.unknown')).toBe('command');
src/assets/planetIndustryRuntimeAssets.ts:8:  | 'command'
src/assets/planetIndustryRuntimeAssets.ts:26:  if (parsed?.kind !== 'building' || parsed.factionId === 'shared') return 'command';
src/assets/planetIndustryRuntimeAssets.ts:38:  return 'command';
src/assets/runtimeMechanicalAssets.ts:12:  | 'commander';
src/assets/runtimeMechanicalAssets.ts:22:  return category === 'commander' ? 'commander' : category;
src/game/scenes/BootScene.ts:2:import { GALAXY_BACKGROUND_ASSET } from '../../assets/galaxyFleetRuntimeAssets';
src/game/scenes/SpaceMapScene.ts:29:import { GALAXY_BACKGROUND_ASSET } from '../../assets/galaxyFleetRuntimeAssets';
src/main.ts:13:import './styles/missionReports.css';
src/main.ts:22:import './styles/missions.css';
src/main.ts:42:import { mountCommandDoctrineScreen } from './ui/commandDoctrineScreen';
src/main.ts:43:import { mountCommandRankingScreen } from './ui/commandRankingScreen';
src/main.ts:48:import { mountFleetDoctrineScreen } from './ui/fleetDoctrineScreen';
src/main.ts:52:import { mountMissionReportsPanel } from './ui/missionReportsPanel';
src/main.ts:57:  applyPlanetScreenCommand,
src/main.ts:63:import { mountMissionScreen } from './ui/missionScreen';
src/main.ts:158:    applyState: (state, acceptedCommandCount) => {
src/main.ts:161:        acceptedCommandCount > 0
src/main.ts:162:          ? `Боты выполнили действий · ${acceptedCommandCount}`
src/main.ts:172:  const commandBridge = {
src/main.ts:175:    execute: applyPlanetScreenCommand,
src/main.ts:182:  mountExpeditionPanel(commandBridge);
src/main.ts:183:  mountSpaceObjectsPanel(commandBridge);
src/main.ts:185:  mountMissionReportsPanel({ getState: () => runtimeState });
src/main.ts:186:  mountPlanetDevelopmentControls(commandBridge);
src/main.ts:187:  mountLogisticsRoutesPanel(commandBridge);
src/main.ts:188:  mountMarketPanel(commandBridge);
src/main.ts:195:  mountCommandRankingScreen({ getState: () => runtimeState });
src/main.ts:196:  mountCommandDoctrineScreen(commandBridge);
src/main.ts:197:  mountResearchScreen(commandBridge);
src/main.ts:198:  mountProductionScreens(commandBridge);
src/main.ts:199:  mountMissionScreen(commandBridge);
src/main.ts:200:  mountShipUpgradesScreen(commandBridge);
src/main.ts:201:  mountFleetDoctrineScreen(commandBridge);
src/runtime/BotAutomationController.ts:10:  readonly applyState: (state: GameState, acceptedCommandCount: number) => void;
src/runtime/BotAutomationController.ts:81:    const acceptedCommandCount = response.audit.filter((entry) => entry.accepted).length;
src/runtime/BotAutomationController.ts:82:    if (acceptedCommandCount > 0 || automationStateChanged(current, response.state)) {
src/runtime/BotAutomationController.ts:85:        this.options.applyState(response.state, acceptedCommandCount);
src/simulation/bots/economyPlanner.ts:15:import type { GameCommand, GameState } from '../types';
src/simulation/bots/economyPlanner.ts:36:  readonly command: GameCommand | null;
src/simulation/bots/economyPlanner.ts:39:function buildingCommand(
src/simulation/bots/economyPlanner.ts:43:): GameCommand {
src/simulation/bots/economyPlanner.ts:96:        command: buildingCommand(empireId, planet.id, definition.id),
src/simulation/bots/economyPlanner.ts:115:      command: null,
src/simulation/bots/economyPlanner.ts:129:      command: null,
src/simulation/bots/economyPlanner.ts:169:      command: {
src/simulation/bots/economyPlanner.ts:207:  if (getBuildingLevel(planet.buildings, roles.command) < 2) {
src/simulation/bots/economyPlanner.ts:213:      [roles.command],
src/simulation/bots/economyPlanner.ts:244:    [roles.power, roles.metal, roles.crystal, roles.gas, roles.command].sort(
src/simulation/bots/economyPlanner.ts:259:    command: null,
src/simulation/bots/fleetMissionPlanner.ts:3:import { createFleet } from '../fleets/fleetCommands';
src/simulation/bots/fleetMissionPlanner.ts:4:import { sendFleet } from '../fleets/flightCommands';
src/simulation/bots/fleetMissionPlanner.ts:5:import type { FleetMissionKind } from '../fleets/types';
src/simulation/bots/fleetMissionPlanner.ts:7:import type { GameCommand, GameState } from '../types';
src/simulation/bots/fleetMissionPlanner.ts:12:export type BotFleetReasonCode =
src/simulation/bots/fleetMissionPlanner.ts:13:  | 'fleet-created'
src/simulation/bots/fleetMissionPlanner.ts:14:  | 'mission-transport-selected'
src/simulation/bots/fleetMissionPlanner.ts:15:  | 'mission-recycle-selected'
src/simulation/bots/fleetMissionPlanner.ts:16:  | 'mission-colonize-selected'
src/simulation/bots/fleetMissionPlanner.ts:17:  | 'mission-scout-selected'
src/simulation/bots/fleetMissionPlanner.ts:18:  | 'mission-attack-selected'
src/simulation/bots/fleetMissionPlanner.ts:19:  | 'mission-deploy-selected'
src/simulation/bots/fleetMissionPlanner.ts:20:  | 'fleet-busy'
src/simulation/bots/fleetMissionPlanner.ts:21:  | 'fleet-unavailable'
src/simulation/bots/fleetMissionPlanner.ts:22:  | 'mission-unavailable';
src/simulation/bots/fleetMissionPlanner.ts:24:export interface BotFleetMissionPlan {
src/simulation/bots/fleetMissionPlanner.ts:26:  readonly reasonCode: BotFleetReasonCode;
src/simulation/bots/fleetMissionPlanner.ts:28:  readonly command: GameCommand | null;
src/simulation/bots/fleetMissionPlanner.ts:31:type PerceivedFleet = BotPerception['ownFleets'][number];
src/simulation/bots/fleetMissionPlanner.ts:41:function hasRole(fleet: PerceivedFleet, role: ShipRole): boolean {
src/simulation/bots/fleetMissionPlanner.ts:42:  return Object.entries(fleet.ships).some(
src/simulation/bots/fleetMissionPlanner.ts:59:function isArmed(fleet: PerceivedFleet): boolean {
src/simulation/bots/fleetMissionPlanner.ts:60:  return Object.entries(fleet.ships).some(
src/simulation/bots/fleetMissionPlanner.ts:75:  return (snapshot.stationedFleets ?? []).reduce(
src/simulation/bots/fleetMissionPlanner.ts:76:    (total, fleet) => total + shipPower(fleet.ships),
src/simulation/bots/fleetMissionPlanner.ts:81:function sendCommand(
src/simulation/bots/fleetMissionPlanner.ts:83:  fleetId: string,
src/simulation/bots/fleetMissionPlanner.ts:84:  targetPlanetId: string,
src/simulation/bots/fleetMissionPlanner.ts:85:  mission: FleetMissionKind,
src/simulation/bots/fleetMissionPlanner.ts:86:): Extract<GameCommand, { readonly type: 'SEND_FLEET' }> {
src/simulation/bots/fleetMissionPlanner.ts:87:  return { type: 'SEND_FLEET', empireId, fleetId, targetPlanetId, mission };
src/simulation/bots/fleetMissionPlanner.ts:92:  command: Extract<GameCommand, { readonly type: 'SEND_FLEET' }>,
src/simulation/bots/fleetMissionPlanner.ts:94:  return sendFleet(state, command).ok;
src/simulation/bots/fleetMissionPlanner.ts:97:function missionPlan(
src/simulation/bots/fleetMissionPlanner.ts:100:): BotFleetMissionPlan | null {
src/simulation/bots/fleetMissionPlanner.ts:101:  const fleets = perception.ownFleets
src/simulation/bots/fleetMissionPlanner.ts:102:    .filter((fleet) => fleet.status === 'stationed' && fleet.location.type === 'planet')
src/simulation/bots/fleetMissionPlanner.ts:106:  for (const fleet of fleets) {
src/simulation/bots/fleetMissionPlanner.ts:107:    if (fleet.location.type !== 'planet') continue;
src/simulation/bots/fleetMissionPlanner.ts:108:    const originPlanetId = fleet.location.planetId;
src/simulation/bots/fleetMissionPlanner.ts:110:    if (sumCargo(fleet.cargo) > 0) {
src/simulation/bots/fleetMissionPlanner.ts:112:        (left, right) => fleet.cargo[right] - fleet.cargo[left] || left.localeCompare(right),
src/simulation/bots/fleetMissionPlanner.ts:114:      if (resourceId !== undefined && fleet.cargo[resourceId] > 0) {
src/simulation/bots/fleetMissionPlanner.ts:123:          const command = sendCommand(perception.empireId, fleet.id, target.id, 'transport');
src/simulation/bots/fleetMissionPlanner.ts:124:          if (validSend(state, command)) {
src/simulation/bots/fleetMissionPlanner.ts:127:              reasonCode: 'mission-transport-selected',
src/simulation/bots/fleetMissionPlanner.ts:128:              explanation: `Транспорт ${fleet.id} направлен на ${target.name} с ресурсом ${resourceId}.`,
src/simulation/bots/fleetMissionPlanner.ts:129:              command,
src/simulation/bots/fleetMissionPlanner.ts:136:    if (hasRole(fleet, 'recycler')) {
src/simulation/bots/fleetMissionPlanner.ts:144:        const command = sendCommand(perception.empireId, fleet.id, target.planetId, 'recycle');
src/simulation/bots/fleetMissionPlanner.ts:145:        if (validSend(state, command)) {
src/simulation/bots/fleetMissionPlanner.ts:148:            reasonCode: 'mission-recycle-selected',
src/simulation/bots/fleetMissionPlanner.ts:149:            explanation: `Переработчик ${fleet.id} направлен к обломкам ${target.planetId}.`,
src/simulation/bots/fleetMissionPlanner.ts:150:            command,
src/simulation/bots/fleetMissionPlanner.ts:156:    if (hasRole(fleet, 'colonizer')) {
src/simulation/bots/fleetMissionPlanner.ts:165:        const command = sendCommand(perception.empireId, fleet.id, target.id, 'colonize');
src/simulation/bots/fleetMissionPlanner.ts:166:        if (validSend(state, command)) {
src/simulation/bots/fleetMissionPlanner.ts:169:            reasonCode: 'mission-colonize-selected',
src/simulation/bots/fleetMissionPlanner.ts:170:            explanation: `Колонизатор ${fleet.id} направлен к позиции ${target.id}.`,
src/simulation/bots/fleetMissionPlanner.ts:171:            command,
src/simulation/bots/fleetMissionPlanner.ts:177:    if (hasRole(fleet, 'scout')) {
src/simulation/bots/fleetMissionPlanner.ts:187:      for (const targetPlanetId of [...new Set([...observed, ...publicTargets])]) {
src/simulation/bots/fleetMissionPlanner.ts:188:        const command = sendCommand(perception.empireId, fleet.id, targetPlanetId, 'scout');
src/simulation/bots/fleetMissionPlanner.ts:189:        if (validSend(state, command)) {
src/simulation/bots/fleetMissionPlanner.ts:192:            reasonCode: 'mission-scout-selected',
src/simulation/bots/fleetMissionPlanner.ts:193:            explanation: `Разведчик ${fleet.id} проверяет ${targetPlanetId}.`,
src/simulation/bots/fleetMissionPlanner.ts:194:            command,
src/simulation/bots/fleetMissionPlanner.ts:200:    if (isArmed(fleet)) {
src/simulation/bots/fleetMissionPlanner.ts:201:      const ownPower = shipPower(fleet.ships);
src/simulation/bots/fleetMissionPlanner.ts:217:        const command = sendCommand(
src/simulation/bots/fleetMissionPlanner.ts:219:          fleet.id,
src/simulation/bots/fleetMissionPlanner.ts:223:        if (validSend(state, command)) {
src/simulation/bots/fleetMissionPlanner.ts:226:            reasonCode: 'mission-attack-selected',
src/simulation/bots/fleetMissionPlanner.ts:227:            explanation: `Флот ${fleet.id} атакует ${target.planet.planetId}: ${ownPower} против ${target.power}.`,
src/simulation/bots/fleetMissionPlanner.ts:228:            command,
src/simulation/bots/fleetMissionPlanner.ts:247:        const command = sendCommand(perception.empireId, fleet.id, target.planet.id, 'deploy');
src/simulation/bots/fleetMissionPlanner.ts:248:        if (validSend(state, command)) {
src/simulation/bots/fleetMissionPlanner.ts:251:            reasonCode: 'mission-deploy-selected',
src/simulation/bots/fleetMissionPlanner.ts:252:            explanation: `Флот ${fleet.id} усиливает колонию ${target.planet.name}.`,
src/simulation/bots/fleetMissionPlanner.ts:253:            command,
src/simulation/bots/fleetMissionPlanner.ts:263:interface FleetCandidate {
src/simulation/bots/fleetMissionPlanner.ts:273:): readonly FleetCandidate[] {
src/simulation/bots/fleetMissionPlanner.ts:276:  const candidates: FleetCandidate[] = [];
src/simulation/bots/fleetMissionPlanner.ts:351:function fleetCreationPlan(
src/simulation/bots/fleetMissionPlanner.ts:354:): BotFleetMissionPlan | null {
src/simulation/bots/fleetMissionPlanner.ts:358:      const command: Extract<GameCommand, { readonly type: 'CREATE_FLEET' }> = {
src/simulation/bots/fleetMissionPlanner.ts:365:      if (createFleet(state, command).ok) {
src/simulation/bots/fleetMissionPlanner.ts:368:          reasonCode: 'fleet-created',
src/simulation/bots/fleetMissionPlanner.ts:370:          command,
src/simulation/bots/fleetMissionPlanner.ts:378:export function planBotFleetMission(
src/simulation/bots/fleetMissionPlanner.ts:381:): BotFleetMissionPlan {
src/simulation/bots/fleetMissionPlanner.ts:383:  const mission = missionPlan(state, perception);
src/simulation/bots/fleetMissionPlanner.ts:384:  if (mission !== null) return mission;
src/simulation/bots/fleetMissionPlanner.ts:386:  const creation = fleetCreationPlan(state, perception);
src/simulation/bots/fleetMissionPlanner.ts:389:  if (perception.ownFleets.some((fleet) => fleet.status !== 'stationed')) {
src/simulation/bots/fleetMissionPlanner.ts:392:      reasonCode: 'fleet-busy',
src/simulation/bots/fleetMissionPlanner.ts:394:      command: null,
src/simulation/bots/fleetMissionPlanner.ts:397:  if (perception.ownFleets.length > 0) {
src/simulation/bots/fleetMissionPlanner.ts:400:      reasonCode: 'mission-unavailable',
src/simulation/bots/fleetMissionPlanner.ts:402:      command: null,
src/simulation/bots/fleetMissionPlanner.ts:407:    reasonCode: 'fleet-unavailable',
src/simulation/bots/fleetMissionPlanner.ts:409:    command: null,
src/simulation/bots/fleetMissionPlanner.ts:413:export function planAllBotFleetMissions(
src/simulation/bots/fleetMissionPlanner.ts:415:): readonly BotFleetMissionPlan[] {
src/simulation/bots/fleetMissionPlanner.ts:418:    .map((empireId) => planBotFleetMission(state, empireId));
src/simulation/bots/memory.ts:1:import { getEmpireIntelligence } from '../intelligence/intelligenceState';
src/simulation/bots/memory.ts:36:  const intelligence = getEmpireIntelligence(state.intelligence, empireId);
src/simulation/bots/memory.ts:37:  if (intelligence === undefined) return [];
src/simulation/bots/memory.ts:39:  const observations: BotMemoryEntry[] = intelligence.observations.map((observation) => ({
src/simulation/bots/memory.ts:43:    subjectId: observation.targetPlanetId,
src/simulation/bots/memory.ts:47:  const alerts: BotMemoryEntry[] = intelligence.alerts.map((alert) => ({
src/simulation/bots/memory.ts:51:    subjectId: alert.targetPlanetId,
src/simulation/bots/perception.ts:1:import { getEmpireIntelligence } from '../intelligence/intelligenceState';
src/simulation/bots/perception.ts:4:  IntelligenceAlert,
src/simulation/bots/perception.ts:5:} from '../intelligence/types';
src/simulation/bots/perception.ts:55:  readonly alerts: readonly IntelligenceAlert[];
src/simulation/bots/perception.ts:58:  readonly ownFleets: readonly {
src/simulation/bots/perception.ts:60:    readonly status: GameState['fleets'][number]['status'];
src/simulation/bots/perception.ts:61:    readonly location: GameState['fleets'][number]['location'];
src/simulation/bots/perception.ts:63:    readonly cargo: GameState['fleets'][number]['cargo'];
src/simulation/bots/perception.ts:66:    readonly mission: GameState['fleets'][number]['mission'];
src/simulation/bots/perception.ts:100:  const intelligence = getEmpireIntelligence(state.intelligence, empireId);
src/simulation/bots/perception.ts:102:  const observations = [...(intelligence?.observations ?? [])].sort(
src/simulation/bots/perception.ts:107:    if (!latestByPlanet.has(observation.targetPlanetId)) {
src/simulation/bots/perception.ts:108:      latestByPlanet.set(observation.targetPlanetId, observation);
src/simulation/bots/perception.ts:126:        planetId: observation.targetPlanetId,
src/simulation/bots/perception.ts:146:    alerts: [...(intelligence?.alerts ?? [])].map((alert) => ({ ...alert })),
src/simulation/bots/perception.ts:149:    ownFleets: state.fleets
src/simulation/bots/perception.ts:150:      .filter((fleet) => fleet.empireId === empireId)
src/simulation/bots/perception.ts:151:      .map((fleet) => ({
src/simulation/bots/perception.ts:152:        id: fleet.id,
src/simulation/bots/perception.ts:153:        status: fleet.status,
src/simulation/bots/perception.ts:154:        location: { ...fleet.location },
src/simulation/bots/perception.ts:155:        ships: { ...fleet.ships },
src/simulation/bots/perception.ts:156:        cargo: { ...fleet.cargo },
src/simulation/bots/perception.ts:157:        speed: fleet.speed,
src/simulation/bots/perception.ts:158:        cargoCapacity: fleet.cargoCapacity,
src/simulation/bots/perception.ts:159:        mission: fleet.mission === null ? null : { ...fleet.mission },
src/simulation/bots/profiles.ts:10:  readonly maxCommandsPerDecision: number;
src/simulation/bots/profiles.ts:20:    maxCommandsPerDecision: 2,
src/simulation/bots/profiles.ts:28:    maxCommandsPerDecision: 3,
src/simulation/bots/profiles.ts:36:    maxCommandsPerDecision: 2,
src/simulation/bots/researchProductionPlanner.ts:1:import { getEmpireCommandState } from '../command/commandDoctrine';
src/simulation/bots/researchProductionPlanner.ts:3:  getCommanderShipCatalog,
src/simulation/bots/researchProductionPlanner.ts:9:import { queueResearch } from '../research/researchCommands';
src/simulation/bots/researchProductionPlanner.ts:10:import type { GameCommand, GameState } from '../types';
src/simulation/bots/researchProductionPlanner.ts:13:import { queueUnitBatch } from '../units/productionCommands';
src/simulation/bots/researchProductionPlanner.ts:31:  readonly command: GameCommand | null;
src/simulation/bots/researchProductionPlanner.ts:50:      command: null,
src/simulation/bots/researchProductionPlanner.ts:70:      command: null,
src/simulation/bots/researchProductionPlanner.ts:99:      const command: Extract<GameCommand, { readonly type: 'QUEUE_RESEARCH' }> = {
src/simulation/bots/researchProductionPlanner.ts:105:      if (queueResearch(state, command).ok) {
src/simulation/bots/researchProductionPlanner.ts:111:          command,
src/simulation/bots/researchProductionPlanner.ts:120:    command: null,
src/simulation/bots/researchProductionPlanner.ts:141:  return state.fleets
src/simulation/bots/researchProductionPlanner.ts:142:    .filter((fleet) => fleet.empireId === empireId)
src/simulation/bots/researchProductionPlanner.ts:143:    .reduce((total, fleet) => total + ids.reduce(
src/simulation/bots/researchProductionPlanner.ts:144:      (subtotal, id) => subtotal + (fleet.ships[id] ?? 0),
src/simulation/bots/researchProductionPlanner.ts:149:function commanderCandidates(
src/simulation/bots/researchProductionPlanner.ts:153:  const level = getEmpireCommandState(state.commanders, empireId)?.level ?? 0;
src/simulation/bots/researchProductionPlanner.ts:154:  return getCommanderShipCatalog()
src/simulation/bots/researchProductionPlanner.ts:160:      (left.commanderAbility?.battlePriority ?? 999) -
src/simulation/bots/researchProductionPlanner.ts:161:        (right.commanderAbility?.battlePriority ?? 999) ||
src/simulation/bots/researchProductionPlanner.ts:179:  const commanderIds = getCommanderShipCatalog().map((definition) => definition.id);
src/simulation/bots/researchProductionPlanner.ts:182:    ...commanderIds,
src/simulation/bots/researchProductionPlanner.ts:196:      command: null,
src/simulation/bots/researchProductionPlanner.ts:209:      command: null,
src/simulation/bots/researchProductionPlanner.ts:213:  const availableCommanders = commanderCandidates(state, empireId);
src/simulation/bots/researchProductionPlanner.ts:216:        ...availableCommanders.slice(0, 1),
src/simulation/bots/researchProductionPlanner.ts:240:        ...availableCommanders.slice(0, 1),
src/simulation/bots/researchProductionPlanner.ts:254:      const command: Extract<GameCommand, { readonly type: 'QUEUE_UNIT_BATCH' }> = {
src/simulation/bots/researchProductionPlanner.ts:261:      if (queueUnitBatch(state, command).ok) {
src/simulation/bots/researchProductionPlanner.ts:267:          command,
src/simulation/bots/researchProductionPlanner.ts:276:    command: null,
src/simulation/bots/scheduler.ts:1:import { executeCommand } from '../reducer';
src/simulation/bots/scheduler.ts:2:import type { GameCommand, GameState } from '../types';
src/simulation/bots/scheduler.ts:4:import { planBotFleetMission } from './fleetMissionPlanner';
src/simulation/bots/scheduler.ts:13:export type BotPlannerSource = 'economy' | 'research' | 'production' | 'fleet' | 'threat';
src/simulation/bots/scheduler.ts:22:  readonly command: GameCommand;
src/simulation/bots/scheduler.ts:34:interface CommandCandidate {
src/simulation/bots/scheduler.ts:36:  readonly command: GameCommand | null;
src/simulation/bots/scheduler.ts:47:): readonly CommandCandidate[] {
src/simulation/bots/scheduler.ts:50:  const fleet = planBotFleetMission(state, profile.empireId);
src/simulation/bots/scheduler.ts:52:  const candidates: Readonly<Record<BotPersonality, readonly CommandCandidate[]>> = {
src/simulation/bots/scheduler.ts:54:      { source: 'economy', command: economy.command },
src/simulation/bots/scheduler.ts:55:      { source: 'research', command: science.research.command },
src/simulation/bots/scheduler.ts:56:      { source: 'production', command: science.production.command },
src/simulation/bots/scheduler.ts:57:      { source: 'threat', command: threat.command },
src/simulation/bots/scheduler.ts:58:      { source: 'fleet', command: fleet.command },
src/simulation/bots/scheduler.ts:61:      { source: 'fleet', command: fleet.command },
src/simulation/bots/scheduler.ts:62:      { source: 'economy', command: economy.command },
src/simulation/bots/scheduler.ts:63:      { source: 'research', command: science.research.command },
src/simulation/bots/scheduler.ts:64:      { source: 'production', command: science.production.command },
src/simulation/bots/scheduler.ts:65:      { source: 'threat', command: threat.command },
src/simulation/bots/scheduler.ts:68:      { source: 'threat', command: threat.command },
src/simulation/bots/scheduler.ts:69:      { source: 'production', command: science.production.command },
src/simulation/bots/scheduler.ts:70:      { source: 'research', command: science.research.command },
src/simulation/bots/scheduler.ts:71:      { source: 'fleet', command: fleet.command },
src/simulation/bots/scheduler.ts:72:      { source: 'economy', command: economy.command },
src/simulation/bots/scheduler.ts:78:function isSameCommand(left: GameCommand, right: GameCommand): boolean {
src/simulation/bots/scheduler.ts:89:  const attempted: GameCommand[] = [];
src/simulation/bots/scheduler.ts:91:  for (let index = 0; index < profile.maxCommandsPerDecision; index += 1) {
src/simulation/bots/scheduler.ts:94:        item.command !== null &&
src/simulation/bots/scheduler.ts:95:        !attempted.some((command) =>
src/simulation/bots/scheduler.ts:96:          item.command === null ? false : isSameCommand(command, item.command),
src/simulation/bots/scheduler.ts:99:    if (candidate?.command === null || candidate === undefined) break;
src/simulation/bots/scheduler.ts:100:    attempted.push(candidate.command);
src/simulation/bots/scheduler.ts:101:    const result = executeCommand(working, candidate.command);
src/simulation/bots/scheduler.ts:108:      command: candidate.command,
src/simulation/bots/threatRecoveryPlanner.ts:3:import type { IntelPlanetSnapshot } from '../intelligence/types';
src/simulation/bots/threatRecoveryPlanner.ts:4:import type { GameCommand, GameState } from '../types';
src/simulation/bots/threatRecoveryPlanner.ts:6:import { queueUnitBatch } from '../units/productionCommands';
src/simulation/bots/threatRecoveryPlanner.ts:8:import { planBotFleetMission } from './fleetMissionPlanner';
src/simulation/bots/threatRecoveryPlanner.ts:13:export type BotRecoveryPhase = 'stable' | 'economic' | 'fleet' | 'critical';
src/simulation/bots/threatRecoveryPlanner.ts:18:  readonly intelligenceLevel: 1 | 2 | 3;
src/simulation/bots/threatRecoveryPlanner.ts:44:  readonly command: GameCommand | null;
src/simulation/bots/threatRecoveryPlanner.ts:47:type Action = Pick<BotThreatRecoveryPlan, 'reasonCode' | 'explanation' | 'command'>;
src/simulation/bots/threatRecoveryPlanner.ts:69:  return perception.ownFleets.reduce(
src/simulation/bots/threatRecoveryPlanner.ts:70:    (total, fleet) => total + compositionPower(fleet.ships),
src/simulation/bots/threatRecoveryPlanner.ts:77:  return (snapshot.stationedFleets ?? []).reduce(
src/simulation/bots/threatRecoveryPlanner.ts:78:    (total, fleet) => total + compositionPower(fleet.ships),
src/simulation/bots/threatRecoveryPlanner.ts:127:        intelligenceLevel: planet.snapshot.level,
src/simulation/bots/threatRecoveryPlanner.ts:171:      (planet.buildings[roles.command] ?? 0) < 1 ||
src/simulation/bots/threatRecoveryPlanner.ts:189:    perception.ownFleets.reduce(
src/simulation/bots/threatRecoveryPlanner.ts:190:      (total, fleet) =>
src/simulation/bots/threatRecoveryPlanner.ts:191:        total + Object.values(fleet.ships).reduce((sum, count) => sum + count, 0),
src/simulation/bots/threatRecoveryPlanner.ts:194:  return shipCount < 2 || threat === 'high' ? 'fleet' : 'stable';
src/simulation/bots/threatRecoveryPlanner.ts:197:function militaryRecoveryCommand(
src/simulation/bots/threatRecoveryPlanner.ts:200:): GameCommand | null {
src/simulation/bots/threatRecoveryPlanner.ts:212:      const command: Extract<GameCommand, { readonly type: 'QUEUE_UNIT_BATCH' }> = {
src/simulation/bots/threatRecoveryPlanner.ts:219:      if (queueUnitBatch(state, command).ok) return command;
src/simulation/bots/threatRecoveryPlanner.ts:234:    if (economy.command !== null) {
src/simulation/bots/threatRecoveryPlanner.ts:239:        command: economy.command,
src/simulation/bots/threatRecoveryPlanner.ts:244:  if (phase === 'fleet' || threat === 'high') {
src/simulation/bots/threatRecoveryPlanner.ts:245:    const combatCommand = militaryRecoveryCommand(state, empireId);
src/simulation/bots/threatRecoveryPlanner.ts:246:    if (combatCommand !== null) {
src/simulation/bots/threatRecoveryPlanner.ts:253:        command: combatCommand,
src/simulation/bots/threatRecoveryPlanner.ts:257:    if (production.command !== null) {
src/simulation/bots/threatRecoveryPlanner.ts:261:        command: production.command,
src/simulation/bots/threatRecoveryPlanner.ts:266:  const fleet = planBotFleetMission(state, empireId);
src/simulation/bots/threatRecoveryPlanner.ts:267:  if (fleet.command !== null) {
src/simulation/bots/threatRecoveryPlanner.ts:270:      explanation: fleet.explanation,
src/simulation/bots/threatRecoveryPlanner.ts:271:      command: fleet.command,
src/simulation/bots/threatRecoveryPlanner.ts:276:  if (research.command !== null) {
src/simulation/bots/threatRecoveryPlanner.ts:280:      command: research.command,
src/simulation/bots/threatRecoveryPlanner.ts:287:    command: null,
src/simulation/colonization/colonization.ts:6:import type { FleetState } from '../fleets/types';
src/simulation/colonization/colonization.ts:150:export function findFleetShipByRole(
src/simulation/colonization/colonization.ts:168:  readonly fleet: FleetState | undefined;
src/simulation/colonization/colonization.ts:173:  fleet: FleetState,
src/simulation/colonization/colonization.ts:181:    getEmpireColonyCount(state, fleet.empireId) >=
src/simulation/colonization/colonization.ts:182:      getColonyLimit(state, fleet.empireId)
src/simulation/colonization/colonization.ts:187:  const colonyShipId = findFleetShipByRole(fleet.ships, 'colonizer');
src/simulation/colonization/colonization.ts:188:  if (colonyShipId === undefined || getColonizationLevel(state, fleet.empireId) <= 0) {
src/simulation/colonization/colonization.ts:192:  const factionId = getFactionIdForEmpire(state, fleet.empireId);
src/simulation/colonization/colonization.ts:193:  const baseColony = createColonyPlanet(location, fleet.empireId, factionId);
src/simulation/colonization/colonization.ts:194:  const unloaded = unloadCargo(baseColony, fleet.cargo);
src/simulation/colonization/colonization.ts:195:  const ships = { ...fleet.ships };
src/simulation/colonization/colonization.ts:200:  const survivingFleet: FleetState | undefined =
src/simulation/colonization/colonization.ts:203:          ...fleet,
src/simulation/colonization/colonization.ts:209:          mission: null,
src/simulation/colonization/colonization.ts:213:  const fleets = survivingFleet === undefined
src/simulation/colonization/colonization.ts:214:    ? state.fleets.filter((candidate) => candidate.id !== fleet.id)
src/simulation/colonization/colonization.ts:215:    : state.fleets.map((candidate) =>
src/simulation/colonization/colonization.ts:216:        candidate.id === fleet.id ? survivingFleet : candidate,
src/simulation/colonization/colonization.ts:221:    fleet: survivingFleet,
src/simulation/colonization/colonization.ts:227:        fleet.empireId,
src/simulation/colonization/colonization.ts:230:      fleets,
src/simulation/combat/combatProfiles.ts:1:import { getCompleteCommanderShipClass } from '../units/completeCommanderShipCatalog';
src/simulation/combat/combatProfiles.ts:137:  const commanderClass = getCompleteCommanderShipClass(canonicalId);
src/simulation/combat/combatProfiles.ts:138:  if (commanderClass !== undefined) return COMMANDER_PROFILE;
src/simulation/combat/debris.ts:3:import type { FleetState } from '../fleets/types';
src/simulation/combat/debris.ts:4:import { getCargoAmount } from '../fleets/fleetCalculations';
src/simulation/combat/debris.ts:95:  readonly fleet: FleetState;
src/simulation/combat/debris.ts:101:  fleet: FleetState,
src/simulation/combat/debris.ts:104:  let freeCapacity = Math.max(0, fleet.cargoCapacity - getCargoAmount(fleet.cargo));
src/simulation/combat/debris.ts:105:  const cargo = { ...fleet.cargo };
src/simulation/combat/debris.ts:126:    fleet: { ...fleet, cargo },
src/simulation/combat/debris.ts:132:  readonly fleet: FleetState;
src/simulation/combat/debris.ts:140:  fleet: FleetState,
src/simulation/combat/debris.ts:144:    return { fleet, fields, collected: { metal: 0, crystal: 0 } };
src/simulation/combat/debris.ts:147:  let freeCapacity = Math.max(0, fleet.cargoCapacity - getCargoAmount(fleet.cargo));
src/simulation/combat/debris.ts:152:    ...fleet.cargo,
src/simulation/combat/debris.ts:153:    metal: fleet.cargo.metal + metal,
src/simulation/combat/debris.ts:154:    crystal: fleet.cargo.crystal + crystal,
src/simulation/combat/debris.ts:172:    fleet: { ...fleet, cargo },
src/simulation/combat/fleetDoctrine.ts:7:export type FleetFormation = 'line' | 'screen' | 'wedge';
src/simulation/combat/fleetDoctrine.ts:8:export type FleetTargetPriority = 'balanced' | 'interceptors' | 'capitals' | 'installations';
src/simulation/combat/fleetDoctrine.ts:11:  readonly id: FleetFormation;
src/simulation/combat/fleetDoctrine.ts:23:  readonly formation: FleetFormation;
src/simulation/combat/fleetDoctrine.ts:28:export const FLEET_FORMATIONS: Readonly<Record<FleetFormation, FormationDefinition>> = {
src/simulation/combat/fleetDoctrine.ts:83:  Record<FleetTargetPriority, Readonly<Record<TargetSize, number>>>
src/simulation/combat/fleetDoctrine.ts:91:export function isFleetFormation(value: unknown): value is FleetFormation {
src/simulation/combat/fleetDoctrine.ts:95:export function isFleetTargetPriority(value: unknown): value is FleetTargetPriority {
src/simulation/combat/fleetDoctrine.ts:105:  priority: FleetTargetPriority,
src/simulation/combat/fleetDoctrine.ts:124:  formation: FleetFormation,
src/simulation/combat/resolveAttackMission.ts:2:  getCommanderFleetEffects,
src/simulation/combat/resolveAttackMission.ts:3:  recoverFleetShipsWithCommander,
src/simulation/combat/resolveAttackMission.ts:4:  type CommanderFleetEffects,
src/simulation/combat/resolveAttackMission.ts:5:} from '../command/commanderShips';
src/simulation/combat/resolveAttackMission.ts:7:  awardBattleCommandExperience,
src/simulation/combat/resolveAttackMission.ts:8:  getCommandCombatEffects,
src/simulation/combat/resolveAttackMission.ts:9:} from '../command/commandDoctrine';
src/simulation/combat/resolveAttackMission.ts:16:import type { FleetState } from '../fleets/types';
src/simulation/combat/resolveAttackMission.ts:36:import type { BattleReport } from './types';
src/simulation/combat/resolveAttackMission.ts:42:  fleetId: string | undefined,
src/simulation/combat/resolveAttackMission.ts:43:  commander: CommanderFleetEffects,
src/simulation/combat/resolveAttackMission.ts:44:  opponentCommander: CommanderFleetEffects,
src/simulation/combat/resolveAttackMission.ts:48:  const command = getCommandCombatEffects(state.commanders, empireId, fleetId);
src/simulation/combat/resolveAttackMission.ts:49:  const demolitionBonus = installationsPresent && commander.demolitionBasisPoints > 0
src/simulation/combat/resolveAttackMission.ts:50:    ? Math.max(1, Math.ceil(commander.demolitionBasisPoints / 100))
src/simulation/combat/resolveAttackMission.ts:55:      command.weaponBonusPercent +
src/simulation/combat/resolveAttackMission.ts:56:      commander.weaponBonusPercent +
src/simulation/combat/resolveAttackMission.ts:58:      opponentCommander.enemyWeaponPenaltyPercent,
src/simulation/combat/resolveAttackMission.ts:61:      command.armorBonusPercent +
src/simulation/combat/resolveAttackMission.ts:62:      commander.armorBonusPercent -
src/simulation/combat/resolveAttackMission.ts:63:      opponentCommander.enemyArmorPenaltyPercent,
src/simulation/combat/resolveAttackMission.ts:142:  fleets: readonly FleetState[],
src/simulation/combat/resolveAttackMission.ts:143:  targetPlanetId: string,
src/simulation/combat/resolveAttackMission.ts:146:): readonly FleetState[] {
src/simulation/combat/resolveAttackMission.ts:148:  const result: FleetState[] = [];
src/simulation/combat/resolveAttackMission.ts:150:  for (const fleet of fleets) {
src/simulation/combat/resolveAttackMission.ts:152:      fleet.empireId === defenderEmpireId &&
src/simulation/combat/resolveAttackMission.ts:153:      fleet.status === 'stationed' &&
src/simulation/combat/resolveAttackMission.ts:154:      fleet.location.type === 'planet' &&
src/simulation/combat/resolveAttackMission.ts:155:      fleet.location.planetId === targetPlanetId;
src/simulation/combat/resolveAttackMission.ts:157:      result.push(fleet);
src/simulation/combat/resolveAttackMission.ts:162:    for (const [unitId, originalCount] of Object.entries(fleet.ships)) {
src/simulation/combat/resolveAttackMission.ts:169:    if (Object.keys(ships).length > 0) result.push({ ...fleet, ships });
src/simulation/combat/resolveAttackMission.ts:177:  fleet: FleetState,
src/simulation/combat/resolveAttackMission.ts:182:    metal: debris.metal + Math.floor(fleet.cargo.metal * 0.5),
src/simulation/combat/resolveAttackMission.ts:183:    crystal: debris.crystal + Math.floor(fleet.cargo.crystal * 0.5),
src/simulation/combat/resolveAttackMission.ts:187:export interface AttackMissionResolution {
src/simulation/combat/resolveAttackMission.ts:189:  readonly report: BattleReport;
src/simulation/combat/resolveAttackMission.ts:190:  readonly attackerFleet: FleetState | undefined;
src/simulation/combat/resolveAttackMission.ts:193:export function resolveAttackMission(
src/simulation/combat/resolveAttackMission.ts:195:  attackerFleet: FleetState,
src/simulation/combat/resolveAttackMission.ts:198:): AttackMissionResolution {
src/simulation/combat/resolveAttackMission.ts:201:  const defenderFleets = state.fleets.filter(
src/simulation/combat/resolveAttackMission.ts:202:    (fleet) =>
src/simulation/combat/resolveAttackMission.ts:203:      fleet.empireId === target.ownerEmpireId &&
src/simulation/combat/resolveAttackMission.ts:204:      fleet.status === 'stationed' &&
src/simulation/combat/resolveAttackMission.ts:205:      fleet.location.type === 'planet' &&
src/simulation/combat/resolveAttackMission.ts:206:      fleet.location.planetId === target.id,
src/simulation/combat/resolveAttackMission.ts:208:  for (const fleet of defenderFleets) mergeUnits(defenderUnits, fleet.ships);
src/simulation/combat/resolveAttackMission.ts:209:  const defenderDoctrine = defenderFleets[0];
src/simulation/combat/resolveAttackMission.ts:210:  const attackerFormation = attackerFleet.formation ?? 'line';
src/simulation/combat/resolveAttackMission.ts:211:  const attackerTargetPriority = attackerFleet.targetPriority ?? 'balanced';
src/simulation/combat/resolveAttackMission.ts:218:    ? calculatePirateThreatMultiplier(state, attackerFleet.empireId)
src/simulation/combat/resolveAttackMission.ts:223:        attackerFleet.empireId,
src/simulation/combat/resolveAttackMission.ts:231:  const attackerCommander = getCommanderFleetEffects(state, attackerFleet);
src/simulation/combat/resolveAttackMission.ts:232:  const defenderCommander = defenderDoctrine === undefined
src/simulation/combat/resolveAttackMission.ts:233:    ? getCommanderFleetEffects(state, target.ownerEmpireId, undefined, effectiveDefenderUnits)
src/simulation/combat/resolveAttackMission.ts:234:    : getCommanderFleetEffects(state, defenderDoctrine);
src/simulation/combat/resolveAttackMission.ts:236:  const seed = (state.seed ^ eventSequence ^ attackerFleet.id.length) >>> 0;
src/simulation/combat/resolveAttackMission.ts:240:      empireId: attackerFleet.empireId,
src/simulation/combat/resolveAttackMission.ts:241:      units: attackerFleet.ships,
src/simulation/combat/resolveAttackMission.ts:246:        attackerFleet.empireId,
src/simulation/combat/resolveAttackMission.ts:247:        attackerFleet.ships,
src/simulation/combat/resolveAttackMission.ts:248:        attackerFleet.id,
src/simulation/combat/resolveAttackMission.ts:249:        attackerCommander,
src/simulation/combat/resolveAttackMission.ts:250:        defenderCommander,
src/simulation/combat/resolveAttackMission.ts:264:        defenderCommander,
src/simulation/combat/resolveAttackMission.ts:265:        attackerCommander,
src/simulation/combat/resolveAttackMission.ts:270:  const attackerAfterCommanderRecovery = recoverFleetShipsWithCommander(
src/simulation/combat/resolveAttackMission.ts:271:    attackerFleet.ships,
src/simulation/combat/resolveAttackMission.ts:273:    attackerCommander.recoveryPermille,
src/simulation/combat/resolveAttackMission.ts:276:  const defenderAfterCommanderRecovery = recoverFleetShipsWithCommander(
src/simulation/combat/resolveAttackMission.ts:279:    defenderCommander.recoveryPermille,
src/simulation/combat/resolveAttackMission.ts:282:  const defenderRemaining = splitDefenderRemaining(defenderAfterCommanderRecovery);
src/simulation/combat/resolveAttackMission.ts:293:    defenderAfterCommanderRecovery,
src/simulation/combat/resolveAttackMission.ts:304:  let fleets = redistributeDefenderShips(
src/simulation/combat/resolveAttackMission.ts:305:    state.fleets,
src/simulation/combat/resolveAttackMission.ts:310:  const attackerSurvived = Object.keys(attackerAfterCommanderRecovery).length > 0;
src/simulation/combat/resolveAttackMission.ts:312:    ? { ...attackerFleet, ships: attackerAfterCommanderRecovery }
src/simulation/combat/resolveAttackMission.ts:320:      attackerCommander.plunderBonusPercent,
src/simulation/combat/resolveAttackMission.ts:325:        plunder.fleet,
src/simulation/combat/resolveAttackMission.ts:330:      updatedAttacker = adjusted.fleet;
src/simulation/combat/resolveAttackMission.ts:334:      updatedAttacker = plunder.fleet;
src/simulation/combat/resolveAttackMission.ts:339:  fleets = updatedAttacker === undefined
src/simulation/combat/resolveAttackMission.ts:340:    ? fleets.filter((fleet) => fleet.id !== attackerFleet.id)
src/simulation/combat/resolveAttackMission.ts:341:    : fleets.map((fleet) =>
src/simulation/combat/resolveAttackMission.ts:342:        fleet.id === attackerFleet.id ? updatedAttacker : fleet,
src/simulation/combat/resolveAttackMission.ts:346:    attackerFleet.ships,
src/simulation/combat/resolveAttackMission.ts:347:    attackerAfterCommanderRecovery,
src/simulation/combat/resolveAttackMission.ts:353:    attackerFleet,
src/simulation/combat/resolveAttackMission.ts:364:  const report: BattleReport = {
src/simulation/combat/resolveAttackMission.ts:365:    id: `battle-${eventSequence}-${attackerFleet.id}`,
src/simulation/combat/resolveAttackMission.ts:368:    targetPlanetId: target.id,
src/simulation/combat/resolveAttackMission.ts:369:    attackerEmpireId: attackerFleet.empireId,
src/simulation/combat/resolveAttackMission.ts:373:    attackerInitial: { ...attackerFleet.ships },
src/simulation/combat/resolveAttackMission.ts:375:    attackerRemaining: attackerAfterCommanderRecovery,
src/simulation/combat/resolveAttackMission.ts:377:    attackerCommanderId: attackerCommander.activeCommanderId,
src/simulation/combat/resolveAttackMission.ts:378:    defenderCommanderId: defenderCommander.activeCommanderId,
src/simulation/combat/resolveAttackMission.ts:379:    commanderRecoveredShips: {
src/simulation/combat/resolveAttackMission.ts:380:      attacker: recoveredDelta(resolution.attackerRemaining, attackerAfterCommanderRecovery),
src/simulation/combat/resolveAttackMission.ts:381:      defender: recoveredDelta(resolution.defenderRemaining, defenderAfterCommanderRecovery),
src/simulation/combat/resolveAttackMission.ts:401:      fleets,
src/simulation/combat/resolveAttackMission.ts:403:      commanders: awardBattleCommandExperience(state.commanders, report),
src/simulation/combat/resolveAttackMission.ts:405:    report,
src/simulation/combat/resolveAttackMission.ts:406:    attackerFleet: updatedAttacker,
src/simulation/combat/resolveBattle.ts:12:  type FleetFormation,
src/simulation/combat/resolveBattle.ts:13:  type FleetTargetPriority,
src/simulation/combat/resolveBattle.ts:14:} from './fleetDoctrine';
src/simulation/combat/resolveBattle.ts:17:  BattleRoundReport,
src/simulation/combat/resolveBattle.ts:19:  BattleTargetDamageReport,
src/simulation/combat/resolveBattle.ts:20:  BattleWeaponContributionReport,
src/simulation/combat/resolveBattle.ts:35:  readonly breakdown: readonly BattleTargetDamageReport[];
src/simulation/combat/resolveBattle.ts:46:  readonly formation: FleetFormation;
src/simulation/combat/resolveBattle.ts:47:  readonly targetPriority: FleetTargetPriority;
src/simulation/combat/resolveBattle.ts:153:  readonly reports: readonly BattleWeaponContributionReport[];
src/simulation/combat/resolveBattle.ts:158:    return { weightedModifierPermille: 1_000, reports: [] };
src/simulation/combat/resolveBattle.ts:167:  const reports = contributions.map((contribution): BattleWeaponContributionReport => {
src/simulation/combat/resolveBattle.ts:180:    reports.reduce(
src/simulation/combat/resolveBattle.ts:181:      (sum, report) => sum + report.baseDamage * report.modifierPermille,
src/simulation/combat/resolveBattle.ts:185:  return { weightedModifierPermille, reports };
src/simulation/combat/resolveBattle.ts:195:  targetPriority: FleetTargetPriority,
src/simulation/combat/resolveBattle.ts:202:  const breakdown: BattleTargetDamageReport[] = [];
src/simulation/combat/resolveBattle.ts:285:      weaponContributions: weaponBreakdown.reports,
src/simulation/combat/resolveBattle.ts:322:  const rounds: BattleRoundReport[] = [];
src/simulation/combat/shipAbilities.ts:13:function scaledFleetBonus(count: number, valuePerUnitPermille: number, maxPercent: number): number {
src/simulation/combat/shipAbilities.ts:29:    const fleetBonus = scaledFleetBonus(
src/simulation/combat/shipAbilities.ts:37:        addBonus(weapon, unitId, Math.max(8, fleetBonus));
src/simulation/combat/shipAbilities.ts:40:        addBonus(weapon, unitId, Math.max(12, fleetBonus));
src/simulation/combat/shipAbilities.ts:42:      case 'fleet-vitality':
src/simulation/combat/shipAbilities.ts:43:      case 'fleet-armor':
src/simulation/combat/shipAbilities.ts:45:        for (const friendlyId of activeIds) addBonus(armor, friendlyId, fleetBonus);
src/simulation/combat/shipAbilities.ts:48:        for (const friendlyId of activeIds) addBonus(weapon, friendlyId, fleetBonus);
src/simulation/combat/shipAbilities.ts:51:        addBonus(weapon, unitId, Math.max(10, fleetBonus));
src/simulation/combat/shipAbilities.ts:52:        for (const friendlyId of activeIds) addBonus(armor, friendlyId, Math.ceil(fleetBonus / 2));
src/simulation/combat/shipAbilities.ts:55:        addBonus(weapon, unitId, Math.max(20, fleetBonus));
src/simulation/combat/shipAbilities.ts:58:        addBonus(weapon, unitId, Math.max(15, fleetBonus));
src/simulation/combat/types.ts:8:import type { FleetFormation, FleetTargetPriority } from './fleetDoctrine';
src/simulation/combat/types.ts:20:  readonly formation?: FleetFormation;
src/simulation/combat/types.ts:21:  readonly targetPriority?: FleetTargetPriority;
src/simulation/combat/types.ts:24:export interface BattleWeaponContributionReport {
src/simulation/combat/types.ts:30:export interface BattleTargetDamageReport {
src/simulation/combat/types.ts:42:  readonly weaponContributions: readonly BattleWeaponContributionReport[];
src/simulation/combat/types.ts:45:export interface BattleRoundReport {
src/simulation/combat/types.ts:51:  readonly attackerTargetBreakdown: readonly BattleTargetDamageReport[];
src/simulation/combat/types.ts:52:  readonly defenderTargetBreakdown: readonly BattleTargetDamageReport[];
src/simulation/combat/types.ts:55:export interface BattleReport {
src/simulation/combat/types.ts:59:  readonly targetPlanetId: string;
src/simulation/combat/types.ts:63:  readonly rounds: readonly BattleRoundReport[];
src/simulation/combat/types.ts:68:  readonly attackerCommanderId?: string | null;
src/simulation/combat/types.ts:69:  readonly defenderCommanderId?: string | null;
src/simulation/combat/types.ts:70:  readonly commanderRecoveredShips?: {
src/simulation/combat/types.ts:80:  readonly attackerFormation?: FleetFormation;
src/simulation/combat/types.ts:81:  readonly attackerTargetPriority?: FleetTargetPriority;
src/simulation/combat/types.ts:82:  readonly defenderFormation?: FleetFormation;
src/simulation/combat/types.ts:83:  readonly defenderTargetPriority?: FleetTargetPriority;
src/simulation/combat/types.ts:88:  readonly rounds: readonly BattleRoundReport[];
src/simulation/command/commandDoctrine.ts:1:import { appendCommandHistory } from '../history/stateHistory';
src/simulation/command/commandDoctrine.ts:2:import type { BattleReport } from '../combat/types';
src/simulation/command/commandDoctrine.ts:3:import type { GameCommand, GameState } from '../types';
src/simulation/command/commandDoctrine.ts:6:  CommandCombatEffects,
src/simulation/command/commandDoctrine.ts:7:  CommandDoctrineId,
src/simulation/command/commandDoctrine.ts:8:  EmpireCommandState,
src/simulation/command/commandDoctrine.ts:11:export interface CommandDoctrineDefinition {
src/simulation/command/commandDoctrine.ts:12:  readonly id: CommandDoctrineId;
src/simulation/command/commandDoctrine.ts:22:function createCommandLevelThresholds(): readonly number[] {
src/simulation/command/commandDoctrine.ts:32:export const COMMAND_LEVEL_THRESHOLDS = createCommandLevelThresholds();
src/simulation/command/commandDoctrine.ts:37:  Record<CommandDoctrineId, CommandDoctrineDefinition>
src/simulation/command/commandDoctrine.ts:65:export function isCommandDoctrineId(value: unknown): value is CommandDoctrineId {
src/simulation/command/commandDoctrine.ts:69:export function calculateCommandLevel(experience: number): number {
src/simulation/command/commandDoctrine.ts:78:export function createInitialCommandStates(
src/simulation/command/commandDoctrine.ts:80:): readonly EmpireCommandState[] {
src/simulation/command/commandDoctrine.ts:86:    flagshipFleetId: null,
src/simulation/command/commandDoctrine.ts:90:export function getEmpireCommandState(
src/simulation/command/commandDoctrine.ts:91:  states: readonly EmpireCommandState[],
src/simulation/command/commandDoctrine.ts:93:): EmpireCommandState | undefined {
src/simulation/command/commandDoctrine.ts:97:function replaceCommandState(
src/simulation/command/commandDoctrine.ts:98:  states: readonly EmpireCommandState[],
src/simulation/command/commandDoctrine.ts:99:  replacement: EmpireCommandState,
src/simulation/command/commandDoctrine.ts:100:): readonly EmpireCommandState[] {
src/simulation/command/commandDoctrine.ts:106:function appendCommand(state: GameState, command: GameCommand): GameState['commandLog'] {
src/simulation/command/commandDoctrine.ts:107:  return appendCommandHistory(state.commandLog, command);
src/simulation/command/commandDoctrine.ts:110:export function addCommandExperience(
src/simulation/command/commandDoctrine.ts:111:  states: readonly EmpireCommandState[],
src/simulation/command/commandDoctrine.ts:114:): readonly EmpireCommandState[] {
src/simulation/command/commandDoctrine.ts:115:  const current = getEmpireCommandState(states, empireId);
src/simulation/command/commandDoctrine.ts:120:  return replaceCommandState(states, {
src/simulation/command/commandDoctrine.ts:123:    level: calculateCommandLevel(experience),
src/simulation/command/commandDoctrine.ts:127:export function awardBattleCommandExperience(
src/simulation/command/commandDoctrine.ts:128:  states: readonly EmpireCommandState[],
src/simulation/command/commandDoctrine.ts:129:  report: BattleReport,
src/simulation/command/commandDoctrine.ts:130:): readonly EmpireCommandState[] {
src/simulation/command/commandDoctrine.ts:131:  const rounds = Math.max(1, report.rounds.length);
src/simulation/command/commandDoctrine.ts:132:  const attackerBase = 18 + rounds * 4 + (report.winner === 'attacker' ? 24 : 8);
src/simulation/command/commandDoctrine.ts:133:  const defenderBase = 18 + rounds * 4 + (report.winner === 'defender' ? 24 : 8);
src/simulation/command/commandDoctrine.ts:134:  return addCommandExperience(
src/simulation/command/commandDoctrine.ts:135:    addCommandExperience(states, report.attackerEmpireId, attackerBase),
src/simulation/command/commandDoctrine.ts:136:    report.defenderEmpireId,
src/simulation/command/commandDoctrine.ts:141:export function setCommandDoctrine(
src/simulation/command/commandDoctrine.ts:143:  command: Extract<GameCommand, { readonly type: 'SET_COMMAND_DOCTRINE' }>,
src/simulation/command/commandDoctrine.ts:145:  const current = getEmpireCommandState(state.commanders, command.empireId);
src/simulation/command/commandDoctrine.ts:147:    return { ok: false, code: 'COMMAND_STATE_NOT_FOUND', message: 'Command profile is unavailable.' } as const;
src/simulation/command/commandDoctrine.ts:153:      commanders: replaceCommandState(state.commanders, {
src/simulation/command/commandDoctrine.ts:155:        doctrineId: command.doctrineId,
src/simulation/command/commandDoctrine.ts:157:      commandLog: appendCommand(state, command),
src/simulation/command/commandDoctrine.ts:164:  command: Extract<GameCommand, { readonly type: 'ASSIGN_FLAGSHIP' }>,
src/simulation/command/commandDoctrine.ts:166:  const current = getEmpireCommandState(state.commanders, command.empireId);
src/simulation/command/commandDoctrine.ts:168:    return { ok: false, code: 'COMMAND_STATE_NOT_FOUND', message: 'Command profile is unavailable.' } as const;
src/simulation/command/commandDoctrine.ts:170:  if (command.fleetId === null) {
src/simulation/command/commandDoctrine.ts:175:        commanders: replaceCommandState(state.commanders, {
src/simulation/command/commandDoctrine.ts:177:          flagshipFleetId: null,
src/simulation/command/commandDoctrine.ts:179:        commandLog: appendCommand(state, command),
src/simulation/command/commandDoctrine.ts:187:      message: `Command level ${FLAGSHIP_UNLOCK_LEVEL} is required to appoint a flagship.`,
src/simulation/command/commandDoctrine.ts:190:  const fleet = state.fleets.find((candidate) => candidate.id === command.fleetId);
src/simulation/command/commandDoctrine.ts:192:    fleet === undefined ||
src/simulation/command/commandDoctrine.ts:193:    fleet.empireId !== command.empireId ||
src/simulation/command/commandDoctrine.ts:194:    fleet.status !== 'stationed' ||
src/simulation/command/commandDoctrine.ts:195:    fleet.location.type !== 'planet'
src/simulation/command/commandDoctrine.ts:197:    return { ok: false, code: 'FLAGSHIP_FLEET_UNAVAILABLE', message: 'Flagship fleet must be owned and stationed.' } as const;
src/simulation/command/commandDoctrine.ts:199:  const hasArmedShip = Object.entries(fleet.ships).some(
src/simulation/command/commandDoctrine.ts:203:    return { ok: false, code: 'FLAGSHIP_ARMED_SHIP_REQUIRED', message: 'Flagship fleet requires an armed ship.' } as const;
src/simulation/command/commandDoctrine.ts:209:      commanders: replaceCommandState(state.commanders, {
src/simulation/command/commandDoctrine.ts:211:        flagshipFleetId: fleet.id,
src/simulation/command/commandDoctrine.ts:213:      commandLog: appendCommand(state, command),
src/simulation/command/commandDoctrine.ts:218:export function getCommandCombatEffects(
src/simulation/command/commandDoctrine.ts:219:  states: readonly EmpireCommandState[],
src/simulation/command/commandDoctrine.ts:221:  fleetId?: string,
src/simulation/command/commandDoctrine.ts:222:): CommandCombatEffects {
src/simulation/command/commandDoctrine.ts:223:  const state = getEmpireCommandState(states, empireId);
src/simulation/command/commandDoctrine.ts:234:  const isFlagship = fleetId !== undefined && state.flagshipFleetId === fleetId;
src/simulation/command/commanderShips.ts:1:import type { FleetState } from '../fleets/types';
src/simulation/command/commanderShips.ts:3:import { getCompleteCommanderShipDefinition } from '../units/completeCommanderShipCatalog';
src/simulation/command/commanderShips.ts:5:import type { CommanderAbilityEffect, UnitDefinition } from '../units/types';
src/simulation/command/commanderShips.ts:6:import { getEmpireCommandState } from './commandDoctrine';
src/simulation/command/commanderShips.ts:7:import type { EmpireCommandState } from './types';
src/simulation/command/commanderShips.ts:9:export interface ActiveCommanderShip {
src/simulation/command/commanderShips.ts:15:export interface CommanderFleetEffects {
src/simulation/command/commanderShips.ts:16:  readonly activeCommanderId: string | null;
src/simulation/command/commanderShips.ts:17:  readonly activeEffect: CommanderAbilityEffect | null;
src/simulation/command/commanderShips.ts:33:const NO_COMMANDER_EFFECTS: CommanderFleetEffects = {
src/simulation/command/commanderShips.ts:34:  activeCommanderId: null,
src/simulation/command/commanderShips.ts:60:export function calculateCommanderAbilityLevel(
src/simulation/command/commanderShips.ts:61:  commandLevel: number,
src/simulation/command/commanderShips.ts:65:  const maximum = definition.commanderAbility?.maximumLevel ?? 0;
src/simulation/command/commanderShips.ts:66:  if (commandLevel < required || maximum <= 0) return 0;
src/simulation/command/commanderShips.ts:67:  return Math.min(maximum, 1 + Math.floor((commandLevel - required) / 4));
src/simulation/command/commanderShips.ts:70:export function findCommanderShipIds(
src/simulation/command/commanderShips.ts:74:    .filter(([unitId, quantity]) => quantity > 0 && getUnitDefinition(unitId)?.commanderClass !== undefined)
src/simulation/command/commanderShips.ts:79:export function selectActiveCommanderShip(
src/simulation/command/commanderShips.ts:80:  commandState: EmpireCommandState | undefined,
src/simulation/command/commanderShips.ts:81:  fleetId: string | undefined,
src/simulation/command/commanderShips.ts:83:): ActiveCommanderShip | undefined {
src/simulation/command/commanderShips.ts:84:  if (commandState === undefined || fleetId === undefined) return undefined;
src/simulation/command/commanderShips.ts:86:    commandState.flagshipFleetId !== null &&
src/simulation/command/commanderShips.ts:87:    commandState.flagshipFleetId !== fleetId
src/simulation/command/commanderShips.ts:92:  return findCommanderShipIds(units)
src/simulation/command/commanderShips.ts:94:      const definition = getCompleteCommanderShipDefinition(unitId);
src/simulation/command/commanderShips.ts:95:      if (definition === undefined || definition.commanderAbility === undefined) return undefined;
src/simulation/command/commanderShips.ts:96:      const level = calculateCommanderAbilityLevel(commandState.level, definition);
src/simulation/command/commanderShips.ts:99:    .filter((candidate): candidate is ActiveCommanderShip => candidate !== undefined)
src/simulation/command/commanderShips.ts:101:      left.definition.commanderAbility!.battlePriority -
src/simulation/command/commanderShips.ts:102:        right.definition.commanderAbility!.battlePriority ||
src/simulation/command/commanderShips.ts:111:export function getCommanderFleetEffects(
src/simulation/command/commanderShips.ts:112:  state: Pick<GameState, 'commanders'>,
src/simulation/command/commanderShips.ts:113:  empireOrFleet: string | Pick<FleetState, 'id' | 'empireId' | 'ships'>,
src/simulation/command/commanderShips.ts:114:  fleetId?: string,
src/simulation/command/commanderShips.ts:116:): CommanderFleetEffects {
src/simulation/command/commanderShips.ts:117:  const empireId = typeof empireOrFleet === 'string' ? empireOrFleet : empireOrFleet.empireId;
src/simulation/command/commanderShips.ts:118:  const resolvedFleetId = typeof empireOrFleet === 'string' ? fleetId : empireOrFleet.id;
src/simulation/command/commanderShips.ts:119:  const resolvedUnits = typeof empireOrFleet === 'string' ? units : empireOrFleet.ships;
src/simulation/command/commanderShips.ts:120:  const active = selectActiveCommanderShip(
src/simulation/command/commanderShips.ts:121:    getEmpireCommandState(state.commanders, empireId),
src/simulation/command/commanderShips.ts:122:    resolvedFleetId,
src/simulation/command/commanderShips.ts:125:  const ability = active?.definition.commanderAbility;
src/simulation/command/commanderShips.ts:132:    activeCommanderId: active.unitId,
src/simulation/command/commanderShips.ts:169:export function getFleetCommanderEffects(
src/simulation/command/commanderShips.ts:170:  state: Pick<GameState, 'commanders'>,
src/simulation/command/commanderShips.ts:171:  fleet: Pick<FleetState, 'id' | 'empireId' | 'ships'>,
src/simulation/command/commanderShips.ts:172:): CommanderFleetEffects {
src/simulation/command/commanderShips.ts:173:  return getCommanderFleetEffects(state, fleet);
src/simulation/command/commanderShips.ts:176:export function countCommanderShipForEmpire(
src/simulation/command/commanderShips.ts:177:  state: Pick<GameState, 'planets' | 'fleets'>,
src/simulation/command/commanderShips.ts:179:  commanderId: string,
src/simulation/command/commanderShips.ts:185:      (planet.inventory.ships[commanderId] ?? 0) +
src/simulation/command/commanderShips.ts:187:        (queued, item) => queued + (item.unitId === commanderId ? item.quantity : 0),
src/simulation/command/commanderShips.ts:190:  return state.fleets
src/simulation/command/commanderShips.ts:191:    .filter((fleet) => fleet.empireId === empireId)
src/simulation/command/commanderShips.ts:192:    .reduce((total, fleet) => total + (fleet.ships[commanderId] ?? 0), planets);
src/simulation/command/commanderShips.ts:195:export function recoverFleetShipsWithCommander(
src/simulation/command/commanderShips.ts:205:    if (definition?.kind !== 'ship' || definition.commanderClass !== undefined) continue;
src/simulation/command/commanderShips.ts:211:    const bonus = hashText(`${seed}:${unitId}:commander-recovery`) % 1_000 < remainder ? 1 : 0;
src/simulation/command/types.ts:1:export type CommandDoctrineId = 'vanguard' | 'sentinel' | 'adaptive';
src/simulation/command/types.ts:3:export interface EmpireCommandState {
src/simulation/command/types.ts:5:  readonly doctrineId: CommandDoctrineId;
src/simulation/command/types.ts:8:  readonly flagshipFleetId: string | null;
src/simulation/command/types.ts:11:export interface CommandCombatEffects {
src/simulation/createInitialGameState.ts:2:import { createInitialCommandStates } from './command/commandDoctrine';
src/simulation/createInitialGameState.ts:3:import { createInitialIntelligenceStates } from './intelligence/intelligenceState';
src/simulation/createInitialGameState.ts:47:    commanders: createInitialCommandStates(empires),
src/simulation/createInitialGameState.ts:48:    fleets: neutralForces.fleets,
src/simulation/createInitialGameState.ts:49:    intelligence: createInitialIntelligenceStates(empires),
src/simulation/createInitialGameState.ts:59:    commandLog: [],
src/simulation/defense/planetaryDefense.ts:1:import { appendCommandHistory } from '../history/stateHistory';
src/simulation/defense/planetaryDefense.ts:13:  CommandLogEntry,
src/simulation/defense/planetaryDefense.ts:14:  CommandResult,
src/simulation/defense/planetaryDefense.ts:15:  GameCommand,
src/simulation/defense/planetaryDefense.ts:29:function appendCommand(state: GameState, command: GameCommand): readonly CommandLogEntry[] {
src/simulation/defense/planetaryDefense.ts:30:  return appendCommandHistory(state.commandLog, command);
src/simulation/defense/planetaryDefense.ts:157:  command: Extract<GameCommand, { readonly type: 'QUEUE_DEFENSE_REPAIR' }>,
src/simulation/defense/planetaryDefense.ts:158:): CommandResult<GameState> {
src/simulation/defense/planetaryDefense.ts:159:  if (!Number.isInteger(command.quantity) || command.quantity <= 0 || command.quantity > 100) {
src/simulation/defense/planetaryDefense.ts:166:  const planet = state.planets.find((candidate) => candidate.id === command.planetId);
src/simulation/defense/planetaryDefense.ts:167:  if (planet === undefined || planet.ownerEmpireId !== command.empireId) {
src/simulation/defense/planetaryDefense.ts:173:  const definition = getDefenseDefinition(command.unitId);
src/simulation/defense/planetaryDefense.ts:181:  if (command.quantity > damagedAvailable) {
src/simulation/defense/planetaryDefense.ts:186:      details: { requested: command.quantity, available: damagedAvailable },
src/simulation/defense/planetaryDefense.ts:189:  const cost = calculateDefenseRepairCost(definition, command.quantity);
src/simulation/defense/planetaryDefense.ts:196:    calculateDefenseRepairSeconds(definition, command.quantity);
src/simulation/defense/planetaryDefense.ts:200:    quantity: command.quantity,
src/simulation/defense/planetaryDefense.ts:214:      quantity: command.quantity,
src/simulation/defense/planetaryDefense.ts:229:      commandLog: appendCommand(state, command),
src/simulation/defense/planetaryDefense.ts:236:  command: Extract<GameCommand, { readonly type: 'CANCEL_DEFENSE_REPAIR' }>,
src/simulation/defense/planetaryDefense.ts:237:): CommandResult<GameState> {
src/simulation/defense/planetaryDefense.ts:238:  const planet = state.planets.find((candidate) => candidate.id === command.planetId);
src/simulation/defense/planetaryDefense.ts:239:  if (planet === undefined || planet.ownerEmpireId !== command.empireId) {
src/simulation/defense/planetaryDefense.ts:242:  const item = planet.defense.repairQueue.find((candidate) => candidate.id === command.queueItemId);
src/simulation/defense/planetaryDefense.ts:262:      commandLog: appendCommand(state, command),
src/simulation/executeGameCommand.ts:1:export { executeCommand as executeGameCommand } from './reducer';
src/simulation/factions/completeCatalogTargets.ts:8:  sharedCommanderShips: 13,
src/simulation/factions/completeCatalogTargets.ts:16:  | 'commanderShips';
src/simulation/factions/completeCatalogTargets.ts:24:  | 'commander-ships'
src/simulation/factions/completeCatalogTargets.ts:32:  readonly commanderShips: number;
src/simulation/factions/completeCatalogTargets.ts:52:    commanderShips: COMPLETE_CATALOG_TARGETS.sharedCommanderShips,
src/simulation/factions/completeCatalogTargets.ts:59:    commanderShips: 'commander.shared.annihilator',
src/simulation/factions/completeCatalogTargets.ts:159:export function validateCompleteCatalogTargetManifest(
src/simulation/factions/completeCatalogTargets.ts:168:    commanderShips: COMPLETE_COMMANDER_ROLES.length,
src/simulation/factions/factionMechanicalCatalogRegistry.ts:10:import { COMPLETE_COMMANDER_SHIP_CATALOG } from '../units/completeCommanderShipCatalog';
src/simulation/factions/factionMechanicalCatalogRegistry.ts:115:export function getCommanderShipCatalog(): readonly UnitDefinition[] {
src/simulation/factions/factionMechanicalCatalogRegistry.ts:128:    commanderShips: COMPLETE_COMMANDER_SHIP_CATALOG.length,
src/simulation/factions/factionMechanicalCatalogRegistry.ts:135:    commanderShips: COMPLETE_CATALOG_TARGETS.sharedCommanderShips,
src/simulation/factions/factionMechanicalCatalogRegistry.ts:202:export function validateFactionMechanicalCatalog(
src/simulation/factions/factionMechanicalRoles.ts:10:    readonly command: string;
src/simulation/factions/factionMechanicalRoles.ts:62:    command: complete.government,
src/simulation/factions/factionMechanicalRoles.ts:153:    { buildingId: buildings.command, level: 1 },
src/simulation/factions/factionResearchEffects.ts:10:  fleetSpeedPercent: 0,
src/simulation/factions/mechanicalIds.ts:8:  | 'commander';
src/simulation/factions/mechanicalIds.ts:33:  'commander',
src/simulation/factions/synodMechanicalCatalog.ts:255:    category: 'intelligence',
src/simulation/factions/synodMechanicalCatalog.ts:362:    id: 'technology.synod.chorus-command',
src/simulation/factions/synodMechanicalCatalog.ts:376:    assetId: 'technology.synod.chorus-command',
src/simulation/factions/synodMechanicalCatalog.ts:554:      { technologyId: 'technology.synod.chorus-command', level: 2 },
src/simulation/factions/synodMechanicalCatalog.ts:671:      { technologyId: 'technology.synod.chorus-command', level: 2 },
src/simulation/factions/veyraMechanicalCatalog.ts:255:    category: 'intelligence',
src/simulation/fleets/fleetCalculations.ts:4:export interface FleetCompositionSummary {
src/simulation/fleets/fleetCalculations.ts:10:export function validateShipComposition(
src/simulation/fleets/fleetCalculations.ts:31:export function calculateFleetComposition(
src/simulation/fleets/fleetCalculations.ts:33:): FleetCompositionSummary {
src/simulation/fleets/fleetCalculations.ts:34:  const errors = validateShipComposition(ships);
src/simulation/fleets/fleetCalculations.ts:36:    throw new Error(`Invalid fleet composition: ${errors.join(', ')}`);
src/simulation/fleets/fleetCalculations.ts:62:export function validateCargo(cargo: ResourceCost): readonly string[] {
src/simulation/fleets/fleetCommands.ts:1:import { appendCommandHistory } from '../history/stateHistory';
src/simulation/fleets/fleetCommands.ts:5:  CommandLogEntry,
src/simulation/fleets/fleetCommands.ts:6:  CommandResult,
src/simulation/fleets/fleetCommands.ts:7:  GameCommand,
src/simulation/fleets/fleetCommands.ts:12:  calculateFleetComposition,
src/simulation/fleets/fleetCommands.ts:14:  validateCargo,
src/simulation/fleets/fleetCommands.ts:15:  validateShipComposition,
src/simulation/fleets/fleetCommands.ts:16:} from './fleetCalculations';
src/simulation/fleets/fleetCommands.ts:17:import type { FleetState } from './types';
src/simulation/fleets/fleetCommands.ts:21:function appendCommand(state: GameState, command: GameCommand): readonly CommandLogEntry[] {
src/simulation/fleets/fleetCommands.ts:22:  return appendCommandHistory(state.commandLog, command);
src/simulation/fleets/fleetCommands.ts:56:export function createFleet(
src/simulation/fleets/fleetCommands.ts:58:  command: Extract<GameCommand, { readonly type: 'CREATE_FLEET' }>,
src/simulation/fleets/fleetCommands.ts:59:): CommandResult<GameState> {
src/simulation/fleets/fleetCommands.ts:60:  const planet = state.planets.find((candidate) => candidate.id === command.planetId);
src/simulation/fleets/fleetCommands.ts:62:    return { ok: false, code: 'PLANET_NOT_FOUND', message: 'Fleet planet not found.' };
src/simulation/fleets/fleetCommands.ts:64:  if (planet.ownerEmpireId !== command.empireId) {
src/simulation/fleets/fleetCommands.ts:65:    return { ok: false, code: 'NOT_PLANET_OWNER', message: 'Empire does not own the fleet planet.' };
src/simulation/fleets/fleetCommands.ts:68:  const compositionErrors = validateShipComposition(command.ships);
src/simulation/fleets/fleetCommands.ts:69:  const cargoErrors = validateCargo(command.cargo);
src/simulation/fleets/fleetCommands.ts:74:      message: 'Fleet composition or cargo is invalid.',
src/simulation/fleets/fleetCommands.ts:79:  for (const [unitId, quantity] of Object.entries(command.ships)) {
src/simulation/fleets/fleetCommands.ts:84:        message: 'Planet does not have enough ships for the fleet.',
src/simulation/fleets/fleetCommands.ts:91:        planet.economy.resources[resourceId].amount < command.cargo[resourceId],
src/simulation/fleets/fleetCommands.ts:97:      message: 'Planet does not have enough resources for fleet cargo.',
src/simulation/fleets/fleetCommands.ts:101:  const composition = calculateFleetComposition(command.ships);
src/simulation/fleets/fleetCommands.ts:104:    command.empireId,
src/simulation/fleets/fleetCommands.ts:105:    command.ships,
src/simulation/fleets/fleetCommands.ts:108:  if (getCargoAmount(command.cargo) > cargoCapacity) {
src/simulation/fleets/fleetCommands.ts:112:      message: 'Fleet cargo exceeds its upgraded capacity.',
src/simulation/fleets/fleetCommands.ts:117:  for (const [unitId, quantity] of Object.entries(command.ships)) {
src/simulation/fleets/fleetCommands.ts:123:  const fleet: FleetState = {
src/simulation/fleets/fleetCommands.ts:124:    id: `fleet-${state.nextEventSequence}`,
src/simulation/fleets/fleetCommands.ts:125:    empireId: command.empireId,
src/simulation/fleets/fleetCommands.ts:129:    ships: { ...command.ships },
src/simulation/fleets/fleetCommands.ts:130:    cargo: { ...command.cargo },
src/simulation/fleets/fleetCommands.ts:135:    mission: null,
src/simulation/fleets/fleetCommands.ts:140:    economy: updateCargo(planet.economy, command.cargo, -1),
src/simulation/fleets/fleetCommands.ts:148:      fleets: [...state.fleets, fleet],
src/simulation/fleets/fleetCommands.ts:150:      commandLog: appendCommand(state, command),
src/simulation/fleets/fleetCommands.ts:155:export function disbandFleet(
src/simulation/fleets/fleetCommands.ts:157:  command: Extract<GameCommand, { readonly type: 'DISBAND_FLEET' }>,
src/simulation/fleets/fleetCommands.ts:158:): CommandResult<GameState> {
src/simulation/fleets/fleetCommands.ts:159:  const fleet = state.fleets.find((candidate) => candidate.id === command.fleetId);
src/simulation/fleets/fleetCommands.ts:160:  if (fleet === undefined) {
src/simulation/fleets/fleetCommands.ts:161:    return { ok: false, code: 'FLEET_NOT_FOUND', message: 'Fleet not found.' };
src/simulation/fleets/fleetCommands.ts:163:  if (fleet.empireId !== command.empireId) {
src/simulation/fleets/fleetCommands.ts:164:    return { ok: false, code: 'NOT_FLEET_OWNER', message: 'Empire does not own the fleet.' };
src/simulation/fleets/fleetCommands.ts:166:  if (fleet.status !== 'stationed' || fleet.location.type !== 'planet') {
src/simulation/fleets/fleetCommands.ts:167:    return { ok: false, code: 'FLEET_NOT_STATIONED', message: 'Only a stationed fleet can be disbanded.' };
src/simulation/fleets/fleetCommands.ts:170:  const fleetPlanetId = fleet.location.planetId;
src/simulation/fleets/fleetCommands.ts:171:  const planet = state.planets.find((candidate) => candidate.id === fleetPlanetId);
src/simulation/fleets/fleetCommands.ts:172:  if (planet === undefined || planet.ownerEmpireId !== command.empireId) {
src/simulation/fleets/fleetCommands.ts:173:    return { ok: false, code: 'FLEET_PLANET_NOT_AVAILABLE', message: 'Fleet planet is unavailable.' };
src/simulation/fleets/fleetCommands.ts:178:        planet.economy.resources[resourceId].amount + fleet.cargo[resourceId] >
src/simulation/fleets/fleetCommands.ts:185:      message: 'Planet storage cannot accept fleet cargo.',
src/simulation/fleets/fleetCommands.ts:190:  for (const [unitId, quantity] of Object.entries(fleet.ships)) {
src/simulation/fleets/fleetCommands.ts:196:    economy: updateCargo(planet.economy, fleet.cargo, 1),
src/simulation/fleets/fleetCommands.ts:204:      fleets: state.fleets.filter((candidate) => candidate.id !== fleet.id),
src/simulation/fleets/fleetCommands.ts:205:      commandLog: appendCommand(state, command),
src/simulation/fleets/fleetDoctrineCommands.ts:1:import { appendCommandHistory } from '../history/stateHistory';
src/simulation/fleets/fleetDoctrineCommands.ts:3:  isFleetFormation,
src/simulation/fleets/fleetDoctrineCommands.ts:4:  isFleetTargetPriority,
src/simulation/fleets/fleetDoctrineCommands.ts:5:} from '../combat/fleetDoctrine';
src/simulation/fleets/fleetDoctrineCommands.ts:7:  CommandLogEntry,
src/simulation/fleets/fleetDoctrineCommands.ts:8:  CommandResult,
src/simulation/fleets/fleetDoctrineCommands.ts:9:  GameCommand,
src/simulation/fleets/fleetDoctrineCommands.ts:13:function appendCommand(state: GameState, command: GameCommand): readonly CommandLogEntry[] {
src/simulation/fleets/fleetDoctrineCommands.ts:14:  return appendCommandHistory(state.commandLog, command);
src/simulation/fleets/fleetDoctrineCommands.ts:17:export function setFleetCombatDoctrine(
src/simulation/fleets/fleetDoctrineCommands.ts:19:  command: Extract<GameCommand, { readonly type: 'SET_FLEET_COMBAT_DOCTRINE' }>,
src/simulation/fleets/fleetDoctrineCommands.ts:20:): CommandResult<GameState> {
src/simulation/fleets/fleetDoctrineCommands.ts:21:  const fleet = state.fleets.find((candidate) => candidate.id === command.fleetId);
src/simulation/fleets/fleetDoctrineCommands.ts:22:  if (fleet === undefined) {
src/simulation/fleets/fleetDoctrineCommands.ts:23:    return { ok: false, code: 'FLEET_NOT_FOUND', message: 'Fleet not found.' };
src/simulation/fleets/fleetDoctrineCommands.ts:25:  if (fleet.empireId !== command.empireId) {
src/simulation/fleets/fleetDoctrineCommands.ts:26:    return { ok: false, code: 'NOT_FLEET_OWNER', message: 'Empire does not own the fleet.' };
src/simulation/fleets/fleetDoctrineCommands.ts:28:  if (fleet.status !== 'stationed' || fleet.location.type !== 'planet') {
src/simulation/fleets/fleetDoctrineCommands.ts:32:      message: 'Combat doctrine can only be changed while the fleet is stationed.',
src/simulation/fleets/fleetDoctrineCommands.ts:35:  if (!isFleetFormation(command.formation) || !isFleetTargetPriority(command.targetPriority)) {
src/simulation/fleets/fleetDoctrineCommands.ts:39:      message: 'Fleet formation or target priority is invalid.',
src/simulation/fleets/fleetDoctrineCommands.ts:46:      fleets: state.fleets.map((candidate) =>
src/simulation/fleets/fleetDoctrineCommands.ts:47:        candidate.id === fleet.id
src/simulation/fleets/fleetDoctrineCommands.ts:50:              formation: command.formation,
src/simulation/fleets/fleetDoctrineCommands.ts:51:              targetPriority: command.targetPriority,
src/simulation/fleets/fleetDoctrineCommands.ts:55:      commandLog: appendCommand(state, command),
src/simulation/fleets/flightCalculations.ts:8:import { calculateFleetComposition } from './fleetCalculations';
src/simulation/fleets/flightCalculations.ts:9:import type { FleetState } from './types';
src/simulation/fleets/flightCalculations.ts:37:  targetSystemId: string,
src/simulation/fleets/flightCalculations.ts:40:  const parsedTarget = parseSystemCoordinate(targetSystemId);
src/simulation/fleets/flightCalculations.ts:41:  const targetSystem = parsedTarget === undefined ? requireSystem(galaxy, targetSystemId) : undefined;
src/simulation/fleets/flightCalculations.ts:74:    throw new Error('Fleet speed must be a positive integer.');
src/simulation/fleets/flightCalculations.ts:83:export function calculateFlightFuel(distance: number, fleet: FleetState): number {
src/simulation/fleets/flightCalculations.ts:86:    Math.ceil((distance * calculateFleetComposition(fleet.ships).shipCount) / 25),
src/simulation/fleets/flightCalculations.ts:92:  fleet: FleetState,
src/simulation/fleets/flightCalculations.ts:97:    durationSeconds: calculateFlightDuration(distance, fleet.speed, speedBonusPercent),
src/simulation/fleets/flightCalculations.ts:98:    fuelCost: calculateFlightFuel(distance, fleet),
src/simulation/fleets/flightCalculations.ts:105:  fleet: FleetState,
src/simulation/fleets/flightCalculations.ts:106:  targetPlanetId: string,
src/simulation/fleets/flightCalculations.ts:109:  const location = fleet.location;
src/simulation/fleets/flightCalculations.ts:111:    throw new Error('Only a stationed fleet can estimate a new flight.');
src/simulation/fleets/flightCalculations.ts:114:  const target = planets.find((planet) => planet.id === targetPlanetId);
src/simulation/fleets/flightCalculations.ts:120:    fleet,
src/simulation/fleets/flightCalculations.ts:128:  fleet: FleetState,
src/simulation/fleets/flightCalculations.ts:132:  const location = fleet.location;
src/simulation/fleets/flightCalculations.ts:134:    throw new Error('Only a stationed fleet can estimate a new flight.');
src/simulation/fleets/flightCalculations.ts:143:    fleet,
src/simulation/fleets/flightCommands.ts:1:import { getCommanderFleetEffects } from '../command/commanderShips';
src/simulation/fleets/flightCommands.ts:2:import { appendCommandHistory } from '../history/stateHistory';
src/simulation/fleets/flightCommands.ts:4:import { resolveAttackMission } from '../combat/resolveAttackMission';
src/simulation/fleets/flightCommands.ts:5:import type { BattleReport } from '../combat/types';
src/simulation/fleets/flightCommands.ts:7:  findFleetShipByRole,
src/simulation/fleets/flightCommands.ts:17:import { resolveScoutArrival } from '../intelligence/resolveScout';
src/simulation/fleets/flightCommands.ts:22:  CommandLogEntry,
src/simulation/fleets/flightCommands.ts:23:  CommandResult,
src/simulation/fleets/flightCommands.ts:24:  GameCommand,
src/simulation/fleets/flightCommands.ts:34:import type { FleetState } from './types';
src/simulation/fleets/flightCommands.ts:36:function appendCommand(state: GameState, command: GameCommand): readonly CommandLogEntry[] {
src/simulation/fleets/flightCommands.ts:37:  return appendCommandHistory(state.commandLog, command);
src/simulation/fleets/flightCommands.ts:40:function replaceFleet(
src/simulation/fleets/flightCommands.ts:41:  fleets: readonly FleetState[],
src/simulation/fleets/flightCommands.ts:42:  replacement: FleetState,
src/simulation/fleets/flightCommands.ts:43:): readonly FleetState[] {
src/simulation/fleets/flightCommands.ts:44:  return fleets.map((fleet) => (fleet.id === replacement.id ? replacement : fleet));
src/simulation/fleets/flightCommands.ts:54:function getFleetSpeedBonus(state: GameState, fleet: FleetState): number {
src/simulation/fleets/flightCommands.ts:55:  const research = getEmpireResearch(state.research, fleet.empireId);
src/simulation/fleets/flightCommands.ts:60:        getResearchCatalogForEmpire(state, fleet.empireId),
src/simulation/fleets/flightCommands.ts:61:      ).fleetSpeedPercent;
src/simulation/fleets/flightCommands.ts:62:  return researchBonus + getCommanderFleetEffects(state, fleet).speedBonusPercent;
src/simulation/fleets/flightCommands.ts:65:function enqueueBattleReport(state: GameState, report: BattleReport): GameState {
src/simulation/fleets/flightCommands.ts:71:    payload: { type: 'BATTLE_REPORT', report },
src/simulation/fleets/flightCommands.ts:82:  fleet: FleetState,
src/simulation/fleets/flightCommands.ts:94:      fleetId: fleet.id,
src/simulation/fleets/flightCommands.ts:95:      originPlanetId: fleet.originPlanetId,
src/simulation/fleets/flightCommands.ts:98:  const updatedFleet: FleetState = {
src/simulation/fleets/flightCommands.ts:99:    ...fleet,
src/simulation/fleets/flightCommands.ts:104:      toPlanetId: fleet.originPlanetId,
src/simulation/fleets/flightCommands.ts:112:    fleets: replaceFleet(state.fleets, updatedFleet),
src/simulation/fleets/flightCommands.ts:120:  fleet: FleetState,
src/simulation/fleets/flightCommands.ts:121:): { readonly planet: PlanetState; readonly fleet: FleetState } {
src/simulation/fleets/flightCommands.ts:123:  const cargo = { ...fleet.cargo };
src/simulation/fleets/flightCommands.ts:137:    fleet: { ...fleet, cargo },
src/simulation/fleets/flightCommands.ts:141:function validateColonizationTarget(
src/simulation/fleets/flightCommands.ts:143:  fleet: FleetState,
src/simulation/fleets/flightCommands.ts:144:  targetPlanetId: string,
src/simulation/fleets/flightCommands.ts:145:): CommandResult<FlightEstimate> {
src/simulation/fleets/flightCommands.ts:146:  const target = findGalaxyPlanet(state.galaxy, targetPlanetId);
src/simulation/fleets/flightCommands.ts:156:    state.planets.some((planet) => planet.galaxyPlanetId === targetPlanetId)
src/simulation/fleets/flightCommands.ts:164:  if (getColonizationLevel(state, fleet.empireId) <= 0) {
src/simulation/fleets/flightCommands.ts:172:    getEmpireColonyCount(state, fleet.empireId) >=
src/simulation/fleets/flightCommands.ts:173:    getColonyLimit(state, fleet.empireId)
src/simulation/fleets/flightCommands.ts:181:  if (findFleetShipByRole(fleet.ships, 'colonizer') === undefined) {
src/simulation/fleets/flightCommands.ts:185:      message: 'A colonization mission requires a colonizer hull.',
src/simulation/fleets/flightCommands.ts:194:      fleet,
src/simulation/fleets/flightCommands.ts:195:      targetPlanetId,
src/simulation/fleets/flightCommands.ts:196:      getFleetSpeedBonus(state, fleet),
src/simulation/fleets/flightCommands.ts:201:export function sendFleet(
src/simulation/fleets/flightCommands.ts:203:  command: Extract<GameCommand, { readonly type: 'SEND_FLEET' }>,
src/simulation/fleets/flightCommands.ts:204:): CommandResult<GameState> {
src/simulation/fleets/flightCommands.ts:205:  const fleet = state.fleets.find((candidate) => candidate.id === command.fleetId);
src/simulation/fleets/flightCommands.ts:206:  if (fleet === undefined) {
src/simulation/fleets/flightCommands.ts:207:    return { ok: false, code: 'FLEET_NOT_FOUND', message: 'Fleet not found.' };
src/simulation/fleets/flightCommands.ts:209:  if (fleet.empireId !== command.empireId) {
src/simulation/fleets/flightCommands.ts:210:    return { ok: false, code: 'NOT_FLEET_OWNER', message: 'Empire does not own the fleet.' };
src/simulation/fleets/flightCommands.ts:212:  if (fleet.status !== 'stationed' || fleet.location.type !== 'planet') {
src/simulation/fleets/flightCommands.ts:213:    return { ok: false, code: 'FLEET_NOT_STATIONED', message: 'Fleet is not ready to depart.' };
src/simulation/fleets/flightCommands.ts:216:  const originPlanetId = fleet.location.planetId;
src/simulation/fleets/flightCommands.ts:221:  if (origin.ownerEmpireId !== command.empireId) {
src/simulation/fleets/flightCommands.ts:222:    return { ok: false, code: 'FLIGHT_ORIGIN_NOT_OWNED', message: 'Fleet must depart from an owned planet.' };
src/simulation/fleets/flightCommands.ts:226:  if (command.mission === 'colonize') {
src/simulation/fleets/flightCommands.ts:227:    const validation = validateColonizationTarget(
src/simulation/fleets/flightCommands.ts:229:      fleet,
src/simulation/fleets/flightCommands.ts:230:      command.targetPlanetId,
src/simulation/fleets/flightCommands.ts:235:    if (originPlanetId === command.targetPlanetId) {
src/simulation/fleets/flightCommands.ts:236:      return { ok: false, code: 'FLEET_TARGET_IS_ORIGIN', message: 'Fleet target must differ from its origin.' };
src/simulation/fleets/flightCommands.ts:239:    const target = state.planets.find((planet) => planet.id === command.targetPlanetId);
src/simulation/fleets/flightCommands.ts:244:      (command.mission === 'transport' || command.mission === 'deploy') &&
src/simulation/fleets/flightCommands.ts:245:      target.ownerEmpireId !== command.empireId
src/simulation/fleets/flightCommands.ts:250:        message: 'Transport and deploy missions require an owned target planet.',
src/simulation/fleets/flightCommands.ts:253:    if (command.mission === 'attack' && target.ownerEmpireId === command.empireId) {
src/simulation/fleets/flightCommands.ts:261:      command.mission === 'scout' &&
src/simulation/fleets/flightCommands.ts:262:      findFleetShipByRole(fleet.ships, 'scout') === undefined
src/simulation/fleets/flightCommands.ts:267:        message: 'A scouting mission requires a scout hull.',
src/simulation/fleets/flightCommands.ts:271:      command.mission === 'attack' &&
src/simulation/fleets/flightCommands.ts:272:      !Object.entries(fleet.ships).some(
src/simulation/fleets/flightCommands.ts:280:        message: 'An attack mission requires at least one armed ship.',
src/simulation/fleets/flightCommands.ts:284:      command.mission === 'recycle' &&
src/simulation/fleets/flightCommands.ts:285:      findFleetShipByRole(fleet.ships, 'recycler') === undefined
src/simulation/fleets/flightCommands.ts:290:        message: 'A recycling mission requires a recycler hull.',
src/simulation/fleets/flightCommands.ts:294:      command.mission === 'recycle' &&
src/simulation/fleets/flightCommands.ts:311:      fleet,
src/simulation/fleets/flightCommands.ts:313:      getFleetSpeedBonus(state, fleet),
src/simulation/fleets/flightCommands.ts:318:    command.mission === 'colonize'
src/simulation/fleets/flightCommands.ts:325:      message: 'Origin planet does not have enough gas for the mission.',
src/simulation/fleets/flightCommands.ts:338:      fleetId: fleet.id,
src/simulation/fleets/flightCommands.ts:339:      targetPlanetId: command.targetPlanetId,
src/simulation/fleets/flightCommands.ts:342:  const updatedFleet: FleetState = {
src/simulation/fleets/flightCommands.ts:343:    ...fleet,
src/simulation/fleets/flightCommands.ts:345:    mission: { kind: command.mission, targetPlanetId: command.targetPlanetId },
src/simulation/fleets/flightCommands.ts:349:      toPlanetId: command.targetPlanetId,
src/simulation/fleets/flightCommands.ts:373:      fleets: replaceFleet(state.fleets, updatedFleet),
src/simulation/fleets/flightCommands.ts:376:      commandLog: appendCommand(state, command),
src/simulation/fleets/flightCommands.ts:381:export function recallFleet(
src/simulation/fleets/flightCommands.ts:383:  command: Extract<GameCommand, { readonly type: 'RECALL_FLEET' }>,
src/simulation/fleets/flightCommands.ts:384:): CommandResult<GameState> {
src/simulation/fleets/flightCommands.ts:385:  const fleet = state.fleets.find((candidate) => candidate.id === command.fleetId);
src/simulation/fleets/flightCommands.ts:386:  if (fleet === undefined) {
src/simulation/fleets/flightCommands.ts:387:    return { ok: false, code: 'FLEET_NOT_FOUND', message: 'Fleet not found.' };
src/simulation/fleets/flightCommands.ts:389:  if (fleet.empireId !== command.empireId) {
src/simulation/fleets/flightCommands.ts:390:    return { ok: false, code: 'NOT_FLEET_OWNER', message: 'Empire does not own the fleet.' };
src/simulation/fleets/flightCommands.ts:392:  if (fleet.status !== 'outbound' || fleet.location.type !== 'transit') {
src/simulation/fleets/flightCommands.ts:393:    return { ok: false, code: 'FLEET_NOT_RECALLABLE', message: 'Only an outbound fleet can be recalled.' };
src/simulation/fleets/flightCommands.ts:396:  const elapsed = Math.max(1, state.clock.elapsedSeconds - fleet.location.departedAt);
src/simulation/fleets/flightCommands.ts:403:          event.payload.fleetId === fleet.id
src/simulation/fleets/flightCommands.ts:406:    commandLog: appendCommand(state, command),
src/simulation/fleets/flightCommands.ts:412:      fleet,
src/simulation/fleets/flightCommands.ts:413:      fleet.location.toPlanetId,
src/simulation/fleets/flightCommands.ts:425:  const fleet = state.fleets.find((candidate) => candidate.id === payload.fleetId);
src/simulation/fleets/flightCommands.ts:426:  if (fleet === undefined) return state;
src/simulation/fleets/flightCommands.ts:429:    if (fleet.status !== 'returning') return state;
src/simulation/fleets/flightCommands.ts:432:      fleets: replaceFleet(state.fleets, {
src/simulation/fleets/flightCommands.ts:433:        ...fleet,
src/simulation/fleets/flightCommands.ts:435:        mission: null,
src/simulation/fleets/flightCommands.ts:442:    fleet.status !== 'outbound' ||
src/simulation/fleets/flightCommands.ts:443:    fleet.location.type !== 'transit' ||
src/simulation/fleets/flightCommands.ts:444:    fleet.location.toPlanetId !== payload.targetPlanetId ||
src/simulation/fleets/flightCommands.ts:445:    fleet.mission === null
src/simulation/fleets/types.ts:1:import type { FleetFormation, FleetTargetPriority } from '../combat/fleetDoctrine';
src/simulation/fleets/types.ts:4:export type FleetStatus = 'stationed' | 'outbound' | 'holding' | 'returning';
src/simulation/fleets/types.ts:5:export type FleetMissionKind =
src/simulation/fleets/types.ts:15:export interface FleetMission {
src/simulation/fleets/types.ts:16:  readonly kind: FleetMissionKind;
src/simulation/fleets/types.ts:17:  readonly targetPlanetId: string;
src/simulation/fleets/types.ts:20:export interface FleetPlanetLocation {
src/simulation/fleets/types.ts:25:export interface FleetTransitLocation {
src/simulation/fleets/types.ts:33:export type FleetLocation = FleetPlanetLocation | FleetTransitLocation;
src/simulation/fleets/types.ts:35:export interface FleetState {
src/simulation/fleets/types.ts:39:  readonly location: FleetLocation;
src/simulation/fleets/types.ts:40:  readonly status: FleetStatus;
src/simulation/fleets/types.ts:45:  readonly formation?: FleetFormation;
src/simulation/fleets/types.ts:46:  readonly targetPriority?: FleetTargetPriority;
src/simulation/fleets/types.ts:47:  readonly mission: FleetMission | null;
src/simulation/galaxy/intelligenceView.ts:1:import { getEmpireIntelligence } from '../intelligence/intelligenceState';
src/simulation/galaxy/intelligenceView.ts:2:import type { IntelObservation, IntelPlanetSnapshot } from '../intelligence/types';
src/simulation/galaxy/intelligenceView.ts:29:  readonly fleets: IntelPlanetSnapshot['stationedFleets'] | null;
src/simulation/galaxy/intelligenceView.ts:53:  const intelligence = getEmpireIntelligence(state.intelligence, empireId);
src/simulation/galaxy/intelligenceView.ts:55:  const sorted = [...(intelligence?.observations ?? [])].sort(
src/simulation/galaxy/intelligenceView.ts:59:    const planet = state.planets.find((candidate) => candidate.id === observation.targetPlanetId);
src/simulation/galaxy/intelligenceView.ts:67:export function createGalaxyIntelligenceView(
src/simulation/galaxy/intelligenceView.ts:99:            fleets: null,
src/simulation/galaxy/intelligenceView.ts:132:            fleets: state.fleets
src/simulation/galaxy/intelligenceView.ts:134:                (fleet) =>
src/simulation/galaxy/intelligenceView.ts:135:                  fleet.empireId === empireId &&
src/simulation/galaxy/intelligenceView.ts:136:                  fleet.status === 'stationed' &&
src/simulation/galaxy/intelligenceView.ts:137:                  fleet.location.type === 'planet' &&
src/simulation/galaxy/intelligenceView.ts:138:                  fleet.location.planetId === colony.id,
src/simulation/galaxy/intelligenceView.ts:140:              .map((fleet) => ({ fleetId: fleet.id, ships: { ...fleet.ships } })),
src/simulation/galaxy/intelligenceView.ts:166:            fleets: null,
src/simulation/galaxy/intelligenceView.ts:191:          fleets: observation.snapshot.stationedFleets ?? null,
src/simulation/galaxy/intelligenceView.ts:201:export function filterGalaxyIntelligence(
src/simulation/galaxy/intelligenceView.ts:231:export function summarizeGalaxyIntelligence(
src/simulation/history/stateHistory.ts:1:import type { GameCommand, GameState, CommandLogEntry, ExecutedGameEvent } from '../types';
src/simulation/history/stateHistory.ts:4:  commands: 512,
src/simulation/history/stateHistory.ts:8:  intelligenceObservationsPerEmpire: 64,
src/simulation/history/stateHistory.ts:9:  intelligenceAlertsPerEmpire: 128,
src/simulation/history/stateHistory.ts:19:export function appendCommandHistory(
src/simulation/history/stateHistory.ts:20:  log: readonly CommandLogEntry[],
src/simulation/history/stateHistory.ts:21:  command: GameCommand,
src/simulation/history/stateHistory.ts:22:): readonly CommandLogEntry[] {
src/simulation/history/stateHistory.ts:24:  return retainNewest([...log, { index, command }], STATE_HISTORY_LIMITS.commands);
src/simulation/history/stateHistory.ts:37:    commandLog: retainNewest(state.commandLog, STATE_HISTORY_LIMITS.commands),
src/simulation/history/stateHistory.ts:47:    intelligence: state.intelligence.map((entry) => ({
src/simulation/history/stateHistory.ts:51:        STATE_HISTORY_LIMITS.intelligenceObservationsPerEmpire,
src/simulation/history/stateHistory.ts:55:        STATE_HISTORY_LIMITS.intelligenceAlertsPerEmpire,
src/simulation/intelligence/intelligenceState.ts:2:  EmpireIntelligenceState,
src/simulation/intelligence/intelligenceState.ts:6:export function createInitialIntelligenceStates(
src/simulation/intelligence/intelligenceState.ts:8:): readonly EmpireIntelligenceState[] {
src/simulation/intelligence/intelligenceState.ts:16:export function getEmpireIntelligence(
src/simulation/intelligence/intelligenceState.ts:17:  states: readonly EmpireIntelligenceState[],
src/simulation/intelligence/intelligenceState.ts:19:): EmpireIntelligenceState | undefined {
src/simulation/intelligence/intelligenceState.ts:24:  state: EmpireIntelligenceState,
src/simulation/intelligence/resolveScout.ts:4:import type { FleetState } from '../fleets/types';
src/simulation/intelligence/resolveScout.ts:9:import { getEmpireIntelligence } from './intelligenceState';
src/simulation/intelligence/resolveScout.ts:11:  EmpireIntelligenceState,
src/simulation/intelligence/resolveScout.ts:13:  IntelligenceAlert,
src/simulation/intelligence/resolveScout.ts:25:function replaceIntelligence(
src/simulation/intelligence/resolveScout.ts:26:  states: readonly EmpireIntelligenceState[],
src/simulation/intelligence/resolveScout.ts:27:  replacement: EmpireIntelligenceState,
src/simulation/intelligence/resolveScout.ts:28:): readonly EmpireIntelligenceState[] {
src/simulation/intelligence/resolveScout.ts:73:    stationedFleets: state.fleets
src/simulation/intelligence/resolveScout.ts:75:        (fleet) =>
src/simulation/intelligence/resolveScout.ts:76:          fleet.status === 'stationed' &&
src/simulation/intelligence/resolveScout.ts:77:          fleet.location.type === 'planet' &&
src/simulation/intelligence/resolveScout.ts:78:          fleet.location.planetId === target.id,
src/simulation/intelligence/resolveScout.ts:80:      .map((fleet) => ({ fleetId: fleet.id, ships: { ...fleet.ships } })),
src/simulation/intelligence/resolveScout.ts:86:  fleet: FleetState,
src/simulation/intelligence/resolveScout.ts:90:  const observer = getEmpireIntelligence(state.intelligence, fleet.empireId);
src/simulation/intelligence/resolveScout.ts:93:  const scoutCount = getShipCountByRole(fleet.ships, 'scout');
src/simulation/intelligence/resolveScout.ts:94:  const observerStrength = getSensorStrength(state, fleet.empireId) + scoutCount;
src/simulation/intelligence/resolveScout.ts:108:    hashText(`${state.seed}:${eventSequence}:${fleet.id}:${target.id}`) % 100;
src/simulation/intelligence/resolveScout.ts:112:    id: `intel-${eventSequence}-${fleet.id}`,
src/simulation/intelligence/resolveScout.ts:113:    observerEmpireId: fleet.empireId,
src/simulation/intelligence/resolveScout.ts:114:    targetPlanetId: target.id,
src/simulation/intelligence/resolveScout.ts:121:  let intelligence = replaceIntelligence(state.intelligence, {
src/simulation/intelligence/resolveScout.ts:126:          (item) => item.targetPlanetId !== target.id,
src/simulation/intelligence/resolveScout.ts:130:      STATE_HISTORY_LIMITS.intelligenceObservationsPerEmpire,
src/simulation/intelligence/resolveScout.ts:134:  if (detected && target.ownerEmpireId !== fleet.empireId) {
src/simulation/intelligence/resolveScout.ts:135:    const defender = getEmpireIntelligence(intelligence, target.ownerEmpireId);
src/simulation/intelligence/resolveScout.ts:137:      const confidence: IntelligenceAlert['confidence'] =
src/simulation/intelligence/resolveScout.ts:143:      const alert: IntelligenceAlert = {
src/simulation/intelligence/resolveScout.ts:144:        id: `alert-${eventSequence}-${fleet.id}`,
src/simulation/intelligence/resolveScout.ts:146:        sourceEmpireId: confidence === 'low' ? null : fleet.empireId,
src/simulation/intelligence/resolveScout.ts:147:        targetPlanetId: target.id,
src/simulation/intelligence/resolveScout.ts:152:      intelligence = replaceIntelligence(intelligence, {
src/simulation/intelligence/resolveScout.ts:156:          STATE_HISTORY_LIMITS.intelligenceAlertsPerEmpire,
src/simulation/intelligence/resolveScout.ts:162:  return { ...state, intelligence };
src/simulation/intelligence/types.ts:22:  readonly stationedFleets?: readonly {
src/simulation/intelligence/types.ts:23:    readonly fleetId: string;
src/simulation/intelligence/types.ts:31:  readonly targetPlanetId: string;
src/simulation/intelligence/types.ts:39:export interface IntelligenceAlert {
src/simulation/intelligence/types.ts:43:  readonly targetPlanetId: string;
src/simulation/intelligence/types.ts:49:export interface EmpireIntelligenceState {
src/simulation/intelligence/types.ts:52:  readonly alerts: readonly IntelligenceAlert[];
src/simulation/logistics/routes.ts:1:import { appendCommandHistory } from '../history/stateHistory';
src/simulation/logistics/routes.ts:4:import type { CommandLogEntry, CommandResult, GameCommand, GameState } from '../types';
src/simulation/logistics/routes.ts:12:function appendCommand(state: GameState, command: GameCommand): readonly CommandLogEntry[] {
src/simulation/logistics/routes.ts:13:  return appendCommandHistory(state.commandLog, command);
src/simulation/logistics/routes.ts:28:function validateRouteNumbers(
src/simulation/logistics/routes.ts:65:  command: Extract<GameCommand, { readonly type: 'CREATE_LOGISTICS_ROUTE' }>,
src/simulation/logistics/routes.ts:66:): CommandResult<GameState> {
src/simulation/logistics/routes.ts:67:  if (!isResourceId(command.resourceId)) {
src/simulation/logistics/routes.ts:70:  const numberError = validateRouteNumbers(
src/simulation/logistics/routes.ts:71:    command.amountPerTrip,
src/simulation/logistics/routes.ts:72:    command.originReserve,
src/simulation/logistics/routes.ts:73:    command.intervalSeconds,
src/simulation/logistics/routes.ts:74:    command.priority,
src/simulation/logistics/routes.ts:79:  if (command.originPlanetId === command.targetPlanetId) {
src/simulation/logistics/routes.ts:82:  const origin = findOwnedPlanet(state, command.empireId, command.originPlanetId);
src/simulation/logistics/routes.ts:83:  const target = findOwnedPlanet(state, command.empireId, command.targetPlanetId);
src/simulation/logistics/routes.ts:93:    empireId: command.empireId,
src/simulation/logistics/routes.ts:95:    targetPlanetId: target.id,
src/simulation/logistics/routes.ts:96:    resourceId: command.resourceId,
src/simulation/logistics/routes.ts:97:    amountPerTrip: command.amountPerTrip,
src/simulation/logistics/routes.ts:98:    originReserve: command.originReserve,
src/simulation/logistics/routes.ts:99:    intervalSeconds: command.intervalSeconds,
src/simulation/logistics/routes.ts:100:    priority: command.priority,
src/simulation/logistics/routes.ts:102:    nextDepartureAt: state.clock.elapsedSeconds + command.intervalSeconds,
src/simulation/logistics/routes.ts:112:      commandLog: appendCommand(state, command),
src/simulation/logistics/routes.ts:119:  command: Extract<GameCommand, { readonly type: 'UPDATE_LOGISTICS_ROUTE' }>,
src/simulation/logistics/routes.ts:120:): CommandResult<GameState> {
src/simulation/logistics/routes.ts:122:    (candidate) => candidate.id === command.routeId && candidate.empireId === command.empireId,
src/simulation/logistics/routes.ts:127:  const amountPerTrip = command.amountPerTrip ?? route.amountPerTrip;
src/simulation/logistics/routes.ts:128:  const originReserve = command.originReserve ?? route.originReserve;
src/simulation/logistics/routes.ts:129:  const intervalSeconds = command.intervalSeconds ?? route.intervalSeconds;
src/simulation/logistics/routes.ts:130:  const priority = command.priority ?? route.priority;
src/simulation/logistics/routes.ts:131:  const numberError = validateRouteNumbers(
src/simulation/logistics/routes.ts:146:    status: command.status ?? route.status,
src/simulation/logistics/routes.ts:148:      command.intervalSeconds === undefined
src/simulation/logistics/routes.ts:159:      commandLog: appendCommand(state, command),
src/simulation/logistics/routes.ts:166:  command: Extract<GameCommand, { readonly type: 'DELETE_LOGISTICS_ROUTE' }>,
src/simulation/logistics/routes.ts:167:): CommandResult<GameState> {
src/simulation/logistics/routes.ts:169:    (candidate) => candidate.id === command.routeId && candidate.empireId === command.empireId,
src/simulation/logistics/routes.ts:179:      commandLog: appendCommand(state, command),
src/simulation/logistics/routes.ts:190:  const target = findOwnedPlanet(state, route.empireId, route.targetPlanetId);
src/simulation/logistics/types.ts:21:  readonly targetPlanetId: string;
src/simulation/market/market.ts:1:import { appendCommandHistory, STATE_HISTORY_LIMITS } from '../history/stateHistory';
src/simulation/market/market.ts:4:import type { CommandLogEntry, CommandResult, GameCommand, GameState } from '../types';
src/simulation/market/market.ts:102:function appendCommand(state: GameState, command: GameCommand): readonly CommandLogEntry[] {
src/simulation/market/market.ts:103:  return appendCommandHistory(state.commandLog, command);
src/simulation/market/market.ts:115:  command: Extract<GameCommand, { readonly type: 'MARKET_SWAP' }>,
src/simulation/market/market.ts:116:): CommandResult<GameState> {
src/simulation/market/market.ts:117:  if (!isResourceId(command.giveResourceId) || !isResourceId(command.receiveResourceId)) {
src/simulation/market/market.ts:120:  const planet = state.planets.find((candidate) => candidate.id === command.planetId);
src/simulation/market/market.ts:121:  if (planet === undefined || planet.ownerEmpireId !== command.empireId) {
src/simulation/market/market.ts:126:    command.giveResourceId,
src/simulation/market/market.ts:127:    command.receiveResourceId,
src/simulation/market/market.ts:128:    command.giveAmount,
src/simulation/market/market.ts:138:  const giveStock = planet.economy.resources[command.giveResourceId];
src/simulation/market/market.ts:139:  const receiveStock = planet.economy.resources[command.receiveResourceId];
src/simulation/market/market.ts:140:  if (giveStock.amount < command.giveAmount) {
src/simulation/market/market.ts:153:        [command.giveResourceId]: {
src/simulation/market/market.ts:155:          amount: giveStock.amount - command.giveAmount,
src/simulation/market/market.ts:157:        [command.receiveResourceId]: {
src/simulation/market/market.ts:166:    empireId: command.empireId,
src/simulation/market/market.ts:168:    giveResourceId: command.giveResourceId,
src/simulation/market/market.ts:169:    receiveResourceId: command.receiveResourceId,
src/simulation/market/market.ts:170:    giveAmount: command.giveAmount,
src/simulation/market/market.ts:180:      [command.giveResourceId]:
src/simulation/market/market.ts:181:        state.market.reserves[command.giveResourceId] + command.giveAmount,
src/simulation/market/market.ts:182:      [command.receiveResourceId]:
src/simulation/market/market.ts:183:        state.market.reserves[command.receiveResourceId] - quote.receiveAmount,
src/simulation/market/market.ts:194:      commandLog: appendCommand(state, command),
src/simulation/planet/aegisBuildingCatalog.ts:5:    id: 'building.aegis.command',
src/simulation/planet/aegisBuildingCatalog.ts:11:    assetId: 'building.aegis.command',
src/simulation/planet/aegisBuildingCatalog.ts:31:    requirements: [{ buildingId: 'building.aegis.command', level: 1 }],
src/simulation/planet/aegisBuildingCatalog.ts:49:    requirements: [{ buildingId: 'building.aegis.command', level: 1 }],
src/simulation/planet/aegisBuildingCatalog.ts:67:    requirements: [{ buildingId: 'building.aegis.command', level: 1 }],
src/simulation/planet/aegisBuildingCatalog.ts:85:    requirements: [{ buildingId: 'building.aegis.command', level: 1 }],
src/simulation/planet/aegisBuildingCatalog.ts:102:    requirements: [{ buildingId: 'building.aegis.command', level: 2 }],
src/simulation/planet/aegisBuildingCatalog.ts:119:    requirements: [{ buildingId: 'building.aegis.command', level: 2 }],
src/simulation/planet/aegisBuildingCatalog.ts:136:    requirements: [{ buildingId: 'building.aegis.command', level: 1 }],
src/simulation/planet/aegisBuildingCatalog.ts:153:    requirements: [{ buildingId: 'building.aegis.command', level: 3 }],
src/simulation/planet/aegisBuildingCatalog.ts:171:    requirements: [{ buildingId: 'building.aegis.command', level: 3 }],
src/simulation/planet/buildingAliases.ts:5:  'building.aegis.command': 'government',
src/simulation/planet/specialization.ts:92:    recommendedBuildingRoles: ['command', 'power', 'metal', 'crystal', 'gas'],
src/simulation/planet/specialization.ts:98:    recommendedBuildingRoles: ['metal', 'crystal', 'gas', 'power', 'command'],
src/simulation/planet/specialization.ts:104:    recommendedBuildingRoles: ['command', 'power', 'laboratory', 'shipyard', 'sensorGrid'],
src/simulation/planet/specialization.ts:110:    recommendedBuildingRoles: ['command', 'power', 'sensorGrid', 'shipyard', 'laboratory'],
src/simulation/planet/specializationCommands.ts:1:import { appendCommandHistory } from '../history/stateHistory';
src/simulation/planet/specializationCommands.ts:3:import type { CommandLogEntry, CommandResult, GameCommand, GameState } from '../types';
src/simulation/planet/specializationCommands.ts:11:function appendCommand(state: GameState, command: GameCommand): readonly CommandLogEntry[] {
src/simulation/planet/specializationCommands.ts:12:  return appendCommandHistory(state.commandLog, command);
src/simulation/planet/specializationCommands.ts:26:): CommandResult<PlanetState> {
src/simulation/planet/specializationCommands.ts:39:  command: Extract<GameCommand, { readonly type: 'SET_PLANET_SPECIALIZATION' }>,
src/simulation/planet/specializationCommands.ts:40:): CommandResult<GameState> {
src/simulation/planet/specializationCommands.ts:41:  if (!isPlanetSpecializationId(command.specializationId)) {
src/simulation/planet/specializationCommands.ts:48:  const owned = getOwnedPlanet(state, command.empireId, command.planetId);
src/simulation/planet/specializationCommands.ts:50:  if (owned.value.specializationId === command.specializationId) {
src/simulation/planet/specializationCommands.ts:67:    specializationId: command.specializationId,
src/simulation/planet/specializationCommands.ts:72:      command.specializationId,
src/simulation/planet/specializationCommands.ts:80:      commandLog: appendCommand(state, command),
src/simulation/planet/specializationCommands.ts:87:  command: Extract<GameCommand, { readonly type: 'SET_PLANET_DEVELOPMENT_TEMPLATE' }>,
src/simulation/planet/specializationCommands.ts:88:): CommandResult<GameState> {
src/simulation/planet/specializationCommands.ts:89:  if (!isPlanetDevelopmentTemplateId(command.developmentTemplateId)) {
src/simulation/planet/specializationCommands.ts:96:  const owned = getOwnedPlanet(state, command.empireId, command.planetId);
src/simulation/planet/specializationCommands.ts:98:  if (owned.value.developmentTemplateId === command.developmentTemplateId) {
src/simulation/planet/specializationCommands.ts:108:    developmentTemplateId: command.developmentTemplateId,
src/simulation/planet/specializationCommands.ts:115:      commandLog: appendCommand(state, command),
src/simulation/pve/expeditionFleetCommands.ts:2:  recallFleet,
src/simulation/pve/expeditionFleetCommands.ts:3:  sendFleet,
src/simulation/pve/expeditionFleetCommands.ts:4:} from '../fleets/flightCommands';
src/simulation/pve/expeditionFleetCommands.ts:5:import type { CommandResult, GameCommand, GameState } from '../types';
src/simulation/pve/expeditionFleetCommands.ts:7:export function sendFleetWithExpeditionGuard(
src/simulation/pve/expeditionFleetCommands.ts:9:  command: Extract<GameCommand, { readonly type: 'SEND_FLEET' }>,
src/simulation/pve/expeditionFleetCommands.ts:10:): CommandResult<GameState> {
src/simulation/pve/expeditionFleetCommands.ts:11:  if (command.mission === 'expedition') {
src/simulation/pve/expeditionFleetCommands.ts:15:      message: 'Expeditions must use the dedicated expedition command.',
src/simulation/pve/expeditionFleetCommands.ts:18:  if (command.mission === 'space-object') {
src/simulation/pve/expeditionFleetCommands.ts:22:      message: 'Space object operations must use the dedicated mission command.',
src/simulation/pve/expeditionFleetCommands.ts:25:  return sendFleet(state, command);
src/simulation/pve/expeditionFleetCommands.ts:28:export function recallFleetWithExpeditionSupport(
src/simulation/pve/expeditionFleetCommands.ts:30:  command: Extract<GameCommand, { readonly type: 'RECALL_FLEET' }>,
src/simulation/pve/expeditionFleetCommands.ts:31:): CommandResult<GameState> {
src/simulation/pve/expeditionFleetCommands.ts:32:  const fleet = state.fleets.find((candidate) => candidate.id === command.fleetId);
src/simulation/pve/expeditionFleetCommands.ts:33:  if (fleet?.mission?.kind !== 'expedition' && fleet?.mission?.kind !== 'space-object') {
src/simulation/pve/expeditionFleetCommands.ts:34:    return recallFleet(state, command);
src/simulation/pve/expeditionFleetCommands.ts:37:  return recallFleet(
src/simulation/pve/expeditionFleetCommands.ts:43:          event.payload.report.fleetId === fleet.id
src/simulation/pve/expeditionFleetCommands.ts:49:          event.payload.report.fleetId === fleet.id
src/simulation/pve/expeditionFleetCommands.ts:56:    command,
src/simulation/pve/expeditions.ts:1:import { appendCommandHistory } from '../history/stateHistory';
src/simulation/pve/expeditions.ts:4:import { estimateFlightToGalaxyPlanet } from '../fleets/flightCalculations';
src/simulation/pve/expeditions.ts:5:import type { FleetState } from '../fleets/types';
src/simulation/pve/expeditions.ts:8:  CommandLogEntry,
src/simulation/pve/expeditions.ts:9:  CommandResult,
src/simulation/pve/expeditions.ts:10:  GameCommand,
src/simulation/pve/expeditions.ts:22:export interface ExpeditionReport {
src/simulation/pve/expeditions.ts:25:  readonly fleetId: string;
src/simulation/pve/expeditions.ts:41:function appendCommand(state: GameState, command: GameCommand): readonly CommandLogEntry[] {
src/simulation/pve/expeditions.ts:42:  return appendCommandHistory(state.commandLog, command);
src/simulation/pve/expeditions.ts:54:function getFleetSpeedBonus(state: GameState, empireId: string): number {
src/simulation/pve/expeditions.ts:55:  return getResearchEffectsForEmpire(state, empireId).fleetSpeedPercent;
src/simulation/pve/expeditions.ts:58:function replaceFleet(
src/simulation/pve/expeditions.ts:59:  fleets: readonly FleetState[],
src/simulation/pve/expeditions.ts:60:  replacement: FleetState,
src/simulation/pve/expeditions.ts:61:): readonly FleetState[] {
src/simulation/pve/expeditions.ts:62:  return fleets.map((fleet) => (fleet.id === replacement.id ? replacement : fleet));
src/simulation/pve/expeditions.ts:74:  fleet: FleetState,
src/simulation/pve/expeditions.ts:78:): ExpeditionReport {
src/simulation/pve/expeditions.ts:80:    `${state.seed}:${state.nextEventSequence}:${fleet.id}:${targetGalaxyPlanetId}:expedition`,
src/simulation/pve/expeditions.ts:84:  const firstShip = Object.keys(fleet.ships).sort()[0];
src/simulation/pve/expeditions.ts:87:    fleet.empireId,
src/simulation/pve/expeditions.ts:98:      empireId: fleet.empireId,
src/simulation/pve/expeditions.ts:99:      fleetId: fleet.id,
src/simulation/pve/expeditions.ts:100:      originPlanetId: fleet.location.type === 'planet' ? fleet.location.planetId : fleet.originPlanetId,
src/simulation/pve/expeditions.ts:114:      empireId: fleet.empireId,
src/simulation/pve/expeditions.ts:115:      fleetId: fleet.id,
src/simulation/pve/expeditions.ts:116:      originPlanetId: fleet.location.type === 'planet' ? fleet.location.planetId : fleet.originPlanetId,
src/simulation/pve/expeditions.ts:130:      empireId: fleet.empireId,
src/simulation/pve/expeditions.ts:131:      fleetId: fleet.id,
src/simulation/pve/expeditions.ts:132:      originPlanetId: fleet.location.type === 'planet' ? fleet.location.planetId : fleet.originPlanetId,
src/simulation/pve/expeditions.ts:145:    empireId: fleet.empireId,
src/simulation/pve/expeditions.ts:146:    fleetId: fleet.id,
src/simulation/pve/expeditions.ts:147:    originPlanetId: fleet.location.type === 'planet' ? fleet.location.planetId : fleet.originPlanetId,
src/simulation/pve/expeditions.ts:161:  command: Extract<GameCommand, { readonly type: 'START_EXPEDITION' }>,
src/simulation/pve/expeditions.ts:162:): CommandResult<GameState> {
src/simulation/pve/expeditions.ts:163:  const fleet = state.fleets.find((candidate) => candidate.id === command.fleetId);
src/simulation/pve/expeditions.ts:164:  if (fleet === undefined) {
src/simulation/pve/expeditions.ts:165:    return { ok: false, code: 'FLEET_NOT_FOUND', message: 'Fleet not found.' };
src/simulation/pve/expeditions.ts:167:  if (fleet.empireId !== command.empireId) {
src/simulation/pve/expeditions.ts:168:    return { ok: false, code: 'NOT_FLEET_OWNER', message: 'Empire does not own the fleet.' };
src/simulation/pve/expeditions.ts:170:  if (fleet.status !== 'stationed' || fleet.location.type !== 'planet') {
src/simulation/pve/expeditions.ts:171:    return { ok: false, code: 'FLEET_NOT_STATIONED', message: 'Fleet is not ready for an expedition.' };
src/simulation/pve/expeditions.ts:173:  if (!hasShipRole(fleet.ships, 'scout')) {
src/simulation/pve/expeditions.ts:180:  const originPlanetId = fleet.location.planetId;
src/simulation/pve/expeditions.ts:182:  if (origin === undefined || origin.ownerEmpireId !== command.empireId) {
src/simulation/pve/expeditions.ts:187:    .find((planet) => planet.id === command.targetGalaxyPlanetId);
src/simulation/pve/expeditions.ts:202:    fleet,
src/simulation/pve/expeditions.ts:204:    getFleetSpeedBonus(state, command.empireId),
src/simulation/pve/expeditions.ts:218:  const report = createOutcome(state, fleet, target.id, startedAt, resolvesAt);
src/simulation/pve/expeditions.ts:223:    payload: { type: 'EXPEDITION_RESOLVE', report },
src/simulation/pve/expeditions.ts:225:  const updatedFleet: FleetState = {
src/simulation/pve/expeditions.ts:226:    ...fleet,
src/simulation/pve/expeditions.ts:228:    mission: { kind: 'expedition', targetPlanetId: target.id },
src/simulation/pve/expeditions.ts:255:      fleets: replaceFleet(state.fleets, updatedFleet),
src/simulation/pve/expeditions.ts:259:      commandLog: appendCommand(state, command),
src/simulation/pve/expeditions.ts:264:function addReward(planet: PlanetState, report: ExpeditionReport): PlanetState {
src/simulation/pve/expeditions.ts:274:            planet.economy.resources.metal.amount + report.reward.metal,
src/simulation/pve/expeditions.ts:281:            planet.economy.resources.crystal.amount + report.reward.crystal,
src/simulation/pve/expeditions.ts:288:            planet.economy.resources.gas.amount + report.reward.gas,
src/simulation/pve/expeditions.ts:312:  const report = event.payload.report;
src/simulation/pve/expeditions.ts:313:  const fleet = state.fleets.find((candidate) => candidate.id === report.fleetId);
src/simulation/pve/expeditions.ts:315:    fleet === undefined ||
src/simulation/pve/expeditions.ts:316:    fleet.mission?.kind !== 'expedition' ||
src/simulation/pve/expeditions.ts:317:    fleet.mission.targetPlanetId !== report.targetGalaxyPlanetId
src/simulation/pve/expeditions.ts:321:  const origin = state.planets.find((planet) => planet.id === report.originPlanetId);
src/simulation/pve/expeditions.ts:322:  const ships = applyLosses(fleet.ships, report.losses);
src/simulation/pve/expeditions.ts:323:  const fleets =
src/simulation/pve/expeditions.ts:325:      ? state.fleets.filter((candidate) => candidate.id !== fleet.id)
src/simulation/pve/expeditions.ts:326:      : replaceFleet(state.fleets, {
src/simulation/pve/expeditions.ts:327:          ...fleet,
src/simulation/pve/expeditions.ts:330:          mission: null,
src/simulation/pve/expeditions.ts:331:          location: { type: 'planet', planetId: report.originPlanetId },
src/simulation/pve/expeditions.ts:335:    fleets,
src/simulation/pve/expeditions.ts:336:    planets: origin === undefined ? state.planets : replacePlanet(state.planets, addReward(origin, report)),
src/simulation/pve/neutralForces.ts:3:import type { FleetState } from '../fleets/types';
src/simulation/pve/neutralForces.ts:13:  readonly fleets: readonly FleetState[];
src/simulation/pve/neutralForces.ts:33:    { buildingId: 'building.aegis.command', level: tier + 1 },
src/simulation/pve/neutralForces.ts:118:  return { planets, fleets: [] };
src/simulation/pve/pveBalance.ts:2:import type { FleetState } from '../fleets/types';
src/simulation/pve/pveBalance.ts:28:        payload.report.attackerEmpireId === empireId &&
src/simulation/pve/pveBalance.ts:29:        payload.report.defenderEmpireId === PIRATE_EMPIRE_ID &&
src/simulation/pve/pveBalance.ts:30:        payload.report.targetPlanetId === targetId &&
src/simulation/pve/pveBalance.ts:31:        payload.report.winner === 'attacker'
src/simulation/pve/pveBalance.ts:36:        payload.report.empireId === empireId &&
src/simulation/pve/pveBalance.ts:37:        payload.report.targetGalaxyPlanetId === targetId
src/simulation/pve/pveBalance.ts:41:      return payload.report.empireId === empireId && payload.report.objectId === targetId;
src/simulation/pve/pveBalance.ts:69:      payload.report.attackerEmpireId === empireId &&
src/simulation/pve/pveBalance.ts:70:      payload.report.defenderEmpireId === PIRATE_EMPIRE_ID &&
src/simulation/pve/pveBalance.ts:71:      payload.report.winner === 'attacker'
src/simulation/pve/pveBalance.ts:113:  readonly fleet: FleetState;
src/simulation/pve/pveBalance.ts:119:  fleet: FleetState,
src/simulation/pve/pveBalance.ts:150:    fleet: {
src/simulation/pve/pveBalance.ts:151:      ...fleet,
src/simulation/pve/pveBalance.ts:153:        metal: fleet.cargo.metal - withheld.metal,
src/simulation/pve/pveBalance.ts:154:        crystal: fleet.cargo.crystal - withheld.crystal,
src/simulation/pve/pveBalance.ts:155:        gas: fleet.cargo.gas - withheld.gas,
src/simulation/pve/spaceObjects.ts:1:import { appendCommandHistory } from '../history/stateHistory';
src/simulation/pve/spaceObjects.ts:11:} from '../fleets/flightCalculations';
src/simulation/pve/spaceObjects.ts:12:import type { FleetState } from '../fleets/types';
src/simulation/pve/spaceObjects.ts:17:  CommandLogEntry,
src/simulation/pve/spaceObjects.ts:18:  CommandResult,
src/simulation/pve/spaceObjects.ts:19:  GameCommand,
src/simulation/pve/spaceObjects.ts:52:export interface SpaceObjectMissionReport {
src/simulation/pve/spaceObjects.ts:55:  readonly fleetId: string;
src/simulation/pve/spaceObjects.ts:75:function appendCommand(state: GameState, command: GameCommand): readonly CommandLogEntry[] {
src/simulation/pve/spaceObjects.ts:76:  return appendCommandHistory(state.commandLog, command);
src/simulation/pve/spaceObjects.ts:88:function replaceFleet(
src/simulation/pve/spaceObjects.ts:89:  fleets: readonly FleetState[],
src/simulation/pve/spaceObjects.ts:90:  replacement: FleetState,
src/simulation/pve/spaceObjects.ts:91:): readonly FleetState[] {
src/simulation/pve/spaceObjects.ts:92:  return fleets.map((fleet) => (fleet.id === replacement.id ? replacement : fleet));
src/simulation/pve/spaceObjects.ts:109:function getFleetSpeedBonus(state: GameState, empireId: string): number {
src/simulation/pve/spaceObjects.ts:110:  return getResearchEffectsForEmpire(state, empireId).fleetSpeedPercent;
src/simulation/pve/spaceObjects.ts:164:export function estimateSpaceObjectMission(
src/simulation/pve/spaceObjects.ts:166:  fleet: FleetState,
src/simulation/pve/spaceObjects.ts:169:  if (fleet.status !== 'stationed' || fleet.location.type !== 'planet') {
src/simulation/pve/spaceObjects.ts:170:    throw new Error('Only a stationed fleet can estimate a space object mission.');
src/simulation/pve/spaceObjects.ts:172:  const originPlanetId = fleet.location.planetId;
src/simulation/pve/spaceObjects.ts:174:  if (origin === undefined) throw new Error('Space object mission origin not found.');
src/simulation/pve/spaceObjects.ts:183:    fleet.speed,
src/simulation/pve/spaceObjects.ts:184:    getFleetSpeedBonus(state, fleet.empireId),
src/simulation/pve/spaceObjects.ts:186:  const fuelCost = calculateFlightFuel(distance, fleet);
src/simulation/pve/spaceObjects.ts:197:function createMissionReport(
src/simulation/pve/spaceObjects.ts:199:  fleet: FleetState,
src/simulation/pve/spaceObjects.ts:204:): SpaceObjectMissionReport {
src/simulation/pve/spaceObjects.ts:206:    `${state.seed}:${state.nextEventSequence}:${fleet.id}:${object.id}:space-object-mission`,
src/simulation/pve/spaceObjects.ts:212:  const controlledBySelf = controlActive && object.controllerEmpireId === fleet.empireId;
src/simulation/pve/spaceObjects.ts:213:  const controlledByOther = controlActive && object.controllerEmpireId !== fleet.empireId;
src/simulation/pve/spaceObjects.ts:218:    fleet.empireId,
src/simulation/pve/spaceObjects.ts:265:    id: `space-object-report-${state.nextEventSequence}`,
src/simulation/pve/spaceObjects.ts:266:    empireId: fleet.empireId,
src/simulation/pve/spaceObjects.ts:267:    fleetId: fleet.id,
src/simulation/pve/spaceObjects.ts:281:export function startSpaceObjectMission(
src/simulation/pve/spaceObjects.ts:283:  command: Extract<GameCommand, { readonly type: 'START_SPACE_OBJECT_MISSION' }>,
src/simulation/pve/spaceObjects.ts:284:): CommandResult<GameState> {
src/simulation/pve/spaceObjects.ts:285:  const fleet = state.fleets.find((candidate) => candidate.id === command.fleetId);
src/simulation/pve/spaceObjects.ts:286:  if (fleet === undefined) {
src/simulation/pve/spaceObjects.ts:287:    return { ok: false, code: 'FLEET_NOT_FOUND', message: 'Fleet not found.' };
src/simulation/pve/spaceObjects.ts:289:  if (fleet.empireId !== command.empireId) {
src/simulation/pve/spaceObjects.ts:290:    return { ok: false, code: 'NOT_FLEET_OWNER', message: 'Empire does not own the fleet.' };
src/simulation/pve/spaceObjects.ts:292:  if (fleet.status !== 'stationed' || fleet.location.type !== 'planet') {
src/simulation/pve/spaceObjects.ts:293:    return { ok: false, code: 'FLEET_NOT_STATIONED', message: 'Fleet is not ready for a space object mission.' };
src/simulation/pve/spaceObjects.ts:295:  const originPlanetId = fleet.location.planetId;
src/simulation/pve/spaceObjects.ts:297:  if (origin === undefined || origin.ownerEmpireId !== command.empireId) {
src/simulation/pve/spaceObjects.ts:298:    return { ok: false, code: 'SPACE_OBJECT_ORIGIN_UNAVAILABLE', message: 'Mission origin is unavailable.' };
src/simulation/pve/spaceObjects.ts:300:  const object = state.spaceObjects.find((candidate) => candidate.id === command.objectId);
src/simulation/pve/spaceObjects.ts:318:        event.payload.report.objectId === object.id,
src/simulation/pve/spaceObjects.ts:328:  const requiredShipId = findShipIdByRole(fleet.ships, requiredRole);
src/simulation/pve/spaceObjects.ts:333:      message: `Mission requires a ${requiredRole} hull.`,
src/simulation/pve/spaceObjects.ts:337:  const estimate = estimateSpaceObjectMission(state, fleet, object);
src/simulation/pve/spaceObjects.ts:342:      message: 'Origin planet does not have enough gas for the full mission cycle.',
src/simulation/pve/spaceObjects.ts:351:  const report = createMissionReport(
src/simulation/pve/spaceObjects.ts:353:    fleet,
src/simulation/pve/spaceObjects.ts:363:    payload: { type: 'SPACE_OBJECT_MISSION_RESOLVE', report },
src/simulation/pve/spaceObjects.ts:365:  const updatedFleet: FleetState = {
src/simulation/pve/spaceObjects.ts:366:    ...fleet,
src/simulation/pve/spaceObjects.ts:368:    mission: { kind: 'space-object', targetPlanetId: object.id },
src/simulation/pve/spaceObjects.ts:395:      fleets: replaceFleet(state.fleets, updatedFleet),
src/simulation/pve/spaceObjects.ts:398:      commandLog: appendCommand(state, command),
src/simulation/pve/spaceObjects.ts:416:  report: SpaceObjectMissionReport,
src/simulation/pve/spaceObjects.ts:427:            planet.economy.resources.metal.amount + report.reward.metal,
src/simulation/pve/spaceObjects.ts:434:            planet.economy.resources.crystal.amount + report.reward.crystal,
src/simulation/pve/spaceObjects.ts:441:            planet.economy.resources.gas.amount + report.reward.gas,
src/simulation/pve/spaceObjects.ts:449:export function applySpaceObjectMissionEvent(
src/simulation/pve/spaceObjects.ts:454:  const report = event.payload.report;
src/simulation/pve/spaceObjects.ts:455:  const fleet = state.fleets.find((candidate) => candidate.id === report.fleetId);
src/simulation/pve/spaceObjects.ts:456:  const object = state.spaceObjects.find((candidate) => candidate.id === report.objectId);
src/simulation/pve/spaceObjects.ts:458:    fleet === undefined ||
src/simulation/pve/spaceObjects.ts:460:    fleet.mission?.kind !== 'space-object' ||
src/simulation/pve/spaceObjects.ts:461:    fleet.mission.targetPlanetId !== object.id
src/simulation/pve/spaceObjects.ts:465:  const origin = state.planets.find((planet) => planet.id === report.originPlanetId);
src/simulation/pve/spaceObjects.ts:466:  const depletion = Math.min(object.remainingYield, report.depletion);
src/simulation/pve/spaceObjects.ts:467:  const ships = applyLosses(fleet.ships, report.losses);
src/simulation/pve/spaceObjects.ts:472:    controllerEmpireId: survived ? report.empireId : object.controllerEmpireId,
src/simulation/pve/spaceObjects.ts:473:    controlExpiresAt: survived ? report.controllerUntil : object.controlExpiresAt,
src/simulation/pve/spaceObjects.ts:476:  const fleets = survived
src/simulation/pve/spaceObjects.ts:477:    ? replaceFleet(state.fleets, {
src/simulation/pve/spaceObjects.ts:478:        ...fleet,
src/simulation/pve/spaceObjects.ts:481:        mission: null,
src/simulation/pve/spaceObjects.ts:482:        location: { type: 'planet', planetId: report.originPlanetId },
src/simulation/pve/spaceObjects.ts:484:    : state.fleets.filter((candidate) => candidate.id !== fleet.id);
src/simulation/pve/spaceObjects.ts:486:    entry.empireId === report.empireId
src/simulation/pve/spaceObjects.ts:487:      ? { ...entry, exoticMatter: entry.exoticMatter + report.reward.exoticMatter }
src/simulation/pve/spaceObjects.ts:494:    fleets,
src/simulation/pve/spaceObjects.ts:498:        : replacePlanet(state.planets, addPlanetReward(origin, report)),
src/simulation/reducer.ts:1:import { appendCommandHistory, appendExecutedEventHistory } from './history/stateHistory';
src/simulation/reducer.ts:2:import { assignFlagship, setCommandDoctrine } from './command/commandDoctrine';
src/simulation/reducer.ts:12:import { createFleet, disbandFleet } from './fleets/fleetCommands';
src/simulation/reducer.ts:13:import { setFleetCombatDoctrine } from './fleets/fleetDoctrineCommands';
src/simulation/reducer.ts:14:import { applyFlightEvent } from './fleets/flightCommands';
src/simulation/reducer.ts:42:} from './planet/specializationCommands';
src/simulation/reducer.ts:45:  recallFleetWithExpeditionSupport,
src/simulation/reducer.ts:46:  sendFleetWithExpeditionGuard,
src/simulation/reducer.ts:47:} from './pve/expeditionFleetCommands';
src/simulation/reducer.ts:50:  applySpaceObjectMissionEvent,
src/simulation/reducer.ts:51:  startSpaceObjectMission,
src/simulation/reducer.ts:63:} from './research/researchCommands';
src/simulation/reducer.ts:65:  CommandLogEntry,
src/simulation/reducer.ts:66:  CommandResult,
src/simulation/reducer.ts:68:  GameCommand,
src/simulation/reducer.ts:76:} from './units/productionCommands';
src/simulation/reducer.ts:83:function appendCommand(state: GameState, command: GameCommand): readonly CommandLogEntry[] {
src/simulation/reducer.ts:84:  return appendCommandHistory(state.commandLog, command);
src/simulation/reducer.ts:101:  command: Extract<GameCommand, { readonly type: 'SCHEDULE_EVENT' }>,
src/simulation/reducer.ts:102:): CommandResult<GameState> {
src/simulation/reducer.ts:104:    command.payload.type === 'BUILDING_COMPLETE' ||
src/simulation/reducer.ts:105:    command.payload.type === 'RESEARCH_COMPLETE' ||
src/simulation/reducer.ts:106:    command.payload.type === 'UNIT_PRODUCTION_COMPLETE' ||
src/simulation/reducer.ts:107:    command.payload.type === 'DEFENSE_REPAIR_COMPLETE' ||
src/simulation/reducer.ts:108:    command.payload.type === 'SHIP_UPGRADE_COMPLETE' ||
src/simulation/reducer.ts:109:    command.payload.type === 'FLEET_ARRIVE' ||
src/simulation/reducer.ts:110:    command.payload.type === 'FLEET_RETURN' ||
src/simulation/reducer.ts:111:    command.payload.type === 'EXPEDITION_RESOLVE' ||
src/simulation/reducer.ts:112:    command.payload.type === 'SPACE_OBJECT_MISSION_RESOLVE' ||
src/simulation/reducer.ts:113:    command.payload.type === 'WORLD_EVENT_END' ||
src/simulation/reducer.ts:114:    command.payload.type === 'WORLD_EVENT_START'
src/simulation/reducer.ts:122:  if (!isNonNegativeInteger(command.executeAt)) {
src/simulation/reducer.ts:127:      details: { executeAt: command.executeAt },
src/simulation/reducer.ts:130:  if (command.executeAt < state.clock.elapsedSeconds) {
src/simulation/reducer.ts:139:    executeAt: command.executeAt,
src/simulation/reducer.ts:141:    payload: command.payload,
src/simulation/reducer.ts:149:      commandLog: appendCommand(state, command),
src/simulation/reducer.ts:156:  command: Extract<GameCommand, { readonly type: 'QUEUE_BUILDING' }>,
src/simulation/reducer.ts:157:): CommandResult<GameState> {
src/simulation/reducer.ts:158:  const planet = state.planets.find((candidate) => candidate.id === command.planetId);
src/simulation/reducer.ts:162:  if (planet.ownerEmpireId !== command.empireId) {
src/simulation/reducer.ts:168:  const definition = getBuildingDefinition(command.buildingId);
src/simulation/reducer.ts:209:  const effects = getResearchEffectsForEmpire(state, command.empireId);
src/simulation/reducer.ts:252:      commandLog: appendCommand(state, command),
src/simulation/reducer.ts:259:  command: Extract<GameCommand, { readonly type: 'CANCEL_BUILDING' }>,
src/simulation/reducer.ts:260:): CommandResult<GameState> {
src/simulation/reducer.ts:261:  const planet = state.planets.find((candidate) => candidate.id === command.planetId);
src/simulation/reducer.ts:262:  if (planet === undefined || planet.ownerEmpireId !== command.empireId) {
src/simulation/reducer.ts:265:  const queueItem = planet.buildQueue.find((item) => item.id === command.queueItemId);
src/simulation/reducer.ts:286:      commandLog: appendCommand(state, command),
src/simulation/reducer.ts:296:    return applySpaceObjectMissionEvent(state, event);
src/simulation/reducer.ts:356:  command: Extract<GameCommand, { readonly type: 'ADVANCE_TIME' }>,
src/simulation/reducer.ts:357:): CommandResult<GameState> {
src/simulation/reducer.ts:358:  if (!isNonNegativeInteger(command.seconds)) {
src/simulation/reducer.ts:361:  const targetTime = state.clock.elapsedSeconds + command.seconds;
src/simulation/reducer.ts:393:      commandLog: appendCommand(state, command),
src/simulation/reducer.ts:399:export function executeCommand(state: GameState, command: GameCommand): CommandResult<GameState> {
src/simulation/reducer.ts:400:  switch (command.type) {
src/simulation/reducer.ts:401:    case 'SCHEDULE_EVENT': return scheduleEvent(state, command);
src/simulation/reducer.ts:402:    case 'QUEUE_BUILDING': return queueBuilding(state, command);
src/simulation/reducer.ts:403:    case 'CANCEL_BUILDING': return cancelBuilding(state, command);
src/simulation/reducer.ts:404:    case 'SET_PLANET_SPECIALIZATION': return setPlanetSpecialization(state, command);
src/simulation/reducer.ts:405:    case 'SET_PLANET_DEVELOPMENT_TEMPLATE': return setPlanetDevelopmentTemplate(state, command);
src/simulation/reducer.ts:406:    case 'CREATE_LOGISTICS_ROUTE': return createLogisticsRoute(state, command);
src/simulation/reducer.ts:407:    case 'UPDATE_LOGISTICS_ROUTE': return updateLogisticsRoute(state, command);
src/simulation/reducer.ts:408:    case 'DELETE_LOGISTICS_ROUTE': return deleteLogisticsRoute(state, command);
src/simulation/reducer.ts:409:    case 'MARKET_SWAP': return executeMarketSwap(state, command);
src/simulation/reducer.ts:410:    case 'QUEUE_RESEARCH': return queueResearch(state, command);
src/simulation/reducer.ts:411:    case 'CANCEL_RESEARCH': return cancelResearch(state, command);
src/simulation/reducer.ts:412:    case 'QUEUE_UNIT_BATCH': return queueUnitBatch(state, command);
src/simulation/reducer.ts:413:    case 'CANCEL_UNIT_BATCH': return cancelUnitBatch(state, command);
src/simulation/reducer.ts:414:    case 'QUEUE_DEFENSE_REPAIR': return queueDefenseRepair(state, command);
src/simulation/reducer.ts:415:    case 'CANCEL_DEFENSE_REPAIR': return cancelDefenseRepair(state, command);
src/simulation/reducer.ts:416:    case 'QUEUE_SHIP_UPGRADE': return queueShipUpgrade(state, command);
src/simulation/reducer.ts:417:    case 'CANCEL_SHIP_UPGRADE': return cancelShipUpgrade(state, command);
src/simulation/reducer.ts:418:    case 'CREATE_FLEET': return createFleet(state, command);
src/simulation/reducer.ts:419:    case 'DISBAND_FLEET': return disbandFleet(state, command);
src/simulation/reducer.ts:420:    case 'SET_FLEET_COMBAT_DOCTRINE': return setFleetCombatDoctrine(state, command);
src/simulation/reducer.ts:421:    case 'SET_COMMAND_DOCTRINE': return setCommandDoctrine(state, command);
src/simulation/reducer.ts:422:    case 'ASSIGN_FLAGSHIP': return assignFlagship(state, command);
src/simulation/reducer.ts:423:    case 'SEND_FLEET': return sendFleetWithExpeditionGuard(state, command);
src/simulation/reducer.ts:424:    case 'START_EXPEDITION': return startExpedition(state, command);
src/simulation/reducer.ts:425:    case 'START_SPACE_OBJECT_MISSION': return startSpaceObjectMission(state, command);
src/simulation/reducer.ts:426:    case 'RECALL_FLEET': return recallFleetWithExpeditionSupport(state, command);
src/simulation/reducer.ts:427:    case 'ADVANCE_TIME': return advanceTime(state, command);
src/simulation/replay.ts:2:import { executeCommand } from './reducer';
src/simulation/replay.ts:3:import type { CommandResult, GameCommand, GameState } from './types';
src/simulation/replay.ts:5:export function replayCommands(
src/simulation/replay.ts:7:  commands: readonly GameCommand[],
src/simulation/replay.ts:8:): CommandResult<GameState> {
src/simulation/replay.ts:11:  for (const command of commands) {
src/simulation/replay.ts:12:    const result = executeCommand(state, command);
src/simulation/reports/missionReports.ts:6:import type { BattleRoundReport } from '../combat/types';
src/simulation/reports/missionReports.ts:11:export type MissionReportKind = 'battle' | 'expedition' | 'space-object' | 'world-event';
src/simulation/reports/missionReports.ts:12:export type MissionReportMode = 'pve' | 'pvp' | 'system';
src/simulation/reports/missionReports.ts:13:export type MissionReportOutcome = 'success' | 'failure' | 'draw' | 'completed' | 'recovered';
src/simulation/reports/missionReports.ts:14:export type MissionCombatSide = 'attacker' | 'defender';
src/simulation/reports/missionReports.ts:16:export interface MissionReportReward extends ResourceCost {
src/simulation/reports/missionReports.ts:20:export interface MissionCombatWeaponContribution {
src/simulation/reports/missionReports.ts:26:export interface MissionCombatBreakdownLine {
src/simulation/reports/missionReports.ts:28:  readonly side: MissionCombatSide;
src/simulation/reports/missionReports.ts:40:  readonly weaponContributions: readonly MissionCombatWeaponContribution[];
src/simulation/reports/missionReports.ts:43:export interface UnifiedMissionReport {
src/simulation/reports/missionReports.ts:46:  readonly kind: MissionReportKind;
src/simulation/reports/missionReports.ts:47:  readonly mode: MissionReportMode;
src/simulation/reports/missionReports.ts:53:  readonly outcome: MissionReportOutcome;
src/simulation/reports/missionReports.ts:54:  readonly reward: MissionReportReward;
src/simulation/reports/missionReports.ts:59:  readonly combatBreakdown?: readonly MissionCombatBreakdownLine[];
src/simulation/reports/missionReports.ts:62:export interface MissionReportQuery {
src/simulation/reports/missionReports.ts:63:  readonly kind?: MissionReportKind | 'all';
src/simulation/reports/missionReports.ts:64:  readonly mode?: MissionReportMode | 'all';
src/simulation/reports/missionReports.ts:69:export interface MissionReportSummary {
src/simulation/reports/missionReports.ts:75:  readonly rewards: MissionReportReward;
src/simulation/reports/missionReports.ts:85:  readonly reward: MissionReportReward;
src/simulation/reports/missionReports.ts:89:const ZERO_REWARD: MissionReportReward = {
src/simulation/reports/missionReports.ts:123:  left: MissionReportReward,
src/simulation/reports/missionReports.ts:124:  right: MissionReportReward,
src/simulation/reports/missionReports.ts:125:): MissionReportReward {
src/simulation/reports/missionReports.ts:136:): MissionReportOutcome {
src/simulation/reports/missionReports.ts:143:  rounds: readonly BattleRoundReport[],
src/simulation/reports/missionReports.ts:144:): readonly MissionCombatBreakdownLine[] {
src/simulation/reports/missionReports.ts:147:      (target): MissionCombatBreakdownLine => ({
src/simulation/reports/missionReports.ts:165:      (target): MissionCombatBreakdownLine => ({
src/simulation/reports/missionReports.ts:186:function createEventReports(state: GameState): UnifiedMissionReport[] {
src/simulation/reports/missionReports.ts:187:  const reports: UnifiedMissionReport[] = [];
src/simulation/reports/missionReports.ts:191:      const report = payload.report;
src/simulation/reports/missionReports.ts:192:      const mode = report.mode ?? (
src/simulation/reports/missionReports.ts:193:        report.attackerEmpireId === PIRATE_EMPIRE_ID || report.defenderEmpireId === PIRATE_EMPIRE_ID
src/simulation/reports/missionReports.ts:197:      const defensesRecovered = report.defensesRecovered ?? {};
src/simulation/reports/missionReports.ts:199:      reports.push({
src/simulation/reports/missionReports.ts:200:        id: report.id,
src/simulation/reports/missionReports.ts:205:        summary: `${report.attackerEmpireId} → ${report.defenderEmpireId} · ${report.winner}${recoveredCount > 0 ? ` · восстановлено установок ${recoveredCount}` : ''}`,
src/simulation/reports/missionReports.ts:206:        targetId: report.targetPlanetId,
src/simulation/reports/missionReports.ts:207:        primaryEmpireId: report.attackerEmpireId,
src/simulation/reports/missionReports.ts:208:        secondaryEmpireId: report.defenderEmpireId,
src/simulation/reports/missionReports.ts:209:        outcome: battleOutcome(report.winner),
src/simulation/reports/missionReports.ts:211:          metal: report.plunderedCargo?.metal ?? 0,
src/simulation/reports/missionReports.ts:212:          crystal: report.plunderedCargo?.crystal ?? 0,
src/simulation/reports/missionReports.ts:213:          gas: report.plunderedCargo?.gas ?? 0,
src/simulation/reports/missionReports.ts:216:        primaryLosses: unitLosses(report.attackerInitial, report.attackerRemaining),
src/simulation/reports/missionReports.ts:218:          report.defenderInitial,
src/simulation/reports/missionReports.ts:219:          addUnits(report.defenderRemaining, defensesRecovered),
src/simulation/reports/missionReports.ts:221:        threatMultiplierPermille: report.threatMultiplierPermille ?? 1_000,
src/simulation/reports/missionReports.ts:222:        rewardMultiplierPermille: report.rewardMultiplierPermille ?? 1_000,
src/simulation/reports/missionReports.ts:223:        combatBreakdown: createCombatBreakdown(report.rounds),
src/simulation/reports/missionReports.ts:228:      const report = payload.report;
src/simulation/reports/missionReports.ts:229:      reports.push({
src/simulation/reports/missionReports.ts:230:        id: report.id,
src/simulation/reports/missionReports.ts:235:        summary: `${report.outcome} · ${report.narrative}`,
src/simulation/reports/missionReports.ts:236:        targetId: report.targetGalaxyPlanetId,
src/simulation/reports/missionReports.ts:237:        primaryEmpireId: report.empireId,
src/simulation/reports/missionReports.ts:239:        outcome: report.outcome === 'empty' ? 'failure' : 'success',
src/simulation/reports/missionReports.ts:240:        reward: { ...report.reward, exoticMatter: 0 },
src/simulation/reports/missionReports.ts:241:        primaryLosses: report.losses,
src/simulation/reports/missionReports.ts:244:        rewardMultiplierPermille: report.rewardMultiplierPermille ?? 1_000,
src/simulation/reports/missionReports.ts:249:      const report = payload.report;
src/simulation/reports/missionReports.ts:250:      reports.push({
src/simulation/reports/missionReports.ts:251:        id: report.id,
src/simulation/reports/missionReports.ts:256:        summary: report.narrative,
src/simulation/reports/missionReports.ts:257:        targetId: report.objectId,
src/simulation/reports/missionReports.ts:258:        primaryEmpireId: report.empireId,
src/simulation/reports/missionReports.ts:260:        outcome: report.depletion > 0 ? 'success' : 'failure',
src/simulation/reports/missionReports.ts:261:        reward: { ...report.reward },
src/simulation/reports/missionReports.ts:262:        primaryLosses: report.losses,
src/simulation/reports/missionReports.ts:265:        rewardMultiplierPermille: report.rewardMultiplierPermille ?? 1_000,
src/simulation/reports/missionReports.ts:269:  return reports;
src/simulation/reports/missionReports.ts:272:function createWorldEventReports(state: GameState): UnifiedMissionReport[] {
src/simulation/reports/missionReports.ts:273:  return state.worldEvents.history.map((event): UnifiedMissionReport => ({
src/simulation/reports/missionReports.ts:292:export function createUnifiedMissionReports(
src/simulation/reports/missionReports.ts:294:): readonly UnifiedMissionReport[] {
src/simulation/reports/missionReports.ts:295:  return [...createEventReports(state), ...createWorldEventReports(state)]
src/simulation/reports/missionReports.ts:299:export function filterMissionReports(
src/simulation/reports/missionReports.ts:300:  reports: readonly UnifiedMissionReport[],
src/simulation/reports/missionReports.ts:301:  query: MissionReportQuery,
src/simulation/reports/missionReports.ts:302:): readonly UnifiedMissionReport[] {
src/simulation/reports/missionReports.ts:307:  return reports.filter((report) => {
src/simulation/reports/missionReports.ts:308:    if (kind !== 'all' && report.kind !== kind) return false;
src/simulation/reports/missionReports.ts:309:    if (mode !== 'all' && report.mode !== mode) return false;
src/simulation/reports/missionReports.ts:312:      report.primaryEmpireId !== empireId &&
src/simulation/reports/missionReports.ts:313:      report.secondaryEmpireId !== empireId
src/simulation/reports/missionReports.ts:318:      const haystack = `${report.title} ${report.summary} ${report.targetId} ${report.primaryEmpireId ?? ''} ${report.secondaryEmpireId ?? ''}`
src/simulation/reports/missionReports.ts:326:export function summarizeMissionReports(
src/simulation/reports/missionReports.ts:327:  reports: readonly UnifiedMissionReport[],
src/simulation/reports/missionReports.ts:328:): MissionReportSummary {
src/simulation/reports/missionReports.ts:329:  return reports.reduce<MissionReportSummary>(
src/simulation/reports/missionReports.ts:330:    (summary, report) => ({
src/simulation/reports/missionReports.ts:332:      pve: summary.pve + (report.mode === 'pve' ? 1 : 0),
src/simulation/reports/missionReports.ts:333:      pvp: summary.pvp + (report.mode === 'pvp' ? 1 : 0),
src/simulation/reports/missionReports.ts:334:      system: summary.system + (report.mode === 'system' ? 1 : 0),
src/simulation/reports/missionReports.ts:335:      successes: summary.successes + (report.outcome === 'success' ? 1 : 0),
src/simulation/reports/missionReports.ts:336:      rewards: addReward(summary.rewards, report.reward),
src/simulation/reports/missionReports.ts:337:      losses: summary.losses + totalUnits(report.primaryLosses),
src/simulation/reports/missionReports.ts:354:  const reports = createUnifiedMissionReports(state);
src/simulation/reports/missionReports.ts:356:    const related = reports.filter(
src/simulation/reports/missionReports.ts:357:      (report) => report.primaryEmpireId === empireId || report.secondaryEmpireId === empireId,
src/simulation/reports/missionReports.ts:360:      .filter((report) => report.primaryEmpireId === empireId)
src/simulation/reports/missionReports.ts:361:      .reduce((total, report) => addReward(total, report.reward), ZERO_REWARD);
src/simulation/reports/missionReports.ts:362:    const losses = related.reduce((total, report) => {
src/simulation/reports/missionReports.ts:363:      if (report.primaryEmpireId === empireId) return total + totalUnits(report.primaryLosses);
src/simulation/reports/missionReports.ts:364:      if (report.secondaryEmpireId === empireId) return total + totalUnits(report.secondaryLosses);
src/simulation/reports/missionReports.ts:369:      pveOperations: related.filter((report) => report.mode === 'pve').length,
src/simulation/reports/missionReports.ts:371:        (report) =>
src/simulation/reports/missionReports.ts:372:          report.mode === 'pve' &&
src/simulation/reports/missionReports.ts:373:          report.primaryEmpireId === empireId &&
src/simulation/reports/missionReports.ts:374:          report.outcome === 'success',
src/simulation/reports/missionReports.ts:376:      pvpBattles: related.filter((report) => report.mode === 'pvp').length,
src/simulation/reports/missionReports.ts:378:        (report) =>
src/simulation/reports/missionReports.ts:379:          report.mode === 'pvp' &&
src/simulation/reports/missionReports.ts:380:          ((report.primaryEmpireId === empireId && report.outcome === 'success') ||
src/simulation/reports/missionReports.ts:381:            (report.secondaryEmpireId === empireId && report.outcome === 'failure')),
src/simulation/research/aegisResearchCatalog.ts:36:    category: 'intelligence',
src/simulation/research/completeResearchCatalog.ts:80:    category: 'intelligence',
src/simulation/research/progression.ts:39:  readonly fleetSpeedPercent: number;
src/simulation/research/progression.ts:58:  let fleetSpeedPercent = 0;
src/simulation/research/progression.ts:89:          fleetSpeedPercent += effect.percentPerLevel * level;
src/simulation/research/progression.ts:126:    fleetSpeedPercent,
src/simulation/research/researchAliases.ts:27:    battleNetwork: 'chorus-command',
src/simulation/research/researchCommands.ts:1:import { appendCommandHistory } from '../history/stateHistory';
src/simulation/research/researchCommands.ts:13:  CommandLogEntry,
src/simulation/research/researchCommands.ts:14:  CommandResult,
src/simulation/research/researchCommands.ts:15:  GameCommand,
src/simulation/research/researchCommands.ts:35:function appendCommand(state: GameState, command: GameCommand): readonly CommandLogEntry[] {
src/simulation/research/researchCommands.ts:36:  return appendCommandHistory(state.commandLog, command);
src/simulation/research/researchCommands.ts:57:  command: Extract<GameCommand, { readonly type: 'QUEUE_RESEARCH' }>,
src/simulation/research/researchCommands.ts:58:): CommandResult<GameState> {
src/simulation/research/researchCommands.ts:59:  const planet = state.planets.find((candidate) => candidate.id === command.planetId);
src/simulation/research/researchCommands.ts:63:  if (planet.ownerEmpireId !== command.empireId) {
src/simulation/research/researchCommands.ts:71:  const research = getEmpireResearch(state.research, command.empireId);
src/simulation/research/researchCommands.ts:87:  const definition = getResearchDefinition(command.technologyId);
src/simulation/research/researchCommands.ts:162:      empireId: command.empireId,
src/simulation/research/researchCommands.ts:185:      commandLog: appendCommand(state, command),
src/simulation/research/researchCommands.ts:192:  command: Extract<GameCommand, { readonly type: 'CANCEL_RESEARCH' }>,
src/simulation/research/researchCommands.ts:193:): CommandResult<GameState> {
src/simulation/research/researchCommands.ts:194:  const research = getEmpireResearch(state.research, command.empireId);
src/simulation/research/researchCommands.ts:202:  const queueItem = research.queue.find((item) => item.id === command.queueItemId);
src/simulation/research/researchCommands.ts:211:  if (planet === undefined || planet.ownerEmpireId !== command.empireId) {
src/simulation/research/researchCommands.ts:241:      commandLog: appendCommand(state, command),
src/simulation/research/types.ts:8:  | 'intelligence'
src/simulation/types.ts:3:import type { FleetFormation, FleetTargetPriority } from './combat/fleetDoctrine';
src/simulation/types.ts:4:import type { BattleReport } from './combat/types';
src/simulation/types.ts:5:import type { CommandDoctrineId, EmpireCommandState } from './command/types';
src/simulation/types.ts:7:import type { FleetMissionKind, FleetState } from './fleets/types';
src/simulation/types.ts:9:import type { EmpireIntelligenceState } from './intelligence/types';
src/simulation/types.ts:17:import type { ExpeditionReport } from './pve/expeditions';
src/simulation/types.ts:20:  SpaceObjectMissionReport,
src/simulation/types.ts:80:      readonly fleetId: string;
src/simulation/types.ts:81:      readonly targetPlanetId: string;
src/simulation/types.ts:85:      readonly fleetId: string;
src/simulation/types.ts:90:      readonly report: BattleReport;
src/simulation/types.ts:94:      readonly report: ExpeditionReport;
src/simulation/types.ts:98:      readonly report: SpaceObjectMissionReport;
src/simulation/types.ts:119:export type GameCommand =
src/simulation/types.ts:154:      readonly targetPlanetId: string;
src/simulation/types.ts:243:      readonly fleetId: string;
src/simulation/types.ts:248:      readonly fleetId: string;
src/simulation/types.ts:249:      readonly formation: FleetFormation;
src/simulation/types.ts:250:      readonly targetPriority: FleetTargetPriority;
src/simulation/types.ts:255:      readonly doctrineId: CommandDoctrineId;
src/simulation/types.ts:260:      readonly fleetId: string | null;
src/simulation/types.ts:265:      readonly fleetId: string;
src/simulation/types.ts:266:      readonly targetPlanetId: string;
src/simulation/types.ts:267:      readonly mission: FleetMissionKind;
src/simulation/types.ts:272:      readonly fleetId: string;
src/simulation/types.ts:278:      readonly fleetId: string;
src/simulation/types.ts:284:      readonly fleetId: string;
src/simulation/types.ts:287:export interface CommandLogEntry {
src/simulation/types.ts:289:  readonly command: GameCommand;
src/simulation/types.ts:307:  readonly commanders: readonly EmpireCommandState[];
src/simulation/types.ts:308:  readonly fleets: readonly FleetState[];
src/simulation/types.ts:309:  readonly intelligence: readonly EmpireIntelligenceState[];
src/simulation/types.ts:319:  readonly commandLog: readonly CommandLogEntry[];
src/simulation/types.ts:323:export type CommandResult<T> =
src/simulation/units/catalog.ts:12:  getCompleteCommanderShipClass,
src/simulation/units/catalog.ts:13:  getCompleteCommanderShipDefinition,
src/simulation/units/catalog.ts:14:  getCompleteCommanderShipId,
src/simulation/units/catalog.ts:15:  getCompleteCommanderShipIds,
src/simulation/units/catalog.ts:16:} from './completeCommanderShipCatalog';
src/simulation/units/completeCommanderShipCatalog.ts:2:  CommanderAbilityDefinition,
src/simulation/units/completeCommanderShipCatalog.ts:3:  CommanderShipClass,
src/simulation/units/completeCommanderShipCatalog.ts:8:interface CommanderTemplate {
src/simulation/units/completeCommanderShipCatalog.ts:9:  readonly commanderClass: CommanderShipClass;
src/simulation/units/completeCommanderShipCatalog.ts:17:  readonly ability: CommanderAbilityDefinition;
src/simulation/units/completeCommanderShipCatalog.ts:20:export interface CompleteCommanderShipIds {
src/simulation/units/completeCommanderShipCatalog.ts:36:export const COMPLETE_COMMANDER_SHIP_CLASSES: readonly CommanderShipClass[] = [
src/simulation/units/completeCommanderShipCatalog.ts:61:const TEMPLATES: readonly CommanderTemplate[] = [
src/simulation/units/completeCommanderShipCatalog.ts:63:    commanderClass: 'annihilator',
src/simulation/units/completeCommanderShipCatalog.ts:78:    commanderClass: 'corsair',
src/simulation/units/completeCommanderShipCatalog.ts:93:    commanderClass: 'regenerator',
src/simulation/units/completeCommanderShipCatalog.ts:108:    commanderClass: 'viper',
src/simulation/units/completeCommanderShipCatalog.ts:123:    commanderClass: 'scorpion',
src/simulation/units/completeCommanderShipCatalog.ts:138:    commanderClass: 'phantom',
src/simulation/units/completeCommanderShipCatalog.ts:153:    commanderClass: 'hunter',
src/simulation/units/completeCommanderShipCatalog.ts:168:    commanderClass: 'typhoon',
src/simulation/units/completeCommanderShipCatalog.ts:183:    commanderClass: 'executor',
src/simulation/units/completeCommanderShipCatalog.ts:198:    commanderClass: 'juggernaut',
src/simulation/units/completeCommanderShipCatalog.ts:213:    commanderClass: 'argo',
src/simulation/units/completeCommanderShipCatalog.ts:228:    commanderClass: 'judge',
src/simulation/units/completeCommanderShipCatalog.ts:243:    commanderClass: 'polias',
src/simulation/units/completeCommanderShipCatalog.ts:259:export function getCompleteCommanderShipId(commanderClass: CommanderShipClass): string {
src/simulation/units/completeCommanderShipCatalog.ts:260:  return `commander.shared.${commanderClass}`;
src/simulation/units/completeCommanderShipCatalog.ts:263:export function getCompleteCommanderShipIds(): CompleteCommanderShipIds {
src/simulation/units/completeCommanderShipCatalog.ts:265:    COMPLETE_COMMANDER_SHIP_CLASSES.map((commanderClass) => [
src/simulation/units/completeCommanderShipCatalog.ts:266:      commanderClass,
src/simulation/units/completeCommanderShipCatalog.ts:267:      getCompleteCommanderShipId(commanderClass),
src/simulation/units/completeCommanderShipCatalog.ts:269:  ) as unknown as CompleteCommanderShipIds;
src/simulation/units/completeCommanderShipCatalog.ts:274:    id: getCompleteCommanderShipId(template.commanderClass),
src/simulation/units/completeCommanderShipCatalog.ts:278:    role: 'commander',
src/simulation/units/completeCommanderShipCatalog.ts:279:    commanderClass: template.commanderClass,
src/simulation/units/completeCommanderShipCatalog.ts:280:    commanderAbility: template.ability,
src/simulation/units/completeCommanderShipCatalog.ts:284:    assetId: getCompleteCommanderShipId(template.commanderClass),
src/simulation/units/completeCommanderShipCatalog.ts:300:export function getCompleteCommanderShipDefinition(
src/simulation/units/completeCommanderShipCatalog.ts:301:  commanderId: string,
src/simulation/units/completeCommanderShipCatalog.ts:303:  return COMMANDERS_BY_ID.get(commanderId);
src/simulation/units/completeCommanderShipCatalog.ts:306:export function getCompleteCommanderShipClass(
src/simulation/units/completeCommanderShipCatalog.ts:307:  commanderId: string,
src/simulation/units/completeCommanderShipCatalog.ts:308:): CommanderShipClass | undefined {
src/simulation/units/completeCommanderShipCatalog.ts:309:  return getCompleteCommanderShipDefinition(commanderId)?.commanderClass;
src/simulation/units/completeShipCatalog.ts:115:    ability: { id: 'fleet-vitality', valuePerUnitPermille: 5, maxPercent: 30 },
src/simulation/units/completeShipCatalog.ts:123:    ability: { id: 'fleet-armor', valuePerUnitPermille: 4, maxPercent: 30 },
src/simulation/units/productionCommands.ts:1:import { countCommanderShipForEmpire } from '../command/commanderShips';
src/simulation/units/productionCommands.ts:2:import { getEmpireCommandState } from '../command/commandDoctrine';
src/simulation/units/productionCommands.ts:7:import { appendCommandHistory } from '../history/stateHistory';
src/simulation/units/productionCommands.ts:20:  CommandLogEntry,
src/simulation/units/productionCommands.ts:21:  CommandResult,
src/simulation/units/productionCommands.ts:22:  GameCommand,
src/simulation/units/productionCommands.ts:43:function appendCommand(state: GameState, command: GameCommand): readonly CommandLogEntry[] {
src/simulation/units/productionCommands.ts:44:  return appendCommandHistory(state.commandLog, command);
src/simulation/units/productionCommands.ts:58:function validateCommanderProduction(
src/simulation/units/productionCommands.ts:63:): CommandResult<true> {
src/simulation/units/productionCommands.ts:64:  if (definition.commanderClass === undefined) return { ok: true, value: true };
src/simulation/units/productionCommands.ts:69:      message: 'Commander Ships can only be produced one at a time.',
src/simulation/units/productionCommands.ts:72:  if (countCommanderShipForEmpire(state, planet.ownerEmpireId, definition.id) > 0) {
src/simulation/units/productionCommands.ts:76:      message: 'An empire may own at most one Commander Ship of each type.',
src/simulation/units/productionCommands.ts:79:  const command = getEmpireCommandState(state.commanders, planet.ownerEmpireId);
src/simulation/units/productionCommands.ts:81:  if ((command?.level ?? 0) < requiredAdmiralLevel) {
src/simulation/units/productionCommands.ts:85:      message: `Admiral level ${requiredAdmiralLevel} is required for this Commander Ship.`,
src/simulation/units/productionCommands.ts:86:      details: { requiredAdmiralLevel, currentAdmiralLevel: command?.level ?? 0 },
src/simulation/units/productionCommands.ts:96:      message: `Shipyard level ${requiredShipyardLevel} is required for this Commander Ship.`,
src/simulation/units/productionCommands.ts:105:  command: Extract<GameCommand, { readonly type: 'QUEUE_UNIT_BATCH' }>,
src/simulation/units/productionCommands.ts:106:): CommandResult<GameState> {
src/simulation/units/productionCommands.ts:107:  if (!Number.isInteger(command.quantity) || command.quantity <= 0 || command.quantity > 100) {
src/simulation/units/productionCommands.ts:115:  const planet = state.planets.find((candidate) => candidate.id === command.planetId);
src/simulation/units/productionCommands.ts:119:  if (planet.ownerEmpireId !== command.empireId) {
src/simulation/units/productionCommands.ts:123:  const definition = getUnitDefinition(command.unitId);
src/simulation/units/productionCommands.ts:130:  const commanderValidation = validateCommanderProduction(
src/simulation/units/productionCommands.ts:134:    command.quantity,
src/simulation/units/productionCommands.ts:136:  if (!commanderValidation.ok) return commanderValidation;
src/simulation/units/productionCommands.ts:138:  const research = getEmpireResearch(state.research, command.empireId);
src/simulation/units/productionCommands.ts:162:  const populationReserved = definition.populationCost * command.quantity;
src/simulation/units/productionCommands.ts:177:  const hangarReserved = definition.hangarCost * command.quantity;
src/simulation/units/productionCommands.ts:192:    const defenseGridRequired = definition.defenseGridCost * command.quantity;
src/simulation/units/productionCommands.ts:207:  const cost = calculateUnitBatchCost(definition, command.quantity);
src/simulation/units/productionCommands.ts:219:  const duration = calculateUnitBatchSeconds(definition, command.quantity, planet);
src/simulation/units/productionCommands.ts:225:    quantity: command.quantity,
src/simulation/units/productionCommands.ts:242:      quantity: command.quantity,
src/simulation/units/productionCommands.ts:261:      commandLog: appendCommand(state, command),
src/simulation/units/productionCommands.ts:268:  command: Extract<GameCommand, { readonly type: 'CANCEL_UNIT_BATCH' }>,
src/simulation/units/productionCommands.ts:269:): CommandResult<GameState> {
src/simulation/units/productionCommands.ts:270:  const planet = state.planets.find((candidate) => candidate.id === command.planetId);
src/simulation/units/productionCommands.ts:271:  if (planet === undefined || planet.ownerEmpireId !== command.empireId) {
src/simulation/units/productionCommands.ts:276:    (candidate) => candidate.id === command.queueItemId,
src/simulation/units/productionCommands.ts:306:      commandLog: appendCommand(state, command),
src/simulation/units/types.ts:18:  | 'commander';
src/simulation/units/types.ts:39:  | 'fleet-vitality'
src/simulation/units/types.ts:40:  | 'fleet-armor'
src/simulation/units/types.ts:59:export type CommanderShipClass =
src/simulation/units/types.ts:74:export type CommanderAbilityEffect =
src/simulation/units/types.ts:89:export interface CommanderAbilityDefinition {
src/simulation/units/types.ts:90:  readonly id: CommanderShipClass;
src/simulation/units/types.ts:91:  readonly effect: CommanderAbilityEffect;
src/simulation/units/types.ts:167:  readonly commanderClass?: CommanderShipClass;
src/simulation/units/types.ts:168:  readonly commanderAbility?: CommanderAbilityDefinition;
src/simulation/upgrades/shipUpgrades.ts:1:import { appendCommandHistory } from '../history/stateHistory';
src/simulation/upgrades/shipUpgrades.ts:14:  CommandLogEntry,
src/simulation/upgrades/shipUpgrades.ts:15:  CommandResult,
src/simulation/upgrades/shipUpgrades.ts:16:  GameCommand,
src/simulation/upgrades/shipUpgrades.ts:64:function appendCommand(state: GameState, command: GameCommand): readonly CommandLogEntry[] {
src/simulation/upgrades/shipUpgrades.ts:65:  return appendCommandHistory(state.commandLog, command);
src/simulation/upgrades/shipUpgrades.ts:126:  command: Extract<GameCommand, { readonly type: 'QUEUE_SHIP_UPGRADE' }>,
src/simulation/upgrades/shipUpgrades.ts:127:): CommandResult<GameState> {
src/simulation/upgrades/shipUpgrades.ts:128:  const planet = state.planets.find((candidate) => candidate.id === command.planetId);
src/simulation/upgrades/shipUpgrades.ts:129:  if (planet === undefined || planet.ownerEmpireId !== command.empireId) {
src/simulation/upgrades/shipUpgrades.ts:132:  const empireState = getEmpireShipUpgradeState(state.shipUpgrades, command.empireId);
src/simulation/upgrades/shipUpgrades.ts:139:  const definition = getUnitDefinition(command.unitId);
src/simulation/upgrades/shipUpgrades.ts:161:    command.empireId,
src/simulation/upgrades/shipUpgrades.ts:162:    command.unitId,
src/simulation/upgrades/shipUpgrades.ts:163:  )[command.track];
src/simulation/upgrades/shipUpgrades.ts:168:  const cost = calculateShipUpgradeCost(command.unitId, command.track, targetLevel);
src/simulation/upgrades/shipUpgrades.ts:176:    unitId: command.unitId,
src/simulation/upgrades/shipUpgrades.ts:177:    track: command.track,
src/simulation/upgrades/shipUpgrades.ts:182:      state.clock.elapsedSeconds + calculateShipUpgradeSeconds(command.unitId, targetLevel),
src/simulation/upgrades/shipUpgrades.ts:191:      empireId: command.empireId,
src/simulation/upgrades/shipUpgrades.ts:193:      unitId: command.unitId,
src/simulation/upgrades/shipUpgrades.ts:194:      track: command.track,
src/simulation/upgrades/shipUpgrades.ts:213:      commandLog: appendCommand(state, command),
src/simulation/upgrades/shipUpgrades.ts:220:  command: Extract<GameCommand, { readonly type: 'CANCEL_SHIP_UPGRADE' }>,
src/simulation/upgrades/shipUpgrades.ts:221:): CommandResult<GameState> {
src/simulation/upgrades/shipUpgrades.ts:222:  const empireState = getEmpireShipUpgradeState(state.shipUpgrades, command.empireId);
src/simulation/upgrades/shipUpgrades.ts:223:  const queueItem = empireState?.queue.find((item) => item.id === command.queueItemId);
src/simulation/upgrades/shipUpgrades.ts:228:    (candidate) => candidate.id === queueItem.planetId && candidate.ownerEmpireId === command.empireId,
src/simulation/upgrades/shipUpgrades.ts:249:      commandLog: appendCommand(state, command),
src/simulation/validation/fullGameValidation.ts:5:  validateFactionMechanicalCatalog,
src/simulation/validation/fullGameValidation.ts:10:import { queueUnitBatch } from '../units/productionCommands';
src/simulation/validation/fullGameValidation.ts:11:import { COMPLETE_COMMANDER_SHIP_CATALOG } from '../units/completeCommanderShipCatalog';
src/simulation/validation/fullGameValidation.ts:57:    commanders: state.commanders.map((command) =>
src/simulation/validation/fullGameValidation.ts:58:      command.empireId === 'player'
src/simulation/validation/fullGameValidation.ts:59:        ? { ...command, experience: 1_000_000, level: 40 }
src/simulation/validation/fullGameValidation.ts:60:        : command,
src/simulation/validation/fullGameValidation.ts:65:export function validateFullGameCatalogClosure(): readonly FullGameValidationIssue[] {
src/simulation/validation/fullGameValidation.ts:69:    for (const details of validateFactionMechanicalCatalog(catalog)) {
src/simulation/validation/fullGameValidation.ts:90:export function validateFullGameProductionPaths(
src/simulation/validation/fullGameValidation.ts:128:    ...validateFullGameCatalogClosure(),
src/simulation/validation/fullGameValidation.ts:129:    ...validateFullGameProductionPaths(seed),
src/storage/migrateGameState.ts:11:import type { FleetState } from '../simulation/fleets/types';
src/storage/migrateGameState.ts:13:import { createInitialIntelligenceStates } from '../simulation/intelligence/intelligenceState';
src/storage/migrateGameState.ts:14:import type { EmpireIntelligenceState } from '../simulation/intelligence/types';
src/storage/migrateGameState.ts:143:function readFleets(value: unknown): readonly FleetState[] | undefined {
src/storage/migrateGameState.ts:146:  const fleets: FleetState[] = [];
src/storage/migrateGameState.ts:149:    fleets.push({
src/storage/migrateGameState.ts:150:      ...(item as unknown as FleetState),
src/storage/migrateGameState.ts:151:      mission: isRecord(item.mission) &&
src/storage/migrateGameState.ts:152:        (item.mission.kind === 'deploy' || item.mission.kind === 'transport' || item.mission.kind === 'scout' ||
src/storage/migrateGameState.ts:153:          item.mission.kind === 'attack' || item.mission.kind === 'recycle' || item.mission.kind === 'colonize' ||
src/storage/migrateGameState.ts:154:          item.mission.kind === 'expedition' || item.mission.kind === 'space-object') &&
src/storage/migrateGameState.ts:155:        typeof item.mission.targetPlanetId === 'string'
src/storage/migrateGameState.ts:156:        ? { kind: item.mission.kind, targetPlanetId: item.mission.targetPlanetId }
src/storage/migrateGameState.ts:160:  return fleets;
src/storage/migrateGameState.ts:162:function readIntelligenceStates(value: unknown, empireIds: readonly string[]): readonly EmpireIntelligenceState[] | undefined {
src/storage/migrateGameState.ts:163:  if (value === undefined) return createInitialIntelligenceStates(empireIds);
src/storage/migrateGameState.ts:165:  const states: EmpireIntelligenceState[] = [];
src/storage/migrateGameState.ts:168:    states.push({ empireId: item.empireId, observations: item.observations as EmpireIntelligenceState['observations'], alerts: item.alerts as EmpireIntelligenceState['alerts'] });
src/storage/migrateGameState.ts:189:      typeof item.originPlanetId !== 'string' || typeof item.targetPlanetId !== 'string' || !isResourceId(item.resourceId) ||
src/storage/migrateGameState.ts:321:  const fleets = readFleets(value.fleets);
src/storage/migrateGameState.ts:322:  const intelligence = readIntelligenceStates(value.intelligence, empireIds);
src/storage/migrateGameState.ts:329:  if (research === undefined || fleets === undefined || intelligence === undefined || debrisFields === undefined ||
src/storage/migrateGameState.ts:337:    fleets,
src/storage/migrateGameState.ts:338:    intelligence,
src/storage/migrateGameStateV13.ts:4:  calculateCommandLevel,
src/storage/migrateGameStateV13.ts:5:  createInitialCommandStates,
src/storage/migrateGameStateV13.ts:6:  isCommandDoctrineId,
src/storage/migrateGameStateV13.ts:7:} from '../simulation/command/commandDoctrine';
src/storage/migrateGameStateV13.ts:8:import type { EmpireCommandState } from '../simulation/command/types';
src/storage/migrateGameStateV13.ts:113:function readCommandStates(
src/storage/migrateGameStateV13.ts:116:): readonly EmpireCommandState[] | undefined {
src/storage/migrateGameStateV13.ts:117:  if (value === undefined) return createInitialCommandStates(empireIds);
src/storage/migrateGameStateV13.ts:119:  const states: EmpireCommandState[] = [];
src/storage/migrateGameStateV13.ts:124:      !isCommandDoctrineId(item.doctrineId) ||
src/storage/migrateGameStateV13.ts:126:      (item.flagshipFleetId !== null && typeof item.flagshipFleetId !== 'string')
src/storage/migrateGameStateV13.ts:134:      level: calculateCommandLevel(item.experience),
src/storage/migrateGameStateV13.ts:135:      flagshipFleetId: item.flagshipFleetId,
src/storage/migrateGameStateV13.ts:145:        flagshipFleetId: null,
src/storage/migrateGameStateV13.ts:155:  const commanders = readCommandStates(value.commanders, empireIds);
src/storage/migrateGameStateV13.ts:165:  if (shipUpgrades === undefined || commanders === undefined || botAutomation === undefined) return undefined;
src/storage/migrateGameStateV13.ts:173:    commanders,
src/storage/migrateGameStateV14.ts:3:import type { EmpireIntelligenceState } from '../simulation/intelligence/types';
src/storage/migrateGameStateV14.ts:69:function normalizeIntelligence(
src/storage/migrateGameStateV14.ts:70:  value: readonly EmpireIntelligenceState[],
src/storage/migrateGameStateV14.ts:72:): readonly EmpireIntelligenceState[] | undefined {
src/storage/migrateGameStateV14.ts:73:  const states: EmpireIntelligenceState[] = [];
src/storage/migrateGameStateV14.ts:78:        coordinateByPlanetId(planets, observation.targetPlanetId);
src/storage/migrateGameStateV14.ts:88:      const coordinate = alert.coordinate ?? coordinateByPlanetId(planets, alert.targetPlanetId);
src/storage/migrateGameStateV14.ts:94:      observations: observations as EmpireIntelligenceState['observations'],
src/storage/migrateGameStateV14.ts:95:      alerts: alerts as EmpireIntelligenceState['alerts'],
src/storage/migrateGameStateV14.ts:149:  const intelligence = normalizeIntelligence(legacy.intelligence, planets);
src/storage/migrateGameStateV14.ts:152:  if (intelligence === undefined || debrisFields === undefined || spaceObjects === undefined) {
src/storage/migrateGameStateV14.ts:161:    intelligence,
src/storage/migrateLegacySynodAliases.ts:8:  'building.aegis.command': 'building.synod.concord-nexus',
src/storage/migrateLegacySynodAliases.ts:29:  'technology.aegis.battle-network': 'technology.synod.chorus-command',
src/storage/migrateLegacySynodAliases.ts:67:  fleetIds: ReadonlySet<string>,
src/storage/migrateLegacySynodAliases.ts:70:    return value.some((item) => hasSynodContext(item, empireIds, planetIds, fleetIds));
src/storage/migrateLegacySynodAliases.ts:79:        (key.toLowerCase().includes('fleetid') && fleetIds.has(nested)))
src/storage/migrateLegacySynodAliases.ts:83:    if (hasSynodContext(nested, empireIds, planetIds, fleetIds)) return true;
src/storage/migrateLegacySynodAliases.ts:107:  const synodFleetIds = new Set(
src/storage/migrateLegacySynodAliases.ts:108:    state.fleets
src/storage/migrateLegacySynodAliases.ts:109:      .filter((fleet) => synodEmpireIds.has(fleet.empireId))
src/storage/migrateLegacySynodAliases.ts:110:      .map((fleet) => fleet.id),
src/storage/migrateLegacySynodAliases.ts:114:    hasSynodContext(value, synodEmpireIds, synodPlanetIds, synodFleetIds)
src/storage/migrateLegacySynodAliases.ts:141:    fleets: state.fleets.map((fleet) =>
src/storage/migrateLegacySynodAliases.ts:142:      synodEmpireIds.has(fleet.empireId) ? mapMechanicalIds(fleet) : fleet,
src/storage/migrateLegacySynodAliases.ts:144:    intelligence: state.intelligence.map((intelligence) => ({
src/storage/migrateLegacySynodAliases.ts:145:      ...intelligence,
src/storage/migrateLegacySynodAliases.ts:146:      observations: intelligence.observations.map((observation) =>
src/storage/migrateLegacySynodAliases.ts:147:        synodPlanetIds.has(observation.targetPlanetId)
src/storage/migrateLegacySynodAliases.ts:153:    commandLog: state.commandLog.map(migrateContextual),
src/storage/migrateLegacyVeyraAliases.ts:8:  'building.aegis.command': 'building.veyra.swarm-heart',
src/storage/migrateLegacyVeyraAliases.ts:67:  fleetIds: ReadonlySet<string>,
src/storage/migrateLegacyVeyraAliases.ts:70:    return value.some((item) => hasVeyraContext(item, empireIds, planetIds, fleetIds));
src/storage/migrateLegacyVeyraAliases.ts:79:        (key.toLowerCase().includes('fleetid') && fleetIds.has(nested)))
src/storage/migrateLegacyVeyraAliases.ts:83:    if (hasVeyraContext(nested, empireIds, planetIds, fleetIds)) return true;
src/storage/migrateLegacyVeyraAliases.ts:107:  const veyraFleetIds = new Set(
src/storage/migrateLegacyVeyraAliases.ts:108:    state.fleets
src/storage/migrateLegacyVeyraAliases.ts:109:      .filter((fleet) => veyraEmpireIds.has(fleet.empireId))
src/storage/migrateLegacyVeyraAliases.ts:110:      .map((fleet) => fleet.id),
src/storage/migrateLegacyVeyraAliases.ts:114:    hasVeyraContext(value, veyraEmpireIds, veyraPlanetIds, veyraFleetIds)
src/storage/migrateLegacyVeyraAliases.ts:141:    fleets: state.fleets.map((fleet) =>
src/storage/migrateLegacyVeyraAliases.ts:142:      veyraEmpireIds.has(fleet.empireId) ? mapMechanicalIds(fleet) : fleet,
src/storage/migrateLegacyVeyraAliases.ts:144:    intelligence: state.intelligence.map((intelligence) => ({
src/storage/migrateLegacyVeyraAliases.ts:145:      ...intelligence,
src/storage/migrateLegacyVeyraAliases.ts:146:      observations: intelligence.observations.map((observation) =>
src/storage/migrateLegacyVeyraAliases.ts:147:        veyraPlanetIds.has(observation.targetPlanetId)
src/storage/migrateLegacyVeyraAliases.ts:153:    commandLog: state.commandLog.map(migrateContextual),
src/storage/saveFormat.ts:4:import { COMMAND_MAX_LEVEL, isCommandDoctrineId } from '../simulation/command/commandDoctrine';
src/storage/saveFormat.ts:43:    Array.isArray(value.commandLog) && Array.isArray(value.eventLog);
src/storage/saveFormat.ts:99:function isCommandState(value: unknown): boolean {
src/storage/saveFormat.ts:101:    isCommandDoctrineId(value.doctrineId) && isNonNegativeInteger(value.experience) &&
src/storage/saveFormat.ts:103:    (value.flagshipFleetId === null || typeof value.flagshipFleetId === 'string');
src/storage/saveFormat.ts:105:function isFleetLocation(value: unknown): boolean {
src/storage/saveFormat.ts:112:function isFleet(value: unknown): boolean {
src/storage/saveFormat.ts:113:  const validMission = value !== null && isRecord(value) && (value.mission === null ||
src/storage/saveFormat.ts:114:    (isRecord(value.mission) &&
src/storage/saveFormat.ts:115:      (value.mission.kind === 'deploy' || value.mission.kind === 'transport' || value.mission.kind === 'scout' ||
src/storage/saveFormat.ts:116:        value.mission.kind === 'attack' || value.mission.kind === 'recycle' || value.mission.kind === 'colonize' ||
src/storage/saveFormat.ts:117:        value.mission.kind === 'expedition' || value.mission.kind === 'space-object') &&
src/storage/saveFormat.ts:118:      typeof value.mission.targetPlanetId === 'string'));
src/storage/saveFormat.ts:119:  return validMission && typeof value.id === 'string' && typeof value.empireId === 'string' &&
src/storage/saveFormat.ts:121:    isFleetLocation(value.location) && isRecord(value.ships) && Object.keys(value.ships).length > 0 &&
src/storage/saveFormat.ts:127:    typeof value.targetPlanetId === 'string' && isSpaceCoordinate(value.coordinate) &&
src/storage/saveFormat.ts:134:function isIntelligenceAlert(value: unknown): boolean {
src/storage/saveFormat.ts:136:    (value.sourceEmpireId === null || typeof value.sourceEmpireId === 'string') && typeof value.targetPlanetId === 'string' &&
src/storage/saveFormat.ts:139:function isIntelligenceState(value: unknown): boolean {
src/storage/saveFormat.ts:141:    value.observations.length <= STATE_HISTORY_LIMITS.intelligenceObservationsPerEmpire &&
src/storage/saveFormat.ts:143:    value.alerts.length <= STATE_HISTORY_LIMITS.intelligenceAlertsPerEmpire &&
src/storage/saveFormat.ts:144:    value.alerts.every(isIntelligenceAlert);
src/storage/saveFormat.ts:158:    typeof value.originPlanetId === 'string' && typeof value.targetPlanetId === 'string' &&
src/storage/saveFormat.ts:159:    value.originPlanetId !== value.targetPlanetId && isResourceId(value.resourceId) &&
src/storage/saveFormat.ts:231:    Array.isArray(value.commandLog) && value.commandLog.length <= STATE_HISTORY_LIMITS.commands &&
src/storage/saveFormat.ts:237:    Array.isArray(value.commanders) && value.commanders.every(isCommandState) &&
src/storage/saveFormat.ts:238:    value.commanders.length === value.empires.length && Array.isArray(value.fleets) && value.fleets.every(isFleet) &&
src/storage/saveFormat.ts:239:    Array.isArray(value.intelligence) && value.intelligence.every(isIntelligenceState) &&
src/storage/saveFormat.ts:240:    value.intelligence.length === value.empires.length && Array.isArray(value.debrisFields) &&
src/styles/accessibilityRuntime.css:113:html[data-viewport-mode='mobile'] .command-panel {
src/styles/commandDoctrine.css:1:.command-doctrine-dialog {
src/styles/commandDoctrine.css:11:.command-doctrine-dialog::backdrop { background: rgb(2 8 18 / 78%); backdrop-filter: blur(7px); }
src/styles/commandDoctrine.css:12:.command-doctrine-header { display: flex; justify-content: space-between; gap: 24px; padding: 24px; border-bottom: 1px solid var(--border-subtle); }
src/styles/commandDoctrine.css:13:.command-doctrine-header h2 { margin: 4px 0 6px; }
src/styles/commandDoctrine.css:14:.command-doctrine-header p { margin: 0; color: var(--text-secondary); }
src/styles/commandDoctrine.css:15:.command-progress, .command-doctrine-cards { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 12px; padding: 18px 24px; }
src/styles/commandDoctrine.css:16:.command-progress article, .command-doctrine-cards article { padding: 16px; border: 1px solid var(--border-subtle); border-radius: 14px; background: var(--surface-panel); }
src/styles/commandDoctrine.css:17:.command-progress span { display: block; color: var(--text-secondary); font-size: .82rem; margin-bottom: 6px; }
src/styles/commandDoctrine.css:18:.command-progress strong { display: block; font-size: 1.1rem; }
src/styles/commandDoctrine.css:19:.command-progress small { display: block; margin-top: 6px; color: var(--text-secondary); }
src/styles/commandDoctrine.css:20:.command-progress progress { width: 100%; margin-top: 10px; }
src/styles/commandDoctrine.css:21:.command-doctrine-form, .flagship-form { display: grid; grid-template-columns: minmax(0, 1fr) auto; gap: 12px; align-items: end; padding: 12px 24px; }
src/styles/commandDoctrine.css:22:.command-doctrine-form label, .flagship-form label { display: grid; gap: 7px; color: var(--text-secondary); }
src/styles/commandDoctrine.css:23:.command-doctrine-form select, .flagship-form select { min-height: 42px; border: 1px solid var(--border-strong); border-radius: 10px; padding: 0 12px; color: var(--text-primary); background: var(--surface-panel); }
src/styles/commandDoctrine.css:24:.command-doctrine-cards { padding-bottom: 24px; }
src/styles/commandDoctrine.css:25:.command-doctrine-cards strong { display: block; margin-bottom: 8px; }
src/styles/commandDoctrine.css:26:.command-doctrine-cards p { margin: 0; color: var(--text-secondary); line-height: 1.45; }
src/styles/commandDoctrine.css:27:.commander-roster-heading { padding: 6px 24px 0; border-top: 1px solid var(--border-subtle); }
src/styles/commandDoctrine.css:28:.commander-roster-heading h3 { margin: 4px 0 0; }
src/styles/commandDoctrine.css:29:.commander-ship-cards { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 12px; padding: 18px 24px 24px; }
src/styles/commandDoctrine.css:30:.commander-ship-card { overflow: hidden; padding: 16px; border: 1px solid var(--border-subtle); border-radius: 14px; background: var(--surface-panel); opacity: .78; }
src/styles/commandDoctrine.css:31:.commander-ship-card.is-owned, .commander-ship-card.is-available { opacity: 1; border-color: var(--border-strong); }
src/styles/commandDoctrine.css:32:.commander-ship-card.is-locked { filter: saturate(.55); }
src/styles/commandDoctrine.css:33:.commander-ship-card__art { height: 190px; margin: -16px -16px 14px; background-position: center; background-repeat: no-repeat; }
src/styles/commandDoctrine.css:34:.commander-ship-card.is-active { outline: 2px solid var(--accent-primary); outline-offset: 1px; }
src/styles/commandDoctrine.css:35:.commander-ship-card header { display: flex; justify-content: space-between; gap: 14px; align-items: start; }
src/styles/commandDoctrine.css:36:.commander-ship-card header span { max-width: 45%; text-align: right; color: var(--text-secondary); font-size: .78rem; }
src/styles/commandDoctrine.css:37:.commander-ship-card p { margin: 10px 0; color: var(--text-secondary); line-height: 1.4; }
src/styles/commandDoctrine.css:38:.commander-ship-card dl { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; margin: 12px 0; }
src/styles/commandDoctrine.css:39:.commander-ship-card dl div { padding: 8px; border-radius: 9px; background: color-mix(in srgb, var(--surface-elevated) 75%, transparent); }
src/styles/commandDoctrine.css:40:.commander-ship-card dt { color: var(--text-secondary); font-size: .72rem; }
src/styles/commandDoctrine.css:41:.commander-ship-card dd { margin: 3px 0 0; font-size: .82rem; }
src/styles/commandDoctrine.css:42:.commander-ship-card > small { color: var(--text-secondary); line-height: 1.35; }
src/styles/commandDoctrine.css:44:  .command-progress, .command-doctrine-cards, .commander-ship-cards { grid-template-columns: 1fr; }
src/styles/commandDoctrine.css:45:  .command-doctrine-form, .flagship-form { grid-template-columns: 1fr; }
src/styles/commandDoctrine.css:46:  .commander-ship-card dl { grid-template-columns: 1fr; }
src/styles/commandRanking.css:1:.command-ranking-dialog {
src/styles/commandRanking.css:13:.command-ranking-dialog::backdrop {
src/styles/commandRanking.css:18:.command-ranking-header {
src/styles/commandRanking.css:25:.command-ranking-header h2,
src/styles/commandRanking.css:26:.command-ranking-header p,
src/styles/commandRanking.css:27:.command-profile h3,
src/styles/commandRanking.css:28:.command-profile p,
src/styles/commandRanking.css:29:.command-ranking-list h3,
src/styles/commandRanking.css:30:.command-ranking-list p {
src/styles/commandRanking.css:34:.command-profile {
src/styles/commandRanking.css:43:  background-image: linear-gradient(90deg, rgba(2, 8, 15, 0.25), rgba(2, 8, 15, 0.96) 48%), var(--command-profile-bg);
src/styles/commandRanking.css:48:.command-profile-hero {
src/styles/commandRanking.css:56:.command-profile-body {
src/styles/commandRanking.css:63:.command-profile-identity {
src/styles/commandRanking.css:70:.command-profile-identity img {
src/styles/commandRanking.css:76:.command-profile-identity h3 {
src/styles/commandRanking.css:81:.command-profile-identity div > p:last-child {
src/styles/commandRanking.css:86:.command-profile-stats {
src/styles/commandRanking.css:92:.command-profile-stats div {
src/styles/commandRanking.css:101:.command-profile-stats span {
src/styles/commandRanking.css:107:.command-ranking-list {
src/styles/commandRanking.css:111:.command-ranking-list > header {
src/styles/commandRanking.css:119:.command-ranking-list > header p {
src/styles/commandRanking.css:124:.command-ranking-entry {
src/styles/commandRanking.css:135:.command-ranking-entry + .command-ranking-entry {
src/styles/commandRanking.css:139:.command-ranking-entry.is-player {
src/styles/commandRanking.css:144:.command-ranking-entry img {
src/styles/commandRanking.css:150:.command-ranking-entry > div {
src/styles/commandRanking.css:155:.command-ranking-entry small {
src/styles/commandRanking.css:159:.command-ranking-place {
src/styles/commandRanking.css:164:.command-ranking-entry b {
src/styles/commandRanking.css:170:  .command-profile {
src/styles/commandRanking.css:174:  .command-profile-hero {
src/styles/commandRanking.css:179:  .command-profile-stats {
src/styles/expeditions.css:33:.expedition-reports {
src/styles/expeditions.css:41:.expedition-reports {
src/styles/expeditions.css:83:.expedition-reports article {
src/styles/expeditions.css:98:.expedition-reports article p,
src/styles/expeditions.css:99:.expedition-reports article small,
src/styles/factionTheme.css:49:html[data-faction] .commander-emblem {
src/styles/factions.css:1:.commander-emblem {
src/styles/fleetDoctrine.css:1:.fleet-doctrine-dialog {
src/styles/fleetDoctrine.css:12:.fleet-doctrine-dialog::backdrop {
src/styles/fleetDoctrine.css:17:.fleet-doctrine-header {
src/styles/fleetDoctrine.css:25:.fleet-doctrine-header h2,
src/styles/fleetDoctrine.css:26:.fleet-doctrine-header p,
src/styles/fleetDoctrine.css:27:.fleet-doctrine-summary p,
src/styles/fleetDoctrine.css:28:.fleet-doctrine-skills p {
src/styles/fleetDoctrine.css:32:.fleet-doctrine-header > div > p:last-child {
src/styles/fleetDoctrine.css:37:.fleet-doctrine-form {
src/styles/fleetDoctrine.css:45:.fleet-doctrine-form label {
src/styles/fleetDoctrine.css:53:.fleet-doctrine-form select {
src/styles/fleetDoctrine.css:61:.fleet-doctrine-summary,
src/styles/fleetDoctrine.css:62:.fleet-doctrine-skills {
src/styles/fleetDoctrine.css:69:.fleet-doctrine-summary article,
src/styles/fleetDoctrine.css:70:.fleet-doctrine-skills article {
src/styles/fleetDoctrine.css:80:.fleet-doctrine-summary span {
src/styles/fleetDoctrine.css:86:.fleet-doctrine-summary p,
src/styles/fleetDoctrine.css:87:.fleet-doctrine-skills p {
src/styles/fleetDoctrine.css:93:  .fleet-doctrine-form,
src/styles/fleetDoctrine.css:94:  .fleet-doctrine-summary,
src/styles/fleetDoctrine.css:95:  .fleet-doctrine-skills {
src/styles/galaxyIntel.css:164:.galaxy-intel-mission-button {
src/styles/globalHud.css:237:.command-panel {
src/styles/main.css:378:.command-panel {
src/styles/main.css:442:.commander-row {
src/styles/main.css:450:.commander-mark {
src/styles/main.css:632:  .command-panel {
src/styles/main.css:698:  .command-panel {
src/styles/missionReports.css:1:.mission-reports-dialog {
src/styles/missionReports.css:12:.mission-reports-dialog::backdrop {
src/styles/missionReports.css:17:.mission-reports-header {
src/styles/missionReports.css:29:.mission-reports-header h2,
src/styles/missionReports.css:30:.mission-reports-dialog h3 {
src/styles/missionReports.css:34:.mission-reports-summary,
src/styles/missionReports.css:35:.mission-reports-controls,
src/styles/missionReports.css:36:.mission-reports-comparison,
src/styles/missionReports.css:37:.mission-reports-list {
src/styles/missionReports.css:47:.mission-reports-controls {
src/styles/missionReports.css:51:.mission-report-control {
src/styles/missionReports.css:58:.mission-report-control input,
src/styles/missionReports.css:59:.mission-report-control select {
src/styles/missionReports.css:68:.mission-reports-comparison {
src/styles/missionReports.css:72:.mission-reports-comparison h3 {
src/styles/missionReports.css:76:.mission-reports-comparison article,
src/styles/missionReports.css:77:.mission-report-card {
src/styles/missionReports.css:86:.mission-reports-comparison p,
src/styles/missionReports.css:87:.mission-reports-comparison small,
src/styles/missionReports.css:88:.mission-report-card p,
src/styles/missionReports.css:89:.mission-report-card small {
src/styles/missionReports.css:93:.mission-reports-comparison small,
src/styles/missionReports.css:94:.mission-report-card small {
src/styles/missionReports.css:98:.mission-report-card header {
src/styles/missionReports.css:105:.mission-report-card header div {
src/styles/missionReports.css:112:.mission-report-card header span {
src/styles/missionReports.css:120:.mission-report-card.is-pve {
src/styles/missionReports.css:124:.mission-report-card.is-pvp {
src/styles/missionReports.css:128:.mission-report-card.is-system {
src/styles/missionReports.css:132:.mission-combat-details {
src/styles/missionReports.css:139:.mission-combat-details > summary {
src/styles/missionReports.css:148:.mission-combat-details[open] > summary {
src/styles/missionReports.css:152:.mission-combat-table-wrap {
src/styles/missionReports.css:157:.mission-combat-details table {
src/styles/missionReports.css:164:.mission-combat-details th,
src/styles/missionReports.css:165:.mission-combat-details td {
src/styles/missionReports.css:173:.mission-combat-details th {
src/styles/missionReports.css:181:.mission-combat-details td:nth-child(7) {
src/styles/missionReports.css:186:.mission-combat-details td:nth-child(12) {
src/styles/missionReports.css:191:.mission-combat-details > small {
src/styles/missionReports.css:198:  .mission-reports-controls {
src/styles/missionReports.css:204:  .mission-reports-controls {
src/styles/missionReports.css:208:  .mission-report-card header {
src/styles/missionReports.css:212:  .mission-report-card header div {
src/styles/missions.css:1:.mission-screen-dialog {
src/styles/missions.css:12:.mission-screen-dialog::backdrop {
src/styles/missions.css:17:.mission-screen-dialog > header {
src/styles/missions.css:27:.mission-screen-dialog > header h2 {
src/styles/missions.css:31:.mission-screen-dialog > header p:last-child {
src/styles/missions.css:37:.mission-screen-content {
src/styles/missions.css:45:.mission-overview {
src/styles/missions.css:51:.mission-overview > div {
src/styles/missions.css:61:.mission-overview span {
src/styles/missions.css:68:.mission-overview strong {
src/styles/missions.css:72:.mission-overview .mission-target-notice {
src/styles/missions.css:81:.mission-workspace {
src/styles/missions.css:88:.mission-create,
src/styles/missions.css:89:.mission-fleet-list,
src/styles/missions.css:90:.mission-intelligence,
src/styles/missions.css:91:.mission-battles,
src/styles/missions.css:92:.mission-debris {
src/styles/missions.css:98:.mission-create,
src/styles/missions.css:99:.mission-fleet-list {
src/styles/missions.css:106:.mission-section-heading h3,
src/styles/missions.css:107:.mission-section-heading p {
src/styles/missions.css:111:.mission-section-heading p {
src/styles/missions.css:117:.mission-origin-field,
src/styles/missions.css:118:.mission-cargo-grid label,
src/styles/missions.css:119:.mission-ship-option label {
src/styles/missions.css:126:.mission-screen-dialog input,
src/styles/missions.css:127:.mission-screen-dialog select,
src/styles/missions.css:128:.mission-screen-dialog button {
src/styles/missions.css:137:.mission-screen-dialog button:not(:disabled) {
src/styles/missions.css:141:.mission-screen-dialog button:disabled {
src/styles/missions.css:145:.mission-ship-grid {
src/styles/missions.css:151:.mission-ship-option {
src/styles/missions.css:162:.mission-ship-option > img {
src/styles/missions.css:169:.mission-ship-option > div {
src/styles/missions.css:176:.mission-ship-option strong,
src/styles/missions.css:177:.mission-ship-option small {
src/styles/missions.css:181:.mission-ship-option small {
src/styles/missions.css:186:.mission-ship-option input {
src/styles/missions.css:191:.mission-cargo-grid {
src/styles/missions.css:197:.mission-create hr {
src/styles/missions.css:203:.mission-composer-status,
src/styles/missions.css:204:.mission-route-preview {
src/styles/missions.css:211:.mission-composer-status.is-blocked,
src/styles/missions.css:212:.mission-route-preview.is-blocked {
src/styles/missions.css:216:.mission-primary-action {
src/styles/missions.css:222:.mission-fleet-card {
src/styles/missions.css:231:.mission-fleet-card__visual {
src/styles/missions.css:239:.mission-fleet-card__visual img {
src/styles/missions.css:246:.mission-fleet-card__visual span {
src/styles/missions.css:258:.mission-fleet-card__body {
src/styles/missions.css:265:.mission-fleet-card__body > p,
src/styles/missions.css:266:.mission-transit p {
src/styles/missions.css:272:.mission-transit {
src/styles/missions.css:277:.mission-transit progress {
src/styles/missions.css:283:.mission-actions {
src/styles/missions.css:290:.mission-actions .mission-route-preview {
src/styles/missions.css:294:.mission-operational-grid {
src/styles/missions.css:300:.mission-intelligence,
src/styles/missions.css:301:.mission-battles,
src/styles/missions.css:302:.mission-debris {
src/styles/missions.css:309:.mission-intelligence h3,
src/styles/missions.css:310:.mission-battles h3,
src/styles/missions.css:311:.mission-debris h3 {
src/styles/missions.css:315:.mission-intelligence article,
src/styles/missions.css:316:.mission-battles article,
src/styles/missions.css:317:.mission-debris article {
src/styles/missions.css:323:.mission-battles article {
src/styles/missions.css:328:.mission-debris article {
src/styles/missions.css:333:.mission-intelligence p,
src/styles/missions.css:334:.mission-battles p,
src/styles/missions.css:335:.mission-debris p {
src/styles/missions.css:341:.mission-empty {
src/styles/missions.css:351:  .mission-workspace,
src/styles/missions.css:352:  .mission-operational-grid {
src/styles/missions.css:355:  .mission-overview {
src/styles/missions.css:361:  .mission-ship-grid,
src/styles/missions.css:362:  .mission-cargo-grid,
src/styles/missions.css:363:  .mission-actions {
src/styles/missions.css:366:  .mission-fleet-card {
src/styles/missions.css:369:  .mission-fleet-card__visual img {
src/styles/planet.css:363:.economy-report dl {
src/styles/planet.css:367:.economy-report dl > div {
src/styles/planet.css:375:.economy-report dt {
src/styles/planet.css:380:.economy-report dd {
src/styles/planetZones.css:17:.game-layout.is-planet-view .command-panel {
src/styles/planetZones.css:1004:.economy-report dl {
src/styles/planetZones.css:1014:.economy-report dl > div {
src/styles/planetZones.css:1023:.economy-report dt {
src/styles/planetZones.css:1028:.economy-report dd {
src/styles/planetZones.css:1185:  .economy-report {
src/styles/spaceObjects.css:36:.space-objects-reports {
src/styles/spaceObjects.css:94:.space-objects-reports article {
src/styles/spaceObjects.css:117:.space-objects-reports p,
src/styles/spaceObjects.css:119:.space-objects-reports small {
src/styles/spaceObjects.css:124:.space-objects-reports small {
src/ui/accessibilityRuntime.ts:52:    if (!image.classList.contains('brand-logo') && !image.classList.contains('commander-emblem')) {
src/ui/commandDoctrineScreen.ts:1:import '../styles/commandDoctrine.css';
src/ui/commandDoctrineScreen.ts:5:  countCommanderShipForEmpire,
src/ui/commandDoctrineScreen.ts:6:  selectActiveCommanderShip,
src/ui/commandDoctrineScreen.ts:7:} from '../simulation/command/commanderShips';
src/ui/commandDoctrineScreen.ts:12:  getEmpireCommandState,
src/ui/commandDoctrineScreen.ts:13:} from '../simulation/command/commandDoctrine';
src/ui/commandDoctrineScreen.ts:14:import type { CommandDoctrineId } from '../simulation/command/types';
src/ui/commandDoctrineScreen.ts:15:import type { GameCommand, GameState } from '../simulation/types';
src/ui/commandDoctrineScreen.ts:16:import { COMPLETE_COMMANDER_SHIP_CATALOG } from '../simulation/units/completeCommanderShipCatalog';
src/ui/commandDoctrineScreen.ts:18:interface CommandDoctrineBridge {
src/ui/commandDoctrineScreen.ts:20:  readonly execute: (command: GameCommand, successMessage: string) => boolean;
src/ui/commandDoctrineScreen.ts:24:  const existing = document.querySelector<HTMLButtonElement>('#nav-command-doctrine');
src/ui/commandDoctrineScreen.ts:26:  const fleetDoctrine = document.querySelector<HTMLButtonElement>('#nav-fleet-doctrine');
src/ui/commandDoctrineScreen.ts:28:  button.id = 'nav-command-doctrine';
src/ui/commandDoctrineScreen.ts:31:  button.setAttribute('aria-label', 'Адмирал и Commander Ships');
src/ui/commandDoctrineScreen.ts:33:  fleetDoctrine?.insertAdjacentElement('afterend', button);
src/ui/commandDoctrineScreen.ts:34:  if (fleetDoctrine === null) document.querySelector<HTMLElement>('.side-rail')?.append(button);
src/ui/commandDoctrineScreen.ts:39:  const existing = document.querySelector<HTMLDialogElement>('#command-doctrine-dialog');
src/ui/commandDoctrineScreen.ts:42:  dialog.id = 'command-doctrine-dialog';
src/ui/commandDoctrineScreen.ts:43:  dialog.className = 'command-doctrine-dialog';
src/ui/commandDoctrineScreen.ts:45:    <header class="command-doctrine-header">
src/ui/commandDoctrineScreen.ts:46:      <div><p class="panel-label">Admiral Command</p><h2>Адмирал и Commander Ships</h2><p>Боевой опыт открывает 13 общих командирских кораблей. В назначенном флагманском флоте действует только одна способность.</p></div>
src/ui/commandDoctrineScreen.ts:49:    <section class="command-progress" aria-live="polite"></section>
src/ui/commandDoctrineScreen.ts:50:    <form class="command-doctrine-form">
src/ui/commandDoctrineScreen.ts:55:      <label>Флагманский флот<select name="fleet"></select></label>
src/ui/commandDoctrineScreen.ts:58:    <section class="command-doctrine-cards"></section>
src/ui/commandDoctrineScreen.ts:59:    <div class="commander-roster-heading"><p class="panel-label">Shared Commander Catalog</p><h3>Командирские корабли</h3></div>
src/ui/commandDoctrineScreen.ts:60:    <section class="commander-ship-cards" aria-live="polite"></section>
src/ui/commandDoctrineScreen.ts:67:export function mountCommandDoctrineScreen(bridge: CommandDoctrineBridge): void {
src/ui/commandDoctrineScreen.ts:70:  const doctrineForm = dialog.querySelector<HTMLFormElement>('.command-doctrine-form');
src/ui/commandDoctrineScreen.ts:73:  const fleetSelect = flagshipForm?.elements.namedItem('fleet');
src/ui/commandDoctrineScreen.ts:74:  const progress = dialog.querySelector<HTMLElement>('.command-progress');
src/ui/commandDoctrineScreen.ts:75:  const cards = dialog.querySelector<HTMLElement>('.command-doctrine-cards');
src/ui/commandDoctrineScreen.ts:76:  const commanderCards = dialog.querySelector<HTMLElement>('.commander-ship-cards');
src/ui/commandDoctrineScreen.ts:81:    !(fleetSelect instanceof HTMLSelectElement) ||
src/ui/commandDoctrineScreen.ts:84:    commanderCards === null
src/ui/commandDoctrineScreen.ts:85:  ) throw new Error('Command doctrine controls are missing.');
src/ui/commandDoctrineScreen.ts:109:    const command = getEmpireCommandState(state.commanders, 'player');
src/ui/commandDoctrineScreen.ts:110:    if (command === undefined) return;
src/ui/commandDoctrineScreen.ts:111:    doctrineSelect.value = command.doctrineId;
src/ui/commandDoctrineScreen.ts:112:    const nextThreshold = COMMAND_LEVEL_THRESHOLDS[command.level] ?? COMMAND_LEVEL_THRESHOLDS.at(-1)!;
src/ui/commandDoctrineScreen.ts:113:    const currentThreshold = COMMAND_LEVEL_THRESHOLDS[command.level - 1] ?? 0;
src/ui/commandDoctrineScreen.ts:115:    const progressPercent = command.level >= COMMAND_LEVEL_THRESHOLDS.length
src/ui/commandDoctrineScreen.ts:117:      : Math.min(100, Math.floor(((command.experience - currentThreshold) * 100) / range));
src/ui/commandDoctrineScreen.ts:118:    const flagship = state.fleets.find((fleet) => fleet.id === command.flagshipFleetId);
src/ui/commandDoctrineScreen.ts:119:    const activeCommander = flagship === undefined
src/ui/commandDoctrineScreen.ts:121:      : selectActiveCommanderShip(command, flagship.id, flagship.ships);
src/ui/commandDoctrineScreen.ts:123:      <article><span>Уровень Адмирала</span><strong>${command.level} / 40</strong></article>
src/ui/commandDoctrineScreen.ts:124:      <article><span>Боевой опыт</span><strong>${command.experience} / ${nextThreshold}</strong><progress max="100" value="${progressPercent}"></progress></article>
src/ui/commandDoctrineScreen.ts:125:      <article><span>Активная способность</span><strong>${activeCommander?.definition.commanderAbility?.name ?? 'не активна'}</strong><small>${activeCommander?.definition.name ?? command.flagshipFleetId ?? 'флагман не назначен'}</small></article>
src/ui/commandDoctrineScreen.ts:127:    const stationed = state.fleets.filter((fleet) =>
src/ui/commandDoctrineScreen.ts:128:      fleet.empireId === 'player' && fleet.status === 'stationed' && fleet.location.type === 'planet',
src/ui/commandDoctrineScreen.ts:130:    fleetSelect.replaceChildren();
src/ui/commandDoctrineScreen.ts:134:    fleetSelect.append(none);
src/ui/commandDoctrineScreen.ts:135:    for (const fleet of stationed) {
src/ui/commandDoctrineScreen.ts:137:      option.value = fleet.id;
src/ui/commandDoctrineScreen.ts:138:      const commander = Object.keys(fleet.ships).find((unitId) =>
src/ui/commandDoctrineScreen.ts:141:      option.textContent = `${commander === undefined ? '' : '✦ '}${fleet.id} · ${Object.values(fleet.ships).reduce((sum, count) => sum + count, 0)} кораблей`;
src/ui/commandDoctrineScreen.ts:142:      fleetSelect.append(option);
src/ui/commandDoctrineScreen.ts:144:    fleetSelect.value = command.flagshipFleetId ?? '';
src/ui/commandDoctrineScreen.ts:146:      command.level < FLAGSHIP_UNLOCK_LEVEL && command.flagshipFleetId === null;
src/ui/commandDoctrineScreen.ts:148:    commanderCards.replaceChildren(
src/ui/commandDoctrineScreen.ts:150:        const owned = countCommanderShipForEmpire(state, 'player', definition.id) > 0;
src/ui/commandDoctrineScreen.ts:151:        const unlocked = command.level >= (definition.requiredAdmiralLevel ?? 1);
src/ui/commandDoctrineScreen.ts:152:        const active = activeCommander?.unitId === definition.id;
src/ui/commandDoctrineScreen.ts:154:        card.className = `commander-ship-card${active ? ' is-active' : ''}${owned ? ' is-owned' : ''}${!owned && unlocked ? ' is-available' : ''}${!unlocked ? ' is-locked' : ''}`;
src/ui/commandDoctrineScreen.ts:164:        art.className = 'commander-ship-card__art';
src/ui/commandDoctrineScreen.ts:172:          <dl><div><dt>Адмирал</dt><dd>${definition.requiredAdmiralLevel}</dd></div><div><dt>Верфь</dt><dd>${definition.requiredShipyardLevel}</dd></div><div><dt>Способность</dt><dd>${definition.commanderAbility?.name ?? '—'}</dd></div></dl>
src/ui/commandDoctrineScreen.ts:173:          <small>${definition.commanderAbility?.description ?? ''}</small>
src/ui/commandDoctrineScreen.ts:186:      doctrineId: doctrineSelect.value as CommandDoctrineId,
src/ui/commandDoctrineScreen.ts:194:      fleetId: fleetSelect.value.length > 0 ? fleetSelect.value : null,
src/ui/commandRanking.test.ts:3:import { createEmpireRanking, createPlayerCommandProfile } from './commandRanking';
src/ui/commandRanking.test.ts:5:describe('command ranking', () => {
src/ui/commandRanking.test.ts:39:    expect(createPlayerCommandProfile(boosted).rank).toBe(1);
src/ui/commandRanking.ts:3:import { createUnifiedMissionReports } from '../simulation/reports/missionReports';
src/ui/commandRanking.ts:19:  readonly fleets: number;
src/ui/commandRanking.ts:36:  return createUnifiedMissionReports(state).filter((report) => {
src/ui/commandRanking.ts:37:    if (report.primaryEmpireId === empireId && report.outcome === 'success') return true;
src/ui/commandRanking.ts:39:      report.kind === 'battle' &&
src/ui/commandRanking.ts:40:      report.secondaryEmpireId === empireId &&
src/ui/commandRanking.ts:41:      report.outcome === 'failure'
src/ui/commandRanking.ts:81:  const fleets = state.fleets.filter((fleet) => fleet.empireId === empireId).length;
src/ui/commandRanking.ts:90:    fleets * 120 +
src/ui/commandRanking.ts:105:    fleets,
src/ui/commandRanking.ts:117:export function createPlayerCommandProfile(state: GameState): EmpireRankingEntry {
src/ui/commandRankingScreen.ts:1:import '../styles/commandRanking.css';
src/ui/commandRankingScreen.ts:4:import { createEmpireRanking, createPlayerCommandProfile } from './commandRanking';
src/ui/commandRankingScreen.ts:6:export interface CommandRankingScreenOptions {
src/ui/commandRankingScreen.ts:13:  const existing = document.querySelector<HTMLDialogElement>('#command-ranking-dialog');
src/ui/commandRankingScreen.ts:16:  dialog.id = 'command-ranking-dialog';
src/ui/commandRankingScreen.ts:17:  dialog.className = 'command-ranking-dialog';
src/ui/commandRankingScreen.ts:19:    <header class="command-ranking-header">
src/ui/commandRankingScreen.ts:20:      <div><p class="panel-label">Strategic Command</p><h2>Командный профиль и рейтинг</h2></div>
src/ui/commandRankingScreen.ts:23:    <section class="command-profile"></section>
src/ui/commandRankingScreen.ts:24:    <section class="command-ranking-list" aria-label="Локальный рейтинг империй"></section>
src/ui/commandRankingScreen.ts:43:export function mountCommandRankingScreen(options: CommandRankingScreenOptions): void {
src/ui/commandRankingScreen.ts:50:  const profileHost = dialog.querySelector<HTMLElement>('.command-profile');
src/ui/commandRankingScreen.ts:51:  const rankingHost = dialog.querySelector<HTMLElement>('.command-ranking-list');
src/ui/commandRankingScreen.ts:53:    throw new Error('Command ranking containers are missing.');
src/ui/commandRankingScreen.ts:58:    const profile = createPlayerCommandProfile(state);
src/ui/commandRankingScreen.ts:60:    profileHost.style.setProperty('--command-profile-bg', `url("${identity.backgroundUrl}")`);
src/ui/commandRankingScreen.ts:64:    hero.className = 'command-profile-hero';
src/ui/commandRankingScreen.ts:68:    body.className = 'command-profile-body';
src/ui/commandRankingScreen.ts:70:    identityRow.className = 'command-profile-identity';
src/ui/commandRankingScreen.ts:85:    stats.className = 'command-profile-stats';
src/ui/commandRankingScreen.ts:93:      createStat('Флоты', String(profile.fleets)),
src/ui/commandRankingScreen.ts:110:      row.className = `command-ranking-entry${entry.empireId === 'player' ? ' is-player' : ''}`;
src/ui/commandRankingScreen.ts:112:      place.className = 'command-ranking-place';
src/ui/empireOverview.ts:37:  eyebrow.textContent = 'Strategic Command · empire';
src/ui/empireOverview.ts:63:    const fleetStatus = document.createElement('p');
src/ui/empireOverview.ts:64:    fleetStatus.textContent = `Флоты ${view.totalFleetCount} · активные миссии ${view.activeFleetCount}`;
src/ui/empireOverview.ts:87:    summary.append(summaryTitle, fleetStatus, resourceGrid);
src/ui/empireOverview.ts:106:      operations.textContent = `Эффективность ${colony.efficiencyPermille / 10}% · очереди ${colony.buildingQueueCount + colony.shipQueueCount + colony.defenseQueueCount} · флоты ${colony.stationedFleetCount} · вылеты ${colony.activeMissionCount}`;
src/ui/empireOverviewViewModel.ts:23:  readonly stationedFleetCount: number;
src/ui/empireOverviewViewModel.ts:24:  readonly activeMissionCount: number;
src/ui/empireOverviewViewModel.ts:32:  readonly activeFleetCount: number;
src/ui/empireOverviewViewModel.ts:33:  readonly totalFleetCount: number;
src/ui/empireOverviewViewModel.ts:71:    const stationedFleetCount = state.fleets.filter(
src/ui/empireOverviewViewModel.ts:72:      (fleet) =>
src/ui/empireOverviewViewModel.ts:73:        fleet.empireId === empireId &&
src/ui/empireOverviewViewModel.ts:74:        fleet.status === 'stationed' &&
src/ui/empireOverviewViewModel.ts:75:        fleet.location.type === 'planet' &&
src/ui/empireOverviewViewModel.ts:76:        fleet.location.planetId === planet.id,
src/ui/empireOverviewViewModel.ts:78:    const activeMissionCount = state.fleets.filter(
src/ui/empireOverviewViewModel.ts:79:      (fleet) =>
src/ui/empireOverviewViewModel.ts:80:        fleet.empireId === empireId &&
src/ui/empireOverviewViewModel.ts:81:        fleet.status !== 'stationed' &&
src/ui/empireOverviewViewModel.ts:82:        fleet.originPlanetId === planet.id,
src/ui/empireOverviewViewModel.ts:98:      stationedFleetCount,
src/ui/empireOverviewViewModel.ts:99:      activeMissionCount,
src/ui/empireOverviewViewModel.ts:103:  const empireFleets = state.fleets.filter(
src/ui/empireOverviewViewModel.ts:104:    (fleet) => fleet.empireId === empireId,
src/ui/empireOverviewViewModel.ts:112:    activeFleetCount: empireFleets.filter(
src/ui/empireOverviewViewModel.ts:113:      (fleet) => fleet.status !== 'stationed',
src/ui/empireOverviewViewModel.ts:115:    totalFleetCount: empireFleets.length,
src/ui/expeditionPanel.ts:2:import { estimateFlightToGalaxyPlanet } from '../simulation/fleets/flightCalculations';
src/ui/expeditionPanel.ts:3:import type { FleetState } from '../simulation/fleets/types';
src/ui/expeditionPanel.ts:4:import type { GameCommand, GameState } from '../simulation/types';
src/ui/expeditionPanel.ts:10:  readonly execute: (command: GameCommand, successMessage: string) => boolean;
src/ui/expeditionPanel.ts:43:    <section class="expedition-reports"></section>
src/ui/expeditionPanel.ts:52:function fleetSpeedBonus(state: GameState, empireId: string): number {
src/ui/expeditionPanel.ts:53:  return getResearchEffectsForEmpire(state, empireId).fleetSpeedPercent;
src/ui/expeditionPanel.ts:65:function expeditionFleets(state: GameState): readonly FleetState[] {
src/ui/expeditionPanel.ts:66:  return state.fleets
src/ui/expeditionPanel.ts:68:      (fleet) =>
src/ui/expeditionPanel.ts:69:        fleet.empireId === 'player' &&
src/ui/expeditionPanel.ts:70:        fleet.status === 'stationed' &&
src/ui/expeditionPanel.ts:71:        fleet.location.type === 'planet' &&
src/ui/expeditionPanel.ts:72:        hasShipRole(fleet.ships, 'scout'),
src/ui/expeditionPanel.ts:86:  const reports = dialog.querySelector<HTMLElement>('.expedition-reports');
src/ui/expeditionPanel.ts:87:  if (launch === null || active === null || reports === null) {
src/ui/expeditionPanel.ts:93:    const fleets = expeditionFleets(state);
src/ui/expeditionPanel.ts:100:    if (fleets.length === 0 || targets.length === 0) {
src/ui/expeditionPanel.ts:103:        fleets.length === 0
src/ui/expeditionPanel.ts:108:      const fleetSelect = document.createElement('select');
src/ui/expeditionPanel.ts:109:      for (const fleet of fleets) {
src/ui/expeditionPanel.ts:111:        option.value = fleet.id;
src/ui/expeditionPanel.ts:112:        option.textContent = `${fleet.id} · скорость ${fleet.speed} · разведчиков ${getShipCountByRole(fleet.ships, 'scout')}`;
src/ui/expeditionPanel.ts:113:        fleetSelect.append(option);
src/ui/expeditionPanel.ts:129:        const fleet = state.fleets.find((candidate) => candidate.id === fleetSelect.value);
src/ui/expeditionPanel.ts:131:          fleet?.location.type === 'planet' ? fleet.location.planetId : undefined;
src/ui/expeditionPanel.ts:136:        if (fleet === undefined || origin === undefined || targetSelect.value.length === 0) {
src/ui/expeditionPanel.ts:145:            fleet,
src/ui/expeditionPanel.ts:147:            fleetSpeedBonus(state, fleet.empireId),
src/ui/expeditionPanel.ts:160:      fleetSelect.addEventListener('change', refresh);
src/ui/expeditionPanel.ts:168:              fleetId: fleetSelect.value,
src/ui/expeditionPanel.ts:177:      launch.append(fleetSelect, targetSelect, preview, start);
src/ui/expeditionPanel.ts:185:    const activeFleets = state.fleets.filter(
src/ui/expeditionPanel.ts:186:      (fleet) => fleet.empireId === 'player' && fleet.mission?.kind === 'expedition',
src/ui/expeditionPanel.ts:188:    for (const fleet of activeFleets) {
src/ui/expeditionPanel.ts:191:      summary.textContent = `${fleet.id} · цель ${fleet.mission?.targetPlanetId ?? 'unknown'}`;
src/ui/expeditionPanel.ts:194:        fleet.location.type === 'transit'
src/ui/expeditionPanel.ts:195:          ? `Завершение цикла через ${formatGameDuration(Math.max(0, fleet.location.arrivesAt - state.clock.elapsedSeconds))}`
src/ui/expeditionPanel.ts:196:          : fleet.status;
src/ui/expeditionPanel.ts:203:            { type: 'RECALL_FLEET', empireId: 'player', fleetId: fleet.id },
src/ui/expeditionPanel.ts:213:    if (activeFleets.length === 0) {
src/ui/expeditionPanel.ts:219:    reports.replaceChildren();
src/ui/expeditionPanel.ts:220:    const reportTitle = document.createElement('h3');
src/ui/expeditionPanel.ts:221:    reportTitle.textContent = 'Журнал экспедиций';
src/ui/expeditionPanel.ts:222:    reports.append(reportTitle);
src/ui/expeditionPanel.ts:227:          entry.event.payload.report.empireId === 'player',
src/ui/expeditionPanel.ts:233:      const report = entry.event.payload.report;
src/ui/expeditionPanel.ts:236:      summary.textContent = `${report.outcome} · ${report.targetGalaxyPlanetId}`;
src/ui/expeditionPanel.ts:238:      const losses = Object.entries(report.losses)
src/ui/expeditionPanel.ts:241:      result.textContent = `Награда ${rewardText(report.reward)}${losses.length > 0 ? ` · потери ${losses}` : ''}`;
src/ui/expeditionPanel.ts:243:      narrative.textContent = report.narrative;
src/ui/expeditionPanel.ts:245:      reports.append(card);
src/ui/expeditionPanel.ts:250:      reports.append(empty);
src/ui/factionShellIdentity.test.ts:5:  it('uses faction-specific naming for the command shell', () => {
src/ui/factionShellIdentity.ts:51:  const emblem = document.querySelector<HTMLImageElement>('.commander-emblem');
src/ui/factionShellIdentity.ts:57:  const commanderName = document.querySelector<HTMLElement>('.commander-row h2');
src/ui/factionShellIdentity.ts:58:  const commanderProfile = document.querySelector<HTMLElement>('.commander-row p');
src/ui/factionShellIdentity.ts:59:  if (commanderName !== null) commanderName.textContent = copy.name;
src/ui/factionShellIdentity.ts:60:  if (commanderProfile !== null) commanderProfile.textContent = copy.profile;
src/ui/fleetComposerViewModel.ts:2:import { getCommanderFleetEffects } from '../simulation/command/commanderShips';
src/ui/fleetComposerViewModel.ts:5:  calculateFleetComposition,
src/ui/fleetComposerViewModel.ts:7:  validateShipComposition,
src/ui/fleetComposerViewModel.ts:8:} from '../simulation/fleets/fleetCalculations';
src/ui/fleetComposerViewModel.ts:12:} from '../simulation/fleets/flightCalculations';
src/ui/fleetComposerViewModel.ts:13:import type { FleetMissionKind, FleetState } from '../simulation/fleets/types';
src/ui/fleetComposerViewModel.ts:17:export interface FleetComposerShipOption {
src/ui/fleetComposerViewModel.ts:24:export interface FleetComposerViewModel {
src/ui/fleetComposerViewModel.ts:27:  readonly ships: readonly FleetComposerShipOption[];
src/ui/fleetComposerViewModel.ts:38:export interface FleetRoutePreview {
src/ui/fleetComposerViewModel.ts:59:export function createFleetComposerViewModel(
src/ui/fleetComposerViewModel.ts:65:): FleetComposerViewModel | undefined {
src/ui/fleetComposerViewModel.ts:76:    .map(([unitId, available]): FleetComposerShipOption => {
src/ui/fleetComposerViewModel.ts:98:  const compositionErrors = validateShipComposition(selectedShips);
src/ui/fleetComposerViewModel.ts:102:      ? calculateFleetComposition(selectedShips)
src/ui/fleetComposerViewModel.ts:129:function getFleetSpeedBonus(state: GameState, fleet: FleetState): number {
src/ui/fleetComposerViewModel.ts:130:  return getResearchEffectsForEmpire(state, fleet.empireId).fleetSpeedPercent +
src/ui/fleetComposerViewModel.ts:131:    getCommanderFleetEffects(state, fleet).speedBonusPercent;
src/ui/fleetComposerViewModel.ts:134:export function createFleetRoutePreview(
src/ui/fleetComposerViewModel.ts:136:  fleet: FleetState,
src/ui/fleetComposerViewModel.ts:137:  mission: FleetMissionKind,
src/ui/fleetComposerViewModel.ts:138:  targetPlanetId: string,
src/ui/fleetComposerViewModel.ts:139:): FleetRoutePreview | undefined {
src/ui/fleetComposerViewModel.ts:140:  const location = fleet.location;
src/ui/fleetComposerViewModel.ts:141:  if (fleet.status !== 'stationed' || location.type !== 'planet') {
src/ui/fleetComposerViewModel.ts:151:    estimate = mission === 'colonize'
src/ui/fleetComposerViewModel.ts:155:          fleet,
src/ui/fleetComposerViewModel.ts:156:          targetPlanetId,
src/ui/fleetComposerViewModel.ts:157:          getFleetSpeedBonus(state, fleet),
src/ui/fleetComposerViewModel.ts:162:          fleet,
src/ui/fleetComposerViewModel.ts:163:          targetPlanetId,
src/ui/fleetComposerViewModel.ts:164:          getFleetSpeedBonus(state, fleet),
src/ui/fleetComposerViewModel.ts:171:    mission === 'colonize' ? estimate.fuelCost : estimate.fuelCost * 2;
src/ui/fleetDoctrineScreen.ts:1:import '../styles/fleetDoctrine.css';
src/ui/fleetDoctrineScreen.ts:5:  type FleetFormation,
src/ui/fleetDoctrineScreen.ts:6:  type FleetTargetPriority,
src/ui/fleetDoctrineScreen.ts:7:} from '../simulation/combat/fleetDoctrine';
src/ui/fleetDoctrineScreen.ts:8:import type { GameCommand, GameState } from '../simulation/types';
src/ui/fleetDoctrineScreen.ts:10:interface FleetDoctrineBridge {
src/ui/fleetDoctrineScreen.ts:12:  readonly execute: (command: GameCommand, successMessage: string) => boolean;
src/ui/fleetDoctrineScreen.ts:15:const PRIORITY_NAMES: Readonly<Record<FleetTargetPriority, string>> = {
src/ui/fleetDoctrineScreen.ts:23:  const existing = document.querySelector<HTMLButtonElement>('#nav-fleet-doctrine');
src/ui/fleetDoctrineScreen.ts:27:  button.id = 'nav-fleet-doctrine';
src/ui/fleetDoctrineScreen.ts:38:  const existing = document.querySelector<HTMLDialogElement>('#fleet-doctrine-dialog');
src/ui/fleetDoctrineScreen.ts:41:  dialog.id = 'fleet-doctrine-dialog';
src/ui/fleetDoctrineScreen.ts:42:  dialog.className = 'fleet-doctrine-dialog';
src/ui/fleetDoctrineScreen.ts:44:    <header class="fleet-doctrine-header">
src/ui/fleetDoctrineScreen.ts:48:    <form class="fleet-doctrine-form">
src/ui/fleetDoctrineScreen.ts:49:      <label>Флот<select name="fleet"></select></label>
src/ui/fleetDoctrineScreen.ts:54:    <section class="fleet-doctrine-summary"></section>
src/ui/fleetDoctrineScreen.ts:55:    <section class="fleet-doctrine-skills" aria-label="Классовые навыки"></section>
src/ui/fleetDoctrineScreen.ts:62:export function mountFleetDoctrineScreen(bridge: FleetDoctrineBridge): void {
src/ui/fleetDoctrineScreen.ts:65:  const form = dialog.querySelector<HTMLFormElement>('.fleet-doctrine-form');
src/ui/fleetDoctrineScreen.ts:66:  const fleetSelect = form?.elements.namedItem('fleet');
src/ui/fleetDoctrineScreen.ts:69:  const summary = dialog.querySelector<HTMLElement>('.fleet-doctrine-summary');
src/ui/fleetDoctrineScreen.ts:70:  const skills = dialog.querySelector<HTMLElement>('.fleet-doctrine-skills');
src/ui/fleetDoctrineScreen.ts:73:    !(fleetSelect instanceof HTMLSelectElement) ||
src/ui/fleetDoctrineScreen.ts:79:    throw new Error('Fleet doctrine controls are missing.');
src/ui/fleetDoctrineScreen.ts:111:    const formation = FLEET_FORMATIONS[formationSelect.value as FleetFormation];
src/ui/fleetDoctrineScreen.ts:112:    const priority = prioritySelect.value as FleetTargetPriority;
src/ui/fleetDoctrineScreen.ts:122:    const fleets = state.fleets.filter(
src/ui/fleetDoctrineScreen.ts:123:      (fleet) =>
src/ui/fleetDoctrineScreen.ts:124:        fleet.empireId === 'player' &&
src/ui/fleetDoctrineScreen.ts:125:        fleet.status === 'stationed' &&
src/ui/fleetDoctrineScreen.ts:126:        fleet.location.type === 'planet',
src/ui/fleetDoctrineScreen.ts:128:    const previous = fleetSelect.value;
src/ui/fleetDoctrineScreen.ts:129:    fleetSelect.replaceChildren(
src/ui/fleetDoctrineScreen.ts:130:      ...fleets.map((fleet) => {
src/ui/fleetDoctrineScreen.ts:132:        option.value = fleet.id;
src/ui/fleetDoctrineScreen.ts:133:        option.textContent = `${fleet.id} · ${Object.values(fleet.ships).reduce((total, count) => total + count, 0)} кораблей`;
src/ui/fleetDoctrineScreen.ts:137:    if (previous.length > 0 && fleets.some((fleet) => fleet.id === previous)) {
src/ui/fleetDoctrineScreen.ts:138:      fleetSelect.value = previous;
src/ui/fleetDoctrineScreen.ts:140:    const selected = fleets.find((fleet) => fleet.id === fleetSelect.value) ?? fleets[0];
src/ui/fleetDoctrineScreen.ts:143:      fleetSelect.value = selected.id;
src/ui/fleetDoctrineScreen.ts:152:    if (fleetSelect.value.length === 0) return;
src/ui/fleetDoctrineScreen.ts:158:          fleetId: fleetSelect.value,
src/ui/fleetDoctrineScreen.ts:159:          formation: formationSelect.value as FleetFormation,
src/ui/fleetDoctrineScreen.ts:160:          targetPriority: prioritySelect.value as FleetTargetPriority,
src/ui/fleetDoctrineScreen.ts:166:  fleetSelect.addEventListener('change', render);
src/ui/fleetMissionEvents.test.ts:2:import { inferMissionForGalaxyTarget } from './fleetMissionEvents';
src/ui/fleetMissionEvents.test.ts:4:describe('galaxy target mission inference', () => {
src/ui/fleetMissionEvents.test.ts:6:    expect(inferMissionForGalaxyTarget(null, 'unclaimed')).toBe('colonize');
src/ui/fleetMissionEvents.test.ts:10:    expect(inferMissionForGalaxyTarget('player', 'owned')).toBe('transport');
src/ui/fleetMissionEvents.test.ts:14:    expect(inferMissionForGalaxyTarget('aegis-bot', 'current')).toBe('scout');
src/ui/fleetMissionEvents.test.ts:15:    expect(inferMissionForGalaxyTarget(null, 'contact')).toBe('scout');
src/ui/fleetMissionEvents.ts:1:import type { FleetMissionKind } from '../simulation/fleets/types';
src/ui/fleetMissionEvents.ts:2:import type { GalaxyIntelVisibility } from '../simulation/galaxy/intelligenceView';
src/ui/fleetMissionEvents.ts:4:export const FLEET_MISSION_TARGET_EVENT = 'stellar:fleet-mission-target';
src/ui/fleetMissionEvents.ts:6:export interface FleetMissionTargetRequest {
src/ui/fleetMissionEvents.ts:9:  readonly mission: FleetMissionKind;
src/ui/fleetMissionEvents.ts:12:export function inferMissionForGalaxyTarget(
src/ui/fleetMissionEvents.ts:15:): FleetMissionKind {
src/ui/fleetMissionEvents.ts:21:export function dispatchFleetMissionTarget(detail: FleetMissionTargetRequest): void {
src/ui/fleetMissionEvents.ts:23:    new CustomEvent<FleetMissionTargetRequest>(FLEET_MISSION_TARGET_EVENT, {
src/ui/galaxyIntelPanel.ts:1:import { getPlanetArtUrl } from '../assets/galaxyFleetRuntimeAssets';
src/ui/galaxyIntelPanel.ts:7:  createGalaxyIntelligenceView,
src/ui/galaxyIntelPanel.ts:8:  filterGalaxyIntelligence,
src/ui/galaxyIntelPanel.ts:9:  summarizeGalaxyIntelligence,
src/ui/galaxyIntelPanel.ts:14:} from '../simulation/galaxy/intelligenceView';
src/ui/galaxyIntelPanel.ts:18:  dispatchFleetMissionTarget,
src/ui/galaxyIntelPanel.ts:19:  inferMissionForGalaxyTarget,
src/ui/galaxyIntelPanel.ts:20:} from './fleetMissionEvents';
src/ui/galaxyIntelPanel.ts:73:        <p class="panel-label">Galaxy Intelligence</p>
src/ui/galaxyIntelPanel.ts:96:function missionActionLabel(planet: GalaxyIntelPlanet): string {
src/ui/galaxyIntelPanel.ts:102:function getMissionTargetId(planet: GalaxyIntelPlanet): string {
src/ui/galaxyIntelPanel.ts:152:  action.className = 'galaxy-intel-mission-button';
src/ui/galaxyIntelPanel.ts:153:  action.textContent = missionActionLabel(planet);
src/ui/galaxyIntelPanel.ts:155:    dispatchFleetMissionTarget({
src/ui/galaxyIntelPanel.ts:156:      targetId: getMissionTargetId(planet),
src/ui/galaxyIntelPanel.ts:158:      mission: inferMissionForGalaxyTarget(planet.ownerEmpireId, planet.visibility),
src/ui/galaxyIntelPanel.ts:174:    throw new Error('Galaxy intelligence panel containers are missing.');
src/ui/galaxyIntelPanel.ts:213:    const all = createGalaxyIntelligenceView(state, options.empireId ?? 'player');
src/ui/galaxyIntelPanel.ts:221:    const filtered = filterGalaxyIntelligence(all, query);
src/ui/galaxyIntelPanel.ts:222:    const totals = summarizeGalaxyIntelligence(all);
src/ui/logisticsRoutesPanel.ts:3:import type { GameCommand, GameState } from '../simulation/types';
src/ui/logisticsRoutesPanel.ts:7:  readonly execute: (command: GameCommand, successMessage: string) => boolean;
src/ui/logisticsRoutesPanel.ts:36:  const host = document.querySelector<HTMLElement>('.command-panel');
src/ui/logisticsRoutesPanel.ts:104:          targetPlanetId: target.value,
src/ui/logisticsRoutesPanel.ts:121:      const targetPlanet = planets.find((planet) => planet.id === route.targetPlanetId);
src/ui/marketPanel.ts:3:import type { GameCommand, GameState } from '../simulation/types';
src/ui/marketPanel.ts:8:  readonly execute: (command: GameCommand, successMessage: string) => boolean;
src/ui/marketPanel.ts:30:  const host = document.querySelector<HTMLElement>('.command-panel');
src/ui/militaryZoneViewModel.ts:10:  readonly id: 'defense' | 'fleet';
src/ui/militaryZoneViewModel.ts:24:  const commandLevel = getBuildingLevel(planet.buildings, roles.command);
src/ui/militaryZoneViewModel.ts:38:        id: 'fleet',
src/ui/militaryZoneViewModel.ts:40:        unlocked: commandLevel >= 2,
src/ui/militaryZoneViewModel.ts:41:        hint: commandLevel >= 2 ? `Командный узел ур. ${commandLevel}` : 'Требуется командный узел ур. 2',
src/ui/missionReportsPanel.ts:3:  createUnifiedMissionReports,
src/ui/missionReportsPanel.ts:4:  filterMissionReports,
src/ui/missionReportsPanel.ts:5:  summarizeMissionReports,
src/ui/missionReportsPanel.ts:6:  type MissionCombatBreakdownLine,
src/ui/missionReportsPanel.ts:7:  type MissionReportKind,
src/ui/missionReportsPanel.ts:8:  type MissionReportMode,
src/ui/missionReportsPanel.ts:9:  type MissionReportReward,
src/ui/missionReportsPanel.ts:10:  type UnifiedMissionReport,
src/ui/missionReportsPanel.ts:11:} from '../simulation/reports/missionReports';
src/ui/missionReportsPanel.ts:15:export interface MissionReportsPanelOptions {
src/ui/missionReportsPanel.ts:19:const KIND_LABELS: Readonly<Record<MissionReportKind, string>> = {
src/ui/missionReportsPanel.ts:26:const MODE_LABELS: Readonly<Record<MissionReportMode, string>> = {
src/ui/missionReportsPanel.ts:54:  const existing = document.querySelector<HTMLButtonElement>('#nav-reports');
src/ui/missionReportsPanel.ts:58:    candidate.id = 'nav-reports';
src/ui/missionReportsPanel.ts:63:  button.id = 'nav-reports';
src/ui/missionReportsPanel.ts:73:  const existing = document.querySelector<HTMLDialogElement>('#mission-reports-dialog');
src/ui/missionReportsPanel.ts:76:  dialog.id = 'mission-reports-dialog';
src/ui/missionReportsPanel.ts:77:  dialog.className = 'mission-reports-dialog';
src/ui/missionReportsPanel.ts:79:    <header class="mission-reports-header">
src/ui/missionReportsPanel.ts:80:      <div><p class="panel-label">Operations Intelligence</p><h2>Единые отчёты</h2></div>
src/ui/missionReportsPanel.ts:83:    <section class="mission-reports-summary"></section>
src/ui/missionReportsPanel.ts:84:    <section class="mission-reports-controls"></section>
src/ui/missionReportsPanel.ts:85:    <section class="mission-reports-comparison"></section>
src/ui/missionReportsPanel.ts:86:    <section class="mission-reports-list"></section>
src/ui/missionReportsPanel.ts:100:  label.className = 'mission-report-control';
src/ui/missionReportsPanel.ts:114:function rewardText(reward: MissionReportReward): string {
src/ui/missionReportsPanel.ts:125:function weaponText(line: MissionCombatBreakdownLine): string {
src/ui/missionReportsPanel.ts:136:  breakdown: readonly MissionCombatBreakdownLine[],
src/ui/missionReportsPanel.ts:139:  details.className = 'mission-combat-details';
src/ui/missionReportsPanel.ts:143:  scroller.className = 'mission-combat-table-wrap';
src/ui/missionReportsPanel.ts:183:function createReportCard(report: UnifiedMissionReport): HTMLElement {
src/ui/missionReportsPanel.ts:185:  card.className = `mission-report-card is-${report.kind} is-${report.mode}`;
src/ui/missionReportsPanel.ts:188:  title.textContent = report.title;
src/ui/missionReportsPanel.ts:191:  kind.textContent = KIND_LABELS[report.kind];
src/ui/missionReportsPanel.ts:193:  mode.textContent = MODE_LABELS[report.mode];
src/ui/missionReportsPanel.ts:195:  outcome.textContent = report.outcome;
src/ui/missionReportsPanel.ts:200:  summary.textContent = report.summary;
src/ui/missionReportsPanel.ts:202:  target.textContent = `Цель: ${report.targetId} · участники ${report.primaryEmpireId ?? '—'} / ${report.secondaryEmpireId ?? '—'}`;
src/ui/missionReportsPanel.ts:204:  rewards.textContent = `Награда ${rewardText(report.reward)}`;
src/ui/missionReportsPanel.ts:206:  losses.textContent = `Потери: свои ${lossesText(report.primaryLosses)} · противник ${lossesText(report.secondaryLosses)}`;
src/ui/missionReportsPanel.ts:208:  balance.textContent = `Время ${formatGameDuration(report.resolvedAt)} · угроза ${report.threatMultiplierPermille / 10}% · награда ${report.rewardMultiplierPermille / 10}%`;
src/ui/missionReportsPanel.ts:210:  if ((report.combatBreakdown?.length ?? 0) > 0) {
src/ui/missionReportsPanel.ts:211:    card.append(createCombatDetails(report.combatBreakdown ?? []));
src/ui/missionReportsPanel.ts:216:export function mountMissionReportsPanel(options: MissionReportsPanelOptions): void {
src/ui/missionReportsPanel.ts:219:  const summary = dialog.querySelector<HTMLElement>('.mission-reports-summary');
src/ui/missionReportsPanel.ts:220:  const controls = dialog.querySelector<HTMLElement>('.mission-reports-controls');
src/ui/missionReportsPanel.ts:221:  const comparison = dialog.querySelector<HTMLElement>('.mission-reports-comparison');
src/ui/missionReportsPanel.ts:222:  const list = dialog.querySelector<HTMLElement>('.mission-reports-list');
src/ui/missionReportsPanel.ts:224:    throw new Error('Mission report panel containers are missing.');
src/ui/missionReportsPanel.ts:228:  searchLabel.className = 'mission-report-control mission-report-search';
src/ui/missionReportsPanel.ts:265:    const all = createUnifiedMissionReports(state);
src/ui/missionReportsPanel.ts:266:    const filtered = filterMissionReports(all, {
src/ui/missionReportsPanel.ts:268:      kind: kind.select.value as MissionReportKind | 'all',
src/ui/missionReportsPanel.ts:269:      mode: mode.select.value as MissionReportMode | 'all',
src/ui/missionReportsPanel.ts:272:    const totals = summarizeMissionReports(filtered);
src/ui/missionReportsPanel.ts:298:    list.replaceChildren(...filtered.map(createReportCard));
src/ui/missionScreen.ts:1:import { getFleetShipArtUrl } from '../assets/galaxyFleetRuntimeAssets';
src/ui/missionScreen.ts:8:import type { FleetMissionKind, FleetState } from '../simulation/fleets/types';
src/ui/missionScreen.ts:11:  getEmpireIntelligence,
src/ui/missionScreen.ts:12:} from '../simulation/intelligence/intelligenceState';
src/ui/missionScreen.ts:14:import type { GameCommand, GameState } from '../simulation/types';
src/ui/missionScreen.ts:18:  createFleetComposerViewModel,
src/ui/missionScreen.ts:19:  createFleetRoutePreview,
src/ui/missionScreen.ts:20:} from './fleetComposerViewModel';
src/ui/missionScreen.ts:23:  type FleetMissionTargetRequest,
src/ui/missionScreen.ts:24:} from './fleetMissionEvents';
src/ui/missionScreen.ts:27:export interface MissionScreenOptions {
src/ui/missionScreen.ts:30:  readonly execute: (command: GameCommand, successMessage: string) => boolean;
src/ui/missionScreen.ts:65:function readMissionKind(value: string): FleetMissionKind {
src/ui/missionScreen.ts:92:  fleet: FleetState,
src/ui/missionScreen.ts:93:  mission: FleetMissionKind,
src/ui/missionScreen.ts:95:  if (fleet.location.type !== 'planet') return [];
src/ui/missionScreen.ts:96:  const originId = fleet.location.planetId;
src/ui/missionScreen.ts:97:  if (mission === 'recycle') {
src/ui/missionScreen.ts:107:  if (mission === 'transport' || mission === 'deploy') {
src/ui/missionScreen.ts:109:      (planet) => planet.id !== originId && planet.ownerEmpireId === fleet.empireId,
src/ui/missionScreen.ts:115:function missionLabel(mission: FleetMissionKind): string {
src/ui/missionScreen.ts:116:  switch (mission) {
src/ui/missionScreen.ts:128:function fleetStatusLabel(fleet: FleetState): string {
src/ui/missionScreen.ts:129:  switch (fleet.status) {
src/ui/missionScreen.ts:141:function getFleetFaction(state: GameState, fleet: FleetState): FactionId {
src/ui/missionScreen.ts:142:  return state.planets.find((planet) => planet.id === fleet.originPlanetId)?.factionId
src/ui/missionScreen.ts:176:export function mountMissionScreen(options: MissionScreenOptions): void {
src/ui/missionScreen.ts:178:  dialog.id = 'mission-screen-dialog';
src/ui/missionScreen.ts:179:  dialog.className = 'mission-screen-dialog';
src/ui/missionScreen.ts:184:  eyebrow.textContent = 'Fleet Operations · command';
src/ui/missionScreen.ts:198:  content.className = 'mission-screen-content';
src/ui/missionScreen.ts:202:  let pendingTarget: FleetMissionTargetRequest | null = null;
src/ui/missionScreen.ts:206:    const playerFleets = state.fleets.filter((fleet) => fleet.empireId === 'player');
src/ui/missionScreen.ts:213:    overview.className = 'mission-overview';
src/ui/missionScreen.ts:215:      createMetric('Флоты', String(playerFleets.length)),
src/ui/missionScreen.ts:216:      createMetric('На орбите', String(playerFleets.filter((fleet) => fleet.status === 'stationed').length)),
src/ui/missionScreen.ts:217:      createMetric('В полёте', String(playerFleets.filter((fleet) => fleet.location.type === 'transit').length)),
src/ui/missionScreen.ts:223:      targetNotice.className = 'mission-target-notice';
src/ui/missionScreen.ts:224:      targetNotice.textContent = `Цель с карты: ${pendingTarget.label} · ${missionLabel(pendingTarget.mission)}`;
src/ui/missionScreen.ts:230:    workspace.className = 'mission-workspace';
src/ui/missionScreen.ts:233:    createSection.className = 'mission-create';
src/ui/missionScreen.ts:235:    createTitle.className = 'mission-section-heading';
src/ui/missionScreen.ts:243:    originLabel.className = 'mission-origin-field';
src/ui/missionScreen.ts:259:    shipFields.className = 'mission-ship-grid';
src/ui/missionScreen.ts:261:    cargoFields.className = 'mission-cargo-grid';
src/ui/missionScreen.ts:263:    composerStatus.className = 'mission-composer-status';
src/ui/missionScreen.ts:266:    createButton.className = 'mission-primary-action';
src/ui/missionScreen.ts:287:      const model = createFleetComposerViewModel(
src/ui/missionScreen.ts:316:        card.className = 'mission-ship-option';
src/ui/missionScreen.ts:319:        image.src = getFleetShipArtUrl(origin?.factionId ?? factionId, unitId);
src/ui/missionScreen.ts:336:        empty.className = 'mission-empty';
src/ui/missionScreen.ts:352:      const model = createFleetComposerViewModel(
src/ui/missionScreen.ts:383:    list.className = 'mission-fleet-list';
src/ui/missionScreen.ts:385:    listHeading.className = 'mission-section-heading';
src/ui/missionScreen.ts:387:    listTitle.textContent = `Оперативные группы · ${playerFleets.length}`;
src/ui/missionScreen.ts:393:    for (const fleet of playerFleets) {
src/ui/missionScreen.ts:395:      card.className = `mission-fleet-card is-${fleet.status}`;
src/ui/missionScreen.ts:396:      const primaryUnitId = Object.entries(fleet.ships)
src/ui/missionScreen.ts:399:      visual.className = 'mission-fleet-card__visual';
src/ui/missionScreen.ts:401:      image.src = getFleetShipArtUrl(getFleetFaction(state, fleet), primaryUnitId);
src/ui/missionScreen.ts:405:      status.textContent = fleetStatusLabel(fleet);
src/ui/missionScreen.ts:409:      body.className = 'mission-fleet-card__body';
src/ui/missionScreen.ts:411:      name.textContent = fleet.id;
src/ui/missionScreen.ts:412:      const composition = Object.entries(fleet.ships)
src/ui/missionScreen.ts:416:      meta.textContent = `Скорость ${fleet.speed} · груз ${fleet.cargo.metal + fleet.cargo.crystal + fleet.cargo.gas}/${fleet.cargoCapacity} · ${composition}`;
src/ui/missionScreen.ts:419:      if (fleet.location.type === 'transit') {
src/ui/missionScreen.ts:421:        route.className = 'mission-transit';
src/ui/missionScreen.ts:423:        const targetName = getTargetName(state, fleet.location.toPlanetId);
src/ui/missionScreen.ts:424:        const remaining = Math.max(0, fleet.location.arrivesAt - state.clock.elapsedSeconds);
src/ui/missionScreen.ts:425:        routeText.textContent = `${fleet.mission === null ? 'Перелёт' : missionLabel(fleet.mission.kind)} · ${targetName} · осталось ${formatGameDuration(remaining)}`;
src/ui/missionScreen.ts:427:        progress.max = Math.max(1, fleet.location.arrivesAt - fleet.location.departedAt);
src/ui/missionScreen.ts:428:        progress.value = Math.max(0, state.clock.elapsedSeconds - fleet.location.departedAt);
src/ui/missionScreen.ts:434:      actions.className = 'mission-actions';
src/ui/missionScreen.ts:435:      if (fleet.status === 'stationed' && fleet.location.type === 'planet') {
src/ui/missionScreen.ts:436:        const mission = document.createElement('select');
src/ui/missionScreen.ts:437:        for (const missionKind of ['transport', 'deploy', 'scout', 'attack', 'recycle'] as const) {
src/ui/missionScreen.ts:439:          option.value = missionKind;
src/ui/missionScreen.ts:440:          option.textContent = missionLabel(missionKind);
src/ui/missionScreen.ts:441:          mission.append(option);
src/ui/missionScreen.ts:443:        if (hasShipRole(fleet.ships, 'colonizer')) {
src/ui/missionScreen.ts:446:          option.textContent = missionLabel('colonize');
src/ui/missionScreen.ts:447:          mission.append(option);
src/ui/missionScreen.ts:451:          Array.from(mission.options).some((option) => option.value === pendingTarget?.mission)
src/ui/missionScreen.ts:453:          mission.value = pendingTarget.mission;
src/ui/missionScreen.ts:458:        preview.className = 'mission-route-preview';
src/ui/missionScreen.ts:461:        send.className = 'mission-primary-action';
src/ui/missionScreen.ts:465:          const missionKind = readMissionKind(mission.value);
src/ui/missionScreen.ts:466:          const route = createFleetRoutePreview(state, fleet, missionKind, target.value);
src/ui/missionScreen.ts:479:          const missionKind = readMissionKind(mission.value);
src/ui/missionScreen.ts:480:          if (missionKind === 'colonize') {
src/ui/missionScreen.ts:488:            for (const planet of getRegularTargets(state, fleet, missionKind)) {
src/ui/missionScreen.ts:510:        mission.addEventListener('change', renderTargets);
src/ui/missionScreen.ts:513:          const missionKind = readMissionKind(mission.value);
src/ui/missionScreen.ts:517:            fleetId: fleet.id,
src/ui/missionScreen.ts:518:            targetPlanetId: target.value,
src/ui/missionScreen.ts:519:            mission: missionKind,
src/ui/missionScreen.ts:520:          }, missionKind === 'colonize'
src/ui/missionScreen.ts:522:            : `Флот отправлен · ${missionLabel(missionKind)}`)) {
src/ui/missionScreen.ts:528:        actions.append(mission, target, preview, send);
src/ui/missionScreen.ts:535:            { type: 'DISBAND_FLEET', empireId: 'player', fleetId: fleet.id },
src/ui/missionScreen.ts:540:      } else if (fleet.status === 'outbound') {
src/ui/missionScreen.ts:546:            { type: 'RECALL_FLEET', empireId: 'player', fleetId: fleet.id },
src/ui/missionScreen.ts:557:    if (playerFleets.length === 0) {
src/ui/missionScreen.ts:559:      empty.className = 'mission-empty';
src/ui/missionScreen.ts:567:    const intelligence = getEmpireIntelligence(state.intelligence, 'player');
src/ui/missionScreen.ts:569:    operationalGrid.className = 'mission-operational-grid';
src/ui/missionScreen.ts:572:    intelSection.className = 'mission-intelligence';
src/ui/missionScreen.ts:576:    for (const observation of intelligence === undefined
src/ui/missionScreen.ts:578:      : getCurrentObservations(intelligence, state.clock.elapsedSeconds)) {
src/ui/missionScreen.ts:579:      const report = document.createElement('article');
src/ui/missionScreen.ts:585:      report.append(summary, details);
src/ui/missionScreen.ts:586:      intelSection.append(report);
src/ui/missionScreen.ts:595:    battleSection.className = 'mission-battles';
src/ui/missionScreen.ts:599:    const reports = state.eventLog
src/ui/missionScreen.ts:602:        (entry.event.payload.report.attackerEmpireId === 'player' ||
src/ui/missionScreen.ts:603:          entry.event.payload.report.defenderEmpireId === 'player'))
src/ui/missionScreen.ts:606:    for (const entry of reports) {
src/ui/missionScreen.ts:608:      const report = entry.event.payload.report;
src/ui/missionScreen.ts:609:      const debris = report.debrisCreated ?? { metal: 0, crystal: 0 };
src/ui/missionScreen.ts:610:      const loot = report.plunderedCargo ?? { metal: 0, crystal: 0, gas: 0 };
src/ui/operationsWorkspace.test.ts:16:    expect(summary.reports).toBe(0);
src/ui/operationsWorkspace.test.ts:29:          targetPlanetId: state.planets[1]?.id ?? 'target',
src/ui/operationsWorkspace.test.ts:44:          targetPlanetId: 'target',
src/ui/operationsWorkspace.ts:2:import { createUnifiedMissionReports } from '../simulation/reports/missionReports';
src/ui/operationsWorkspace.ts:17:  readonly reports: number;
src/ui/operationsWorkspace.ts:28:    activeExpeditions: state.fleets.filter(
src/ui/operationsWorkspace.ts:29:      (fleet) => fleet.empireId === 'player' && fleet.mission?.kind === 'expedition',
src/ui/operationsWorkspace.ts:31:    activeObjectOperations: state.fleets.filter(
src/ui/operationsWorkspace.ts:32:      (fleet) => fleet.empireId === 'player' && fleet.mission?.kind === 'space-object',
src/ui/operationsWorkspace.ts:36:    reports: createUnifiedMissionReports(state).length,
src/ui/operationsWorkspace.ts:45:  const reports = document.querySelector<HTMLButtonElement>('#nav-reports')
src/ui/operationsWorkspace.ts:53:  if (reports !== null) reports.insertAdjacentElement('beforebegin', button);
src/ui/operationsWorkspace.ts:100:    selector: '#nav-reports',
src/ui/operationsWorkspace.ts:167:      createMetric('Отчёты', String(data.reports), 'в едином журнале'),
src/ui/planetDevelopmentControls.ts:10:import type { GameCommand, GameState } from '../simulation/types';
src/ui/planetDevelopmentControls.ts:15:  readonly execute: (command: GameCommand, successMessage: string) => boolean;
src/ui/planetScreen.ts:11:import { executeCommand } from '../simulation/reducer';
src/ui/planetScreen.ts:12:import type { GameCommand, GameState } from '../simulation/types';
src/ui/planetScreen.ts:102:function applyCommand(command: GameCommand, successMessage: string): boolean {
src/ui/planetScreen.ts:103:  const result = executeCommand(requireState(), command);
src/ui/planetScreen.ts:376:  const commandLevel = getBuildingLevel(
src/ui/planetScreen.ts:378:    getFactionMechanicalRoles(planet.factionId).buildings.command,
src/ui/planetScreen.ts:385:      commandLevel > 0 ? `Совокупная ёмкость ${NUMBER_FORMAT.format(storageCapacity)}` : 'Требуется центр командования',
src/ui/planetScreen.ts:386:      commandLevel > 0,
src/ui/planetScreen.ts:390:        commandLevel > 0,
src/ui/planetScreen.ts:634:    applyCommand(
src/ui/planetScreen.ts:692:      applyCommand(
src/ui/planetScreen.ts:820:  const fleetButton = document.querySelector<HTMLButtonElement>('[aria-label="Флот"]');
src/ui/planetScreen.ts:822:  fleetButton?.removeAttribute('disabled');
src/ui/planetScreen.ts:829:  fleetButton?.addEventListener('click', () => {
src/ui/planetScreen.ts:840:        applyCommand({ type: 'ADVANCE_TIME', seconds }, `Время ускорено · +${formatGameDuration(seconds)}`);
src/ui/planetScreen.ts:852:    applyCommand({ type: 'ADVANCE_TIME', seconds }, 'Следующее событие обработано');
src/ui/planetScreen.ts:856:export function applyPlanetScreenCommand(
src/ui/planetScreen.ts:857:  command: GameCommand,
src/ui/planetScreen.ts:860:  return applyCommand(command, successMessage);
src/ui/productionScreen.ts:11:import type { GameCommand, GameState } from '../simulation/types';
src/ui/productionScreen.ts:32:  readonly execute: (command: GameCommand, successMessage: string) => boolean;
src/ui/researchScreen.ts:14:import type { GameCommand, GameState } from '../simulation/types';
src/ui/researchScreen.ts:20:  readonly execute: (command: GameCommand, successMessage: string) => boolean;
src/ui/researchScreen.ts:28:  intelligence: 'Разведка',
src/ui/shipUpgradesScreen.ts:2:import { getFleetShipArtUrl } from '../assets/galaxyFleetRuntimeAssets';
src/ui/shipUpgradesScreen.ts:3:import type { GameCommand, GameState } from '../simulation/types';
src/ui/shipUpgradesScreen.ts:17:  readonly execute: (command: GameCommand, successMessage: string) => boolean;
src/ui/shipUpgradesScreen.ts:34:  const fleets = document.querySelector<HTMLButtonElement>('#nav-fleets');
src/ui/shipUpgradesScreen.ts:41:  fleets?.insertAdjacentElement('afterend', button);
src/ui/shipUpgradesScreen.ts:42:  if (fleets === null) document.querySelector<HTMLElement>('.side-rail')?.append(button);
src/ui/shipUpgradesScreen.ts:173:      art.src = getFleetShipArtUrl(factionId, ship.id);
src/ui/spaceObjectsPanel.ts:1:import type { FleetState } from '../simulation/fleets/types';
src/ui/spaceObjectsPanel.ts:3:  estimateSpaceObjectMission,
src/ui/spaceObjectsPanel.ts:7:import type { GameCommand, GameState } from '../simulation/types';
src/ui/spaceObjectsPanel.ts:13:  readonly execute: (command: GameCommand, successMessage: string) => boolean;
src/ui/spaceObjectsPanel.ts:56:    <section class="space-objects-reports"></section>
src/ui/spaceObjectsPanel.ts:65:function compatibleFleets(
src/ui/spaceObjectsPanel.ts:68:): readonly FleetState[] {
src/ui/spaceObjectsPanel.ts:70:  return state.fleets
src/ui/spaceObjectsPanel.ts:72:      (fleet) =>
src/ui/spaceObjectsPanel.ts:73:        fleet.empireId === 'player' &&
src/ui/spaceObjectsPanel.ts:74:        fleet.status === 'stationed' &&
src/ui/spaceObjectsPanel.ts:75:        fleet.location.type === 'planet' &&
src/ui/spaceObjectsPanel.ts:76:        (fleet.ships[requiredShipId] ?? 0) > 0,
src/ui/spaceObjectsPanel.ts:90:        event.payload.report.objectId === object.id,
src/ui/spaceObjectsPanel.ts:109:function rewardText(report: {
src/ui/spaceObjectsPanel.ts:117:  return `M ${report.reward.metal} · C ${report.reward.crystal} · G ${report.reward.gas} · X ${report.reward.exoticMatter}`;
src/ui/spaceObjectsPanel.ts:126:  const reports = dialog.querySelector<HTMLElement>('.space-objects-reports');
src/ui/spaceObjectsPanel.ts:127:  if (launch === null || active === null || grid === null || reports === null) {
src/ui/spaceObjectsPanel.ts:146:    const fleetSelect = document.createElement('select');
src/ui/spaceObjectsPanel.ts:155:      fleetSelect.replaceChildren();
src/ui/spaceObjectsPanel.ts:162:      const fleets = compatibleFleets(state, object);
src/ui/spaceObjectsPanel.ts:163:      for (const fleet of fleets) {
src/ui/spaceObjectsPanel.ts:165:        option.value = fleet.id;
src/ui/spaceObjectsPanel.ts:166:        option.textContent = `${fleet.id} · ${getUnitDefinition(getRequiredSpaceObjectShipId(object.kind))?.name ?? getRequiredSpaceObjectShipId(object.kind)} × ${fleet.ships[getRequiredSpaceObjectShipId(object.kind)] ?? 0}`;
src/ui/spaceObjectsPanel.ts:167:        fleetSelect.append(option);
src/ui/spaceObjectsPanel.ts:169:      const fleet = fleets.find((candidate) => candidate.id === fleetSelect.value) ?? fleets[0];
src/ui/spaceObjectsPanel.ts:175:      if (fleet === undefined) {
src/ui/spaceObjectsPanel.ts:181:        const estimate = estimateSpaceObjectMission(state, fleet, object);
src/ui/spaceObjectsPanel.ts:182:        const originPlanetId = fleet.location.type === 'planet' ? fleet.location.planetId : undefined;
src/ui/spaceObjectsPanel.ts:198:    fleetSelect.addEventListener('change', refresh);
src/ui/spaceObjectsPanel.ts:205:            fleetId: fleetSelect.value,
src/ui/spaceObjectsPanel.ts:214:    launch.append(launchTitle, objectSelect, fleetSelect, preview, start);
src/ui/spaceObjectsPanel.ts:221:    const activeFleets = state.fleets.filter(
src/ui/spaceObjectsPanel.ts:222:      (fleet) => fleet.empireId === 'player' && fleet.mission?.kind === 'space-object',
src/ui/spaceObjectsPanel.ts:224:    for (const fleet of activeFleets) {
src/ui/spaceObjectsPanel.ts:227:      title.textContent = `${fleet.id} · ${fleet.mission?.targetPlanetId ?? 'unknown'}`;
src/ui/spaceObjectsPanel.ts:229:      details.textContent = fleet.location.type === 'transit'
src/ui/spaceObjectsPanel.ts:230:        ? `Завершение через ${formatGameDuration(Math.max(0, fleet.location.arrivesAt - state.clock.elapsedSeconds))}`
src/ui/spaceObjectsPanel.ts:231:        : fleet.status;
src/ui/spaceObjectsPanel.ts:238:            { type: 'RECALL_FLEET', empireId: 'player', fleetId: fleet.id },
src/ui/spaceObjectsPanel.ts:248:    if (activeFleets.length === 0) active.append('Активных операций нет.');
src/ui/spaceObjectsPanel.ts:264:    reports.replaceChildren();
src/ui/spaceObjectsPanel.ts:265:    const reportTitle = document.createElement('h3');
src/ui/spaceObjectsPanel.ts:266:    reportTitle.textContent = 'Отчёты операций';
src/ui/spaceObjectsPanel.ts:267:    reports.append(reportTitle);
src/ui/spaceObjectsPanel.ts:272:          entry.event.payload.report.empireId === 'player',
src/ui/spaceObjectsPanel.ts:278:      const report = entry.event.payload.report;
src/ui/spaceObjectsPanel.ts:281:      title.textContent = report.objectId;
src/ui/spaceObjectsPanel.ts:283:      const losses = Object.entries(report.losses)
src/ui/spaceObjectsPanel.ts:286:      result.textContent = `Добыча ${rewardText(report)} · истощение ${report.depletion}${losses.length > 0 ? ` · потери ${losses}` : ''}`;
src/ui/spaceObjectsPanel.ts:288:      narrative.textContent = report.narrative;
src/ui/spaceObjectsPanel.ts:290:      reports.append(card);
src/ui/spaceObjectsPanel.ts:292:    if (entries.length === 0) reports.append('Завершённых операций пока нет.');
tests/assets/assetPipelineConfig.test.ts:69:    expect(processingPlan.entries.filter((entry) => entry.family === 'commander')).toHaveLength(13);
tests/assets/completeMechanicalAssetManifest.test.ts:5:  validateCompleteMechanicalAssetManifest,
tests/assets/completeMechanicalAssetManifest.test.ts:11:    expect(validateCompleteMechanicalAssetManifest()).toEqual([]);
tests/assets/completeMechanicalAssetManifest.test.ts:24:  it('binds Commander source assets to generated runtime art', () => {
tests/assets/completeMechanicalAssetManifest.test.ts:25:    const resolution = resolveCompleteMechanicalAsset('commander.shared.annihilator');
tests/assets/completeMechanicalAssetManifest.test.ts:27:    expect(resolution.asset?.id).toBe('commander.shared.annihilator');
tests/assets/completeMechanicalAssetManifest.test.ts:28:    expect(resolution.asset?.atlasUrl).toContain('/assets/generated/catalog/commanders/shared/annihilator.webp');
tests/assets/completeMechanicalAssetManifest.test.ts:30:      'assets/source/New assets/comander_ship/commander-ship.annihilator.png',
tests/assets/completeMechanicalAssetManifest.test.ts:34:  it('does not invent art or source provenance for an unknown Commander ID', () => {
tests/assets/completeMechanicalAssetManifest.test.ts:35:    const resolution = resolveCompleteMechanicalAsset('commander.shared.unknown-future');
tests/assets/completeMechanicalAssetManifest.test.ts:40:      'commander.shared.unknown-future'
tests/assets/runtimeMechanicalAssets.test.ts:7:import { COMPLETE_COMMANDER_SHIP_CATALOG } from '../../src/simulation/units/completeCommanderShipCatalog';
tests/assets/runtimeMechanicalAssets.test.ts:55:  it('resolves every defense and Commander through unique generated art', () => {
tests/assets/runtimeMechanicalAssets.test.ts:71:    const commanderUrls = new Set<string>();
tests/assets/runtimeMechanicalAssets.test.ts:77:        '/assets/generated/catalog/commanders/shared/',
tests/assets/runtimeMechanicalAssets.test.ts:79:      commanderUrls.add(resolution.asset?.atlasUrl ?? '');
tests/assets/runtimeMechanicalAssets.test.ts:81:    expect(commanderUrls).toHaveLength(13);
tests/fixtures/gameStateV13Fixture.ts:23:    intelligence: withoutUniverse.intelligence.map((state) => ({
tests/simulation/botEconomyPlanner.test.ts:7:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/botEconomyPlanner.test.ts:21:    expect(first.every((plan) => plan.command?.type === 'SET_PLANET_SPECIALIZATION')).toBe(true);
tests/simulation/botEconomyPlanner.test.ts:29:      expect(specialization.command?.type).toBe('SET_PLANET_SPECIALIZATION');
tests/simulation/botEconomyPlanner.test.ts:30:      if (specialization.command === null) continue;
tests/simulation/botEconomyPlanner.test.ts:31:      const specialized = executeCommand(state, specialization.command);
tests/simulation/botEconomyPlanner.test.ts:37:      expect(build.command?.type).toBe('QUEUE_BUILDING');
tests/simulation/botEconomyPlanner.test.ts:38:      if (build.command === null) continue;
tests/simulation/botEconomyPlanner.test.ts:39:      const queued = executeCommand(state, build.command);
tests/simulation/botEconomyPlanner.test.ts:70:      command: {
tests/simulation/botEconomyPlanner.test.ts:100:  it('waits without issuing invalid commands when every build queue is busy', () => {
tests/simulation/botEconomyPlanner.test.ts:111:                  buildingId: 'building.aegis.command',
tests/simulation/botEconomyPlanner.test.ts:124:      command: null,
tests/simulation/botFleetMissionPlanner.test.ts:3:  planAllBotFleetMissions,
tests/simulation/botFleetMissionPlanner.test.ts:4:  planBotFleetMission,
tests/simulation/botFleetMissionPlanner.test.ts:5:} from '../../src/simulation/bots/fleetMissionPlanner';
tests/simulation/botFleetMissionPlanner.test.ts:7:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/botFleetMissionPlanner.test.ts:35:describe('bot fleet and mission planner', () => {
tests/simulation/botFleetMissionPlanner.test.ts:36:  it('forms a valid scout fleet from owned inventory', () => {
tests/simulation/botFleetMissionPlanner.test.ts:37:    let state = createInitialGameState('bot-fleet-create');
tests/simulation/botFleetMissionPlanner.test.ts:55:    const plan = planBotFleetMission(state, empireId);
tests/simulation/botFleetMissionPlanner.test.ts:57:      reasonCode: 'fleet-created',
tests/simulation/botFleetMissionPlanner.test.ts:58:      command: {
tests/simulation/botFleetMissionPlanner.test.ts:65:    if (plan.command !== null) {
tests/simulation/botFleetMissionPlanner.test.ts:66:      expect(executeCommand(state, plan.command).ok).toBe(true);
tests/simulation/botFleetMissionPlanner.test.ts:70:  it('refreshes observed foreign intelligence without reading hidden resources', () => {
tests/simulation/botFleetMissionPlanner.test.ts:71:    let state = fillGas(createInitialGameState('bot-fleet-scout'), 'synod-bot');
tests/simulation/botFleetMissionPlanner.test.ts:77:      fleets: [
tests/simulation/botFleetMissionPlanner.test.ts:88:          mission: null,
tests/simulation/botFleetMissionPlanner.test.ts:91:      intelligence: state.intelligence.map((entry) =>
tests/simulation/botFleetMissionPlanner.test.ts:99:                  targetPlanetId: target.id,
tests/simulation/botFleetMissionPlanner.test.ts:118:    const before = planBotFleetMission(state, empireId);
tests/simulation/botFleetMissionPlanner.test.ts:136:    expect(planBotFleetMission(hiddenChanged, empireId)).toEqual(before);
tests/simulation/botFleetMissionPlanner.test.ts:137:    expect(before.command).toMatchObject({
tests/simulation/botFleetMissionPlanner.test.ts:139:      fleetId: 'synod-scout',
tests/simulation/botFleetMissionPlanner.test.ts:140:      targetPlanetId: target.id,
tests/simulation/botFleetMissionPlanner.test.ts:141:      mission: 'scout',
tests/simulation/botFleetMissionPlanner.test.ts:143:    if (before.command !== null) {
tests/simulation/botFleetMissionPlanner.test.ts:144:      expect(executeCommand(state, before.command).ok).toBe(true);
tests/simulation/botFleetMissionPlanner.test.ts:149:    let state = fillGas(createInitialGameState('bot-fleet-transport'), 'aegis-bot');
tests/simulation/botFleetMissionPlanner.test.ts:172:      fleets: [
tests/simulation/botFleetMissionPlanner.test.ts:183:          mission: null,
tests/simulation/botFleetMissionPlanner.test.ts:188:    const plan = planBotFleetMission(state, empireId);
tests/simulation/botFleetMissionPlanner.test.ts:189:    expect(plan.command).toMatchObject({
tests/simulation/botFleetMissionPlanner.test.ts:191:      fleetId: 'aegis-transport',
tests/simulation/botFleetMissionPlanner.test.ts:192:      targetPlanetId: second.id,
tests/simulation/botFleetMissionPlanner.test.ts:193:      mission: 'transport',
tests/simulation/botFleetMissionPlanner.test.ts:195:    if (plan.command !== null) {
tests/simulation/botFleetMissionPlanner.test.ts:196:      expect(executeCommand(state, plan.command).ok).toBe(true);
tests/simulation/botFleetMissionPlanner.test.ts:200:  it('selects a legal colonization target through normal mission validation', () => {
tests/simulation/botFleetMissionPlanner.test.ts:201:    let state = fillGas(createInitialGameState('bot-fleet-colonize'), 'veyra-bot');
tests/simulation/botFleetMissionPlanner.test.ts:214:      fleets: [
tests/simulation/botFleetMissionPlanner.test.ts:225:          mission: null,
tests/simulation/botFleetMissionPlanner.test.ts:230:    const plan = planBotFleetMission(state, empireId);
tests/simulation/botFleetMissionPlanner.test.ts:231:    expect(plan.reasonCode).toBe('mission-colonize-selected');
tests/simulation/botFleetMissionPlanner.test.ts:232:    expect(plan.command).toMatchObject({
tests/simulation/botFleetMissionPlanner.test.ts:234:      fleetId: 'veyra-colonizer',
tests/simulation/botFleetMissionPlanner.test.ts:235:      mission: 'colonize',
tests/simulation/botFleetMissionPlanner.test.ts:237:    if (plan.command !== null) {
tests/simulation/botFleetMissionPlanner.test.ts:238:      expect(executeCommand(state, plan.command).ok).toBe(true);
tests/simulation/botFleetMissionPlanner.test.ts:243:    let state = fillGas(createInitialGameState('bot-fleet-attack'), 'aegis-bot');
tests/simulation/botFleetMissionPlanner.test.ts:249:      fleets: [
tests/simulation/botFleetMissionPlanner.test.ts:260:          mission: null,
tests/simulation/botFleetMissionPlanner.test.ts:263:      intelligence: state.intelligence.map((entry) =>
tests/simulation/botFleetMissionPlanner.test.ts:271:                  targetPlanetId: target.id,
tests/simulation/botFleetMissionPlanner.test.ts:282:                    stationedFleets: [],
tests/simulation/botFleetMissionPlanner.test.ts:291:    const plan = planBotFleetMission(state, empireId);
tests/simulation/botFleetMissionPlanner.test.ts:293:      reasonCode: 'mission-attack-selected',
tests/simulation/botFleetMissionPlanner.test.ts:294:      command: {
tests/simulation/botFleetMissionPlanner.test.ts:296:        fleetId: 'aegis-strike',
tests/simulation/botFleetMissionPlanner.test.ts:297:        targetPlanetId: target.id,
tests/simulation/botFleetMissionPlanner.test.ts:298:        mission: 'attack',
tests/simulation/botFleetMissionPlanner.test.ts:304:    const state = createInitialGameState('bot-fleet-determinism');
tests/simulation/botFleetMissionPlanner.test.ts:305:    expect(planAllBotFleetMissions(state)).toEqual(planAllBotFleetMissions(state));
tests/simulation/botPerception.test.ts:25:  it('does not change when hidden player state changes without new intelligence', () => {
tests/simulation/botPerception.test.ts:57:    const intelligence = state.intelligence.map((entry) =>
tests/simulation/botPerception.test.ts:65:                targetPlanetId: playerPlanet.id,
tests/simulation/botPerception.test.ts:82:                  buildings: { 'building.aegis.command': 1 },
tests/simulation/botPerception.test.ts:92:      intelligence,
tests/simulation/botPerception.test.ts:121:      intelligence: state.intelligence.map((entry) =>
tests/simulation/botPerception.test.ts:129:                  targetPlanetId: playerPlanet.id,
tests/simulation/botPerception.test.ts:147:                  targetPlanetId: entry.empireId,
tests/simulation/botResearchProductionPlanner.test.ts:9:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/botResearchProductionPlanner.test.ts:29:                  building.buildingId !== roles.buildings.command &&
tests/simulation/botResearchProductionPlanner.test.ts:34:              { buildingId: roles.buildings.command, level: 3 },
tests/simulation/botResearchProductionPlanner.test.ts:87:        command: null,
tests/simulation/botResearchProductionPlanner.test.ts:91:        command: null,
tests/simulation/botResearchProductionPlanner.test.ts:102:      expect(plan.research.command?.type).toBe('QUEUE_RESEARCH');
tests/simulation/botResearchProductionPlanner.test.ts:103:      expect(plan.production.command?.type).toBe('QUEUE_UNIT_BATCH');
tests/simulation/botResearchProductionPlanner.test.ts:105:      if (plan.research.command?.type === 'QUEUE_RESEARCH') {
tests/simulation/botResearchProductionPlanner.test.ts:106:        expect(plan.research.command.technologyId).toContain(
tests/simulation/botResearchProductionPlanner.test.ts:109:        const researchResult = executeCommand(state, plan.research.command);
tests/simulation/botResearchProductionPlanner.test.ts:113:      if (plan.production.command?.type === 'QUEUE_UNIT_BATCH') {
tests/simulation/botResearchProductionPlanner.test.ts:114:        expect(plan.production.command.unitId).toContain(
tests/simulation/botResearchProductionPlanner.test.ts:117:        const productionResult = executeCommand(state, plan.production.command);
tests/simulation/botResearchProductionPlanner.test.ts:124:  it('prioritizes military research and fighters when current intelligence shows a threat', () => {
tests/simulation/botResearchProductionPlanner.test.ts:139:      intelligence: state.intelligence.map((entry) =>
tests/simulation/botResearchProductionPlanner.test.ts:147:                  targetPlanetId: playerPlanet.id,
tests/simulation/botResearchProductionPlanner.test.ts:159:                    activeFleets: [],
tests/simulation/botResearchProductionPlanner.test.ts:169:    expect(plan.research.command?.type).toBe('QUEUE_RESEARCH');
tests/simulation/botResearchProductionPlanner.test.ts:170:    if (plan.research.command?.type === 'QUEUE_RESEARCH') {
tests/simulation/botResearchProductionPlanner.test.ts:174:      ]).toContain(plan.research.command.technologyId);
tests/simulation/botResearchProductionPlanner.test.ts:176:    expect(plan.production.command).toMatchObject({
tests/simulation/botScheduler.test.ts:9:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/botScheduler.test.ts:18:  const result = executeCommand(state, { type: 'ADVANCE_TIME', seconds });
tests/simulation/botScheduler.test.ts:25:  it('runs due profiles and records accepted normal commands', () => {
tests/simulation/botScheduler.test.ts:30:    expect(result.state.commandLog.length).toBe(result.audit.length);
tests/simulation/botScheduler.test.ts:100:      maxCommandsPerDecision: 1,
tests/simulation/botThreatRecoveryPlanner.test.ts:9:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/botThreatRecoveryPlanner.test.ts:28:                  building.buildingId !== roles.buildings.command &&
tests/simulation/botThreatRecoveryPlanner.test.ts:33:              { buildingId: roles.buildings.command, level: 3 },
tests/simulation/botThreatRecoveryPlanner.test.ts:79:  it('does not react to hidden player changes without intelligence', () => {
tests/simulation/botThreatRecoveryPlanner.test.ts:106:  it('selects a profitable current target from stored intelligence', () => {
tests/simulation/botThreatRecoveryPlanner.test.ts:116:      fleets: [
tests/simulation/botThreatRecoveryPlanner.test.ts:127:          mission: null,
tests/simulation/botThreatRecoveryPlanner.test.ts:130:      intelligence: state.intelligence.map((entry) =>
tests/simulation/botThreatRecoveryPlanner.test.ts:138:                  targetPlanetId: target.id,
tests/simulation/botThreatRecoveryPlanner.test.ts:155:                    buildings: { 'building.aegis.command': 2 },
tests/simulation/botThreatRecoveryPlanner.test.ts:157:                    stationedFleets: [],
tests/simulation/botThreatRecoveryPlanner.test.ts:173:    expect(plan.command).toMatchObject({
tests/simulation/botThreatRecoveryPlanner.test.ts:175:      fleetId: 'strike-target',
tests/simulation/botThreatRecoveryPlanner.test.ts:176:      targetPlanetId: target.id,
tests/simulation/botThreatRecoveryPlanner.test.ts:177:      mission: 'attack',
tests/simulation/botThreatRecoveryPlanner.test.ts:181:  it('rebuilds military production after fleet losses', () => {
tests/simulation/botThreatRecoveryPlanner.test.ts:187:    expect(plan.recoveryPhase).toBe('fleet');
tests/simulation/botThreatRecoveryPlanner.test.ts:189:    expect(plan.command).toMatchObject({
tests/simulation/botThreatRecoveryPlanner.test.ts:195:    if (plan.command !== null) {
tests/simulation/botThreatRecoveryPlanner.test.ts:196:      expect(executeCommand(state, plan.command).ok).toBe(true);
tests/simulation/botThreatRecoveryPlanner.test.ts:209:      intelligence: state.intelligence.map((entry) =>
tests/simulation/botThreatRecoveryPlanner.test.ts:217:                  targetPlanetId: target.id,
tests/simulation/botThreatRecoveryPlanner.test.ts:228:                    stationedFleets: [],
tests/simulation/botThreatRecoveryPlanner.test.ts:237:                  targetPlanetId: entry.empireId,
tests/simulation/botThreatRecoveryPlanner.test.ts:251:    expect(plan.command).toMatchObject({
tests/simulation/botThreatRecoveryPlanner.test.ts:268:      command: null,
tests/simulation/buildingCatalogValidation.test.ts:6:import { validateBuildingCatalog } from '../../src/simulation/planet/buildingCatalogValidation';
tests/simulation/buildingCatalogValidation.test.ts:19:    assetId: 'building.aegis.command',
tests/simulation/buildingCatalogValidation.test.ts:28:    expect(validateBuildingCatalog(AEGIS_BUILDING_CATALOG)).toEqual([]);
tests/simulation/buildingCatalogValidation.test.ts:42:    const issues = validateBuildingCatalog([first, second, unknown]);
tests/simulation/buildingQueue.test.ts:9:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/buildingQueue.test.ts:24:  return executeCommand(state, {
tests/simulation/buildingQueue.test.ts:99:    const advanced = executeCommand(queued.value, {
tests/simulation/buildingQueue.test.ts:116:    const later = executeCommand(advanced.value, { type: 'ADVANCE_TIME', seconds: 3_600 });
tests/simulation/buildingQueue.test.ts:168:    const cancelled = executeCommand(queued.value, {
tests/simulation/buildingQueue.test.ts:190:    const result = executeCommand(state, {
tests/simulation/colonization.test.ts:8:import { executeGameCommand } from '../../src/simulation/executeGameCommand';
tests/simulation/colonization.test.ts:9:import { estimateFlightToGalaxyPlanet } from '../../src/simulation/fleets/flightCalculations';
tests/simulation/colonization.test.ts:91:  const result = executeGameCommand(state, {
tests/simulation/colonization.test.ts:112:    const fleet = initial.fleets[0];
tests/simulation/colonization.test.ts:113:    expect(fleet).toBeDefined();
tests/simulation/colonization.test.ts:114:    if (fleet === undefined) return;
tests/simulation/colonization.test.ts:119:      fleet,
tests/simulation/colonization.test.ts:122:    const sent = executeGameCommand(initial, {
tests/simulation/colonization.test.ts:125:      fleetId: fleet.id,
tests/simulation/colonization.test.ts:126:      targetPlanetId: target.planet.id,
tests/simulation/colonization.test.ts:127:      mission: 'colonize',
tests/simulation/colonization.test.ts:132:    const arrived = executeGameCommand(sent.value, {
tests/simulation/colonization.test.ts:165:    expect(arrived.value.fleets).toHaveLength(0);
tests/simulation/colonization.test.ts:174:    const created = executeGameCommand(prepared, {
tests/simulation/colonization.test.ts:187:    const fleet = created.value.fleets[0];
tests/simulation/colonization.test.ts:188:    if (fleet === undefined) throw new Error('Expedition fleet missing.');
tests/simulation/colonization.test.ts:192:      fleet,
tests/simulation/colonization.test.ts:195:    const sent = executeGameCommand(created.value, {
tests/simulation/colonization.test.ts:198:      fleetId: fleet.id,
tests/simulation/colonization.test.ts:199:      targetPlanetId: target.planet.id,
tests/simulation/colonization.test.ts:200:      mission: 'colonize',
tests/simulation/colonization.test.ts:203:    const arrived = executeGameCommand(sent.value, {
tests/simulation/colonization.test.ts:212:    expect(arrived.value.fleets[0]).toMatchObject({
tests/simulation/colonization.test.ts:217:      mission: null,
tests/simulation/colonization.test.ts:226:    const fleet = initial.fleets[0];
tests/simulation/colonization.test.ts:227:    if (fleet === undefined) throw new Error('Expedition fleet missing.');
tests/simulation/colonization.test.ts:231:      fleet,
tests/simulation/colonization.test.ts:234:    const sent = executeGameCommand(initial, {
tests/simulation/colonization.test.ts:237:      fleetId: fleet.id,
tests/simulation/colonization.test.ts:238:      targetPlanetId: target.planet.id,
tests/simulation/colonization.test.ts:239:      mission: 'colonize',
tests/simulation/colonization.test.ts:251:    const arrived = executeGameCommand(occupied, {
tests/simulation/colonization.test.ts:257:    expect(arrived.value.fleets[0]?.status).toBe('returning');
tests/simulation/colonization.test.ts:271:      executeGameCommand(withoutTech, {
tests/simulation/colonization.test.ts:274:        fleetId: withoutTech.fleets[0]!.id,
tests/simulation/colonization.test.ts:275:        targetPlanetId: target.planet.id,
tests/simulation/colonization.test.ts:276:        mission: 'colonize',
tests/simulation/colonization.test.ts:286:      executeGameCommand(withoutShip, {
tests/simulation/colonization.test.ts:289:        fleetId: withoutShip.fleets[0]!.id,
tests/simulation/colonization.test.ts:290:        targetPlanetId: secondTarget.planet.id,
tests/simulation/colonization.test.ts:291:        mission: 'colonize',
tests/simulation/colonization.test.ts:317:      executeGameCommand(atLimit, {
tests/simulation/colonization.test.ts:320:        fleetId: atLimit.fleets[0]!.id,
tests/simulation/colonization.test.ts:321:        targetPlanetId: limitTarget.planet.id,
tests/simulation/colonization.test.ts:322:        mission: 'colonize',
tests/simulation/combat.test.ts:4:import { executeGameCommand } from '../../src/simulation/executeGameCommand';
tests/simulation/combat.test.ts:5:import { estimateFlight } from '../../src/simulation/fleets/flightCalculations';
tests/simulation/combat.test.ts:68:    fleets: [
tests/simulation/combat.test.ts:70:        id: 'defender-fleet',
tests/simulation/combat.test.ts:79:        mission: null,
tests/simulation/combat.test.ts:87:  const created = executeGameCommand(state, {
tests/simulation/combat.test.ts:102:  const attacker = created.value.fleets.find(
tests/simulation/combat.test.ts:103:    (fleet) => fleet.empireId === 'player',
tests/simulation/combat.test.ts:111:  const sent = executeGameCommand(created.value, {
tests/simulation/combat.test.ts:114:    fleetId: attacker.id,
tests/simulation/combat.test.ts:115:    targetPlanetId: target.id,
tests/simulation/combat.test.ts:116:    mission: 'attack',
tests/simulation/combat.test.ts:119:  const resolved = executeGameCommand(sent.value, {
tests/simulation/combat.test.ts:153:  it('resolves an attack mission and writes a battle report to the event log', () => {
tests/simulation/combat.test.ts:154:    const final = launchAttack(prepareAttackState('attack-mission'));
tests/simulation/combat.test.ts:155:    const reportEntry = final.eventLog.find(
tests/simulation/combat.test.ts:158:    expect(reportEntry).toBeDefined();
tests/simulation/combat.test.ts:159:    if (reportEntry?.event.payload.type !== 'BATTLE_REPORT') return;
tests/simulation/combat.test.ts:161:    const report = reportEntry.event.payload.report;
tests/simulation/combat.test.ts:162:    expect(report.targetPlanetId).toBeTruthy();
tests/simulation/combat.test.ts:163:    expect(report.rounds.length).toBeGreaterThan(0);
tests/simulation/combat.test.ts:164:    expect(report.attackerInitial).toEqual({
tests/simulation/combat.test.ts:168:    expect(report.defenderInitial).toEqual({
tests/simulation/combat.test.ts:175:  it('replays an identical attack into an identical battle report', () => {
tests/simulation/combat.test.ts:178:    const firstReport = first.eventLog.find(
tests/simulation/combat.test.ts:181:    const secondReport = second.eventLog.find(
tests/simulation/combat.test.ts:184:    expect(secondReport).toEqual(firstReport);
tests/simulation/combat.test.ts:199:    const created = executeGameCommand(ownedState, {
tests/simulation/combat.test.ts:209:      executeGameCommand(created.value, {
tests/simulation/combat.test.ts:212:        fleetId: created.value.fleets.find((fleet) => fleet.empireId === 'player')!.id,
tests/simulation/combat.test.ts:213:        targetPlanetId: target.id,
tests/simulation/combat.test.ts:214:        mission: 'attack',
tests/simulation/commandDoctrine.test.ts:3:  addCommandExperience,
tests/simulation/commandDoctrine.test.ts:4:  calculateCommandLevel,
tests/simulation/commandDoctrine.test.ts:5:  getCommandCombatEffects,
tests/simulation/commandDoctrine.test.ts:6:} from '../../src/simulation/command/commandDoctrine';
tests/simulation/commandDoctrine.test.ts:8:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/commandDoctrine.test.ts:10:describe('command doctrine and flagships', () => {
tests/simulation/commandDoctrine.test.ts:12:    const state = createInitialGameState('command-initial');
tests/simulation/commandDoctrine.test.ts:13:    expect(state.commanders).toHaveLength(state.empires.length);
tests/simulation/commandDoctrine.test.ts:14:    expect(state.commanders.every((entry) => entry.level === 1 && entry.experience === 0)).toBe(true);
tests/simulation/commandDoctrine.test.ts:15:    expect(calculateCommandLevel(0)).toBe(1);
tests/simulation/commandDoctrine.test.ts:16:    expect(calculateCommandLevel(100)).toBe(2);
tests/simulation/commandDoctrine.test.ts:17:    expect(calculateCommandLevel(1_500)).toBe(5);
tests/simulation/commandDoctrine.test.ts:21:    const state = createInitialGameState('command-doctrine');
tests/simulation/commandDoctrine.test.ts:22:    const result = executeCommand(state, {
tests/simulation/commandDoctrine.test.ts:29:    expect(result.value.commanders.find((entry) => entry.empireId === 'player')?.doctrineId).toBe('vanguard');
tests/simulation/commandDoctrine.test.ts:30:    expect(getCommandCombatEffects(result.value.commanders, 'player')).toMatchObject({
tests/simulation/commandDoctrine.test.ts:37:  it('requires level two and an armed stationed fleet for a flagship', () => {
tests/simulation/commandDoctrine.test.ts:38:    const initial = createInitialGameState('command-flagship');
tests/simulation/commandDoctrine.test.ts:41:    const fleet = {
tests/simulation/commandDoctrine.test.ts:53:      mission: null,
tests/simulation/commandDoctrine.test.ts:55:    const levelOne = { ...initial, fleets: [...initial.fleets, fleet] };
tests/simulation/commandDoctrine.test.ts:56:    expect(executeCommand(levelOne, { type: 'ASSIGN_FLAGSHIP', empireId: 'player', fleetId: fleet.id })).toMatchObject({
tests/simulation/commandDoctrine.test.ts:62:      commanders: addCommandExperience(levelOne.commanders, 'player', 100),
tests/simulation/commandDoctrine.test.ts:64:    const assigned = executeCommand(levelTwo, {
tests/simulation/commandDoctrine.test.ts:67:      fleetId: fleet.id,
tests/simulation/commandDoctrine.test.ts:71:    expect(getCommandCombatEffects(assigned.value.commanders, 'player', fleet.id)).toMatchObject({
tests/simulation/commandDoctrine.test.ts:79:    const state = createInitialGameState('command-xp');
tests/simulation/commandDoctrine.test.ts:80:    const after = addCommandExperience(state.commanders, 'player', 100);
tests/simulation/completeBuildingCatalog.test.ts:67:    expect(resolveCanonicalBuildingId('building.aegis.command')).toBe(
tests/simulation/completeCommanderShipCatalog.test.ts:4:  getCommanderFleetEffects,
tests/simulation/completeCommanderShipCatalog.test.ts:5:  selectActiveCommanderShip,
tests/simulation/completeCommanderShipCatalog.test.ts:6:} from '../../src/simulation/command/commanderShips';
tests/simulation/completeCommanderShipCatalog.test.ts:7:import { COMMAND_LEVEL_THRESHOLDS } from '../../src/simulation/command/commandDoctrine';
tests/simulation/completeCommanderShipCatalog.test.ts:11:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/completeCommanderShipCatalog.test.ts:14:  getCompleteCommanderShipId,
tests/simulation/completeCommanderShipCatalog.test.ts:15:} from '../../src/simulation/units/completeCommanderShipCatalog';
tests/simulation/completeCommanderShipCatalog.test.ts:19:function prepareCommanderProduction(state: GameState): GameState {
tests/simulation/completeCommanderShipCatalog.test.ts:43:    commanders: state.commanders.map((command) =>
tests/simulation/completeCommanderShipCatalog.test.ts:44:      command.empireId === 'player'
tests/simulation/completeCommanderShipCatalog.test.ts:45:        ? { ...command, experience: 1_000_000, level: 40 }
tests/simulation/completeCommanderShipCatalog.test.ts:46:        : command,
tests/simulation/completeCommanderShipCatalog.test.ts:57:describe('complete Commander Ship catalog', () => {
tests/simulation/completeCommanderShipCatalog.test.ts:62:      expect(definition.id).toMatch(/^commander\.shared\./);
tests/simulation/completeCommanderShipCatalog.test.ts:64:      expect(definition.role).toBe('commander');
tests/simulation/completeCommanderShipCatalog.test.ts:65:      expect(definition.commanderAbility).toBeDefined();
tests/simulation/completeCommanderShipCatalog.test.ts:71:      expect(resolution.asset?.atlasUrl).toContain('/assets/generated/catalog/commanders/shared/');
tests/simulation/completeCommanderShipCatalog.test.ts:72:      expect(resolution.provenancePath).toContain('/comander_ship/commander-ship.');
tests/simulation/completeCommanderShipCatalog.test.ts:86:      [getCompleteCommanderShipId('executor')]: 1,
tests/simulation/completeCommanderShipCatalog.test.ts:87:      [getCompleteCommanderShipId('annihilator')]: 1,
tests/simulation/completeCommanderShipCatalog.test.ts:89:    const command = {
tests/simulation/completeCommanderShipCatalog.test.ts:91:      level: 40, flagshipFleetId: 'flagship-1',
tests/simulation/completeCommanderShipCatalog.test.ts:93:    const active = selectActiveCommanderShip(command, 'flagship-1', units);
tests/simulation/completeCommanderShipCatalog.test.ts:94:    expect(active?.unitId).toBe(getCompleteCommanderShipId('annihilator'));
tests/simulation/completeCommanderShipCatalog.test.ts:95:    expect(selectActiveCommanderShip(command, 'other-fleet', units)).toBeUndefined();
tests/simulation/completeCommanderShipCatalog.test.ts:96:    const state = { commanders: [command] };
tests/simulation/completeCommanderShipCatalog.test.ts:97:    expect(getCommanderFleetEffects(state, 'player', 'flagship-1', units)).toMatchObject({
tests/simulation/completeCommanderShipCatalog.test.ts:98:      activeCommanderId: getCompleteCommanderShipId('annihilator'),
tests/simulation/completeCommanderShipCatalog.test.ts:103:  it('produces a Commander Ship and rejects an empire-wide duplicate', () => {
tests/simulation/completeCommanderShipCatalog.test.ts:104:    const initial = prepareCommanderProduction(createInitialGameState('commander-production'));
tests/simulation/completeCommanderShipCatalog.test.ts:106:    const commanderId = getCompleteCommanderShipId('corsair');
tests/simulation/completeCommanderShipCatalog.test.ts:107:    const queued = executeCommand(initial, {
tests/simulation/completeCommanderShipCatalog.test.ts:109:      unitId: commanderId, quantity: 1,
tests/simulation/completeCommanderShipCatalog.test.ts:113:    const duplicate = executeCommand(queued.value, {
tests/simulation/completeCommanderShipCatalog.test.ts:115:      unitId: commanderId, quantity: 1,
tests/simulation/completeCommanderShipCatalog.test.ts:123:        current: { buildings: 24, technologies: 22, ships: 13, defenses: 9, commanderShips: 13 },
tests/simulation/completeCommanderShipCatalog.test.ts:127:    expect(runFullGameValidation('commander-full-game')).toEqual([]);
tests/simulation/completeShipCatalog.test.ts:3:import { findFleetShipByRole } from '../../src/simulation/colonization/colonization';
tests/simulation/completeShipCatalog.test.ts:7:  validateFactionMechanicalCatalog,
tests/simulation/completeShipCatalog.test.ts:14:import { validateUnitCatalog } from '../../src/simulation/units/validateUnitCatalog';
tests/simulation/completeShipCatalog.test.ts:28:    expect(validateFactionMechanicalCatalog(catalog)).toEqual([]);
tests/simulation/completeShipCatalog.test.ts:29:    expect(validateUnitCatalog(ships, catalog.buildings, catalog.research)).toEqual([]);
tests/simulation/completeShipCatalog.test.ts:47:  it('keeps legacy ship ids readable while new commands resolve canonical definitions', () => {
tests/simulation/completeShipCatalog.test.ts:53:  it('covers scout, transport, colonize and recycle mission roles', () => {
tests/simulation/completeShipCatalog.test.ts:55:    expect(findFleetShipByRole({ [ships.lightFighter]: 1 }, 'scout')).toBe(ships.lightFighter);
tests/simulation/completeShipCatalog.test.ts:56:    expect(findFleetShipByRole({ [ships.spyProbe]: 1 }, 'scout')).toBe(ships.spyProbe);
tests/simulation/completeShipCatalog.test.ts:57:    expect(findFleetShipByRole({ [ships.smallTransport]: 1 }, 'transport')).toBe(ships.smallTransport);
tests/simulation/completeShipCatalog.test.ts:58:    expect(findFleetShipByRole({ [ships.colonizer]: 1 }, 'colonizer')).toBe(ships.colonizer);
tests/simulation/completeShipCatalog.test.ts:59:    expect(findFleetShipByRole({ [ships.recycler]: 1 }, 'recycler')).toBe(ships.recycler);
tests/simulation/debris.test.ts:6:import { resolveAttackMission } from '../../src/simulation/combat/resolveAttackMission';
tests/simulation/debris.test.ts:8:import { executeGameCommand } from '../../src/simulation/executeGameCommand';
tests/simulation/debris.test.ts:9:import { estimateFlight } from '../../src/simulation/fleets/flightCalculations';
tests/simulation/debris.test.ts:10:import type { FleetState } from '../../src/simulation/fleets/types';
tests/simulation/debris.test.ts:65:    const attacker: FleetState = {
tests/simulation/debris.test.ts:75:      mission: { kind: 'attack', targetPlanetId: target.id },
tests/simulation/debris.test.ts:90:      fleets: [attacker],
tests/simulation/debris.test.ts:93:    const result = resolveAttackMission(prepared, attacker, targetBefore, 3);
tests/simulation/debris.test.ts:95:    expect(result.report.debrisCreated?.metal).toBeGreaterThan(0);
tests/simulation/debris.test.ts:98:      metal: result.report.debrisCreated?.metal,
tests/simulation/debris.test.ts:99:      crystal: result.report.debrisCreated?.crystal,
tests/simulation/debris.test.ts:101:    const plunder = result.report.plunderedCargo!;
tests/simulation/debris.test.ts:110:    const fleet: FleetState = {
tests/simulation/debris.test.ts:120:      mission: { kind: 'recycle', targetPlanetId: 'p2' },
tests/simulation/debris.test.ts:125:      fleet,
tests/simulation/debris.test.ts:128:    expect(result.fleet.cargo).toEqual({ metal: 700, crystal: 100, gas: 0 });
tests/simulation/debris.test.ts:132:  it('runs a recycling mission and returns the collected cargo', () => {
tests/simulation/debris.test.ts:133:    let state = withHighFuel(createInitialGameState('recycle-mission'));
tests/simulation/debris.test.ts:159:    const created = executeGameCommand(state, {
tests/simulation/debris.test.ts:168:    const fleet = created.value.fleets[0]!;
tests/simulation/debris.test.ts:172:      fleet,
tests/simulation/debris.test.ts:175:    const sent = executeGameCommand(created.value, {
tests/simulation/debris.test.ts:178:      fleetId: fleet.id,
tests/simulation/debris.test.ts:179:      targetPlanetId: target.id,
tests/simulation/debris.test.ts:180:      mission: 'recycle',
tests/simulation/debris.test.ts:184:    const arrived = executeGameCommand(sent.value, {
tests/simulation/debris.test.ts:190:    expect(arrived.value.fleets[0]).toMatchObject({
tests/simulation/economy.test.ts:8:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/economy.test.ts:44:    const result = executeCommand(state, { type: 'ADVANCE_TIME', seconds: 3_600 });
tests/simulation/economy.test.ts:66:    const single = executeCommand(initial, { type: 'ADVANCE_TIME', seconds: 3_600 });
tests/simulation/economy.test.ts:70:      const result = executeCommand(chunked, { type: 'ADVANCE_TIME', seconds: 60 });
tests/simulation/expeditions.test.ts:3:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/expeditions.test.ts:11:function withExpeditionFleet(
tests/simulation/expeditions.test.ts:13:  fleetId = 'player-expedition',
tests/simulation/expeditions.test.ts:37:    fleets: [
tests/simulation/expeditions.test.ts:38:      ...state.fleets,
tests/simulation/expeditions.test.ts:40:        id: fleetId,
tests/simulation/expeditions.test.ts:49:        mission: null,
tests/simulation/expeditions.test.ts:65:  it('starts a round-trip expedition through a dedicated command', () => {
tests/simulation/expeditions.test.ts:66:    const state = withExpeditionFleet(createInitialGameState('expedition-start'));
tests/simulation/expeditions.test.ts:70:    const started = executeCommand(state, {
tests/simulation/expeditions.test.ts:73:      fleetId: 'player-expedition',
tests/simulation/expeditions.test.ts:78:    const fleet = started.value.fleets.find((candidate) => candidate.id === 'player-expedition');
tests/simulation/expeditions.test.ts:79:    expect(fleet).toMatchObject({
tests/simulation/expeditions.test.ts:81:      mission: { kind: 'expedition', targetPlanetId: target },
tests/simulation/expeditions.test.ts:89:      report: {
tests/simulation/expeditions.test.ts:91:        fleetId: 'player-expedition',
tests/simulation/expeditions.test.ts:100:  it('creates the same report for identical seed, state and command', () => {
tests/simulation/expeditions.test.ts:101:    const first = withExpeditionFleet(createInitialGameState('expedition-determinism'));
tests/simulation/expeditions.test.ts:102:    const second = withExpeditionFleet(createInitialGameState('expedition-determinism'));
tests/simulation/expeditions.test.ts:104:    const command = {
tests/simulation/expeditions.test.ts:107:      fleetId: 'player-expedition',
tests/simulation/expeditions.test.ts:110:    const firstResult = executeCommand(first, command);
tests/simulation/expeditions.test.ts:111:    const secondResult = executeCommand(second, command);
tests/simulation/expeditions.test.ts:126:  it('resolves once, returns survivors and records the event report', () => {
tests/simulation/expeditions.test.ts:127:    const state = withExpeditionFleet(createInitialGameState('expedition-resolve'));
tests/simulation/expeditions.test.ts:129:    const started = executeCommand(state, {
tests/simulation/expeditions.test.ts:132:      fleetId: 'player-expedition',
tests/simulation/expeditions.test.ts:142:    const advanced = executeCommand(started.value, {
tests/simulation/expeditions.test.ts:158:    const fleet = advanced.value.fleets.find((candidate) => candidate.id === 'player-expedition');
tests/simulation/expeditions.test.ts:159:    expect(fleet).toMatchObject({
tests/simulation/expeditions.test.ts:161:      mission: null,
tests/simulation/expeditions.test.ts:162:      location: { type: 'planet', planetId: event.payload.report.originPlanetId },
tests/simulation/expeditions.test.ts:165:    const secondAdvance = executeCommand(advanced.value, { type: 'ADVANCE_TIME', seconds: 1 });
tests/simulation/expeditions.test.ts:177:    const noScout = withExpeditionFleet(
tests/simulation/expeditions.test.ts:184:      executeCommand(noScout, {
tests/simulation/expeditions.test.ts:187:        fleetId: 'no-scout',
tests/simulation/expeditions.test.ts:192:    const state = withExpeditionFleet(createInitialGameState('expedition-reject-target'));
tests/simulation/expeditions.test.ts:195:      executeCommand(state, {
tests/simulation/expeditions.test.ts:198:        fleetId: 'player-expedition',
tests/simulation/expeditions.test.ts:222:      executeCommand(noFuel, {
tests/simulation/expeditions.test.ts:225:        fleetId: 'player-expedition',
tests/simulation/expeditions.test.ts:231:  it('requires the dedicated command instead of SEND_FLEET expedition', () => {
tests/simulation/expeditions.test.ts:232:    const state = withExpeditionFleet(createInitialGameState('expedition-guard'));
tests/simulation/expeditions.test.ts:234:      executeCommand(state, {
tests/simulation/expeditions.test.ts:237:        fleetId: 'player-expedition',
tests/simulation/expeditions.test.ts:238:        targetPlanetId: state.planets.find((planet) => planet.ownerEmpireId === 'aegis-bot')!.id,
tests/simulation/expeditions.test.ts:239:        mission: 'expedition',
tests/simulation/expeditions.test.ts:245:    const state = withExpeditionFleet(createInitialGameState('expedition-recall'));
tests/simulation/expeditions.test.ts:246:    const started = executeCommand(state, {
tests/simulation/expeditions.test.ts:249:      fleetId: 'player-expedition',
tests/simulation/expeditions.test.ts:254:    const recalled = executeCommand(started.value, {
tests/simulation/expeditions.test.ts:257:      fleetId: 'player-expedition',
tests/simulation/expeditions.test.ts:271:    expect(recalled.value.fleets.find((fleet) => fleet.id === 'player-expedition')).toMatchObject({
tests/simulation/expeditions.test.ts:277:    const state = withExpeditionFleet(createInitialGameState('expedition-save'));
tests/simulation/expeditions.test.ts:278:    const started = executeCommand(state, {
tests/simulation/expeditions.test.ts:281:      fleetId: 'player-expedition',
tests/simulation/expeditions.test.ts:297:    expect(parsed.value.state.fleets).toEqual(started.value.fleets);
tests/simulation/factionMechanicalCatalogRegistry.test.ts:5:  validateCompleteCatalogTargetManifest,
tests/simulation/factionMechanicalCatalogRegistry.test.ts:8:  getCommanderShipCatalog,
tests/simulation/factionMechanicalCatalogRegistry.test.ts:11:  validateFactionMechanicalCatalog,
tests/simulation/factionMechanicalCatalogRegistry.test.ts:29:    expect(createMechanicalId('commander', 'shared', 'annihilator')).toBe(
tests/simulation/factionMechanicalCatalogRegistry.test.ts:30:      'commander.shared.annihilator',
tests/simulation/factionMechanicalCatalogRegistry.test.ts:32:    expect(parseMechanicalId('building.aegis.command')).toEqual({
tests/simulation/factionMechanicalCatalogRegistry.test.ts:35:      slug: 'command',
tests/simulation/factionMechanicalCatalogRegistry.test.ts:37:    expect(parseMechanicalId('commander.shared.polias')).toEqual({
tests/simulation/factionMechanicalCatalogRegistry.test.ts:38:      kind: 'commander',
tests/simulation/factionMechanicalCatalogRegistry.test.ts:51:    expect(validateCompleteCatalogTargetManifest()).toEqual([]);
tests/simulation/factionMechanicalCatalogRegistry.test.ts:58:      commanderShips: 13,
tests/simulation/factionMechanicalCatalogRegistry.test.ts:60:    expect(COMPLETE_CATALOG_TARGETS.sharedCommanderShips).toBe(13);
tests/simulation/factionMechanicalCatalogRegistry.test.ts:61:    expect(getCommanderShipCatalog()).toHaveLength(13);
tests/simulation/factionMechanicalCatalogRegistry.test.ts:88:  it('returns one complete validated catalog contract for every faction', () => {
tests/simulation/factionMechanicalCatalogRegistry.test.ts:95:      expect(validateFactionMechanicalCatalog(catalog)).toEqual([]);
tests/simulation/factionMechanicalCatalogRegistry.test.ts:103:        commanderShips: 13,
tests/simulation/fleetDoctrine.test.ts:5:} from '../../src/simulation/combat/fleetDoctrine';
tests/simulation/fleetDoctrine.test.ts:8:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/fleetDoctrine.test.ts:10:describe('fleet doctrine', () => {
tests/simulation/fleetDoctrine.test.ts:25:  it('updates doctrine only for a stationed owned fleet', () => {
tests/simulation/fleetDoctrine.test.ts:26:    const initial = createInitialGameState('fleet-doctrine-command');
tests/simulation/fleetDoctrine.test.ts:31:      fleets: [
tests/simulation/fleetDoctrine.test.ts:32:        ...initial.fleets,
tests/simulation/fleetDoctrine.test.ts:34:          id: 'player-doctrine-fleet',
tests/simulation/fleetDoctrine.test.ts:45:          mission: null,
tests/simulation/fleetDoctrine.test.ts:49:    const result = executeCommand(state, {
tests/simulation/fleetDoctrine.test.ts:52:      fleetId: 'player-doctrine-fleet',
tests/simulation/fleetDoctrine.test.ts:59:      result.value.fleets.find((fleet) => fleet.id === 'player-doctrine-fleet'),
tests/simulation/flightLifecycle.test.ts:3:import { executeGameCommand } from '../../src/simulation/executeGameCommand';
tests/simulation/flightLifecycle.test.ts:4:import { estimateFlight } from '../../src/simulation/fleets/flightCalculations';
tests/simulation/flightLifecycle.test.ts:46:function createFleet(state: GameState): GameState {
tests/simulation/flightLifecycle.test.ts:48:  const result = executeGameCommand(state, {
tests/simulation/flightLifecycle.test.ts:59:describe('transport and deploy missions', () => {
tests/simulation/flightLifecycle.test.ts:61:    const initial = createFleet(prepareState('transport'));
tests/simulation/flightLifecycle.test.ts:63:    const fleet = initial.fleets[0]!;
tests/simulation/flightLifecycle.test.ts:64:    const estimate = estimateFlight(initial.galaxy, initial.planets, fleet, target!.id);
tests/simulation/flightLifecycle.test.ts:65:    const baseline = executeGameCommand(initial, {
tests/simulation/flightLifecycle.test.ts:73:    const sent = executeGameCommand(initial, {
tests/simulation/flightLifecycle.test.ts:76:      fleetId: fleet.id,
tests/simulation/flightLifecycle.test.ts:77:      targetPlanetId: target!.id,
tests/simulation/flightLifecycle.test.ts:78:      mission: 'transport',
tests/simulation/flightLifecycle.test.ts:83:    const arrived = executeGameCommand(sent.value, {
tests/simulation/flightLifecycle.test.ts:89:    expect(arrived.value.fleets[0]?.status).toBe('returning');
tests/simulation/flightLifecycle.test.ts:104:    const returned = executeGameCommand(arrived.value, {
tests/simulation/flightLifecycle.test.ts:110:      expect(returned.value.fleets[0]).toMatchObject({
tests/simulation/flightLifecycle.test.ts:118:  it('deploys a fleet and changes its home planet', () => {
tests/simulation/flightLifecycle.test.ts:119:    const initial = createFleet(prepareState('deploy'));
tests/simulation/flightLifecycle.test.ts:121:    const fleet = initial.fleets[0]!;
tests/simulation/flightLifecycle.test.ts:122:    const estimate = estimateFlight(initial.galaxy, initial.planets, fleet, target.id);
tests/simulation/flightLifecycle.test.ts:123:    const sent = executeGameCommand(initial, {
tests/simulation/flightLifecycle.test.ts:126:      fleetId: fleet.id,
tests/simulation/flightLifecycle.test.ts:127:      targetPlanetId: target.id,
tests/simulation/flightLifecycle.test.ts:128:      mission: 'deploy',
tests/simulation/flightLifecycle.test.ts:132:    const arrived = executeGameCommand(sent.value, {
tests/simulation/flightLifecycle.test.ts:138:      expect(arrived.value.fleets[0]).toMatchObject({
tests/simulation/flightLifecycle.test.ts:142:        mission: null,
tests/simulation/flightLifecycle.test.ts:148:    const initial = createFleet(prepareState('lost-target'));
tests/simulation/flightLifecycle.test.ts:150:    const fleet = initial.fleets[0]!;
tests/simulation/flightLifecycle.test.ts:151:    const estimate = estimateFlight(initial.galaxy, initial.planets, fleet, target.id);
tests/simulation/flightLifecycle.test.ts:152:    const sent = executeGameCommand(initial, {
tests/simulation/flightLifecycle.test.ts:155:      fleetId: fleet.id,
tests/simulation/flightLifecycle.test.ts:156:      targetPlanetId: target.id,
tests/simulation/flightLifecycle.test.ts:157:      mission: 'deploy',
tests/simulation/flightLifecycle.test.ts:167:    const arrived = executeGameCommand(lost, {
tests/simulation/flightLifecycle.test.ts:173:      expect(arrived.value.fleets[0]?.status).toBe('returning');
tests/simulation/flightLifecycle.test.ts:180:  it('recalls an outbound fleet using elapsed travel time', () => {
tests/simulation/flightLifecycle.test.ts:181:    const initial = createFleet(prepareState('recall'));
tests/simulation/flightLifecycle.test.ts:183:    const fleet = initial.fleets[0]!;
tests/simulation/flightLifecycle.test.ts:184:    const estimate = estimateFlight(initial.galaxy, initial.planets, fleet, target.id);
tests/simulation/flightLifecycle.test.ts:185:    const sent = executeGameCommand(initial, {
tests/simulation/flightLifecycle.test.ts:188:      fleetId: fleet.id,
tests/simulation/flightLifecycle.test.ts:189:      targetPlanetId: target.id,
tests/simulation/flightLifecycle.test.ts:190:      mission: 'transport',
tests/simulation/flightLifecycle.test.ts:195:    const halfway = executeGameCommand(sent.value, {
tests/simulation/flightLifecycle.test.ts:201:    const recalled = executeGameCommand(halfway.value, {
tests/simulation/flightLifecycle.test.ts:204:      fleetId: fleet.id,
tests/simulation/galaxyIntelligenceView.test.ts:3:  createGalaxyIntelligenceView,
tests/simulation/galaxyIntelligenceView.test.ts:4:  filterGalaxyIntelligence,
tests/simulation/galaxyIntelligenceView.test.ts:5:  summarizeGalaxyIntelligence,
tests/simulation/galaxyIntelligenceView.test.ts:6:} from '../../src/simulation/galaxy/intelligenceView';
tests/simulation/galaxyIntelligenceView.test.ts:9:describe('galaxy intelligence view', () => {
tests/simulation/galaxyIntelligenceView.test.ts:12:    const view = createGalaxyIntelligenceView(state, 'player');
tests/simulation/galaxyIntelligenceView.test.ts:28:      fleets: null,
tests/simulation/galaxyIntelligenceView.test.ts:32:  it('uses only the stored intelligence snapshot for foreign details', () => {
tests/simulation/galaxyIntelligenceView.test.ts:37:      intelligence: base.intelligence.map((entry) =>
tests/simulation/galaxyIntelligenceView.test.ts:45:                  targetPlanetId: foreign.id,
tests/simulation/galaxyIntelligenceView.test.ts:62:                    buildings: { 'building.aegis.command': 2 },
tests/simulation/galaxyIntelligenceView.test.ts:86:    const contact = createGalaxyIntelligenceView(state, 'player').find(
tests/simulation/galaxyIntelligenceView.test.ts:94:      buildings: { 'building.aegis.command': 2 },
tests/simulation/galaxyIntelligenceView.test.ts:104:      intelligence: base.intelligence.map((entry) =>
tests/simulation/galaxyIntelligenceView.test.ts:112:                  targetPlanetId: foreign.id,
tests/simulation/galaxyIntelligenceView.test.ts:130:      createGalaxyIntelligenceView(state, 'player').find(
tests/simulation/galaxyIntelligenceView.test.ts:143:    const view = createGalaxyIntelligenceView(state, 'player');
tests/simulation/galaxyIntelligenceView.test.ts:145:    expect(filterGalaxyIntelligence(view, { owner: 'self' })).toEqual([own]);
tests/simulation/galaxyIntelligenceView.test.ts:147:      filterGalaxyIntelligence(view, {
tests/simulation/galaxyIntelligenceView.test.ts:154:      filterGalaxyIntelligence(view, { minimumSize: own.size + 100 }),
tests/simulation/galaxyIntelligenceView.test.ts:157:      filterGalaxyIntelligence(view, { owner: 'foreign' }).every((planet) =>
tests/simulation/galaxyIntelligenceView.test.ts:165:    const view = createGalaxyIntelligenceView(state, 'player');
tests/simulation/galaxyIntelligenceView.test.ts:166:    const summary = summarizeGalaxyIntelligence(view);
tests/simulation/intelligence.test.ts:3:import { executeGameCommand } from '../../src/simulation/executeGameCommand';
tests/simulation/intelligence.test.ts:4:import { estimateFlight } from '../../src/simulation/fleets/flightCalculations';
tests/simulation/intelligence.test.ts:5:import { getCurrentObservations } from '../../src/simulation/intelligence/intelligenceState';
tests/simulation/intelligence.test.ts:6:import { resolveScoutArrival } from '../../src/simulation/intelligence/resolveScout';
tests/simulation/intelligence.test.ts:51:function createScoutFleet(state: GameState): GameState {
tests/simulation/intelligence.test.ts:53:  const created = executeGameCommand(state, {
tests/simulation/intelligence.test.ts:64:describe('intelligence and counter-intelligence', () => {
tests/simulation/intelligence.test.ts:66:    const initial = createScoutFleet(prepareScoutState('scout-mission'));
tests/simulation/intelligence.test.ts:67:    const fleet = initial.fleets[0]!;
tests/simulation/intelligence.test.ts:71:    const estimate = estimateFlight(initial.galaxy, initial.planets, fleet, target.id);
tests/simulation/intelligence.test.ts:72:    const sent = executeGameCommand(initial, {
tests/simulation/intelligence.test.ts:75:      fleetId: fleet.id,
tests/simulation/intelligence.test.ts:76:      targetPlanetId: target.id,
tests/simulation/intelligence.test.ts:77:      mission: 'scout',
tests/simulation/intelligence.test.ts:82:    const arrived = executeGameCommand(sent.value, {
tests/simulation/intelligence.test.ts:89:    const playerIntel = arrived.value.intelligence.find(
tests/simulation/intelligence.test.ts:94:      targetPlanetId: target.id,
tests/simulation/intelligence.test.ts:99:    expect(arrived.value.fleets[0]?.status).toBe('returning');
tests/simulation/intelligence.test.ts:105:    const created = executeGameCommand(state, {
tests/simulation/intelligence.test.ts:116:      executeGameCommand(created.value, {
tests/simulation/intelligence.test.ts:119:        fleetId: created.value.fleets[0]!.id,
tests/simulation/intelligence.test.ts:120:        targetPlanetId: target.id,
tests/simulation/intelligence.test.ts:121:        mission: 'scout',
tests/simulation/intelligence.test.ts:127:    const state = createScoutFleet(prepareScoutState('counter-intel'));
tests/simulation/intelligence.test.ts:128:    const fleet = state.fleets[0]!;
tests/simulation/intelligence.test.ts:133:      const result = resolveScoutArrival(state, fleet, target, sequence);
tests/simulation/intelligence.test.ts:134:      const observation = result.intelligence
tests/simulation/intelligence.test.ts:146:      detected.intelligence.find(
tests/simulation/intelligence.test.ts:153:    const state = createScoutFleet(prepareScoutState('intel-staleness'));
tests/simulation/intelligence.test.ts:154:    const fleet = state.fleets[0]!;
tests/simulation/intelligence.test.ts:156:    const observed = resolveScoutArrival(state, fleet, target, 7);
tests/simulation/intelligence.test.ts:157:    const intelligence = observed.intelligence.find(
tests/simulation/intelligence.test.ts:160:    const expiresAt = intelligence.observations[0]!.expiresAt;
tests/simulation/intelligence.test.ts:162:    expect(getCurrentObservations(intelligence, expiresAt - 1)).toHaveLength(1);
tests/simulation/intelligence.test.ts:163:    expect(getCurrentObservations(intelligence, expiresAt)).toHaveLength(0);
tests/simulation/intelligence.test.ts:164:    expect(intelligence.observations).toHaveLength(1);
tests/simulation/logisticsRoutes.test.ts:3:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/logisticsRoutes.test.ts:44:    const created = executeCommand(state, {
tests/simulation/logisticsRoutes.test.ts:48:      targetPlanetId: target.id,
tests/simulation/logisticsRoutes.test.ts:58:    const advanced = executeCommand(created.value, { type: 'ADVANCE_TIME', seconds: 1_200 });
tests/simulation/logisticsRoutes.test.ts:96:    const created = executeCommand(state, {
tests/simulation/logisticsRoutes.test.ts:100:      targetPlanetId: target.id,
tests/simulation/logisticsRoutes.test.ts:109:    const advanced = executeCommand(created.value, { type: 'ADVANCE_TIME', seconds: 600 });
tests/simulation/logisticsRoutes.test.ts:117:  it('pauses and deletes routes through normal commands', () => {
tests/simulation/logisticsRoutes.test.ts:119:    const created = executeCommand(state, {
tests/simulation/logisticsRoutes.test.ts:123:      targetPlanetId: state.planets[1]!.id,
tests/simulation/logisticsRoutes.test.ts:133:    const paused = executeCommand(created.value, {
tests/simulation/logisticsRoutes.test.ts:142:    const deleted = executeCommand(paused.value, {
tests/simulation/market.test.ts:4:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/market.test.ts:18:    const result = executeCommand(state, {
tests/simulation/market.test.ts:53:    const traded = executeCommand(state, {
tests/simulation/market.test.ts:83:      executeCommand(state, {
tests/simulation/neutralPirateBases.test.ts:8:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/neutralPirateBases.test.ts:18:function addPlayerFleet(
tests/simulation/neutralPirateBases.test.ts:44:    fleets: [
tests/simulation/neutralPirateBases.test.ts:45:      ...state.fleets,
tests/simulation/neutralPirateBases.test.ts:56:        mission: null,
tests/simulation/neutralPirateBases.test.ts:84:    expect(state.intelligence.some((entry) => entry.empireId === PIRATE_EMPIRE_ID)).toBe(false);
tests/simulation/neutralPirateBases.test.ts:85:    expect(state.fleets).toEqual([]);
tests/simulation/neutralPirateBases.test.ts:93:  it('reveals a pirate owner through the normal scout mission', () => {
tests/simulation/neutralPirateBases.test.ts:94:    const state = addPlayerFleet(
tests/simulation/neutralPirateBases.test.ts:100:    const sent = executeCommand(state, {
tests/simulation/neutralPirateBases.test.ts:103:      fleetId: 'player-pirate-scout',
tests/simulation/neutralPirateBases.test.ts:104:      targetPlanetId: target.id,
tests/simulation/neutralPirateBases.test.ts:105:      mission: 'scout',
tests/simulation/neutralPirateBases.test.ts:112:        event.payload.fleetId === 'player-pirate-scout',
tests/simulation/neutralPirateBases.test.ts:116:    const advanced = executeCommand(sent.value, {
tests/simulation/neutralPirateBases.test.ts:122:    const observation = advanced.value.intelligence
tests/simulation/neutralPirateBases.test.ts:124:      ?.observations.find((entry) => entry.targetPlanetId === target.id);
tests/simulation/neutralPirateBases.test.ts:129:    const state = addPlayerFleet(
tests/simulation/neutralPirateBases.test.ts:135:    const sent = executeCommand(state, {
tests/simulation/neutralPirateBases.test.ts:138:      fleetId: 'player-pirate-strike',
tests/simulation/neutralPirateBases.test.ts:139:      targetPlanetId: target.id,
tests/simulation/neutralPirateBases.test.ts:140:      mission: 'attack',
tests/simulation/neutralPirateBases.test.ts:144:      expect(sent.value.fleets.find((fleet) => fleet.id === 'player-pirate-strike')).toMatchObject({
tests/simulation/neutralPirateBases.test.ts:146:        mission: { kind: 'attack', targetPlanetId: target.id },
tests/simulation/planetSpecialization.test.ts:3:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/planetSpecialization.test.ts:15:    const specialization = executeCommand(state, {
tests/simulation/planetSpecialization.test.ts:28:    const template = executeCommand(specialization.value, {
tests/simulation/planetSpecialization.test.ts:45:    const queued = executeCommand(state, {
tests/simulation/planetSpecialization.test.ts:55:      executeCommand(queued.value, {
tests/simulation/planetSpecialization.test.ts:66:    const specialized = executeCommand(state, {
tests/simulation/planetSpecialization.test.ts:75:    const queued = executeCommand(specialized.value, {
tests/simulation/planetaryDefense.test.ts:3:import { resolveAttackMission } from '../../src/simulation/combat/resolveAttackMission';
tests/simulation/planetaryDefense.test.ts:10:import type { FleetState } from '../../src/simulation/fleets/types';
tests/simulation/planetaryDefense.test.ts:12:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/planetaryDefense.test.ts:62:  const result = executeCommand(state, {
tests/simulation/planetaryDefense.test.ts:87:    const result = executeCommand(limited, {
tests/simulation/planetaryDefense.test.ts:129:    const queued = executeCommand(state, {
tests/simulation/planetaryDefense.test.ts:154:  it('moves battle losses into the damaged pool and records them in the report', () => {
tests/simulation/planetaryDefense.test.ts:162:    const attacker: FleetState = {
tests/simulation/planetaryDefense.test.ts:172:      mission: { kind: 'attack', targetPlanetId: fortified.id },
tests/simulation/planetaryDefense.test.ts:177:      fleets: [attacker],
tests/simulation/planetaryDefense.test.ts:179:    const result = resolveAttackMission(state, attacker, fortified, 17);
tests/simulation/planetaryDefense.test.ts:181:    expect(result.report.defensesRecovered).toEqual(updated.defense.damaged);
tests/simulation/pveBalanceAndReports.test.ts:3:import type { FleetState } from '../../src/simulation/fleets/types';
tests/simulation/pveBalanceAndReports.test.ts:16:  createUnifiedMissionReports,
tests/simulation/pveBalanceAndReports.test.ts:17:  filterMissionReports,
tests/simulation/pveBalanceAndReports.test.ts:18:  summarizeMissionReports,
tests/simulation/pveBalanceAndReports.test.ts:19:} from '../../src/simulation/reports/missionReports';
tests/simulation/pveBalanceAndReports.test.ts:60:    report: {
tests/simulation/pveBalanceAndReports.test.ts:64:      targetPlanetId: options.targetId ?? 'pirate-base-test',
tests/simulation/pveBalanceAndReports.test.ts:89:    report: {
tests/simulation/pveBalanceAndReports.test.ts:92:      fleetId: 'fleet-expedition',
tests/simulation/pveBalanceAndReports.test.ts:113:    report: {
tests/simulation/pveBalanceAndReports.test.ts:114:      id: `space-object-report-${sequence}`,
tests/simulation/pveBalanceAndReports.test.ts:116:      fleetId: 'fleet-mining',
tests/simulation/pveBalanceAndReports.test.ts:142:function totalPlanetAndFleetResource(
tests/simulation/pveBalanceAndReports.test.ts:144:  fleet: FleetState,
tests/simulation/pveBalanceAndReports.test.ts:147:  return planet.economy.resources[resourceId].amount + fleet.cargo[resourceId];
tests/simulation/pveBalanceAndReports.test.ts:197:    const fleet: FleetState = {
tests/simulation/pveBalanceAndReports.test.ts:198:      id: 'test-plunder-fleet',
tests/simulation/pveBalanceAndReports.test.ts:207:      mission: null,
tests/simulation/pveBalanceAndReports.test.ts:210:      metal: totalPlanetAndFleetResource(planet, fleet, 'metal'),
tests/simulation/pveBalanceAndReports.test.ts:211:      crystal: totalPlanetAndFleetResource(planet, fleet, 'crystal'),
tests/simulation/pveBalanceAndReports.test.ts:212:      gas: totalPlanetAndFleetResource(planet, fleet, 'gas'),
tests/simulation/pveBalanceAndReports.test.ts:216:      fleet,
tests/simulation/pveBalanceAndReports.test.ts:221:    expect(totalPlanetAndFleetResource(result.planet, result.fleet, 'metal')).toBe(before.metal);
tests/simulation/pveBalanceAndReports.test.ts:222:    expect(totalPlanetAndFleetResource(result.planet, result.fleet, 'crystal')).toBe(before.crystal);
tests/simulation/pveBalanceAndReports.test.ts:223:    expect(totalPlanetAndFleetResource(result.planet, result.fleet, 'gas')).toBe(before.gas);
tests/simulation/pveBalanceAndReports.test.ts:227:describe('unified mission reports', () => {
tests/simulation/pveBalanceAndReports.test.ts:229:    const base = createInitialGameState('unified-reports');
tests/simulation/pveBalanceAndReports.test.ts:251:    const reports = createUnifiedMissionReports(state);
tests/simulation/pveBalanceAndReports.test.ts:252:    expect(reports).toHaveLength(4);
tests/simulation/pveBalanceAndReports.test.ts:253:    expect(reports.map((report) => report.kind)).toEqual([
tests/simulation/pveBalanceAndReports.test.ts:259:    expect(new Set(reports.map((report) => report.id)).size).toBe(reports.length);
tests/simulation/pveBalanceAndReports.test.ts:260:    expect(filterMissionReports(reports, { mode: 'pve' })).toHaveLength(3);
tests/simulation/pveBalanceAndReports.test.ts:261:    expect(filterMissionReports(reports, { search: 'аномалия' })).toHaveLength(1);
tests/simulation/pveBalanceAndReports.test.ts:265:    const base = createInitialGameState('unified-report-summary');
tests/simulation/pveBalanceAndReports.test.ts:277:    const reports = createUnifiedMissionReports(state);
tests/simulation/pveBalanceAndReports.test.ts:278:    const summary = summarizeMissionReports(reports);
tests/simulation/reducer.test.ts:4:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/reducer.test.ts:5:import { replayCommands } from '../../src/simulation/replay';
tests/simulation/reducer.test.ts:6:import type { GameCommand, GameState } from '../../src/simulation/types';
tests/simulation/reducer.test.ts:8:function expectSuccess(result: ReturnType<typeof executeCommand>): GameState {
tests/simulation/reducer.test.ts:21:      executeCommand(state, {
tests/simulation/reducer.test.ts:28:      executeCommand(state, {
tests/simulation/reducer.test.ts:35:      executeCommand(state, {
tests/simulation/reducer.test.ts:53:      executeCommand(state, {
tests/simulation/reducer.test.ts:60:      executeCommand(state, {
tests/simulation/reducer.test.ts:66:    state = expectSuccess(executeCommand(state, { type: 'ADVANCE_TIME', seconds: 10 }));
tests/simulation/reducer.test.ts:76:    state = expectSuccess(executeCommand(state, { type: 'ADVANCE_TIME', seconds: 10 }));
tests/simulation/reducer.test.ts:78:    const result = executeCommand(state, {
tests/simulation/reducer.test.ts:87:  it('replays the same commands to the same checksum', () => {
tests/simulation/reducer.test.ts:88:    const commands: readonly GameCommand[] = [
tests/simulation/reducer.test.ts:97:    const first = replayCommands('replay-seed', commands);
tests/simulation/reducer.test.ts:98:    const second = replayCommands('replay-seed', commands);
tests/simulation/researchCatalog.test.ts:3:import { validateResearchCatalog } from '../../src/simulation/research/validateResearchCatalog';
tests/simulation/researchCatalog.test.ts:8:    expect(validateResearchCatalog(AEGIS_RESEARCH_CATALOG)).toEqual([]);
tests/simulation/researchCatalog.test.ts:17:  it('reports duplicate, unknown and cyclic dependencies', () => {
tests/simulation/researchCatalog.test.ts:40:    const issues = validateResearchCatalog([left, right, missing, left]);
tests/simulation/researchQueue.test.ts:5:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/researchQueue.test.ts:76:    const queued = executeCommand(initial, {
tests/simulation/researchQueue.test.ts:102:    const completed = executeCommand(queued.value, {
tests/simulation/researchQueue.test.ts:122:    const later = executeCommand(completed.value, { type: 'ADVANCE_TIME', seconds: 10_000 });
tests/simulation/researchQueue.test.ts:136:    const queued = executeCommand(initial, {
tests/simulation/researchQueue.test.ts:153:    const cancelled = executeCommand(queued.value, {
tests/simulation/researchQueue.test.ts:182:    const baseQueue = executeCommand(base, {
tests/simulation/researchQueue.test.ts:188:    const boostedQueue = executeCommand(boosted, {
tests/simulation/researchQueue.test.ts:203:    const refreshed = executeCommand(boosted, { type: 'ADVANCE_TIME', seconds: 0 });
tests/simulation/shipUpgrades.test.ts:4:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/shipUpgrades.test.ts:52:    const queued = executeCommand(prepared.state, {
tests/simulation/shipUpgrades.test.ts:71:    const completed = executeCommand(queued.value, {
tests/simulation/shipUpgrades.test.ts:92:    const queued = executeCommand(prepared.state, {
tests/simulation/shipUpgrades.test.ts:105:    const canceled = executeCommand(queued.value, {
tests/simulation/spaceObjects.test.ts:7:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/spaceObjects.test.ts:15:function withSpaceObjectFleet(
tests/simulation/spaceObjects.test.ts:18:  fleetId = 'player-space-object',
tests/simulation/spaceObjects.test.ts:39:    fleets: [
tests/simulation/spaceObjects.test.ts:40:      ...state.fleets,
tests/simulation/spaceObjects.test.ts:42:        id: fleetId,
tests/simulation/spaceObjects.test.ts:51:        mission: null,
tests/simulation/spaceObjects.test.ts:57:function startMission(state: GameState, objectId: string, fleetId = 'player-space-object') {
tests/simulation/spaceObjects.test.ts:58:  return executeCommand(state, {
tests/simulation/spaceObjects.test.ts:61:    fleetId,
tests/simulation/spaceObjects.test.ts:84:    const state = withSpaceObjectFleet(base, 'anomaly', 'wrong-specialist');
tests/simulation/spaceObjects.test.ts:85:    expect(startMission(state, asteroid.id, 'wrong-specialist')).toMatchObject({
tests/simulation/spaceObjects.test.ts:94:    const state = withSpaceObjectFleet(base, asteroid.kind);
tests/simulation/spaceObjects.test.ts:95:    const started = startMission(state, asteroid.id);
tests/simulation/spaceObjects.test.ts:103:    const advanced = executeCommand(started.value, {
tests/simulation/spaceObjects.test.ts:118:    expect(advanced.value.fleets.find((fleet) => fleet.id === 'player-space-object')).toMatchObject({
tests/simulation/spaceObjects.test.ts:120:      mission: null,
tests/simulation/spaceObjects.test.ts:129:    const state = withSpaceObjectFleet(base, anomaly.kind);
tests/simulation/spaceObjects.test.ts:130:    const started = startMission(state, anomaly.id);
tests/simulation/spaceObjects.test.ts:137:    const advanced = executeCommand(started.value, {
tests/simulation/spaceObjects.test.ts:163:    const state = withSpaceObjectFleet(controlled, asteroid.kind);
tests/simulation/spaceObjects.test.ts:164:    const started = startMission(state, asteroid.id);
tests/simulation/spaceObjects.test.ts:171:    const advanced = executeCommand(started.value, {
tests/simulation/spaceObjects.test.ts:186:    const state = withSpaceObjectFleet(base, gas.kind);
tests/simulation/spaceObjects.test.ts:187:    const started = startMission(state, gas.id);
tests/simulation/spaceObjects.test.ts:190:    const recalled = executeCommand(started.value, {
tests/simulation/spaceObjects.test.ts:193:      fleetId: 'player-space-object',
tests/simulation/spaceObjects.test.ts:207:  it('round-trips missions and additively migrates old schema-v12 states', () => {
tests/simulation/spaceObjects.test.ts:210:    const state = withSpaceObjectFleet(base, gas.kind);
tests/simulation/spaceObjects.test.ts:211:    const started = startMission(state, gas.id);
tests/simulation/spaceObjects.test.ts:219:      expect(parsed.value.state.fleets).toEqual(started.value.fleets);
tests/simulation/stateHistoryRetention.test.ts:5:  appendCommandHistory,
tests/simulation/stateHistoryRetention.test.ts:10:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/stateHistoryRetention.test.ts:11:import type { CommandLogEntry, ExecutedGameEvent, GameState } from '../../src/simulation/types';
tests/simulation/stateHistoryRetention.test.ts:16:  for (let index = 0; index < STATE_HISTORY_LIMITS.commands + 64; index += 1) {
tests/simulation/stateHistoryRetention.test.ts:17:    const result = executeCommand(state, { type: 'ADVANCE_TIME', seconds: 300 });
tests/simulation/stateHistoryRetention.test.ts:37:  it('keeps a monotonic command index while retaining only the newest window', () => {
tests/simulation/stateHistoryRetention.test.ts:38:    let log: readonly CommandLogEntry[] = [];
tests/simulation/stateHistoryRetention.test.ts:39:    for (let index = 0; index < STATE_HISTORY_LIMITS.commands + 25; index += 1) {
tests/simulation/stateHistoryRetention.test.ts:40:      log = appendCommandHistory(log, { type: 'ADVANCE_TIME', seconds: index });
tests/simulation/stateHistoryRetention.test.ts:42:    expect(log).toHaveLength(STATE_HISTORY_LIMITS.commands);
tests/simulation/stateHistoryRetention.test.ts:44:    expect(log.at(-1)?.index).toBe(STATE_HISTORY_LIMITS.commands + 24);
tests/simulation/stateHistoryRetention.test.ts:60:    expect(first.clock.elapsedSeconds).toBe((STATE_HISTORY_LIMITS.commands + 64) * 300);
tests/simulation/stateHistoryRetention.test.ts:61:    expect(first.commandLog).toHaveLength(STATE_HISTORY_LIMITS.commands);
tests/simulation/stateHistoryRetention.test.ts:71:      commandLog: Array.from(
tests/simulation/stateHistoryRetention.test.ts:72:        { length: STATE_HISTORY_LIMITS.commands + 3 },
tests/simulation/stateHistoryRetention.test.ts:73:        (_, index) => ({ index, command: { type: 'ADVANCE_TIME' as const, seconds: 0 } }),
tests/simulation/stateHistoryRetention.test.ts:96:      intelligence: current.intelligence.map((entry) => ({
tests/simulation/stateHistoryRetention.test.ts:99:          { length: STATE_HISTORY_LIMITS.intelligenceObservationsPerEmpire + 3 },
tests/simulation/stateHistoryRetention.test.ts:103:            targetPlanetId: current.planets[0]!.id,
tests/simulation/stateHistoryRetention.test.ts:117:          { length: STATE_HISTORY_LIMITS.intelligenceAlertsPerEmpire + 3 },
tests/simulation/stateHistoryRetention.test.ts:122:            targetPlanetId: current.planets[0]!.id,
tests/simulation/stateHistoryRetention.test.ts:130:    expect(compacted.commandLog).toHaveLength(STATE_HISTORY_LIMITS.commands);
tests/simulation/stateHistoryRetention.test.ts:133:    expect(compacted.intelligence.every(
tests/simulation/stateHistoryRetention.test.ts:134:      (entry) => entry.observations.length === STATE_HISTORY_LIMITS.intelligenceObservationsPerEmpire,
tests/simulation/stateHistoryRetention.test.ts:136:    expect(compacted.intelligence.every(
tests/simulation/stateHistoryRetention.test.ts:137:      (entry) => entry.alerts.length === STATE_HISTORY_LIMITS.intelligenceAlertsPerEmpire,
tests/simulation/stateHistoryRetention.test.ts:146:      commandLog: Array.from(
tests/simulation/stateHistoryRetention.test.ts:147:        { length: STATE_HISTORY_LIMITS.commands + 5 },
tests/simulation/stateHistoryRetention.test.ts:148:        (_, index) => ({ index, command: { type: 'ADVANCE_TIME' as const, seconds: 0 } }),
tests/simulation/stateHistoryRetention.test.ts:159:    expect(parsed.value.state.commandLog).toHaveLength(STATE_HISTORY_LIMITS.commands);
tests/simulation/stateHistoryRetention.test.ts:160:    expect(parsed.value.state.commandLog[0]?.index).toBe(5);
tests/simulation/synodNativeCatalog.test.ts:6:import { sendFleet } from '../../src/simulation/fleets/flightCommands';
tests/simulation/synodNativeCatalog.test.ts:7:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/synodNativeCatalog.test.ts:108:    const building = executeCommand(initial, {
tests/simulation/synodNativeCatalog.test.ts:118:    expect(executeCommand(ready, {
tests/simulation/synodNativeCatalog.test.ts:122:      technologyId: 'technology.synod.chorus-command',
tests/simulation/synodNativeCatalog.test.ts:124:    expect(executeCommand(ready, {
tests/simulation/synodNativeCatalog.test.ts:131:    expect(executeCommand(ready, {
tests/simulation/synodNativeCatalog.test.ts:147:  it('accepts scout, recycle and colonize missions with Synod hulls', () => {
tests/simulation/synodNativeCatalog.test.ts:148:    const base = addSynodInfrastructure(createInitialGameState('synod-missions', 'synod'), 'player');
tests/simulation/synodNativeCatalog.test.ts:155:    const fleetState = (id: string, ships: Readonly<Record<string, number>>): GameState => ({
tests/simulation/synodNativeCatalog.test.ts:157:      fleets: [{
tests/simulation/synodNativeCatalog.test.ts:167:        mission: null,
tests/simulation/synodNativeCatalog.test.ts:171:    expect(sendFleet(fleetState('synod-scout', { 'ship.synod.whisper': 1 }), {
tests/simulation/synodNativeCatalog.test.ts:174:      fleetId: 'synod-scout',
tests/simulation/synodNativeCatalog.test.ts:175:      targetPlanetId: foreign.id,
tests/simulation/synodNativeCatalog.test.ts:176:      mission: 'scout',
tests/simulation/synodNativeCatalog.test.ts:180:      ...fleetState('synod-recycler', { 'ship.synod.salvage-mind': 1 }),
tests/simulation/synodNativeCatalog.test.ts:189:    expect(sendFleet(recycleState, {
tests/simulation/synodNativeCatalog.test.ts:192:      fleetId: 'synod-recycler',
tests/simulation/synodNativeCatalog.test.ts:193:      targetPlanetId: foreign.id,
tests/simulation/synodNativeCatalog.test.ts:194:      mission: 'recycle',
tests/simulation/synodNativeCatalog.test.ts:197:    expect(sendFleet(fleetState('synod-colonizer', { 'ship.synod.seed-ark': 1 }), {
tests/simulation/synodNativeCatalog.test.ts:200:      fleetId: 'synod-colonizer',
tests/simulation/synodNativeCatalog.test.ts:201:      targetPlanetId: freePlanet.id,
tests/simulation/synodNativeCatalog.test.ts:202:      mission: 'colonize',
tests/simulation/synodNativeCatalog.test.ts:206:  it('resolves Synod combat profiles and produces a valid bot command', () => {
tests/simulation/synodNativeCatalog.test.ts:212:    const state = fillResources(createInitialGameState('synod-bot-command'), 'synod-bot');
tests/simulation/synodNativeCatalog.test.ts:214:    expect(plan.command).not.toBeNull();
tests/simulation/synodNativeCatalog.test.ts:215:    if (plan.command === null) return;
tests/simulation/synodNativeCatalog.test.ts:216:    expect(executeCommand(state, plan.command).ok).toBe(true);
tests/simulation/synodNativeCatalog.test.ts:217:    if (plan.command.type === 'QUEUE_BUILDING') {
tests/simulation/synodNativeCatalog.test.ts:218:      expect(plan.command.buildingId.startsWith('building.synod.')).toBe(true);
tests/simulation/synodNativeCatalog.test.ts:253:      fleets: [{
tests/simulation/synodNativeCatalog.test.ts:263:        mission: null,
tests/simulation/synodNativeCatalog.test.ts:278:    expect(first.value.state.fleets[0]?.ships).toEqual({ 'ship.synod.whisper': 1 });
tests/simulation/unitCatalog.test.ts:7:import { validateUnitCatalog } from '../../src/simulation/units/validateUnitCatalog';
tests/simulation/unitCatalog.test.ts:10:  it('validates the complete Aegis ship and defense set', () => {
tests/simulation/unitCatalog.test.ts:12:      validateUnitCatalog(
tests/simulation/unitCatalog.test.ts:24:  it('initializes empty managed inventories, fleets, intelligence, debris, logistics and market', () => {
tests/simulation/unitCatalog.test.ts:30:    expect(state.fleets).toEqual([]);
tests/simulation/unitCatalog.test.ts:41:    expect(state.intelligence).toHaveLength(state.empires.length);
tests/simulation/unitCatalog.test.ts:42:    expect(state.intelligence.every((entry) => entry.observations.length === 0)).toBe(true);
tests/simulation/unitProduction.test.ts:5:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/unitProduction.test.ts:64:    const queued = executeCommand(initial, {
tests/simulation/unitProduction.test.ts:81:    const completed = executeCommand(queued.value, {
tests/simulation/unitProduction.test.ts:92:    const later = executeCommand(completed.value, { type: 'ADVANCE_TIME', seconds: 10_000 });
tests/simulation/unitProduction.test.ts:102:    const ship = executeCommand(initial, {
tests/simulation/unitProduction.test.ts:113:    const defense = executeCommand(ship.value, {
tests/simulation/unitProduction.test.ts:130:    const queued = executeCommand(initial, {
tests/simulation/unitProduction.test.ts:145:    const cancelled = executeCommand(queued.value, {
tests/simulation/unitProduction.test.ts:166:    const result = executeCommand(initial, {
tests/simulation/veyraNativeCatalog.test.ts:6:import { sendFleet } from '../../src/simulation/fleets/flightCommands';
tests/simulation/veyraNativeCatalog.test.ts:7:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/veyraNativeCatalog.test.ts:108:    const building = executeCommand(initial, {
tests/simulation/veyraNativeCatalog.test.ts:118:    expect(executeCommand(ready, {
tests/simulation/veyraNativeCatalog.test.ts:124:    expect(executeCommand(ready, {
tests/simulation/veyraNativeCatalog.test.ts:131:    expect(executeCommand(ready, {
tests/simulation/veyraNativeCatalog.test.ts:147:  it('accepts scout, recycle and colonize missions with Veyra hulls', () => {
tests/simulation/veyraNativeCatalog.test.ts:148:    const base = addVeyraInfrastructure(createInitialGameState('veyra-missions', 'veyra'), 'player');
tests/simulation/veyraNativeCatalog.test.ts:155:    const fleetState = (id: string, ships: Readonly<Record<string, number>>): GameState => ({
tests/simulation/veyraNativeCatalog.test.ts:157:      fleets: [{
tests/simulation/veyraNativeCatalog.test.ts:167:        mission: null,
tests/simulation/veyraNativeCatalog.test.ts:171:    expect(sendFleet(fleetState('veyra-scout', { 'ship.veyra.wisp': 1 }), {
tests/simulation/veyraNativeCatalog.test.ts:174:      fleetId: 'veyra-scout',
tests/simulation/veyraNativeCatalog.test.ts:175:      targetPlanetId: foreign.id,
tests/simulation/veyraNativeCatalog.test.ts:176:      mission: 'scout',
tests/simulation/veyraNativeCatalog.test.ts:180:      ...fleetState('veyra-recycler', { 'ship.veyra.devourer': 1 }),
tests/simulation/veyraNativeCatalog.test.ts:189:    expect(sendFleet(recycleState, {
tests/simulation/veyraNativeCatalog.test.ts:192:      fleetId: 'veyra-recycler',
tests/simulation/veyraNativeCatalog.test.ts:193:      targetPlanetId: foreign.id,
tests/simulation/veyraNativeCatalog.test.ts:194:      mission: 'recycle',
tests/simulation/veyraNativeCatalog.test.ts:197:    expect(sendFleet(fleetState('veyra-colonizer', { 'ship.veyra.brood-ark': 1 }), {
tests/simulation/veyraNativeCatalog.test.ts:200:      fleetId: 'veyra-colonizer',
tests/simulation/veyraNativeCatalog.test.ts:201:      targetPlanetId: freePlanet.id,
tests/simulation/veyraNativeCatalog.test.ts:202:      mission: 'colonize',
tests/simulation/veyraNativeCatalog.test.ts:206:  it('resolves Veyra combat profiles and produces a valid bot command', () => {
tests/simulation/veyraNativeCatalog.test.ts:212:    const state = fillResources(createInitialGameState('veyra-bot-command'), 'veyra-bot');
tests/simulation/veyraNativeCatalog.test.ts:214:    expect(plan.command).not.toBeNull();
tests/simulation/veyraNativeCatalog.test.ts:215:    if (plan.command === null) return;
tests/simulation/veyraNativeCatalog.test.ts:216:    expect(executeCommand(state, plan.command).ok).toBe(true);
tests/simulation/veyraNativeCatalog.test.ts:217:    if (plan.command.type === 'QUEUE_BUILDING') {
tests/simulation/veyraNativeCatalog.test.ts:218:      expect(plan.command.buildingId.startsWith('building.veyra.')).toBe(true);
tests/simulation/veyraNativeCatalog.test.ts:253:      fleets: [{
tests/simulation/veyraNativeCatalog.test.ts:263:        mission: null,
tests/simulation/veyraNativeCatalog.test.ts:278:    expect(first.value.state.fleets[0]?.ships).toEqual({ 'ship.veyra.wisp': 1 });
tests/simulation/worldEvents.test.ts:10:import { executeCommand } from '../../src/simulation/reducer';
tests/simulation/worldEvents.test.ts:19:  const result = executeCommand(state, { type: 'ADVANCE_TIME', seconds });
tests/simulation/worldEvents.test.ts:127:  it('exposes temporary risk and yield modifiers to mission domains', () => {
tests/storage/autosave.test.ts:3:import { executeCommand } from '../../src/simulation/reducer';
tests/storage/autosave.test.ts:13:    const advanced = executeCommand(initial, { type: 'ADVANCE_TIME', seconds: 600 });
tests/storage/autosave.test.ts:43:    const advanced = executeCommand(initial, { type: 'ADVANCE_TIME', seconds: 900 });
tests/storage/autosave.test.ts:61:  it('restores a checksum-validated autosave', async () => {
tests/storage/autosave.test.ts:119:  it('reports a missing autosave without creating one', async () => {
tests/storage/migrateGameStateV14.test.ts:4:import { replayCommands } from '../../src/simulation/replay';
tests/storage/migrateGameStateV14.test.ts:34:    const replay = replayCommands('schema-v14-replay', [
tests/storage/saveFormat.test.ts:24:      commanders: _commanders,
tests/storage/saveFormat.test.ts:55:      expect(parsed.value.state.commanders).toHaveLength(parsed.value.state.empires.length);
tests/storage/saveFormat.test.ts:92:  it('round-trips active debris and recycling missions', () => {
tests/storage/saveFormat.test.ts:106:      fleets: [
tests/storage/saveFormat.test.ts:117:          mission: { kind: 'recycle' as const, targetPlanetId: current.planets[0]!.id },
tests/storage/saveFormat.test.ts:141:      fleets: [
tests/storage/saveFormat.test.ts:158:          mission: { kind: 'colonize' as const, targetPlanetId: target.id },
tests/storage/saveFormat.test.ts:170:  it('adds a null mission to older fleet saves', () => {
tests/storage/saveFormat.test.ts:171:    const current = createInitialGameState('fleet-migration');
tests/storage/saveFormat.test.ts:172:    const olderFleet = {
tests/storage/saveFormat.test.ts:173:      id: 'fleet-1',
tests/storage/saveFormat.test.ts:183:    const { shipUpgrades: _shipUpgrades, commanders: _commanders, ...legacyBase } = current;
tests/storage/saveFormat.test.ts:187:      fleets: [olderFleet],
tests/storage/saveFormat.test.ts:191:      slotId: 'fleet-v6',
tests/storage/saveFormat.test.ts:199:      expect(parsed.value.state.fleets[0]?.mission).toBeNull();
tests/storage/saveFormat.test.ts:229:  it('adds default command profiles to schema-v13 saves that predate command progression', () => {
tests/storage/saveFormat.test.ts:230:    const current = createInitialGameState('command-migration');
tests/storage/saveFormat.test.ts:231:    const { commanders: _commanders, ...legacyState } = current;
tests/storage/saveFormat.test.ts:234:      slotId: 'command-v13',
tests/storage/saveFormat.test.ts:242:      expect(parsed.value.state.commanders).toHaveLength(parsed.value.state.empires.length);
tests/storage/saveFormat.test.ts:243:      expect(parsed.value.state.commanders.every((entry) => entry.doctrineId === 'adaptive')).toBe(true);
tests/ui/empireOverviewViewModel.test.ts:6:  it('aggregates resources, queues and fleets across owned colonies', () => {
tests/ui/empireOverviewViewModel.test.ts:47:          buildingId: 'building.aegis.command',
tests/ui/empireOverviewViewModel.test.ts:58:      fleets: [
tests/ui/empireOverviewViewModel.test.ts:60:          id: 'fleet-home',
tests/ui/empireOverviewViewModel.test.ts:69:          mission: null,
tests/ui/empireOverviewViewModel.test.ts:72:          id: 'fleet-second-flight',
tests/ui/empireOverviewViewModel.test.ts:87:          mission: { kind: 'scout' as const, targetPlanetId: home.id },
tests/ui/empireOverviewViewModel.test.ts:107:      totalFleetCount: 2,
tests/ui/empireOverviewViewModel.test.ts:108:      activeFleetCount: 1,
tests/ui/empireOverviewViewModel.test.ts:120:      activeMissionCount: 1,
tests/ui/empireOverviewViewModel.test.ts:121:      stationedFleetCount: 0,
tests/ui/empireOverviewViewModel.test.ts:126:      activeMissionCount: 0,
tests/ui/empireOverviewViewModel.test.ts:127:      stationedFleetCount: 1,
tests/ui/empireOverviewViewModel.test.ts:131:  it('does not include colonies or fleets owned by another empire', () => {
tests/ui/fleetComposerViewModel.test.ts:5:  createFleetComposerViewModel,
tests/ui/fleetComposerViewModel.test.ts:6:  createFleetRoutePreview,
tests/ui/fleetComposerViewModel.test.ts:7:} from '../../src/ui/fleetComposerViewModel';
tests/ui/fleetComposerViewModel.test.ts:10:  const state = createInitialGameState('fleet-composer');
tests/ui/fleetComposerViewModel.test.ts:53:describe('fleet composer view model', () => {
tests/ui/fleetComposerViewModel.test.ts:59:    const view = createFleetComposerViewModel(
tests/ui/fleetComposerViewModel.test.ts:84:    const view = createFleetComposerViewModel(
tests/ui/fleetComposerViewModel.test.ts:107:    const fleet = {
tests/ui/fleetComposerViewModel.test.ts:108:      id: 'preview-fleet',
tests/ui/fleetComposerViewModel.test.ts:117:      mission: null,
tests/ui/fleetComposerViewModel.test.ts:119:    const first = createFleetRoutePreview(
tests/ui/fleetComposerViewModel.test.ts:121:      fleet,
tests/ui/fleetComposerViewModel.test.ts:125:    const second = createFleetRoutePreview(
tests/ui/fleetComposerViewModel.test.ts:127:      fleet,
tests/ui/militaryZoneViewModel.test.ts:6:  it('exposes defense and fleet routes without inventing extra planet zones', () => {
tests/ui/militaryZoneViewModel.test.ts:19:    expect(view.gateways.map((gateway) => gateway.id)).toEqual(['defense', 'fleet']);
tests/ui/planetViewModel.test.ts:3:import { executeCommand } from '../../src/simulation/reducer';
tests/ui/planetViewModel.test.ts:44:    const queued = executeCommand(initial, {
tests/ui/spaceMapViewModel.test.ts:51:  it('does not expose hidden ownership, faction, alliance, defence or fleet data', () => {

=== SMALL FILE CONTENTS ===
----- FILE: src/simulation/bots/fleetMissionPlanner.ts (419 lines) -----
import { getFactionIdForEmpire } from '../factions/factionMechanicalCatalogRegistry';
import { getFactionMechanicalRoles } from '../factions/factionMechanicalRoles';
import { createFleet } from '../fleets/fleetCommands';
import { sendFleet } from '../fleets/flightCommands';
import type { FleetMissionKind } from '../fleets/types';
import { compareSpaceCoordinates } from '../space/coordinates';
import type { GameCommand, GameState } from '../types';
import { getUnitDefinition } from '../units/catalog';
import type { ShipRole } from '../units/types';
import { createBotPerception, type BotPerception } from './perception';

export type BotFleetReasonCode =
  | 'fleet-created'
  | 'mission-transport-selected'
  | 'mission-recycle-selected'
  | 'mission-colonize-selected'
  | 'mission-scout-selected'
  | 'mission-attack-selected'
  | 'mission-deploy-selected'
  | 'fleet-busy'
  | 'fleet-unavailable'
  | 'mission-unavailable';

export interface BotFleetMissionPlan {
  readonly empireId: string;
  readonly reasonCode: BotFleetReasonCode;
  readonly explanation: string;
  readonly command: GameCommand | null;
}

type PerceivedFleet = BotPerception['ownFleets'][number];
type ResourceId = 'metal' | 'crystal' | 'gas';

const RESOURCE_IDS: readonly ResourceId[] = ['metal', 'crystal', 'gas'];
const ZERO_CARGO = { metal: 0, crystal: 0, gas: 0 } as const;

function sumCargo(cargo: Readonly<Record<ResourceId, number>>): number {
  return RESOURCE_IDS.reduce((total, resourceId) => total + cargo[resourceId], 0);
}

function hasRole(fleet: PerceivedFleet, role: ShipRole): boolean {
  return Object.entries(fleet.ships).some(
    ([unitId, quantity]) =>
      quantity > 0 &&
      getUnitDefinition(unitId)?.kind === 'ship' &&
      getUnitDefinition(unitId)?.role === role,
  );
}

function shipPower(ships: Readonly<Record<string, number>>): number {
  return Object.entries(ships).reduce((total, [unitId, quantity]) => {
    const stats = getUnitDefinition(unitId)?.stats;
    return stats === undefined
      ? total
      : total + quantity * (stats.attack * 2 + stats.armor + stats.shield);
  }, 0);
}

function isArmed(fleet: PerceivedFleet): boolean {
  return Object.entries(fleet.ships).some(
    ([unitId, quantity]) => quantity > 0 && (getUnitDefinition(unitId)?.stats.attack ?? 0) > 0,
  );
}

function perceivedTargetPower(
  snapshot: BotPerception['foreignPlanets'][number]['snapshot'],
): number | null {
  if (snapshot.level < 3) return null;
  const defenses = Object.entries(snapshot.defenses ?? {}).reduce((total, [unitId, quantity]) => {
    const stats = getUnitDefinition(unitId)?.stats;
    return stats === undefined
      ? total
      : total + quantity * (stats.attack * 2 + stats.armor + stats.shield);
  }, 0);
  return (snapshot.stationedFleets ?? []).reduce(
    (total, fleet) => total + shipPower(fleet.ships),
    defenses,
  );
}

function sendCommand(
  empireId: string,
  fleetId: string,
  targetPlanetId: string,
  mission: FleetMissionKind,
): Extract<GameCommand, { readonly type: 'SEND_FLEET' }> {
  return { type: 'SEND_FLEET', empireId, fleetId, targetPlanetId, mission };
}

function validSend(
  state: GameState,
  command: Extract<GameCommand, { readonly type: 'SEND_FLEET' }>,
): boolean {
  return sendFleet(state, command).ok;
}

function missionPlan(
  state: GameState,
  perception: BotPerception,
): BotFleetMissionPlan | null {
  const fleets = perception.ownFleets
    .filter((fleet) => fleet.status === 'stationed' && fleet.location.type === 'planet')
    .sort((left, right) => left.id.localeCompare(right.id));
  const ownIds = new Set(perception.ownPlanets.map((planet) => planet.id));

  for (const fleet of fleets) {
    if (fleet.location.type !== 'planet') continue;
    const originPlanetId = fleet.location.planetId;

    if (sumCargo(fleet.cargo) > 0) {
      const resourceId = [...RESOURCE_IDS].sort(
        (left, right) => fleet.cargo[right] - fleet.cargo[left] || left.localeCompare(right),
      )[0];
      if (resourceId !== undefined && fleet.cargo[resourceId] > 0) {
        const targets = perception.ownPlanets
          .filter((planet) => planet.id !== originPlanetId)
          .sort(
            (left, right) =>
              left.resources[resourceId] - right.resources[resourceId] ||
              left.id.localeCompare(right.id),
          );
        for (const target of targets) {
          const command = sendCommand(perception.empireId, fleet.id, target.id, 'transport');
          if (validSend(state, command)) {
            return {
              empireId: perception.empireId,
              reasonCode: 'mission-transport-selected',
              explanation: `Транспорт ${fleet.id} направлен на ${target.name} с ресурсом ${resourceId}.`,
              command,
            };
          }
        }
      }
    }

    if (hasRole(fleet, 'recycler')) {
      const debris = [...perception.ownDebrisFields].sort(
        (left, right) =>
          right.metal + right.crystal - (left.metal + left.crystal) ||
          left.planetId.localeCompare(right.planetId),
      );
      for (const target of debris) {
        if (target.planetId === originPlanetId) continue;
        const command = sendCommand(perception.empireId, fleet.id, target.planetId, 'recycle');
        if (validSend(state, command)) {
          return {
            empireId: perception.empireId,
            reasonCode: 'mission-recycle-selected',
            explanation: `Переработчик ${fleet.id} направлен к обломкам ${target.planetId}.`,
            command,
          };
        }
      }
    }

    if (hasRole(fleet, 'colonizer')) {
      const targets = state.galaxy.systems
        .flatMap((system) => system.planets)
        .filter((planet) => planet.biome !== 'gas')
        .sort((left, right) =>
          compareSpaceCoordinates(left.coordinate, right.coordinate) ||
          left.id.localeCompare(right.id),
        );
      for (const target of targets) {
        const command = sendCommand(perception.empireId, fleet.id, target.id, 'colonize');
        if (validSend(state, command)) {
          return {
            empireId: perception.empireId,
            reasonCode: 'mission-colonize-selected',
            explanation: `Колонизатор ${fleet.id} направлен к позиции ${target.id}.`,
            command,
          };
        }
      }
    }

    if (hasRole(fleet, 'scout')) {
      const observed = [...perception.foreignPlanets]
        .sort(
          (left, right) =>
            Number(left.freshness === 'current') - Number(right.freshness === 'current') ||
            right.ageSeconds - left.ageSeconds ||
            left.planetId.localeCompare(right.planetId),
        )
        .map((planet) => planet.planetId);
      const publicTargets = perception.publicColonyIds.filter((planetId) => !ownIds.has(planetId));
      for (const targetPlanetId of [...new Set([...observed, ...publicTargets])]) {
        const command = sendCommand(perception.empireId, fleet.id, targetPlanetId, 'scout');
        if (validSend(state, command)) {
          return {
            empireId: perception.empireId,
            reasonCode: 'mission-scout-selected',
            explanation: `Разведчик ${fleet.id} проверяет ${targetPlanetId}.`,
            command,
          };
        }
      }
    }

    if (isArmed(fleet)) {
      const ownPower = shipPower(fleet.ships);
      const targets = perception.foreignPlanets
        .filter((planet) => planet.freshness === 'current')
        .map((planet) => ({ planet, power: perceivedTargetPower(planet.snapshot) }))
        .filter(
          (candidate): candidate is {
            readonly planet: BotPerception['foreignPlanets'][number];
            readonly power: number;
          } => candidate.power !== null,
        )
        .sort(
          (left, right) =>
            left.power - right.power || left.planet.planetId.localeCompare(right.planet.planetId),
        );
      for (const target of targets) {
        if (ownPower * 10 < Math.max(1, target.power) * 12) continue;
        const command = sendCommand(
          perception.empireId,
          fleet.id,
          target.planet.planetId,
          'attack',
        );
        if (validSend(state, command)) {
          return {
            empireId: perception.empireId,
            reasonCode: 'mission-attack-selected',
            explanation: `Флот ${fleet.id} атакует ${target.planet.planetId}: ${ownPower} против ${target.power}.`,
            command,
          };
        }
      }

      const reinforcements = perception.ownPlanets
        .filter((planet) => planet.id !== originPlanetId)
        .map((planet) => ({
          planet,
          defense: Object.values(planet.defenses).reduce(
            (total, quantity) => total + quantity,
            0,
          ),
        }))
        .sort(
          (left, right) =>
            left.defense - right.defense || left.planet.id.localeCompare(right.planet.id),
        );
      for (const target of reinforcements) {
        const command = sendCommand(perception.empireId, fleet.id, target.planet.id, 'deploy');
        if (validSend(state, command)) {
          return {
            empireId: perception.empireId,
            reasonCode: 'mission-deploy-selected',
            explanation: `Флот ${fleet.id} усиливает колонию ${target.planet.name}.`,
            command,
          };
        }
      }
    }
  }

  return null;
}

interface FleetCandidate {
  readonly ships: Readonly<Record<string, number>>;
  readonly cargo: Readonly<Record<ResourceId, number>>;
  readonly explanation: string;
}

function creationCandidates(
  perception: BotPerception,
  planet: BotPerception['ownPlanets'][number],
  state: GameState,
): readonly FleetCandidate[] {
  const factionId = getFactionIdForEmpire(state, perception.empireId);
  const roles = getFactionMechanicalRoles(factionId);
  const candidates: FleetCandidate[] = [];

  if (
    (perception.researchLevels[roles.research.colonization] ?? 0) > 0 &&
    (planet.ships[roles.ships.colonizer] ?? 0) > 0
  ) {
    candidates.push({
      ships: { [roles.ships.colonizer]: 1 },
      cargo: ZERO_CARGO,
      explanation: `На ${planet.name} подготовлен колонизационный флот.`,
    });
  }

  if (
    perception.ownDebrisFields.length > 0 &&
    (planet.ships[roles.ships.recycler] ?? 0) > 0
  ) {
    candidates.push({
      ships: { [roles.ships.recycler]: 1 },
      cargo: ZERO_CARGO,
      explanation: `На ${planet.name} сформирован флот переработки.`,
    });
  }

  if (perception.ownPlanets.length > 1 && (planet.ships[roles.ships.transport] ?? 0) > 0) {
    const resourceId = [...RESOURCE_IDS].sort(
      (left, right) => planet.resources[right] - planet.resources[left] || left.localeCompare(right),
    )[0];
    if (resourceId !== undefined) {
      const amount = Math.min(400, Math.max(0, planet.resources[resourceId] - 500));
      if (amount > 0) {
        candidates.push({
          ships: { [roles.ships.transport]: 1 },
          cargo: { ...ZERO_CARGO, [resourceId]: amount },
          explanation: `На ${planet.name} сформирован транспорт с грузом ${resourceId}.`,
        });
      }
    }
  }

  if ((planet.ships[roles.ships.scout] ?? 0) > 0) {
    candidates.push({
      ships: { [roles.ships.scout]: 1 },
      cargo: ZERO_CARGO,
      explanation: `На ${planet.name} сформирован разведывательный флот.`,
    });
  }

  const combat: Record<string, number> = {};
  const fighters = Math.min(3, planet.ships[roles.ships.fighter] ?? 0);
  const frigates = Math.min(1, planet.ships[roles.ships.frigate] ?? 0);
  if (fighters > 0) combat[roles.ships.fighter] = fighters;
  if (frigates > 0) combat[roles.ships.frigate] = frigates;
  if (Object.keys(combat).length > 0) {
    candidates.push({
      ships: combat,
      cargo: ZERO_CARGO,
      explanation: `На ${planet.name} сформирована боевая группа.`,
    });
  }

  const fallback = Object.entries(planet.ships)
    .filter(([, quantity]) => quantity > 0)
    .sort(([left], [right]) => left.localeCompare(right))[0];
  if (fallback !== undefined) {
    candidates.push({
      ships: { [fallback[0]]: 1 },
      cargo: ZERO_CARGO,
      explanation: `На ${planet.name} сформирован доступный сервисный флот.`,
    });
  }

  return candidates;
}

function fleetCreationPlan(
  state: GameState,
  perception: BotPerception,
): BotFleetMissionPlan | null {
  const planets = [...perception.ownPlanets].sort((left, right) => left.id.localeCompare(right.id));
  for (const planet of planets) {
    for (const candidate of creationCandidates(perception, planet, state)) {
      const command: Extract<GameCommand, { readonly type: 'CREATE_FLEET' }> = {
        type: 'CREATE_FLEET',
        empireId: perception.empireId,
        planetId: planet.id,
        ships: candidate.ships,
        cargo: candidate.cargo,
      };
      if (createFleet(state, command).ok) {
        return {
          empireId: perception.empireId,
          reasonCode: 'fleet-created',
          explanation: candidate.explanation,
          command,
        };
      }
    }
  }
  return null;
}

export function planBotFleetMission(
  state: GameState,
  empireId: string,
): BotFleetMissionPlan {
  const perception = createBotPerception(state, empireId);
  const mission = missionPlan(state, perception);
  if (mission !== null) return mission;

  const creation = fleetCreationPlan(state, perception);
  if (creation !== null) return creation;

  if (perception.ownFleets.some((fleet) => fleet.status !== 'stationed')) {
    return {
      empireId,
      reasonCode: 'fleet-busy',
      explanation: 'Флоты уже выполняют миссии, свободных кораблей для новой группы нет.',
      command: null,
    };
  }
  if (perception.ownFleets.length > 0) {
    return {
      empireId,
      reasonCode: 'mission-unavailable',
      explanation: 'Станционированные флоты не имеют честно подтверждённой допустимой цели.',
      command: null,
    };
  }
  return {
    empireId,
    reasonCode: 'fleet-unavailable',
    explanation: 'Нет свободных кораблей для формирования флота.',
    command: null,
  };
}

export function planAllBotFleetMissions(
  state: GameState,
): readonly BotFleetMissionPlan[] {
  return state.empires
    .filter((empireId) => empireId !== 'player')
    .map((empireId) => planBotFleetMission(state, empireId));
}
----- FILE: src/simulation/combat/fleetDoctrine.ts (147 lines) -----
import { getUnitDefinition } from '../units/catalog';
import type { ShipRole } from '../units/types';
import { getUnitCombatProfile, type TargetSize } from './combatProfiles';
import { getDefenseAbilityBonusMaps } from './defenseAbilities';
import { getShipAbilityBonusMaps } from './shipAbilities';

export type FleetFormation = 'line' | 'screen' | 'wedge';
export type FleetTargetPriority = 'balanced' | 'interceptors' | 'capitals' | 'installations';

export interface FormationDefinition {
  readonly id: FleetFormation;
  readonly name: string;
  readonly description: string;
  readonly weaponBonusPercent: number;
  readonly armorBonusPercent: number;
}

export interface ClassSkillDefinition {
  readonly id: string;
  readonly name: string;
  readonly description: string;
  readonly role: ShipRole;
  readonly formation: FleetFormation;
  readonly weaponBonusPercent: number;
  readonly armorBonusPercent: number;
}

export const FLEET_FORMATIONS: Readonly<Record<FleetFormation, FormationDefinition>> = {
  line: {
    id: 'line',
    name: 'Линейный строй',
    description: 'Стабильное построение без общего штрафа. Линейные корабли формируют защитную связку.',
    weaponBonusPercent: 0,
    armorBonusPercent: 0,
  },
  screen: {
    id: 'screen',
    name: 'Защитный экран',
    description: 'Корабли рассеивают удар: +12% защита, −5% атака. Разведчики получают уклонение.',
    weaponBonusPercent: -5,
    armorBonusPercent: 12,
  },
  wedge: {
    id: 'wedge',
    name: 'Ударный клин',
    description: 'Концентрированный прорыв: +12% атака, −5% защита. Истребители усиливают натиск.',
    weaponBonusPercent: 12,
    armorBonusPercent: -5,
  },
};

export const CLASS_SKILLS: readonly ClassSkillDefinition[] = [
  {
    id: 'ghost-screen',
    name: 'Призрачный экран',
    description: 'Разведывательные аппараты получают +18% защиты в защитном экране.',
    role: 'scout',
    formation: 'screen',
    weaponBonusPercent: 0,
    armorBonusPercent: 18,
  },
  {
    id: 'spear-swarm',
    name: 'Рой копий',
    description: 'Истребители получают +15% атаки в ударном клине.',
    role: 'fighter',
    formation: 'wedge',
    weaponBonusPercent: 15,
    armorBonusPercent: 0,
  },
  {
    id: 'bulwark-link',
    name: 'Связка бастионов',
    description: 'Линейные корабли получают +8% защиты в линейном строю.',
    role: 'frigate',
    formation: 'line',
    weaponBonusPercent: 0,
    armorBonusPercent: 8,
  },
];

const TARGET_PRIORITY_WEIGHTS: Readonly<
  Record<FleetTargetPriority, Readonly<Record<TargetSize, number>>>
> = {
  balanced: { small: 1_000, medium: 1_000, large: 1_000, installation: 1_000 },
  interceptors: { small: 2_400, medium: 800, large: 350, installation: 250 },
  capitals: { small: 400, medium: 900, large: 2_200, installation: 900 },
  installations: { small: 300, medium: 500, large: 900, installation: 2_600 },
};

export function isFleetFormation(value: unknown): value is FleetFormation {
  return value === 'line' || value === 'screen' || value === 'wedge';
}

export function isFleetTargetPriority(value: unknown): value is FleetTargetPriority {
  return (
    value === 'balanced' ||
    value === 'interceptors' ||
    value === 'capitals' ||
    value === 'installations'
  );
}

export function getTargetPriorityWeightPermille(
  priority: FleetTargetPriority,
  targetUnitId: string,
): number {
  return TARGET_PRIORITY_WEIGHTS[priority][getUnitCombatProfile(targetUnitId).targetSize];
}

function mergeBonusMaps(
  left: Readonly<Record<string, number>>,
  right: Readonly<Record<string, number>>,
): Readonly<Record<string, number>> {
  const result = { ...left };
  for (const [unitId, value] of Object.entries(right)) {
    result[unitId] = (result[unitId] ?? 0) + value;
  }
  return result;
}

export function getClassSkillBonusMaps(
  units: Readonly<Record<string, number>>,
  formation: FleetFormation,
): {
  readonly weapon: Readonly<Record<string, number>>;
  readonly armor: Readonly<Record<string, number>>;
} {
  const weapon: Record<string, number> = {};
  const armor: Record<string, number> = {};
  for (const [unitId, quantity] of Object.entries(units)) {
    if (quantity <= 0) continue;
    const definition = getUnitDefinition(unitId);
    if (definition?.kind !== 'ship') continue;
    for (const skill of CLASS_SKILLS) {
      if (skill.formation !== formation || definition.role !== skill.role) continue;
      if (skill.weaponBonusPercent !== 0) weapon[unitId] = skill.weaponBonusPercent;
      if (skill.armorBonusPercent !== 0) armor[unitId] = skill.armorBonusPercent;
    }
  }
  const shipAbilities = getShipAbilityBonusMaps(units);
  const defenseAbilities = getDefenseAbilityBonusMaps(units);
  return {
    weapon: mergeBonusMaps(mergeBonusMaps(weapon, shipAbilities.weapon), defenseAbilities.weapon),
    armor: mergeBonusMaps(mergeBonusMaps(armor, shipAbilities.armor), defenseAbilities.armor),
  };
}
----- FILE: src/simulation/combat/resolveAttackMission.ts (408 lines) -----
import {
  getCommanderFleetEffects,
  recoverFleetShipsWithCommander,
  type CommanderFleetEffects,
} from '../command/commanderShips';
import {
  awardBattleCommandExperience,
  getCommandCombatEffects,
} from '../command/commandDoctrine';
import {
  addDamagedDefenses,
  calculateRecoveredDefenses,
} from '../defense/planetaryDefense';
import type { ResourceCost } from '../economy/types';
import { getResearchEffectsForEmpire } from '../factions/factionResearchEffects';
import type { FleetState } from '../fleets/types';
import type { PlanetState } from '../planet/types';
import { PIRATE_EMPIRE_ID } from '../pve/neutralForces';
import {
  applyPvePlunderMultiplier,
  calculatePirateThreatMultiplier,
  calculatePveRewardMultiplier,
  scalePveUnits,
} from '../pve/pveBalance';
import type { GameState } from '../types';
import { getUnitDefinition } from '../units/catalog';
import { getShipUpgradeBonusMap } from '../upgrades/shipUpgrades';
import {
  addDebrisField,
  calculateDebrisFromLosses,
  plunderPlanet,
  type DebrisAmount,
} from './debris';
import { getPlanetaryDefenseTargetPriority } from './defenseAbilities';
import { resolveBattle } from './resolveBattle';
import type { BattleReport } from './types';

function getCombatEffects(
  state: GameState,
  empireId: string,
  units: Readonly<Record<string, number>>,
  fleetId: string | undefined,
  commander: CommanderFleetEffects,
  opponentCommander: CommanderFleetEffects,
  installationsPresent: boolean,
) {
  const effects = getResearchEffectsForEmpire(state, empireId);
  const command = getCommandCombatEffects(state.commanders, empireId, fleetId);
  const demolitionBonus = installationsPresent && commander.demolitionBasisPoints > 0
    ? Math.max(1, Math.ceil(commander.demolitionBasisPoints / 100))
    : 0;
  return {
    weaponBonusPercent:
      effects.weaponStrengthPercent +
      command.weaponBonusPercent +
      commander.weaponBonusPercent +
      demolitionBonus -
      opponentCommander.enemyWeaponPenaltyPercent,
    armorBonusPercent:
      effects.armorStrengthPercent +
      command.armorBonusPercent +
      commander.armorBonusPercent -
      opponentCommander.enemyArmorPenaltyPercent,
    unitWeaponBonusPercent: getShipUpgradeBonusMap(
      state.shipUpgrades,
      empireId,
      units,
      'weapons',
    ),
    unitArmorBonusPercent: getShipUpgradeBonusMap(
      state.shipUpgrades,
      empireId,
      units,
      'armor',
    ),
  };
}

function mergeUnits(
  target: Record<string, number>,
  source: Readonly<Record<string, number>>,
): void {
  for (const [unitId, count] of Object.entries(source)) {
    target[unitId] = (target[unitId] ?? 0) + count;
  }
}

function splitDefenderRemaining(
  remaining: Readonly<Record<string, number>>,
): {
  readonly ships: Readonly<Record<string, number>>;
  readonly defenses: Readonly<Record<string, number>>;
} {
  const ships: Record<string, number> = {};
  const defenses: Record<string, number> = {};
  for (const [unitId, count] of Object.entries(remaining)) {
    const definition = getUnitDefinition(unitId);
    if (definition?.kind === 'ship') ships[unitId] = count;
    if (definition?.kind === 'defense') defenses[unitId] = count;
  }
  return { ships, defenses };
}

function clampActiveDefenses(
  initial: Readonly<Record<string, number>>,
  remaining: Readonly<Record<string, number>>,
): Readonly<Record<string, number>> {
  return Object.fromEntries(
    Object.entries(initial)
      .map(([unitId, initialCount]) => [
        unitId,
        Math.min(initialCount, remaining[unitId] ?? 0),
      ] as const)
      .filter(([, count]) => count > 0),
  );
}

function addRecoveredToRemaining(
  remaining: Readonly<Record<string, number>>,
  recovered: Readonly<Record<string, number>>,
): Readonly<Record<string, number>> {
  const result = { ...remaining };
  for (const [unitId, quantity] of Object.entries(recovered)) {
    result[unitId] = (result[unitId] ?? 0) + quantity;
  }
  return result;
}

function recoveredDelta(
  before: Readonly<Record<string, number>>,
  after: Readonly<Record<string, number>>,
): Readonly<Record<string, number>> {
  return Object.fromEntries(
    Object.keys(after)
      .sort()
      .map((unitId) => [unitId, Math.max(0, (after[unitId] ?? 0) - (before[unitId] ?? 0))] as const)
      .filter(([, quantity]) => quantity > 0),
  );
}

function redistributeDefenderShips(
  fleets: readonly FleetState[],
  targetPlanetId: string,
  defenderEmpireId: string,
  remainingShips: Readonly<Record<string, number>>,
): readonly FleetState[] {
  const available = { ...remainingShips };
  const result: FleetState[] = [];

  for (const fleet of fleets) {
    const isDefender =
      fleet.empireId === defenderEmpireId &&
      fleet.status === 'stationed' &&
      fleet.location.type === 'planet' &&
      fleet.location.planetId === targetPlanetId;
    if (!isDefender) {
      result.push(fleet);
      continue;
    }

    const ships: Record<string, number> = {};
    for (const [unitId, originalCount] of Object.entries(fleet.ships)) {
      const assigned = Math.min(originalCount, available[unitId] ?? 0);
      if (assigned > 0) {
        ships[unitId] = assigned;
        available[unitId] = (available[unitId] ?? 0) - assigned;
      }
    }
    if (Object.keys(ships).length > 0) result.push({ ...fleet, ships });
  }

  return result;
}

function addDestroyedCargoDebris(
  debris: DebrisAmount,
  fleet: FleetState,
  attackerSurvived: boolean,
): DebrisAmount {
  if (attackerSurvived) return debris;
  return {
    metal: debris.metal + Math.floor(fleet.cargo.metal * 0.5),
    crystal: debris.crystal + Math.floor(fleet.cargo.crystal * 0.5),
  };
}

export interface AttackMissionResolution {
  readonly state: GameState;
  readonly report: BattleReport;
  readonly attackerFleet: FleetState | undefined;
}

export function resolveAttackMission(
  state: GameState,
  attackerFleet: FleetState,
  target: PlanetState,
  eventSequence: number,
): AttackMissionResolution {
  const defenderUnits: Record<string, number> = {};
  mergeUnits(defenderUnits, target.inventory.defenses);
  const defenderFleets = state.fleets.filter(
    (fleet) =>
      fleet.empireId === target.ownerEmpireId &&
      fleet.status === 'stationed' &&
      fleet.location.type === 'planet' &&
      fleet.location.planetId === target.id,
  );
  for (const fleet of defenderFleets) mergeUnits(defenderUnits, fleet.ships);
  const defenderDoctrine = defenderFleets[0];
  const attackerFormation = attackerFleet.formation ?? 'line';
  const attackerTargetPriority = attackerFleet.targetPriority ?? 'balanced';
  const defenderFormation = defenderDoctrine?.formation ?? 'line';
  const defenderTargetPriority = defenderDoctrine?.targetPriority ??
    getPlanetaryDefenseTargetPriority(target.inventory.defenses);

  const isPve = target.ownerEmpireId === PIRATE_EMPIRE_ID;
  const threatMultiplierPermille = isPve
    ? calculatePirateThreatMultiplier(state, attackerFleet.empireId)
    : 1_000;
  const rewardMultiplierPermille = isPve
    ? calculatePveRewardMultiplier(
        state,
        attackerFleet.empireId,
        'pirate-raid',
        target.id,
      )
    : 1_000;
  const effectiveDefenderUnits = isPve
    ? scalePveUnits(defenderUnits, threatMultiplierPermille)
    : defenderUnits;
  const attackerCommander = getCommanderFleetEffects(state, attackerFleet);
  const defenderCommander = defenderDoctrine === undefined
    ? getCommanderFleetEffects(state, target.ownerEmpireId, undefined, effectiveDefenderUnits)
    : getCommanderFleetEffects(state, defenderDoctrine);

  const seed = (state.seed ^ eventSequence ^ attackerFleet.id.length) >>> 0;
  const resolution = resolveBattle(
    seed,
    {
      empireId: attackerFleet.empireId,
      units: attackerFleet.ships,
      formation: attackerFormation,
      targetPriority: attackerTargetPriority,
      ...getCombatEffects(
        state,
        attackerFleet.empireId,
        attackerFleet.ships,
        attackerFleet.id,
        attackerCommander,
        defenderCommander,
        Object.keys(target.inventory.defenses).length > 0,
      ),
    },
    {
      empireId: target.ownerEmpireId,
      units: effectiveDefenderUnits,
      formation: defenderFormation,
      targetPriority: defenderTargetPriority,
      ...getCombatEffects(
        state,
        target.ownerEmpireId,
        effectiveDefenderUnits,
        defenderDoctrine?.id,
        defenderCommander,
        attackerCommander,
        false,
      ),
    },
  );
  const attackerAfterCommanderRecovery = recoverFleetShipsWithCommander(
    attackerFleet.ships,
    resolution.attackerRemaining,
    attackerCommander.recoveryPermille,
    seed ^ 0xa5a5a5a5,
  );
  const defenderAfterCommanderRecovery = recoverFleetShipsWithCommander(
    effectiveDefenderUnits,
    resolution.defenderRemaining,
    defenderCommander.recoveryPermille,
    seed ^ 0x5a5a5a5a,
  );
  const defenderRemaining = splitDefenderRemaining(defenderAfterCommanderRecovery);
  const activeDefenses = clampActiveDefenses(
    target.inventory.defenses,
    defenderRemaining.defenses,
  );
  const defensesRecovered = calculateRecoveredDefenses(
    target.inventory.defenses,
    activeDefenses,
    seed,
  );
  const finalDefenderRemaining = addRecoveredToRemaining(
    defenderAfterCommanderRecovery,
    defensesRecovered,
  );
  let updatedTarget: PlanetState = {
    ...target,
    inventory: {
      ...target.inventory,
      defenses: activeDefenses,
    },
    defense: addDamagedDefenses(target.defense, defensesRecovered),
  };
  let fleets = redistributeDefenderShips(
    state.fleets,
    target.id,
    target.ownerEmpireId,
    defenderRemaining.ships,
  );
  const attackerSurvived = Object.keys(attackerAfterCommanderRecovery).length > 0;
  let updatedAttacker = attackerSurvived
    ? { ...attackerFleet, ships: attackerAfterCommanderRecovery }
    : undefined;
  let plunderedCargo: ResourceCost = { metal: 0, crystal: 0, gas: 0 };

  if (resolution.winner === 'attacker' && updatedAttacker !== undefined) {
    const plunder = plunderPlanet(
      updatedTarget,
      updatedAttacker,
      attackerCommander.plunderBonusPercent,
    );
    if (isPve) {
      const adjusted = applyPvePlunderMultiplier(
        plunder.planet,
        plunder.fleet,
        plunder.plundered,
        rewardMultiplierPermille,
      );
      updatedTarget = adjusted.planet;
      updatedAttacker = adjusted.fleet;
      plunderedCargo = adjusted.plundered;
    } else {
      updatedTarget = plunder.planet;
      updatedAttacker = plunder.fleet;
      plunderedCargo = plunder.plundered;
    }
  }

  fleets = updatedAttacker === undefined
    ? fleets.filter((fleet) => fleet.id !== attackerFleet.id)
    : fleets.map((fleet) =>
        fleet.id === attackerFleet.id ? updatedAttacker : fleet,
      );

  const baseDebris = calculateDebrisFromLosses(
    attackerFleet.ships,
    attackerAfterCommanderRecovery,
    effectiveDefenderUnits,
    finalDefenderRemaining,
  );
  const debrisCreated = addDestroyedCargoDebris(
    baseDebris,
    attackerFleet,
    attackerSurvived,
  );
  const debrisFields = addDebrisField(
    state.debrisFields,
    target.id,
    debrisCreated,
    state.clock.elapsedSeconds,
    target.coordinate,
  );

  const report: BattleReport = {
    id: `battle-${eventSequence}-${attackerFleet.id}`,
    seed,
    resolvedAt: state.clock.elapsedSeconds,
    targetPlanetId: target.id,
    attackerEmpireId: attackerFleet.empireId,
    defenderEmpireId: target.ownerEmpireId,
    winner: resolution.winner,
    rounds: resolution.rounds,
    attackerInitial: { ...attackerFleet.ships },
    defenderInitial: effectiveDefenderUnits,
    attackerRemaining: attackerAfterCommanderRecovery,
    defenderRemaining: finalDefenderRemaining,
    attackerCommanderId: attackerCommander.activeCommanderId,
    defenderCommanderId: defenderCommander.activeCommanderId,
    commanderRecoveredShips: {
      attacker: recoveredDelta(resolution.attackerRemaining, attackerAfterCommanderRecovery),
      defender: recoveredDelta(resolution.defenderRemaining, defenderAfterCommanderRecovery),
    },
    defensesRecovered,
    debrisCreated,
    plunderedCargo,
    mode: isPve ? 'pve' : 'pvp',
    threatMultiplierPermille,
    rewardMultiplierPermille,
    attackerFormation,
    attackerTargetPriority,
    defenderFormation,
    defenderTargetPriority,
  };

  return {
    state: {
      ...state,
      planets: state.planets.map((planet) =>
        planet.id === target.id ? updatedTarget : planet,
      ),
      fleets,
      debrisFields,
      commanders: awardBattleCommandExperience(state.commanders, report),
    },
    report,
    attackerFleet: updatedAttacker,
  };
}
----- FILE: src/simulation/command/commandDoctrine.ts (241 lines) -----
import { appendCommandHistory } from '../history/stateHistory';
import type { BattleReport } from '../combat/types';
import type { GameCommand, GameState } from '../types';
import { getUnitDefinition } from '../units/catalog';
import type {
  CommandCombatEffects,
  CommandDoctrineId,
  EmpireCommandState,
} from './types';

export interface CommandDoctrineDefinition {
  readonly id: CommandDoctrineId;
  readonly name: string;
  readonly description: string;
  readonly weaponBonusPercent: number;
  readonly armorBonusPercent: number;
  readonly experiencePermille: number;
}

const EARLY_COMMAND_LEVEL_THRESHOLDS: readonly number[] = [0, 100, 300, 700, 1_500];

function createCommandLevelThresholds(): readonly number[] {
  const thresholds = [...EARLY_COMMAND_LEVEL_THRESHOLDS];
  let current = thresholds.at(-1) ?? 0;
  for (let level = thresholds.length + 1; level <= 40; level += 1) {
    current += 1_000 + (level - 6) * 500;
    thresholds.push(current);
  }
  return thresholds;
}

export const COMMAND_LEVEL_THRESHOLDS = createCommandLevelThresholds();
export const COMMAND_MAX_LEVEL = COMMAND_LEVEL_THRESHOLDS.length;
export const FLAGSHIP_UNLOCK_LEVEL = 2;

export const COMMAND_DOCTRINES: Readonly<
  Record<CommandDoctrineId, CommandDoctrineDefinition>
> = {
  vanguard: {
    id: 'vanguard',
    name: 'Доктрина авангарда',
    description: 'Агрессивное управление огнём: +8% атака, −2% защита.',
    weaponBonusPercent: 8,
    armorBonusPercent: -2,
    experiencePermille: 1_000,
  },
  sentinel: {
    id: 'sentinel',
    name: 'Доктрина стража',
    description: 'Удержание строя: +10% защита, −3% атака.',
    weaponBonusPercent: -3,
    armorBonusPercent: 10,
    experiencePermille: 1_000,
  },
  adaptive: {
    id: 'adaptive',
    name: 'Адаптивное командование',
    description: 'Сбалансированные приказы и ускоренное обучение: +4% атака, +4% защита, +10% опыта.',
    weaponBonusPercent: 4,
    armorBonusPercent: 4,
    experiencePermille: 1_100,
  },
};

export function isCommandDoctrineId(value: unknown): value is CommandDoctrineId {
  return value === 'vanguard' || value === 'sentinel' || value === 'adaptive';
}

export function calculateCommandLevel(experience: number): number {
  const normalized = Math.max(0, Math.floor(experience));
  let level = 1;
  for (const [index, threshold] of COMMAND_LEVEL_THRESHOLDS.entries()) {
    if (normalized >= threshold) level = index + 1;
  }
  return Math.min(COMMAND_MAX_LEVEL, level);
}

export function createInitialCommandStates(
  empireIds: readonly string[],
): readonly EmpireCommandState[] {
  return empireIds.map((empireId) => ({
    empireId,
    doctrineId: 'adaptive',
    experience: 0,
    level: 1,
    flagshipFleetId: null,
  }));
}

export function getEmpireCommandState(
  states: readonly EmpireCommandState[],
  empireId: string,
): EmpireCommandState | undefined {
  return states.find((state) => state.empireId === empireId);
}

function replaceCommandState(
  states: readonly EmpireCommandState[],
  replacement: EmpireCommandState,
): readonly EmpireCommandState[] {
  return states.map((state) =>
    state.empireId === replacement.empireId ? replacement : state,
  );
}

function appendCommand(state: GameState, command: GameCommand): GameState['commandLog'] {
  return appendCommandHistory(state.commandLog, command);
}

export function addCommandExperience(
  states: readonly EmpireCommandState[],
  empireId: string,
  baseAmount: number,
): readonly EmpireCommandState[] {
  const current = getEmpireCommandState(states, empireId);
  if (current === undefined || baseAmount <= 0) return states;
  const doctrine = COMMAND_DOCTRINES[current.doctrineId];
  const gained = Math.max(1, Math.floor((baseAmount * doctrine.experiencePermille) / 1_000));
  const experience = current.experience + gained;
  return replaceCommandState(states, {
    ...current,
    experience,
    level: calculateCommandLevel(experience),
  });
}

export function awardBattleCommandExperience(
  states: readonly EmpireCommandState[],
  report: BattleReport,
): readonly EmpireCommandState[] {
  const rounds = Math.max(1, report.rounds.length);
  const attackerBase = 18 + rounds * 4 + (report.winner === 'attacker' ? 24 : 8);
  const defenderBase = 18 + rounds * 4 + (report.winner === 'defender' ? 24 : 8);
  return addCommandExperience(
    addCommandExperience(states, report.attackerEmpireId, attackerBase),
    report.defenderEmpireId,
    defenderBase,
  );
}

export function setCommandDoctrine(
  state: GameState,
  command: Extract<GameCommand, { readonly type: 'SET_COMMAND_DOCTRINE' }>,
) {
  const current = getEmpireCommandState(state.commanders, command.empireId);
  if (current === undefined) {
    return { ok: false, code: 'COMMAND_STATE_NOT_FOUND', message: 'Command profile is unavailable.' } as const;
  }
  return {
    ok: true,
    value: {
      ...state,
      commanders: replaceCommandState(state.commanders, {
        ...current,
        doctrineId: command.doctrineId,
      }),
      commandLog: appendCommand(state, command),
    },
  } as const;
}

export function assignFlagship(
  state: GameState,
  command: Extract<GameCommand, { readonly type: 'ASSIGN_FLAGSHIP' }>,
) {
  const current = getEmpireCommandState(state.commanders, command.empireId);
  if (current === undefined) {
    return { ok: false, code: 'COMMAND_STATE_NOT_FOUND', message: 'Command profile is unavailable.' } as const;
  }
  if (command.fleetId === null) {
    return {
      ok: true,
      value: {
        ...state,
        commanders: replaceCommandState(state.commanders, {
          ...current,
          flagshipFleetId: null,
        }),
        commandLog: appendCommand(state, command),
      },
    } as const;
  }
  if (current.level < FLAGSHIP_UNLOCK_LEVEL) {
    return {
      ok: false,
      code: 'FLAGSHIP_LEVEL_REQUIRED',
      message: `Command level ${FLAGSHIP_UNLOCK_LEVEL} is required to appoint a flagship.`,
    } as const;
  }
  const fleet = state.fleets.find((candidate) => candidate.id === command.fleetId);
  if (
    fleet === undefined ||
    fleet.empireId !== command.empireId ||
    fleet.status !== 'stationed' ||
    fleet.location.type !== 'planet'
  ) {
    return { ok: false, code: 'FLAGSHIP_FLEET_UNAVAILABLE', message: 'Flagship fleet must be owned and stationed.' } as const;
  }
  const hasArmedShip = Object.entries(fleet.ships).some(
    ([unitId, count]) => count > 0 && (getUnitDefinition(unitId)?.stats.attack ?? 0) > 0,
  );
  if (!hasArmedShip) {
    return { ok: false, code: 'FLAGSHIP_ARMED_SHIP_REQUIRED', message: 'Flagship fleet requires an armed ship.' } as const;
  }
  return {
    ok: true,
    value: {
      ...state,
      commanders: replaceCommandState(state.commanders, {
        ...current,
        flagshipFleetId: fleet.id,
      }),
      commandLog: appendCommand(state, command),
    },
  } as const;
}

export function getCommandCombatEffects(
  states: readonly EmpireCommandState[],
  empireId: string,
  fleetId?: string,
): CommandCombatEffects {
  const state = getEmpireCommandState(states, empireId);
  if (state === undefined) {
    return {
      weaponBonusPercent: 0,
      armorBonusPercent: 0,
      experiencePermille: 1_000,
      isFlagship: false,
    };
  }
  const doctrine = COMMAND_DOCTRINES[state.doctrineId];
  const levelBonus = Math.max(0, state.level - 1);
  const isFlagship = fleetId !== undefined && state.flagshipFleetId === fleetId;
  return {
    weaponBonusPercent: doctrine.weaponBonusPercent + levelBonus + (isFlagship ? 6 : 0),
    armorBonusPercent: doctrine.armorBonusPercent + levelBonus + (isFlagship ? 6 : 0),
    experiencePermille: doctrine.experiencePermille,
    isFlagship,
  };
}
----- FILE: src/simulation/command/commanderShips.ts (216 lines) -----
import type { FleetState } from '../fleets/types';
import type { GameState } from '../types';
import { getCompleteCommanderShipDefinition } from '../units/completeCommanderShipCatalog';
import { getUnitDefinition } from '../units/catalog';
import type { CommanderAbilityEffect, UnitDefinition } from '../units/types';
import { getEmpireCommandState } from './commandDoctrine';
import type { EmpireCommandState } from './types';

export interface ActiveCommanderShip {
  readonly unitId: string;
  readonly definition: UnitDefinition;
  readonly level: number;
}

export interface CommanderFleetEffects {
  readonly activeCommanderId: string | null;
  readonly activeEffect: CommanderAbilityEffect | null;
  readonly level: number;
  readonly weaponBonusPercent: number;
  readonly armorBonusPercent: number;
  readonly enemyWeaponPenaltyPercent: number;
  readonly enemyArmorPenaltyPercent: number;
  readonly speedBonusPercent: number;
  readonly cargoBonusPercent: number;
  readonly plunderBonusPercent: number;
  readonly recoveryPermille: number;
  readonly spyDetectionBasisPoints: number;
  readonly demolitionBasisPoints: number;
  readonly planetDestructionReductionBasisPoints: number;
  readonly upgradePointsPercent: number;
}

const NO_COMMANDER_EFFECTS: CommanderFleetEffects = {
  activeCommanderId: null,
  activeEffect: null,
  level: 0,
  weaponBonusPercent: 0,
  armorBonusPercent: 0,
  enemyWeaponPenaltyPercent: 0,
  enemyArmorPenaltyPercent: 0,
  speedBonusPercent: 0,
  cargoBonusPercent: 0,
  plunderBonusPercent: 0,
  recoveryPermille: 0,
  spyDetectionBasisPoints: 0,
  demolitionBasisPoints: 0,
  planetDestructionReductionBasisPoints: 0,
  upgradePointsPercent: 0,
};

function hashText(value: string): number {
  let hash = 2_166_136_261;
  for (let index = 0; index < value.length; index += 1) {
    hash ^= value.charCodeAt(index);
    hash = Math.imul(hash, 16_777_619);
  }
  return hash >>> 0;
}

export function calculateCommanderAbilityLevel(
  commandLevel: number,
  definition: UnitDefinition,
): number {
  const required = definition.requiredAdmiralLevel ?? Number.POSITIVE_INFINITY;
  const maximum = definition.commanderAbility?.maximumLevel ?? 0;
  if (commandLevel < required || maximum <= 0) return 0;
  return Math.min(maximum, 1 + Math.floor((commandLevel - required) / 4));
}

export function findCommanderShipIds(
  units: Readonly<Record<string, number>>,
): readonly string[] {
  return Object.entries(units)
    .filter(([unitId, quantity]) => quantity > 0 && getUnitDefinition(unitId)?.commanderClass !== undefined)
    .map(([unitId]) => unitId)
    .sort();
}

export function selectActiveCommanderShip(
  commandState: EmpireCommandState | undefined,
  fleetId: string | undefined,
  units: Readonly<Record<string, number>>,
): ActiveCommanderShip | undefined {
  if (commandState === undefined || fleetId === undefined) return undefined;
  if (
    commandState.flagshipFleetId !== null &&
    commandState.flagshipFleetId !== fleetId
  ) {
    return undefined;
  }

  return findCommanderShipIds(units)
    .map((unitId) => {
      const definition = getCompleteCommanderShipDefinition(unitId);
      if (definition === undefined || definition.commanderAbility === undefined) return undefined;
      const level = calculateCommanderAbilityLevel(commandState.level, definition);
      return level <= 0 ? undefined : { unitId, definition, level };
    })
    .filter((candidate): candidate is ActiveCommanderShip => candidate !== undefined)
    .sort((left, right) =>
      left.definition.commanderAbility!.battlePriority -
        right.definition.commanderAbility!.battlePriority ||
      left.unitId.localeCompare(right.unitId),
    )[0];
}

function percentFromBasisPoints(basisPoints: number): number {
  return basisPoints <= 0 ? 0 : Math.max(1, Math.ceil(basisPoints / 100));
}

export function getCommanderFleetEffects(
  state: Pick<GameState, 'commanders'>,
  empireOrFleet: string | Pick<FleetState, 'id' | 'empireId' | 'ships'>,
  fleetId?: string,
  units: Readonly<Record<string, number>> = {},
): CommanderFleetEffects {
  const empireId = typeof empireOrFleet === 'string' ? empireOrFleet : empireOrFleet.empireId;
  const resolvedFleetId = typeof empireOrFleet === 'string' ? fleetId : empireOrFleet.id;
  const resolvedUnits = typeof empireOrFleet === 'string' ? units : empireOrFleet.ships;
  const active = selectActiveCommanderShip(
    getEmpireCommandState(state.commanders, empireId),
    resolvedFleetId,
    resolvedUnits,
  );
  const ability = active?.definition.commanderAbility;
  if (active === undefined || ability === undefined) return NO_COMMANDER_EFFECTS;

  const basisPoints = ability.effectPerLevelBasisPoints * active.level;
  const percent = percentFromBasisPoints(basisPoints);
  const base = {
    ...NO_COMMANDER_EFFECTS,
    activeCommanderId: active.unitId,
    activeEffect: ability.effect,
    level: active.level,
  };

  switch (ability.effect) {
    case 'attack':
    case 'critical':
      return { ...base, weaponBonusPercent: percent };
    case 'vitality':
    case 'repulse':
    case 'planet-shield':
      return {
        ...base,
        armorBonusPercent: percent,
        planetDestructionReductionBasisPoints:
          ability.effect === 'planet-shield' ? basisPoints : 0,
      };
    case 'paralysis':
      return { ...base, enemyWeaponPenaltyPercent: percent };
    case 'armor-break':
      return { ...base, enemyArmorPenaltyPercent: percent };
    case 'speed':
      return { ...base, speedBonusPercent: percent };
    case 'plunder':
      return { ...base, plunderBonusPercent: percent };
    case 'recovery':
      return { ...base, recoveryPermille: Math.max(1, Math.floor(basisPoints / 10)) };
    case 'spy-detection':
      return { ...base, spyDetectionBasisPoints: basisPoints };
    case 'demolition':
      return { ...base, demolitionBasisPoints: basisPoints };
    case 'upgrade-points':
      return { ...base, cargoBonusPercent: percent, upgradePointsPercent: percent };
  }
}

export function getFleetCommanderEffects(
  state: Pick<GameState, 'commanders'>,
  fleet: Pick<FleetState, 'id' | 'empireId' | 'ships'>,
): CommanderFleetEffects {
  return getCommanderFleetEffects(state, fleet);
}

export function countCommanderShipForEmpire(
  state: Pick<GameState, 'planets' | 'fleets'>,
  empireId: string,
  commanderId: string,
): number {
  const planets = state.planets
    .filter((planet) => planet.ownerEmpireId === empireId)
    .reduce((total, planet) =>
      total +
      (planet.inventory.ships[commanderId] ?? 0) +
      planet.productionQueues.shipyard.reduce(
        (queued, item) => queued + (item.unitId === commanderId ? item.quantity : 0),
        0,
      ), 0);
  return state.fleets
    .filter((fleet) => fleet.empireId === empireId)
    .reduce((total, fleet) => total + (fleet.ships[commanderId] ?? 0), planets);
}

export function recoverFleetShipsWithCommander(
  initial: Readonly<Record<string, number>>,
  remaining: Readonly<Record<string, number>>,
  recoveryPermille: number,
  seed: number,
): Readonly<Record<string, number>> {
  if (recoveryPermille <= 0) return remaining;
  const result = { ...remaining };
  for (const unitId of Object.keys(initial).sort()) {
    const definition = getUnitDefinition(unitId);
    if (definition?.kind !== 'ship' || definition.commanderClass !== undefined) continue;
    const destroyed = Math.max(0, (initial[unitId] ?? 0) - (remaining[unitId] ?? 0));
    if (destroyed <= 0) continue;
    const scaled = destroyed * recoveryPermille;
    const guaranteed = Math.floor(scaled / 1_000);
    const remainder = scaled % 1_000;
    const bonus = hashText(`${seed}:${unitId}:commander-recovery`) % 1_000 < remainder ? 1 : 0;
    const recovered = Math.min(destroyed, guaranteed + bonus);
    if (recovered > 0) result[unitId] = (result[unitId] ?? 0) + recovered;
  }
  return result;
}
----- FILE: src/simulation/command/types.ts (16 lines) -----
export type CommandDoctrineId = 'vanguard' | 'sentinel' | 'adaptive';

export interface EmpireCommandState {
  readonly empireId: string;
  readonly doctrineId: CommandDoctrineId;
  readonly experience: number;
  readonly level: number;
  readonly flagshipFleetId: string | null;
}

export interface CommandCombatEffects {
  readonly weaponBonusPercent: number;
  readonly armorBonusPercent: number;
  readonly experiencePermille: number;
  readonly isFlagship: boolean;
}
----- FILE: src/simulation/fleets/fleetCalculations.ts (69 lines) -----
import type { ResourceCost } from '../economy/types';
import { getUnitDefinition } from '../units/catalog';

export interface FleetCompositionSummary {
  readonly speed: number;
  readonly cargoCapacity: number;
  readonly shipCount: number;
}

export function validateShipComposition(
  ships: Readonly<Record<string, number>>,
): readonly string[] {
  const errors: string[] = [];

  if (Object.keys(ships).length === 0) {
    errors.push('EMPTY_FLEET');
  }

  for (const [unitId, quantity] of Object.entries(ships)) {
    const definition = getUnitDefinition(unitId);
    if (definition === undefined || definition.kind !== 'ship') {
      errors.push(`UNKNOWN_SHIP:${unitId}`);
    } else if (!Number.isInteger(quantity) || quantity <= 0) {
      errors.push(`INVALID_SHIP_QUANTITY:${unitId}`);
    }
  }

  return errors;
}

export function calculateFleetComposition(
  ships: Readonly<Record<string, number>>,
): FleetCompositionSummary {
  const errors = validateShipComposition(ships);
  if (errors.length > 0) {
    throw new Error(`Invalid fleet composition: ${errors.join(', ')}`);
  }

  let speed = Number.POSITIVE_INFINITY;
  let cargoCapacity = 0;
  let shipCount = 0;

  for (const [unitId, quantity] of Object.entries(ships)) {
    const definition = getUnitDefinition(unitId);
    if (definition === undefined || definition.kind !== 'ship') continue;
    speed = Math.min(speed, definition.stats.speed);
    cargoCapacity += definition.stats.cargo * quantity;
    shipCount += quantity;
  }

  return {
    speed: Number.isFinite(speed) ? speed : 0,
    cargoCapacity,
    shipCount,
  };
}

export function getCargoAmount(cargo: ResourceCost): number {
  return cargo.metal + cargo.crystal + cargo.gas;
}

export function validateCargo(cargo: ResourceCost): readonly string[] {
  return (['metal', 'crystal', 'gas'] as const).flatMap((resourceId) => {
    const value = cargo[resourceId];
    return Number.isInteger(value) && value >= 0
      ? []
      : [`INVALID_CARGO:${resourceId}`];
  });
}
----- FILE: src/simulation/fleets/fleetCommands.ts (208 lines) -----
import { appendCommandHistory } from '../history/stateHistory';
import type { PlanetEconomyState, ResourceCost, ResourceId } from '../economy/types';
import type { PlanetState } from '../planet/types';
import type {
  CommandLogEntry,
  CommandResult,
  GameCommand,
  GameState,
} from '../types';
import { applyCargoUpgrades } from '../upgrades/shipUpgrades';
import {
  calculateFleetComposition,
  getCargoAmount,
  validateCargo,
  validateShipComposition,
} from './fleetCalculations';
import type { FleetState } from './types';

const RESOURCE_IDS: readonly ResourceId[] = ['metal', 'crystal', 'gas'];

function appendCommand(state: GameState, command: GameCommand): readonly CommandLogEntry[] {
  return appendCommandHistory(state.commandLog, command);
}

function replacePlanet(
  planets: readonly PlanetState[],
  replacement: PlanetState,
): readonly PlanetState[] {
  return planets.map((planet) => (planet.id === replacement.id ? replacement : planet));
}

function updateCargo(
  economy: PlanetEconomyState,
  cargo: ResourceCost,
  direction: -1 | 1,
): PlanetEconomyState {
  return {
    ...economy,
    resources: {
      metal: {
        ...economy.resources.metal,
        amount: economy.resources.metal.amount + cargo.metal * direction,
      },
      crystal: {
        ...economy.resources.crystal,
        amount: economy.resources.crystal.amount + cargo.crystal * direction,
      },
      gas: {
        ...economy.resources.gas,
        amount: economy.resources.gas.amount + cargo.gas * direction,
      },
    },
  };
}

export function createFleet(
  state: GameState,
  command: Extract<GameCommand, { readonly type: 'CREATE_FLEET' }>,
): CommandResult<GameState> {
  const planet = state.planets.find((candidate) => candidate.id === command.planetId);
  if (planet === undefined) {
    return { ok: false, code: 'PLANET_NOT_FOUND', message: 'Fleet planet not found.' };
  }
  if (planet.ownerEmpireId !== command.empireId) {
    return { ok: false, code: 'NOT_PLANET_OWNER', message: 'Empire does not own the fleet planet.' };
  }

  const compositionErrors = validateShipComposition(command.ships);
  const cargoErrors = validateCargo(command.cargo);
  if (compositionErrors.length > 0 || cargoErrors.length > 0) {
    return {
      ok: false,
      code: 'INVALID_FLEET_LOADOUT',
      message: 'Fleet composition or cargo is invalid.',
      details: { compositionErrors, cargoErrors },
    };
  }

  for (const [unitId, quantity] of Object.entries(command.ships)) {
    if ((planet.inventory.ships[unitId] ?? 0) < quantity) {
      return {
        ok: false,
        code: 'INSUFFICIENT_SHIPS',
        message: 'Planet does not have enough ships for the fleet.',
      };
    }
  }
  if (
    RESOURCE_IDS.some(
      (resourceId) =>
        planet.economy.resources[resourceId].amount < command.cargo[resourceId],
    )
  ) {
    return {
      ok: false,
      code: 'INSUFFICIENT_CARGO_RESOURCES',
      message: 'Planet does not have enough resources for fleet cargo.',
    };
  }

  const composition = calculateFleetComposition(command.ships);
  const cargoCapacity = applyCargoUpgrades(
    state.shipUpgrades,
    command.empireId,
    command.ships,
    composition.cargoCapacity,
  );
  if (getCargoAmount(command.cargo) > cargoCapacity) {
    return {
      ok: false,
      code: 'FLEET_CARGO_OVER_CAPACITY',
      message: 'Fleet cargo exceeds its upgraded capacity.',
    };
  }

  const remainingShips = { ...planet.inventory.ships };
  for (const [unitId, quantity] of Object.entries(command.ships)) {
    const remaining = (remainingShips[unitId] ?? 0) - quantity;
    if (remaining === 0) delete remainingShips[unitId];
    else remainingShips[unitId] = remaining;
  }

  const fleet: FleetState = {
    id: `fleet-${state.nextEventSequence}`,
    empireId: command.empireId,
    originPlanetId: planet.id,
    location: { type: 'planet', planetId: planet.id },
    status: 'stationed',
    ships: { ...command.ships },
    cargo: { ...command.cargo },
    speed: composition.speed,
    cargoCapacity,
    formation: 'line',
    targetPriority: 'balanced',
    mission: null,
  };
  const updatedPlanet: PlanetState = {
    ...planet,
    inventory: { ...planet.inventory, ships: remainingShips },
    economy: updateCargo(planet.economy, command.cargo, -1),
  };

  return {
    ok: true,
    value: {
      ...state,
      planets: replacePlanet(state.planets, updatedPlanet),
      fleets: [...state.fleets, fleet],
      nextEventSequence: state.nextEventSequence + 1,
      commandLog: appendCommand(state, command),
    },
  };
}

export function disbandFleet(
  state: GameState,
  command: Extract<GameCommand, { readonly type: 'DISBAND_FLEET' }>,
): CommandResult<GameState> {
  const fleet = state.fleets.find((candidate) => candidate.id === command.fleetId);
  if (fleet === undefined) {
    return { ok: false, code: 'FLEET_NOT_FOUND', message: 'Fleet not found.' };
  }
  if (fleet.empireId !== command.empireId) {
    return { ok: false, code: 'NOT_FLEET_OWNER', message: 'Empire does not own the fleet.' };
  }
  if (fleet.status !== 'stationed' || fleet.location.type !== 'planet') {
    return { ok: false, code: 'FLEET_NOT_STATIONED', message: 'Only a stationed fleet can be disbanded.' };
  }

  const fleetPlanetId = fleet.location.planetId;
  const planet = state.planets.find((candidate) => candidate.id === fleetPlanetId);
  if (planet === undefined || planet.ownerEmpireId !== command.empireId) {
    return { ok: false, code: 'FLEET_PLANET_NOT_AVAILABLE', message: 'Fleet planet is unavailable.' };
  }
  if (
    RESOURCE_IDS.some(
      (resourceId) =>
        planet.economy.resources[resourceId].amount + fleet.cargo[resourceId] >
        planet.economy.resources[resourceId].capacity,
    )
  ) {
    return {
      ok: false,
      code: 'FLEET_CARGO_RETURN_BLOCKED',
      message: 'Planet storage cannot accept fleet cargo.',
    };
  }

  const ships = { ...planet.inventory.ships };
  for (const [unitId, quantity] of Object.entries(fleet.ships)) {
    ships[unitId] = (ships[unitId] ?? 0) + quantity;
  }
  const updatedPlanet: PlanetState = {
    ...planet,
    inventory: { ...planet.inventory, ships },
    economy: updateCargo(planet.economy, fleet.cargo, 1),
  };

  return {
    ok: true,
    value: {
      ...state,
      planets: replacePlanet(state.planets, updatedPlanet),
      fleets: state.fleets.filter((candidate) => candidate.id !== fleet.id),
      commandLog: appendCommand(state, command),
    },
  };
}
----- FILE: src/simulation/fleets/fleetDoctrineCommands.ts (58 lines) -----
import { appendCommandHistory } from '../history/stateHistory';
import {
  isFleetFormation,
  isFleetTargetPriority,
} from '../combat/fleetDoctrine';
import type {
  CommandLogEntry,
  CommandResult,
  GameCommand,
  GameState,
} from '../types';

function appendCommand(state: GameState, command: GameCommand): readonly CommandLogEntry[] {
  return appendCommandHistory(state.commandLog, command);
}

export function setFleetCombatDoctrine(
  state: GameState,
  command: Extract<GameCommand, { readonly type: 'SET_FLEET_COMBAT_DOCTRINE' }>,
): CommandResult<GameState> {
  const fleet = state.fleets.find((candidate) => candidate.id === command.fleetId);
  if (fleet === undefined) {
    return { ok: false, code: 'FLEET_NOT_FOUND', message: 'Fleet not found.' };
  }
  if (fleet.empireId !== command.empireId) {
    return { ok: false, code: 'NOT_FLEET_OWNER', message: 'Empire does not own the fleet.' };
  }
  if (fleet.status !== 'stationed' || fleet.location.type !== 'planet') {
    return {
      ok: false,
      code: 'FLEET_DOCTRINE_LOCKED_IN_FLIGHT',
      message: 'Combat doctrine can only be changed while the fleet is stationed.',
    };
  }
  if (!isFleetFormation(command.formation) || !isFleetTargetPriority(command.targetPriority)) {
    return {
      ok: false,
      code: 'INVALID_FLEET_DOCTRINE',
      message: 'Fleet formation or target priority is invalid.',
    };
  }
  return {
    ok: true,
    value: {
      ...state,
      fleets: state.fleets.map((candidate) =>
        candidate.id === fleet.id
          ? {
              ...candidate,
              formation: command.formation,
              targetPriority: command.targetPriority,
            }
          : candidate,
      ),
      commandLog: appendCommand(state, command),
    },
  };
}
----- FILE: src/simulation/fleets/flightCalculations.ts (146 lines) -----
import type { GalaxyModel, PlanetModel, StarSystemModel } from '../galaxy/types';
import type { PlanetState } from '../planet/types';
import {
  calculateCoordinateDistance,
  coordinateFromLegacyReference,
  parseSystemCoordinate,
} from '../space/coordinates';
import { calculateFleetComposition } from './fleetCalculations';
import type { FleetState } from './types';

export interface FlightEstimate {
  readonly distance: number;
  readonly durationSeconds: number;
  readonly fuelCost: number;
}

function requireSystem(galaxy: GalaxyModel, systemId: string): StarSystemModel {
  const system = galaxy.systems.find((candidate) => candidate.id === systemId);
  if (system === undefined) throw new Error(`Galaxy system not found: ${systemId}`);
  return system;
}

function findGalaxyPlanet(
  galaxy: GalaxyModel,
  galaxyPlanetId: string,
): { readonly system: StarSystemModel; readonly planet: PlanetModel } | undefined {
  for (const system of galaxy.systems) {
    const planet = system.planets.find((candidate) => candidate.id === galaxyPlanetId);
    if (planet !== undefined) return { system, planet };
  }
  return undefined;
}

export function calculateTargetDistance(
  galaxy: GalaxyModel,
  origin: PlanetState,
  targetSystemId: string,
  targetPosition: number,
): number {
  const parsedTarget = parseSystemCoordinate(targetSystemId);
  const targetSystem = parsedTarget === undefined ? requireSystem(galaxy, targetSystemId) : undefined;
  const targetCoordinate = {
    galaxy: parsedTarget?.galaxy ?? targetSystem?.galaxy ?? galaxy.galaxy,
    solarSystem: parsedTarget?.solarSystem ?? targetSystem?.solarSystem ?? 1,
    position: targetPosition,
  };
  const originCoordinate = origin.coordinate ??
    coordinateFromLegacyReference(origin.systemId, origin.position);
  if (originCoordinate === undefined) throw new Error(`Planet coordinate not found: ${origin.id}`);
  return calculateCoordinateDistance(originCoordinate, targetCoordinate);
}

export function calculatePlanetDistance(
  galaxy: GalaxyModel,
  origin: PlanetState,
  target: PlanetState,
): number {
  if (origin.id === target.id) return 0;
  if (origin.coordinate !== undefined && target.coordinate !== undefined) {
    return calculateCoordinateDistance(origin.coordinate, target.coordinate);
  }
  return calculateTargetDistance(galaxy, origin, target.systemId, target.position);
}

export function calculateFlightDuration(
  distance: number,
  speed: number,
  speedBonusPercent = 0,
): number {
  if (!Number.isInteger(distance) || distance < 0) {
    throw new Error('Flight distance must be a non-negative integer.');
  }
  if (!Number.isInteger(speed) || speed <= 0) {
    throw new Error('Fleet speed must be a positive integer.');
  }
  const effectiveSpeed = Math.max(
    1,
    Math.floor((speed * (100 + Math.max(0, speedBonusPercent))) / 100),
  );
  return Math.max(1, Math.ceil((distance * 60) / effectiveSpeed));
}

export function calculateFlightFuel(distance: number, fleet: FleetState): number {
  return Math.max(
    1,
    Math.ceil((distance * calculateFleetComposition(fleet.ships).shipCount) / 25),
  );
}

function createEstimate(
  distance: number,
  fleet: FleetState,
  speedBonusPercent: number,
): FlightEstimate {
  return {
    distance,
    durationSeconds: calculateFlightDuration(distance, fleet.speed, speedBonusPercent),
    fuelCost: calculateFlightFuel(distance, fleet),
  };
}

export function estimateFlight(
  galaxy: GalaxyModel,
  planets: readonly PlanetState[],
  fleet: FleetState,
  targetPlanetId: string,
  speedBonusPercent = 0,
): FlightEstimate {
  const location = fleet.location;
  if (location.type !== 'planet') {
    throw new Error('Only a stationed fleet can estimate a new flight.');
  }
  const origin = planets.find((planet) => planet.id === location.planetId);
  const target = planets.find((planet) => planet.id === targetPlanetId);
  if (origin === undefined || target === undefined) {
    throw new Error('Flight origin or target planet not found.');
  }
  return createEstimate(
    calculatePlanetDistance(galaxy, origin, target),
    fleet,
    speedBonusPercent,
  );
}

export function estimateFlightToGalaxyPlanet(
  galaxy: GalaxyModel,
  planets: readonly PlanetState[],
  fleet: FleetState,
  galaxyPlanetId: string,
  speedBonusPercent = 0,
): FlightEstimate {
  const location = fleet.location;
  if (location.type !== 'planet') {
    throw new Error('Only a stationed fleet can estimate a new flight.');
  }
  const origin = planets.find((planet) => planet.id === location.planetId);
  const target = findGalaxyPlanet(galaxy, galaxyPlanetId);
  if (origin === undefined || target === undefined) {
    throw new Error('Flight origin or galaxy target not found.');
  }
  return createEstimate(
    calculateCoordinateDistance(origin.coordinate, target.planet.coordinate),
    fleet,
    speedBonusPercent,
  );
}
----- FILE: src/simulation/fleets/types.ts (48 lines) -----
import type { FleetFormation, FleetTargetPriority } from '../combat/fleetDoctrine';
import type { ResourceCost } from '../economy/types';

export type FleetStatus = 'stationed' | 'outbound' | 'holding' | 'returning';
export type FleetMissionKind =
  | 'deploy'
  | 'transport'
  | 'scout'
  | 'attack'
  | 'recycle'
  | 'colonize'
  | 'expedition'
  | 'space-object';

export interface FleetMission {
  readonly kind: FleetMissionKind;
  readonly targetPlanetId: string;
}

export interface FleetPlanetLocation {
  readonly type: 'planet';
  readonly planetId: string;
}

export interface FleetTransitLocation {
  readonly type: 'transit';
  readonly fromPlanetId: string;
  readonly toPlanetId: string;
  readonly departedAt: number;
  readonly arrivesAt: number;
}

export type FleetLocation = FleetPlanetLocation | FleetTransitLocation;

export interface FleetState {
  readonly id: string;
  readonly empireId: string;
  readonly originPlanetId: string;
  readonly location: FleetLocation;
  readonly status: FleetStatus;
  readonly ships: Readonly<Record<string, number>>;
  readonly cargo: ResourceCost;
  readonly speed: number;
  readonly cargoCapacity: number;
  readonly formation?: FleetFormation;
  readonly targetPriority?: FleetTargetPriority;
  readonly mission: FleetMission | null;
}
----- FILE: src/simulation/galaxy/intelligenceView.ts (242 lines) -----
import { getEmpireIntelligence } from '../intelligence/intelligenceState';
import type { IntelObservation, IntelPlanetSnapshot } from '../intelligence/types';
import type { GameState } from '../types';
import type { PlanetBiome, StarClass } from './types';

export type GalaxyIntelVisibility = 'owned' | 'current' | 'stale' | 'contact' | 'unclaimed';
export type GalaxyOwnerFilter = 'all' | 'self' | 'foreign' | 'unclaimed';

export interface GalaxyIntelPlanet {
  readonly galaxyPlanetId: string;
  readonly colonyId: string | null;
  readonly systemId: string;
  readonly systemName: string;
  readonly systemX: number;
  readonly systemY: number;
  readonly starClass: StarClass;
  readonly position: number;
  readonly biome: PlanetBiome;
  readonly size: number;
  readonly visibility: GalaxyIntelVisibility;
  readonly displayName: string;
  readonly ownerEmpireId: string | null;
  readonly factionId: IntelPlanetSnapshot['factionId'] | null;
  readonly observedAt: number | null;
  readonly expiresAt: number | null;
  readonly resources: IntelPlanetSnapshot['resources'] | null;
  readonly buildings: Readonly<Record<string, number>> | null;
  readonly defenses: Readonly<Record<string, number>> | null;
  readonly fleets: IntelPlanetSnapshot['stationedFleets'] | null;
}

export interface GalaxyIntelQuery {
  readonly search?: string;
  readonly owner?: GalaxyOwnerFilter;
  readonly visibility?: GalaxyIntelVisibility | 'all';
  readonly biome?: PlanetBiome | 'all';
  readonly minimumSize?: number;
}

export interface GalaxyIntelSummary {
  readonly totalPositions: number;
  readonly owned: number;
  readonly current: number;
  readonly stale: number;
  readonly contacts: number;
  readonly unclaimed: number;
}

function latestObservations(
  state: GameState,
  empireId: string,
): ReadonlyMap<string, IntelObservation> {
  const intelligence = getEmpireIntelligence(state.intelligence, empireId);
  const result = new Map<string, IntelObservation>();
  const sorted = [...(intelligence?.observations ?? [])].sort(
    (left, right) => right.observedAt - left.observedAt || left.id.localeCompare(right.id),
  );
  for (const observation of sorted) {
    const planet = state.planets.find((candidate) => candidate.id === observation.targetPlanetId);
    if (planet !== undefined && !result.has(planet.galaxyPlanetId)) {
      result.set(planet.galaxyPlanetId, observation);
    }
  }
  return result;
}

export function createGalaxyIntelligenceView(
  state: GameState,
  empireId: string,
): readonly GalaxyIntelPlanet[] {
  const observations = latestObservations(state, empireId);
  return state.galaxy.systems
    .flatMap((system) =>
      system.planets.map((planet): GalaxyIntelPlanet => {
        const colony = state.planets.find(
          (candidate) => candidate.galaxyPlanetId === planet.id,
        );
        if (colony === undefined) {
          return {
            galaxyPlanetId: planet.id,
            colonyId: null,
            systemId: system.id,
            systemName: system.name,
            systemX: system.x,
            systemY: system.y,
            starClass: system.starClass,
            position: planet.position,
            biome: planet.biome,
            size: planet.size,
            visibility: 'unclaimed',
            displayName: `${system.name} · позиция ${planet.position}`,
            ownerEmpireId: null,
            factionId: null,
            observedAt: null,
            expiresAt: null,
            resources: null,
            buildings: null,
            defenses: null,
            fleets: null,
          };
        }

        if (colony.ownerEmpireId === empireId) {
          return {
            galaxyPlanetId: planet.id,
            colonyId: colony.id,
            systemId: system.id,
            systemName: system.name,
            systemX: system.x,
            systemY: system.y,
            starClass: system.starClass,
            position: planet.position,
            biome: planet.biome,
            size: planet.size,
            visibility: 'owned',
            displayName: colony.name,
            ownerEmpireId: empireId,
            factionId: colony.factionId,
            observedAt: state.clock.elapsedSeconds,
            expiresAt: null,
            resources: {
              metal: colony.economy.resources.metal.amount,
              crystal: colony.economy.resources.crystal.amount,
              gas: colony.economy.resources.gas.amount,
              energyProduced: colony.economy.energy.produced,
              energyConsumed: colony.economy.energy.consumed,
            },
            buildings: Object.fromEntries(
              colony.buildings.map((building) => [building.buildingId, building.level]),
            ),
            defenses: { ...colony.inventory.defenses },
            fleets: state.fleets
              .filter(
                (fleet) =>
                  fleet.empireId === empireId &&
                  fleet.status === 'stationed' &&
                  fleet.location.type === 'planet' &&
                  fleet.location.planetId === colony.id,
              )
              .map((fleet) => ({ fleetId: fleet.id, ships: { ...fleet.ships } })),
          };
        }

        const observation = observations.get(planet.id);
        if (observation === undefined) {
          return {
            galaxyPlanetId: planet.id,
            colonyId: colony.id,
            systemId: system.id,
            systemName: system.name,
            systemX: system.x,
            systemY: system.y,
            starClass: system.starClass,
            position: planet.position,
            biome: planet.biome,
            size: planet.size,
            visibility: 'contact',
            displayName: `${system.name} · неизвестный контакт`,
            ownerEmpireId: null,
            factionId: null,
            observedAt: null,
            expiresAt: null,
            resources: null,
            buildings: null,
            defenses: null,
            fleets: null,
          };
        }

        const current = observation.expiresAt > state.clock.elapsedSeconds;
        return {
          galaxyPlanetId: planet.id,
          colonyId: colony.id,
          systemId: system.id,
          systemName: system.name,
          systemX: system.x,
          systemY: system.y,
          starClass: system.starClass,
          position: planet.position,
          biome: planet.biome,
          size: planet.size,
          visibility: current ? 'current' : 'stale',
          displayName: observation.snapshot.name,
          ownerEmpireId: observation.snapshot.ownerEmpireId,
          factionId: observation.snapshot.factionId,
          observedAt: observation.observedAt,
          expiresAt: observation.expiresAt,
          resources: observation.snapshot.resources ?? null,
          buildings: observation.snapshot.buildings ?? null,
          defenses: observation.snapshot.defenses ?? null,
          fleets: observation.snapshot.stationedFleets ?? null,
        };
      }),
    )
    .sort(
      (left, right) =>
        left.systemName.localeCompare(right.systemName) || left.position - right.position,
    );
}

export function filterGalaxyIntelligence(
  planets: readonly GalaxyIntelPlanet[],
  query: GalaxyIntelQuery,
): readonly GalaxyIntelPlanet[] {
  const search = query.search?.trim().toLocaleLowerCase('ru-RU') ?? '';
  const owner = query.owner ?? 'all';
  const visibility = query.visibility ?? 'all';
  const biome = query.biome ?? 'all';
  const minimumSize = query.minimumSize ?? 0;
  return planets.filter((planet) => {
    if (search.length > 0) {
      const haystack = `${planet.displayName} ${planet.systemName} ${planet.systemId} ${planet.galaxyPlanetId}`
        .toLocaleLowerCase('ru-RU');
      if (!haystack.includes(search)) return false;
    }
    if (visibility !== 'all' && planet.visibility !== visibility) return false;
    if (biome !== 'all' && planet.biome !== biome) return false;
    if (planet.size < minimumSize) return false;
    if (owner === 'self' && planet.visibility !== 'owned') return false;
    if (
      owner === 'foreign' &&
      !['current', 'stale', 'contact'].includes(planet.visibility)
    ) {
      return false;
    }
    if (owner === 'unclaimed' && planet.visibility !== 'unclaimed') return false;
    return true;
  });
}

export function summarizeGalaxyIntelligence(
  planets: readonly GalaxyIntelPlanet[],
): GalaxyIntelSummary {
  return {
    totalPositions: planets.length,
    owned: planets.filter((planet) => planet.visibility === 'owned').length,
    current: planets.filter((planet) => planet.visibility === 'current').length,
    stale: planets.filter((planet) => planet.visibility === 'stale').length,
    contacts: planets.filter((planet) => planet.visibility === 'contact').length,
    unclaimed: planets.filter((planet) => planet.visibility === 'unclaimed').length,
  };
}
----- FILE: src/simulation/intelligence/intelligenceState.ts (30 lines) -----
import type {
  EmpireIntelligenceState,
  IntelObservation,
} from './types';

export function createInitialIntelligenceStates(
  empireIds: readonly string[],
): readonly EmpireIntelligenceState[] {
  return empireIds.map((empireId) => ({
    empireId,
    observations: [],
    alerts: [],
  }));
}

export function getEmpireIntelligence(
  states: readonly EmpireIntelligenceState[],
  empireId: string,
): EmpireIntelligenceState | undefined {
  return states.find((state) => state.empireId === empireId);
}

export function getCurrentObservations(
  state: EmpireIntelligenceState,
  elapsedSeconds: number,
): readonly IntelObservation[] {
  return state.observations.filter(
    (observation) => observation.expiresAt > elapsedSeconds,
  );
}
----- FILE: src/simulation/intelligence/resolveScout.ts (163 lines) -----
import { retainNewest, STATE_HISTORY_LIMITS } from '../history/stateHistory';
import { getFactionMechanicalRoles } from '../factions/factionMechanicalRoles';
import { getResearchEffectsForEmpire } from '../factions/factionResearchEffects';
import type { FleetState } from '../fleets/types';
import { getBuildingLevel } from '../planet/buildingProgression';
import type { PlanetState } from '../planet/types';
import type { GameState } from '../types';
import { getShipCountByRole } from '../units/shipCapabilities';
import { getEmpireIntelligence } from './intelligenceState';
import type {
  EmpireIntelligenceState,
  IntelPlanetSnapshot,
  IntelligenceAlert,
} from './types';

function hashText(value: string): number {
  let hash = 2_166_136_261;
  for (let index = 0; index < value.length; index += 1) {
    hash ^= value.charCodeAt(index);
    hash = Math.imul(hash, 16_777_619);
  }
  return hash >>> 0;
}

function replaceIntelligence(
  states: readonly EmpireIntelligenceState[],
  replacement: EmpireIntelligenceState,
): readonly EmpireIntelligenceState[] {
  return states.map((state) =>
    state.empireId === replacement.empireId ? replacement : state,
  );
}

function getSensorStrength(state: GameState, empireId: string): number {
  return getResearchEffectsForEmpire(state, empireId).sensorStrength;
}

function createSnapshot(
  state: GameState,
  target: PlanetState,
  level: 1 | 2 | 3,
): IntelPlanetSnapshot {
  const snapshot: IntelPlanetSnapshot = {
    planetId: target.id,
    coordinate: target.coordinate,
    name: target.name,
    ownerEmpireId: target.ownerEmpireId,
    factionId: target.factionId,
    level,
  };

  if (level === 1) return snapshot;

  const withEconomy: IntelPlanetSnapshot = {
    ...snapshot,
    resources: {
      metal: target.economy.resources.metal.amount,
      crystal: target.economy.resources.crystal.amount,
      gas: target.economy.resources.gas.amount,
      energyProduced: target.economy.energy.produced,
      energyConsumed: target.economy.energy.consumed,
    },
    buildings: Object.fromEntries(
      target.buildings.map((building) => [building.buildingId, building.level]),
    ),
  };

  if (level === 2) return withEconomy;

  return {
    ...withEconomy,
    defenses: { ...target.inventory.defenses },
    stationedFleets: state.fleets
      .filter(
        (fleet) =>
          fleet.status === 'stationed' &&
          fleet.location.type === 'planet' &&
          fleet.location.planetId === target.id,
      )
      .map((fleet) => ({ fleetId: fleet.id, ships: { ...fleet.ships } })),
  };
}

export function resolveScoutArrival(
  state: GameState,
  fleet: FleetState,
  target: PlanetState,
  eventSequence: number,
): GameState {
  const observer = getEmpireIntelligence(state.intelligence, fleet.empireId);
  if (observer === undefined) return state;

  const scoutCount = getShipCountByRole(fleet.ships, 'scout');
  const observerStrength = getSensorStrength(state, fleet.empireId) + scoutCount;
  const targetSensorGrid = getFactionMechanicalRoles(target.factionId).buildings.sensorGrid;
  const counterStrength =
    getSensorStrength(state, target.ownerEmpireId) +
    getBuildingLevel(target.buildings, targetSensorGrid);
  const level = Math.max(
    1,
    Math.min(3, 1 + Math.floor(observerStrength / 2)),
  ) as 1 | 2 | 3;
  const detectionChance = Math.max(
    5,
    Math.min(90, 30 + counterStrength * 10 - observerStrength * 7),
  );
  const roll =
    hashText(`${state.seed}:${eventSequence}:${fleet.id}:${target.id}`) % 100;
  const detected = roll < detectionChance;
  const observedAt = state.clock.elapsedSeconds;
  const observation = {
    id: `intel-${eventSequence}-${fleet.id}`,
    observerEmpireId: fleet.empireId,
    targetPlanetId: target.id,
    coordinate: target.coordinate,
    observedAt,
    expiresAt: observedAt + (level + 1) * 86_400,
    detected,
    snapshot: createSnapshot(state, target, level),
  } as const;
  let intelligence = replaceIntelligence(state.intelligence, {
    ...observer,
    observations: retainNewest(
      [
        ...observer.observations.filter(
          (item) => item.targetPlanetId !== target.id,
        ),
        observation,
      ],
      STATE_HISTORY_LIMITS.intelligenceObservationsPerEmpire,
    ),
  });

  if (detected && target.ownerEmpireId !== fleet.empireId) {
    const defender = getEmpireIntelligence(intelligence, target.ownerEmpireId);
    if (defender !== undefined) {
      const confidence: IntelligenceAlert['confidence'] =
        counterStrength >= observerStrength + 2
          ? 'high'
          : counterStrength >= observerStrength
            ? 'medium'
            : 'low';
      const alert: IntelligenceAlert = {
        id: `alert-${eventSequence}-${fleet.id}`,
        empireId: target.ownerEmpireId,
        sourceEmpireId: confidence === 'low' ? null : fleet.empireId,
        targetPlanetId: target.id,
        coordinate: target.coordinate,
        detectedAt: observedAt,
        confidence,
      };
      intelligence = replaceIntelligence(intelligence, {
        ...defender,
        alerts: retainNewest(
          [...defender.alerts, alert],
          STATE_HISTORY_LIMITS.intelligenceAlertsPerEmpire,
        ),
      });
    }
  }

  return { ...state, intelligence };
}
----- FILE: src/simulation/intelligence/types.ts (53 lines) -----
import type { FactionId } from '../planet/types';
import type { SpaceCoordinate } from '../space/coordinates';

export interface IntelResourceSnapshot {
  readonly metal: number;
  readonly crystal: number;
  readonly gas: number;
  readonly energyProduced: number;
  readonly energyConsumed: number;
}

export interface IntelPlanetSnapshot {
  readonly planetId: string;
  readonly coordinate?: SpaceCoordinate;
  readonly name: string;
  readonly ownerEmpireId: string;
  readonly factionId: FactionId;
  readonly level: 1 | 2 | 3;
  readonly resources?: IntelResourceSnapshot;
  readonly buildings?: Readonly<Record<string, number>>;
  readonly defenses?: Readonly<Record<string, number>>;
  readonly stationedFleets?: readonly {
    readonly fleetId: string;
    readonly ships: Readonly<Record<string, number>>;
  }[];
}

export interface IntelObservation {
  readonly id: string;
  readonly observerEmpireId: string;
  readonly targetPlanetId: string;
  readonly coordinate?: SpaceCoordinate;
  readonly observedAt: number;
  readonly expiresAt: number;
  readonly detected: boolean;
  readonly snapshot: IntelPlanetSnapshot;
}

export interface IntelligenceAlert {
  readonly id: string;
  readonly empireId: string;
  readonly sourceEmpireId: string | null;
  readonly targetPlanetId: string;
  readonly coordinate?: SpaceCoordinate;
  readonly detectedAt: number;
  readonly confidence: 'low' | 'medium' | 'high';
}

export interface EmpireIntelligenceState {
  readonly empireId: string;
  readonly observations: readonly IntelObservation[];
  readonly alerts: readonly IntelligenceAlert[];
}
----- FILE: src/simulation/reports/missionReports.ts (387 lines) -----
import type {
  ProtectionType,
  TargetSize,
  WeaponType,
} from '../combat/combatProfiles';
import type { BattleRoundReport } from '../combat/types';
import type { ResourceCost } from '../economy/types';
import { PIRATE_EMPIRE_ID } from '../pve/neutralForces';
import type { GameState } from '../types';

export type MissionReportKind = 'battle' | 'expedition' | 'space-object' | 'world-event';
export type MissionReportMode = 'pve' | 'pvp' | 'system';
export type MissionReportOutcome = 'success' | 'failure' | 'draw' | 'completed' | 'recovered';
export type MissionCombatSide = 'attacker' | 'defender';

export interface MissionReportReward extends ResourceCost {
  readonly exoticMatter: number;
}

export interface MissionCombatWeaponContribution {
  readonly weaponType: WeaponType;
  readonly baseDamage: number;
  readonly modifierPermille: number;
}

export interface MissionCombatBreakdownLine {
  readonly round: number;
  readonly side: MissionCombatSide;
  readonly targetUnitId: string;
  readonly targetCount: number;
  readonly protectionType: ProtectionType;
  readonly targetSize: TargetSize;
  readonly allocatedBaseDamage: number;
  readonly weightedModifierPermille: number;
  readonly variancePermille: number;
  readonly effectiveDamage: number;
  readonly carriedDamage: number;
  readonly durability: number;
  readonly losses: number;
  readonly weaponContributions: readonly MissionCombatWeaponContribution[];
}

export interface UnifiedMissionReport {
  readonly id: string;
  readonly resolvedAt: number;
  readonly kind: MissionReportKind;
  readonly mode: MissionReportMode;
  readonly title: string;
  readonly summary: string;
  readonly targetId: string;
  readonly primaryEmpireId: string | null;
  readonly secondaryEmpireId: string | null;
  readonly outcome: MissionReportOutcome;
  readonly reward: MissionReportReward;
  readonly primaryLosses: Readonly<Record<string, number>>;
  readonly secondaryLosses: Readonly<Record<string, number>>;
  readonly threatMultiplierPermille: number;
  readonly rewardMultiplierPermille: number;
  readonly combatBreakdown?: readonly MissionCombatBreakdownLine[];
}

export interface MissionReportQuery {
  readonly kind?: MissionReportKind | 'all';
  readonly mode?: MissionReportMode | 'all';
  readonly empireId?: string | 'all';
  readonly search?: string;
}

export interface MissionReportSummary {
  readonly total: number;
  readonly pve: number;
  readonly pvp: number;
  readonly system: number;
  readonly successes: number;
  readonly rewards: MissionReportReward;
  readonly losses: number;
}

export interface EmpirePvePvpComparison {
  readonly empireId: string;
  readonly pveOperations: number;
  readonly pveSuccesses: number;
  readonly pvpBattles: number;
  readonly pvpWins: number;
  readonly reward: MissionReportReward;
  readonly losses: number;
}

const ZERO_REWARD: MissionReportReward = {
  metal: 0,
  crystal: 0,
  gas: 0,
  exoticMatter: 0,
};

function unitLosses(
  initial: Readonly<Record<string, number>>,
  remaining: Readonly<Record<string, number>>,
): Readonly<Record<string, number>> {
  return Object.fromEntries(
    Object.entries(initial)
      .map(([unitId, count]) => [unitId, Math.max(0, count - (remaining[unitId] ?? 0))] as const)
      .filter(([, count]) => count > 0),
  );
}

function addUnits(
  left: Readonly<Record<string, number>>,
  right: Readonly<Record<string, number>>,
): Readonly<Record<string, number>> {
  const result = { ...left };
  for (const [unitId, count] of Object.entries(right)) {
    result[unitId] = (result[unitId] ?? 0) + count;
  }
  return result;
}

function totalUnits(units: Readonly<Record<string, number>>): number {
  return Object.values(units).reduce((total, count) => total + count, 0);
}

function addReward(
  left: MissionReportReward,
  right: MissionReportReward,
): MissionReportReward {
  return {
    metal: left.metal + right.metal,
    crystal: left.crystal + right.crystal,
    gas: left.gas + right.gas,
    exoticMatter: left.exoticMatter + right.exoticMatter,
  };
}

function battleOutcome(
  winner: 'attacker' | 'defender' | 'draw',
): MissionReportOutcome {
  if (winner === 'attacker') return 'success';
  if (winner === 'defender') return 'failure';
  return 'draw';
}

function createCombatBreakdown(
  rounds: readonly BattleRoundReport[],
): readonly MissionCombatBreakdownLine[] {
  return rounds.flatMap((round) => {
    const attacker = (round.attackerTargetBreakdown ?? []).map(
      (target): MissionCombatBreakdownLine => ({
        round: round.round,
        side: 'attacker',
        targetUnitId: target.targetUnitId,
        targetCount: target.targetCount,
        protectionType: target.protectionType,
        targetSize: target.targetSize,
        allocatedBaseDamage: target.allocatedBaseDamage,
        weightedModifierPermille: target.weightedModifierPermille,
        variancePermille: target.variancePermille,
        effectiveDamage: target.effectiveDamage,
        carriedDamage: target.carriedDamage,
        durability: target.durability,
        losses: target.losses,
        weaponContributions: target.weaponContributions,
      }),
    );
    const defender = (round.defenderTargetBreakdown ?? []).map(
      (target): MissionCombatBreakdownLine => ({
        round: round.round,
        side: 'defender',
        targetUnitId: target.targetUnitId,
        targetCount: target.targetCount,
        protectionType: target.protectionType,
        targetSize: target.targetSize,
        allocatedBaseDamage: target.allocatedBaseDamage,
        weightedModifierPermille: target.weightedModifierPermille,
        variancePermille: target.variancePermille,
        effectiveDamage: target.effectiveDamage,
        carriedDamage: target.carriedDamage,
        durability: target.durability,
        losses: target.losses,
        weaponContributions: target.weaponContributions,
      }),
    );
    return [...attacker, ...defender];
  });
}

function createEventReports(state: GameState): UnifiedMissionReport[] {
  const reports: UnifiedMissionReport[] = [];
  for (const entry of state.eventLog) {
    const payload = entry.event.payload;
    if (payload.type === 'BATTLE_REPORT') {
      const report = payload.report;
      const mode = report.mode ?? (
        report.attackerEmpireId === PIRATE_EMPIRE_ID || report.defenderEmpireId === PIRATE_EMPIRE_ID
          ? 'pve'
          : 'pvp'
      );
      const defensesRecovered = report.defensesRecovered ?? {};
      const recoveredCount = totalUnits(defensesRecovered);
      reports.push({
        id: report.id,
        resolvedAt: entry.executedAt,
        kind: 'battle',
        mode,
        title: mode === 'pve' ? 'Бой с нейтральными силами' : 'Бой империй',
        summary: `${report.attackerEmpireId} → ${report.defenderEmpireId} · ${report.winner}${recoveredCount > 0 ? ` · восстановлено установок ${recoveredCount}` : ''}`,
        targetId: report.targetPlanetId,
        primaryEmpireId: report.attackerEmpireId,
        secondaryEmpireId: report.defenderEmpireId,
        outcome: battleOutcome(report.winner),
        reward: {
          metal: report.plunderedCargo?.metal ?? 0,
          crystal: report.plunderedCargo?.crystal ?? 0,
          gas: report.plunderedCargo?.gas ?? 0,
          exoticMatter: 0,
        },
        primaryLosses: unitLosses(report.attackerInitial, report.attackerRemaining),
        secondaryLosses: unitLosses(
          report.defenderInitial,
          addUnits(report.defenderRemaining, defensesRecovered),
        ),
        threatMultiplierPermille: report.threatMultiplierPermille ?? 1_000,
        rewardMultiplierPermille: report.rewardMultiplierPermille ?? 1_000,
        combatBreakdown: createCombatBreakdown(report.rounds),
      });
      continue;
    }
    if (payload.type === 'EXPEDITION_RESOLVE') {
      const report = payload.report;
      reports.push({
        id: report.id,
        resolvedAt: entry.executedAt,
        kind: 'expedition',
        mode: 'pve',
        title: 'Дальняя экспедиция',
        summary: `${report.outcome} · ${report.narrative}`,
        targetId: report.targetGalaxyPlanetId,
        primaryEmpireId: report.empireId,
        secondaryEmpireId: null,
        outcome: report.outcome === 'empty' ? 'failure' : 'success',
        reward: { ...report.reward, exoticMatter: 0 },
        primaryLosses: report.losses,
        secondaryLosses: {},
        threatMultiplierPermille: 1_000,
        rewardMultiplierPermille: report.rewardMultiplierPermille ?? 1_000,
      });
      continue;
    }
    if (payload.type === 'SPACE_OBJECT_MISSION_RESOLVE') {
      const report = payload.report;
      reports.push({
        id: report.id,
        resolvedAt: entry.executedAt,
        kind: 'space-object',
        mode: 'pve',
        title: 'Операция на космическом объекте',
        summary: report.narrative,
        targetId: report.objectId,
        primaryEmpireId: report.empireId,
        secondaryEmpireId: null,
        outcome: report.depletion > 0 ? 'success' : 'failure',
        reward: { ...report.reward },
        primaryLosses: report.losses,
        secondaryLosses: {},
        threatMultiplierPermille: 1_000,
        rewardMultiplierPermille: report.rewardMultiplierPermille ?? 1_000,
      });
    }
  }
  return reports;
}

function createWorldEventReports(state: GameState): UnifiedMissionReport[] {
  return state.worldEvents.history.map((event): UnifiedMissionReport => ({
    id: event.id,
    resolvedAt: event.completedAt,
    kind: 'world-event',
    mode: 'system',
    title: 'Мировое событие',
    summary: `${event.definitionId} · ${event.completion}`,
    targetId: event.targetId,
    primaryEmpireId: null,
    secondaryEmpireId: null,
    outcome: event.completion,
    reward: ZERO_REWARD,
    primaryLosses: {},
    secondaryLosses: {},
    threatMultiplierPermille: 1_000,
    rewardMultiplierPermille: 1_000,
  }));
}

export function createUnifiedMissionReports(
  state: GameState,
): readonly UnifiedMissionReport[] {
  return [...createEventReports(state), ...createWorldEventReports(state)]
    .sort((left, right) => right.resolvedAt - left.resolvedAt || left.id.localeCompare(right.id));
}

export function filterMissionReports(
  reports: readonly UnifiedMissionReport[],
  query: MissionReportQuery,
): readonly UnifiedMissionReport[] {
  const kind = query.kind ?? 'all';
  const mode = query.mode ?? 'all';
  const empireId = query.empireId ?? 'all';
  const search = query.search?.trim().toLocaleLowerCase('ru-RU') ?? '';
  return reports.filter((report) => {
    if (kind !== 'all' && report.kind !== kind) return false;
    if (mode !== 'all' && report.mode !== mode) return false;
    if (
      empireId !== 'all' &&
      report.primaryEmpireId !== empireId &&
      report.secondaryEmpireId !== empireId
    ) {
      return false;
    }
    if (search.length > 0) {
      const haystack = `${report.title} ${report.summary} ${report.targetId} ${report.primaryEmpireId ?? ''} ${report.secondaryEmpireId ?? ''}`
        .toLocaleLowerCase('ru-RU');
      if (!haystack.includes(search)) return false;
    }
    return true;
  });
}

export function summarizeMissionReports(
  reports: readonly UnifiedMissionReport[],
): MissionReportSummary {
  return reports.reduce<MissionReportSummary>(
    (summary, report) => ({
      total: summary.total + 1,
      pve: summary.pve + (report.mode === 'pve' ? 1 : 0),
      pvp: summary.pvp + (report.mode === 'pvp' ? 1 : 0),
      system: summary.system + (report.mode === 'system' ? 1 : 0),
      successes: summary.successes + (report.outcome === 'success' ? 1 : 0),
      rewards: addReward(summary.rewards, report.reward),
      losses: summary.losses + totalUnits(report.primaryLosses),
    }),
    {
      total: 0,
      pve: 0,
      pvp: 0,
      system: 0,
      successes: 0,
      rewards: ZERO_REWARD,
      losses: 0,
    },
  );
}

export function compareEmpirePvePvp(
  state: GameState,
): readonly EmpirePvePvpComparison[] {
  const reports = createUnifiedMissionReports(state);
  return state.empires.map((empireId): EmpirePvePvpComparison => {
    const related = reports.filter(
      (report) => report.primaryEmpireId === empireId || report.secondaryEmpireId === empireId,
    );
    const reward = related
      .filter((report) => report.primaryEmpireId === empireId)
      .reduce((total, report) => addReward(total, report.reward), ZERO_REWARD);
    const losses = related.reduce((total, report) => {
      if (report.primaryEmpireId === empireId) return total + totalUnits(report.primaryLosses);
      if (report.secondaryEmpireId === empireId) return total + totalUnits(report.secondaryLosses);
      return total;
    }, 0);
    return {
      empireId,
      pveOperations: related.filter((report) => report.mode === 'pve').length,
      pveSuccesses: related.filter(
        (report) =>
          report.mode === 'pve' &&
          report.primaryEmpireId === empireId &&
          report.outcome === 'success',
      ).length,
      pvpBattles: related.filter((report) => report.mode === 'pvp').length,
      pvpWins: related.filter(
        (report) =>
          report.mode === 'pvp' &&
          ((report.primaryEmpireId === empireId && report.outcome === 'success') ||
            (report.secondaryEmpireId === empireId && report.outcome === 'failure')),
      ).length,
      reward,
      losses,
    };
  });
}
----- FILE: src/styles/commandDoctrine.css (47 lines) -----
.command-doctrine-dialog {
  width: min(1040px, calc(100vw - 32px));
  max-height: calc(100vh - 32px);
  overflow: auto;
  border: 1px solid var(--border-strong);
  border-radius: 20px;
  padding: 0;
  color: var(--text-primary);
  background: color-mix(in srgb, var(--surface-elevated) 94%, transparent);
}
.command-doctrine-dialog::backdrop { background: rgb(2 8 18 / 78%); backdrop-filter: blur(7px); }
.command-doctrine-header { display: flex; justify-content: space-between; gap: 24px; padding: 24px; border-bottom: 1px solid var(--border-subtle); }
.command-doctrine-header h2 { margin: 4px 0 6px; }
.command-doctrine-header p { margin: 0; color: var(--text-secondary); }
.command-progress, .command-doctrine-cards { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: 12px; padding: 18px 24px; }
.command-progress article, .command-doctrine-cards article { padding: 16px; border: 1px solid var(--border-subtle); border-radius: 14px; background: var(--surface-panel); }
.command-progress span { display: block; color: var(--text-secondary); font-size: .82rem; margin-bottom: 6px; }
.command-progress strong { display: block; font-size: 1.1rem; }
.command-progress small { display: block; margin-top: 6px; color: var(--text-secondary); }
.command-progress progress { width: 100%; margin-top: 10px; }
.command-doctrine-form, .flagship-form { display: grid; grid-template-columns: minmax(0, 1fr) auto; gap: 12px; align-items: end; padding: 12px 24px; }
.command-doctrine-form label, .flagship-form label { display: grid; gap: 7px; color: var(--text-secondary); }
.command-doctrine-form select, .flagship-form select { min-height: 42px; border: 1px solid var(--border-strong); border-radius: 10px; padding: 0 12px; color: var(--text-primary); background: var(--surface-panel); }
.command-doctrine-cards { padding-bottom: 24px; }
.command-doctrine-cards strong { display: block; margin-bottom: 8px; }
.command-doctrine-cards p { margin: 0; color: var(--text-secondary); line-height: 1.45; }
.commander-roster-heading { padding: 6px 24px 0; border-top: 1px solid var(--border-subtle); }
.commander-roster-heading h3 { margin: 4px 0 0; }
.commander-ship-cards { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 12px; padding: 18px 24px 24px; }
.commander-ship-card { overflow: hidden; padding: 16px; border: 1px solid var(--border-subtle); border-radius: 14px; background: var(--surface-panel); opacity: .78; }
.commander-ship-card.is-owned, .commander-ship-card.is-available { opacity: 1; border-color: var(--border-strong); }
.commander-ship-card.is-locked { filter: saturate(.55); }
.commander-ship-card__art { height: 190px; margin: -16px -16px 14px; background-position: center; background-repeat: no-repeat; }
.commander-ship-card.is-active { outline: 2px solid var(--accent-primary); outline-offset: 1px; }
.commander-ship-card header { display: flex; justify-content: space-between; gap: 14px; align-items: start; }
.commander-ship-card header span { max-width: 45%; text-align: right; color: var(--text-secondary); font-size: .78rem; }
.commander-ship-card p { margin: 10px 0; color: var(--text-secondary); line-height: 1.4; }
.commander-ship-card dl { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; margin: 12px 0; }
.commander-ship-card dl div { padding: 8px; border-radius: 9px; background: color-mix(in srgb, var(--surface-elevated) 75%, transparent); }
.commander-ship-card dt { color: var(--text-secondary); font-size: .72rem; }
.commander-ship-card dd { margin: 3px 0 0; font-size: .82rem; }
.commander-ship-card > small { color: var(--text-secondary); line-height: 1.35; }
@media (max-width: 720px) {
  .command-progress, .command-doctrine-cards, .commander-ship-cards { grid-template-columns: 1fr; }
  .command-doctrine-form, .flagship-form { grid-template-columns: 1fr; }
  .commander-ship-card dl { grid-template-columns: 1fr; }
}
----- FILE: src/styles/commandRanking.css (182 lines) -----
.command-ranking-dialog {
  width: min(1120px, calc(100vw - 24px));
  max-height: calc(100vh - 24px);
  padding: 0;
  overflow: auto;
  border: 1px solid rgba(94, 205, 249, 0.38);
  border-radius: 14px;
  color: #eefaff;
  background: linear-gradient(150deg, rgba(5, 18, 30, 0.99), rgba(1, 6, 12, 0.99));
  box-shadow: 0 34px 100px rgba(0, 0, 0, 0.8);
}

.command-ranking-dialog::backdrop {
  background: rgba(0, 4, 9, 0.84);
  backdrop-filter: blur(6px);
}

.command-ranking-header {
  display: flex;
  justify-content: space-between;
  padding: 22px 26px;
  border-bottom: 1px solid rgba(94, 205, 249, 0.16);
}

.command-ranking-header h2,
.command-ranking-header p,
.command-profile h3,
.command-profile p,
.command-ranking-list h3,
.command-ranking-list p {
  margin: 0;
}

.command-profile {
  position: relative;
  display: grid;
  grid-template-columns: minmax(220px, 0.42fr) minmax(0, 1fr);
  min-height: 330px;
  overflow: hidden;
  margin: 18px 22px;
  border: 1px solid rgba(94, 205, 249, 0.2);
  border-radius: 12px;
  background-image: linear-gradient(90deg, rgba(2, 8, 15, 0.25), rgba(2, 8, 15, 0.96) 48%), var(--command-profile-bg);
  background-size: cover;
  background-position: center;
}

.command-profile-hero {
  width: 100%;
  height: 100%;
  min-height: 330px;
  object-fit: contain;
  object-position: bottom center;
}

.command-profile-body {
  display: grid;
  align-content: center;
  gap: 22px;
  padding: 28px;
}

.command-profile-identity {
  display: grid;
  grid-template-columns: 72px minmax(0, 1fr);
  align-items: center;
  gap: 15px;
}

.command-profile-identity img {
  width: 72px;
  height: 72px;
  object-fit: contain;
}

.command-profile-identity h3 {
  margin-top: 4px;
  font-size: clamp(1.35rem, 3vw, 2.2rem);
}

.command-profile-identity div > p:last-child {
  margin-top: 6px;
  color: #93aaba;
}

.command-profile-stats {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 8px;
}

.command-profile-stats div {
  display: grid;
  gap: 4px;
  padding: 10px;
  border: 1px solid rgba(94, 205, 249, 0.14);
  border-radius: 8px;
  background: rgba(3, 15, 25, 0.72);
}

.command-profile-stats span {
  color: #7892a3;
  font-size: 0.68rem;
  text-transform: uppercase;
}

.command-ranking-list {
  padding: 4px 22px 24px;
}

.command-ranking-list > header {
  display: flex;
  justify-content: space-between;
  gap: 16px;
  align-items: end;
  margin-bottom: 10px;
}

.command-ranking-list > header p {
  color: #7892a3;
  font-size: 0.78rem;
}

.command-ranking-entry {
  display: grid;
  grid-template-columns: 52px 46px minmax(0, 1fr) auto;
  align-items: center;
  gap: 12px;
  min-height: 66px;
  padding: 9px 14px;
  border: 1px solid rgba(94, 205, 249, 0.12);
  background: rgba(4, 16, 27, 0.68);
}

.command-ranking-entry + .command-ranking-entry {
  border-top: 0;
}

.command-ranking-entry.is-player {
  border-color: rgba(225, 184, 85, 0.46);
  background: rgba(55, 42, 12, 0.4);
}

.command-ranking-entry img {
  width: 42px;
  height: 42px;
  object-fit: contain;
}

.command-ranking-entry > div {
  display: grid;
  gap: 3px;
}

.command-ranking-entry small {
  color: #7f98a9;
}

.command-ranking-place {
  color: #67d2fa;
  font-size: 1.15rem;
}

.command-ranking-entry b {
  color: #e7c36c;
  font-variant-numeric: tabular-nums;
}

@media (max-width: 760px) {
  .command-profile {
    grid-template-columns: 1fr;
  }

  .command-profile-hero {
    min-height: 220px;
    max-height: 260px;
  }

  .command-profile-stats {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}
----- FILE: src/styles/fleetDoctrine.css (98 lines) -----
.fleet-doctrine-dialog {
  width: min(1040px, calc(100vw - 24px));
  max-height: calc(100vh - 24px);
  padding: 0;
  overflow: auto;
  border: 1px solid rgba(94, 205, 249, 0.38);
  border-radius: 14px;
  color: #eefaff;
  background: linear-gradient(150deg, rgba(5, 18, 30, 0.99), rgba(1, 6, 12, 0.99));
}

.fleet-doctrine-dialog::backdrop {
  background: rgba(0, 4, 9, 0.84);
  backdrop-filter: blur(6px);
}

.fleet-doctrine-header {
  display: flex;
  justify-content: space-between;
  gap: 18px;
  padding: 22px 26px;
  border-bottom: 1px solid rgba(94, 205, 249, 0.16);
}

.fleet-doctrine-header h2,
.fleet-doctrine-header p,
.fleet-doctrine-summary p,
.fleet-doctrine-skills p {
  margin: 0;
}

.fleet-doctrine-header > div > p:last-child {
  margin-top: 6px;
  color: #839eaf;
}

.fleet-doctrine-form {
  display: grid;
  grid-template-columns: 1.4fr 1fr 1.2fr auto;
  align-items: end;
  gap: 10px;
  padding: 18px 22px;
}

.fleet-doctrine-form label {
  display: grid;
  gap: 6px;
  color: #86a0b1;
  font-size: 0.72rem;
  text-transform: uppercase;
}

.fleet-doctrine-form select {
  min-height: 40px;
  border: 1px solid rgba(94, 205, 249, 0.24);
  border-radius: 6px;
  color: #eefaff;
  background: #06131f;
}

.fleet-doctrine-summary,
.fleet-doctrine-skills {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 10px;
  padding: 0 22px 18px;
}

.fleet-doctrine-summary article,
.fleet-doctrine-skills article {
  display: grid;
  gap: 7px;
  min-height: 112px;
  padding: 14px;
  border: 1px solid rgba(94, 205, 249, 0.16);
  border-radius: 9px;
  background: rgba(4, 16, 27, 0.72);
}

.fleet-doctrine-summary span {
  color: #65d6ff;
  font-size: 0.68rem;
  text-transform: uppercase;
}

.fleet-doctrine-summary p,
.fleet-doctrine-skills p {
  color: #829aab;
  line-height: 1.4;
}

@media (max-width: 760px) {
  .fleet-doctrine-form,
  .fleet-doctrine-summary,
  .fleet-doctrine-skills {
    grid-template-columns: 1fr;
  }
}
----- FILE: src/styles/missionReports.css (215 lines) -----
.mission-reports-dialog {
  width: min(1180px, calc(100vw - 32px));
  max-height: calc(100vh - 32px);
  padding: 0;
  border: 1px solid color-mix(in srgb, var(--faction-accent, #59a9ff) 40%, #30455f);
  border-radius: 18px;
  color: #edf5ff;
  background: linear-gradient(155deg, rgba(7, 14, 26, 0.99), rgba(3, 7, 14, 0.99));
  box-shadow: 0 24px 84px rgba(0, 0, 0, 0.62);
}

.mission-reports-dialog::backdrop {
  background: rgba(0, 3, 8, 0.84);
  backdrop-filter: blur(7px);
}

.mission-reports-header {
  position: sticky;
  top: 0;
  z-index: 3;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20px 24px;
  border-bottom: 1px solid rgba(125, 171, 221, 0.18);
  background: rgba(5, 10, 19, 0.97);
}

.mission-reports-header h2,
.mission-reports-dialog h3 {
  margin: 0;
}

.mission-reports-summary,
.mission-reports-controls,
.mission-reports-comparison,
.mission-reports-list {
  display: grid;
  gap: 12px;
  margin: 18px 22px;
  padding: 18px;
  border: 1px solid rgba(118, 158, 204, 0.18);
  border-radius: 14px;
  background: rgba(10, 20, 34, 0.74);
}

.mission-reports-controls {
  grid-template-columns: minmax(220px, 2fr) repeat(3, minmax(150px, 1fr));
}

.mission-report-control {
  display: grid;
  gap: 6px;
  color: #91abc5;
  font-size: 0.78rem;
}

.mission-report-control input,
.mission-report-control select {
  min-height: 40px;
  padding: 0 10px;
  border: 1px solid rgba(117, 166, 218, 0.28);
  border-radius: 9px;
  color: #e7f2ff;
  background: rgba(12, 28, 47, 0.92);
}

.mission-reports-comparison {
  grid-template-columns: repeat(auto-fit, minmax(210px, 1fr));
}

.mission-reports-comparison h3 {
  grid-column: 1 / -1;
}

.mission-reports-comparison article,
.mission-report-card {
  display: grid;
  gap: 7px;
  padding: 14px;
  border: 1px solid rgba(119, 164, 210, 0.18);
  border-radius: 12px;
  background: rgba(8, 18, 31, 0.86);
}

.mission-reports-comparison p,
.mission-reports-comparison small,
.mission-report-card p,
.mission-report-card small {
  margin: 0;
}

.mission-reports-comparison small,
.mission-report-card small {
  color: #92abc5;
}

.mission-report-card header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12px;
}

.mission-report-card header div {
  display: flex;
  flex-wrap: wrap;
  justify-content: flex-end;
  gap: 6px;
}

.mission-report-card header span {
  padding: 3px 7px;
  border: 1px solid rgba(126, 174, 222, 0.22);
  border-radius: 999px;
  color: #aac2db;
  font-size: 0.7rem;
}

.mission-report-card.is-pve {
  border-left: 3px solid #74d59b;
}

.mission-report-card.is-pvp {
  border-left: 3px solid #ff7c72;
}

.mission-report-card.is-system {
  border-left: 3px solid #b984ff;
}

.mission-combat-details {
  margin-top: 7px;
  border: 1px solid rgba(101, 188, 236, 0.2);
  border-radius: 10px;
  background: rgba(4, 14, 25, 0.72);
}

.mission-combat-details > summary {
  padding: 10px 12px;
  color: #72d0ff;
  font-size: 0.78rem;
  font-weight: 700;
  cursor: pointer;
  user-select: none;
}

.mission-combat-details[open] > summary {
  border-bottom: 1px solid rgba(101, 188, 236, 0.16);
}

.mission-combat-table-wrap {
  max-height: 320px;
  overflow: auto;
}

.mission-combat-details table {
  width: 100%;
  min-width: 1160px;
  border-collapse: collapse;
  font-size: 0.7rem;
}

.mission-combat-details th,
.mission-combat-details td {
  padding: 7px 8px;
  border-right: 1px solid rgba(101, 188, 236, 0.09);
  border-bottom: 1px solid rgba(101, 188, 236, 0.09);
  text-align: left;
  vertical-align: top;
}

.mission-combat-details th {
  position: sticky;
  top: 0;
  z-index: 1;
  color: #a9dfff;
  background: rgba(8, 24, 39, 0.98);
}

.mission-combat-details td:nth-child(7) {
  color: #e6c469;
  font-weight: 700;
}

.mission-combat-details td:nth-child(12) {
  color: #ff9188;
  font-weight: 700;
}

.mission-combat-details > small {
  display: block;
  padding: 9px 12px;
  line-height: 1.4;
}

@media (max-width: 860px) {
  .mission-reports-controls {
    grid-template-columns: 1fr 1fr;
  }
}

@media (max-width: 560px) {
  .mission-reports-controls {
    grid-template-columns: 1fr;
  }

  .mission-report-card header {
    display: grid;
  }

  .mission-report-card header div {
    justify-content: flex-start;
  }
}
----- FILE: src/styles/missions.css (373 lines) -----
.mission-screen-dialog {
  width: min(1240px, calc(100vw - 28px));
  max-height: calc(100vh - 28px);
  padding: 0;
  border: 1px solid color-mix(in srgb, var(--faction-primary, #60cdff) 42%, #183249);
  border-radius: 16px;
  color: #edf9ff;
  background: linear-gradient(145deg, rgba(6, 18, 30, 0.995), rgba(2, 7, 14, 0.995));
  box-shadow: 0 28px 100px rgba(0, 0, 0, 0.76);
}

.mission-screen-dialog::backdrop {
  background: rgba(0, 4, 10, 0.84);
  backdrop-filter: blur(7px);
}

.mission-screen-dialog > header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 20px;
  padding: 20px 22px 16px;
  border-bottom: 1px solid rgba(96, 205, 255, 0.15);
  background: linear-gradient(105deg, rgba(39, 103, 137, 0.18), transparent 65%);
}

.mission-screen-dialog > header h2 {
  margin: 3px 0 5px;
}

.mission-screen-dialog > header p:last-child {
  margin: 0;
  color: #8fa8b8;
  font-size: 12px;
}

.mission-screen-content {
  display: grid;
  gap: 14px;
  max-height: 78vh;
  overflow: auto;
  padding: 16px 20px 22px;
}

.mission-overview {
  display: grid;
  grid-template-columns: repeat(5, minmax(110px, 1fr));
  gap: 8px;
}

.mission-overview > div {
  display: grid;
  gap: 3px;
  min-height: 62px;
  padding: 10px 12px;
  border: 1px solid rgba(96, 205, 255, 0.15);
  border-radius: 9px;
  background: rgba(8, 26, 40, 0.74);
}

.mission-overview span {
  color: #829baa;
  font-size: 10px;
  letter-spacing: 0.07em;
  text-transform: uppercase;
}

.mission-overview strong {
  font-size: 18px;
}

.mission-overview .mission-target-notice {
  grid-column: 1 / -1;
  min-height: auto;
  border-color: rgba(232, 174, 75, 0.38);
  background: rgba(73, 53, 17, 0.48);
  color: #f5d78d;
  font-size: 12px;
}

.mission-workspace {
  display: grid;
  grid-template-columns: minmax(330px, 0.78fr) minmax(520px, 1.22fr);
  gap: 14px;
  align-items: start;
}

.mission-create,
.mission-fleet-list,
.mission-intelligence,
.mission-battles,
.mission-debris {
  border: 1px solid rgba(96, 205, 255, 0.16);
  border-radius: 11px;
  background: rgba(6, 18, 30, 0.78);
}

.mission-create,
.mission-fleet-list {
  display: grid;
  align-content: start;
  gap: 12px;
  padding: 14px;
}

.mission-section-heading h3,
.mission-section-heading p {
  margin: 0;
}

.mission-section-heading p {
  margin-top: 4px;
  color: #829baa;
  font-size: 11px;
}

.mission-origin-field,
.mission-cargo-grid label,
.mission-ship-option label {
  display: grid;
  gap: 5px;
  color: #9db0bd;
  font-size: 0.72rem;
}

.mission-screen-dialog input,
.mission-screen-dialog select,
.mission-screen-dialog button {
  min-height: 36px;
  padding: 0 10px;
  border: 1px solid rgba(96, 205, 255, 0.28);
  border-radius: 7px;
  color: #eaf8ff;
  background: rgba(8, 29, 46, 0.92);
}

.mission-screen-dialog button:not(:disabled) {
  cursor: pointer;
}

.mission-screen-dialog button:disabled {
  opacity: 0.45;
}

.mission-ship-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 8px;
}

.mission-ship-option {
  display: grid;
  grid-template-columns: 78px minmax(0, 1fr);
  gap: 9px;
  min-height: 104px;
  overflow: hidden;
  border: 1px solid rgba(104, 181, 218, 0.16);
  border-radius: 9px;
  background: rgba(9, 28, 43, 0.8);
}

.mission-ship-option > img {
  width: 78px;
  height: 100%;
  object-fit: contain;
  background: radial-gradient(circle, rgba(69, 144, 180, 0.22), transparent 68%);
}

.mission-ship-option > div {
  display: grid;
  align-content: center;
  gap: 4px;
  padding: 8px 8px 8px 0;
}

.mission-ship-option strong,
.mission-ship-option small {
  display: block;
}

.mission-ship-option small {
  color: #8199a9;
  font-size: 9px;
}

.mission-ship-option input {
  width: 100%;
  min-height: 31px;
}

.mission-cargo-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 8px;
}

.mission-create hr {
  width: 100%;
  border: 0;
  border-top: 1px solid rgba(96, 205, 255, 0.12);
}

.mission-composer-status,
.mission-route-preview {
  flex-basis: 100%;
  margin: 0;
  color: #80cfe9;
  font-size: 0.75rem;
}

.mission-composer-status.is-blocked,
.mission-route-preview.is-blocked {
  color: #ff9e87;
}

.mission-primary-action {
  border-color: color-mix(in srgb, var(--faction-primary, #5ed7ff) 60%, transparent) !important;
  background: linear-gradient(135deg, color-mix(in srgb, var(--faction-primary, #5ed7ff) 34%, #10283a), #10283a) !important;
  font-weight: 800;
}

.mission-fleet-card {
  display: grid;
  grid-template-columns: 128px minmax(0, 1fr);
  overflow: hidden;
  border: 1px solid rgba(96, 205, 255, 0.16);
  border-radius: 10px;
  background: rgba(6, 18, 30, 0.84);
}

.mission-fleet-card__visual {
  position: relative;
  display: grid;
  place-items: center;
  min-height: 156px;
  background: radial-gradient(circle at 50% 45%, rgba(69, 150, 190, 0.25), transparent 64%);
}

.mission-fleet-card__visual img {
  width: 118px;
  height: 118px;
  object-fit: contain;
  filter: drop-shadow(0 14px 18px rgba(0, 0, 0, 0.56));
}

.mission-fleet-card__visual span {
  position: absolute;
  left: 8px;
  bottom: 8px;
  border-radius: 999px;
  padding: 4px 7px;
  background: rgba(2, 8, 14, 0.88);
  color: #b6d7e8;
  font-size: 9px;
  text-transform: uppercase;
}

.mission-fleet-card__body {
  display: grid;
  align-content: center;
  gap: 8px;
  padding: 12px;
}

.mission-fleet-card__body > p,
.mission-transit p {
  margin: 0;
  color: #91a8b7;
  font-size: 0.73rem;
}

.mission-transit {
  display: grid;
  gap: 5px;
}

.mission-transit progress {
  width: 100%;
  height: 7px;
  accent-color: var(--faction-primary, #5ed7ff);
}

.mission-actions {
  display: grid;
  grid-template-columns: minmax(120px, 0.8fr) minmax(160px, 1.2fr) auto;
  gap: 7px;
  align-items: center;
}

.mission-actions .mission-route-preview {
  grid-column: 1 / -1;
}

.mission-operational-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 12px;
}

.mission-intelligence,
.mission-battles,
.mission-debris {
  display: grid;
  align-content: start;
  gap: 8px;
  padding: 13px;
}

.mission-intelligence h3,
.mission-battles h3,
.mission-debris h3 {
  margin: 0 0 2px;
}

.mission-intelligence article,
.mission-battles article,
.mission-debris article {
  padding: 9px 10px;
  border-left: 3px solid rgba(89, 210, 255, 0.58);
  background: rgba(13, 40, 58, 0.62);
}

.mission-battles article {
  border-left-color: rgba(255, 122, 92, 0.66);
  background: rgba(63, 24, 25, 0.48);
}

.mission-debris article {
  border-left-color: rgba(226, 189, 91, 0.72);
  background: rgba(61, 50, 19, 0.42);
}

.mission-intelligence p,
.mission-battles p,
.mission-debris p {
  margin: 4px 0 0;
  color: #9db0bd;
  font-size: 0.72rem;
}

.mission-empty {
  margin: 0;
  padding: 16px;
  border: 1px dashed rgba(118, 172, 201, 0.2);
  border-radius: 8px;
  color: #8199a9;
  text-align: center;
}

@media (max-width: 980px) {
  .mission-workspace,
  .mission-operational-grid {
    grid-template-columns: 1fr;
  }
  .mission-overview {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 620px) {
  .mission-ship-grid,
  .mission-cargo-grid,
  .mission-actions {
    grid-template-columns: 1fr;
  }
  .mission-fleet-card {
    grid-template-columns: 96px minmax(0, 1fr);
  }
  .mission-fleet-card__visual img {
    width: 88px;
    height: 88px;
  }
}
----- FILE: src/ui/commandDoctrineScreen.ts (201 lines) -----
import '../styles/commandDoctrine.css';
import { resolveCompleteMechanicalAsset } from '../assets/completeMechanicalAssetManifest';
import { applyMechanicalAssetArtwork } from '../assets/runtimeMechanicalAssets';
import {
  countCommanderShipForEmpire,
  selectActiveCommanderShip,
} from '../simulation/command/commanderShips';
import {
  COMMAND_DOCTRINES,
  COMMAND_LEVEL_THRESHOLDS,
  FLAGSHIP_UNLOCK_LEVEL,
  getEmpireCommandState,
} from '../simulation/command/commandDoctrine';
import type { CommandDoctrineId } from '../simulation/command/types';
import type { GameCommand, GameState } from '../simulation/types';
import { COMPLETE_COMMANDER_SHIP_CATALOG } from '../simulation/units/completeCommanderShipCatalog';

interface CommandDoctrineBridge {
  readonly getState: () => GameState;
  readonly execute: (command: GameCommand, successMessage: string) => boolean;
}

function createNavigationButton(): HTMLButtonElement {
  const existing = document.querySelector<HTMLButtonElement>('#nav-command-doctrine');
  if (existing !== null) return existing;
  const fleetDoctrine = document.querySelector<HTMLButtonElement>('#nav-fleet-doctrine');
  const button = document.createElement('button');
  button.id = 'nav-command-doctrine';
  button.className = 'rail-button';
  button.type = 'button';
  button.setAttribute('aria-label', 'Адмирал и Commander Ships');
  button.innerHTML = '<span class="rail-button__icon">✦</span><small>Адмирал</small>';
  fleetDoctrine?.insertAdjacentElement('afterend', button);
  if (fleetDoctrine === null) document.querySelector<HTMLElement>('.side-rail')?.append(button);
  return button;
}

function createDialog(): HTMLDialogElement {
  const existing = document.querySelector<HTMLDialogElement>('#command-doctrine-dialog');
  if (existing !== null) return existing;
  const dialog = document.createElement('dialog');
  dialog.id = 'command-doctrine-dialog';
  dialog.className = 'command-doctrine-dialog';
  dialog.innerHTML = `
    <header class="command-doctrine-header">
      <div><p class="panel-label">Admiral Command</p><h2>Адмирал и Commander Ships</h2><p>Боевой опыт открывает 13 общих командирских кораблей. В назначенном флагманском флоте действует только одна способность.</p></div>
      <button type="button" class="dialog-close" aria-label="Закрыть командный профиль">×</button>
    </header>
    <section class="command-progress" aria-live="polite"></section>
    <form class="command-doctrine-form">
      <label>Доктрина<select name="doctrine"></select></label>
      <button type="submit" class="primary-button">Применить доктрину</button>
    </form>
    <form class="flagship-form">
      <label>Флагманский флот<select name="fleet"></select></label>
      <button type="submit" class="primary-button">Назначить флагман</button>
    </form>
    <section class="command-doctrine-cards"></section>
    <div class="commander-roster-heading"><p class="panel-label">Shared Commander Catalog</p><h3>Командирские корабли</h3></div>
    <section class="commander-ship-cards" aria-live="polite"></section>
  `;
  dialog.querySelector<HTMLButtonElement>('.dialog-close')?.addEventListener('click', () => dialog.close());
  document.body.append(dialog);
  return dialog;
}

export function mountCommandDoctrineScreen(bridge: CommandDoctrineBridge): void {
  const button = createNavigationButton();
  const dialog = createDialog();
  const doctrineForm = dialog.querySelector<HTMLFormElement>('.command-doctrine-form');
  const flagshipForm = dialog.querySelector<HTMLFormElement>('.flagship-form');
  const doctrineSelect = doctrineForm?.elements.namedItem('doctrine');
  const fleetSelect = flagshipForm?.elements.namedItem('fleet');
  const progress = dialog.querySelector<HTMLElement>('.command-progress');
  const cards = dialog.querySelector<HTMLElement>('.command-doctrine-cards');
  const commanderCards = dialog.querySelector<HTMLElement>('.commander-ship-cards');
  if (
    doctrineForm === null ||
    flagshipForm === null ||
    !(doctrineSelect instanceof HTMLSelectElement) ||
    !(fleetSelect instanceof HTMLSelectElement) ||
    progress === null ||
    cards === null ||
    commanderCards === null
  ) throw new Error('Command doctrine controls are missing.');

  doctrineSelect.replaceChildren(
    ...Object.values(COMMAND_DOCTRINES).map((doctrine) => {
      const option = document.createElement('option');
      option.value = doctrine.id;
      option.textContent = doctrine.name;
      return option;
    }),
  );
  cards.replaceChildren(
    ...Object.values(COMMAND_DOCTRINES).map((doctrine) => {
      const card = document.createElement('article');
      const title = document.createElement('strong');
      title.textContent = doctrine.name;
      const description = document.createElement('p');
      description.textContent = doctrine.description;
      card.append(title, description);
      return card;
    }),
  );

  const render = (): void => {
    const state = bridge.getState();
    const command = getEmpireCommandState(state.commanders, 'player');
    if (command === undefined) return;
    doctrineSelect.value = command.doctrineId;
    const nextThreshold = COMMAND_LEVEL_THRESHOLDS[command.level] ?? COMMAND_LEVEL_THRESHOLDS.at(-1)!;
    const currentThreshold = COMMAND_LEVEL_THRESHOLDS[command.level - 1] ?? 0;
    const range = Math.max(1, nextThreshold - currentThreshold);
    const progressPercent = command.level >= COMMAND_LEVEL_THRESHOLDS.length
      ? 100
      : Math.min(100, Math.floor(((command.experience - currentThreshold) * 100) / range));
    const flagship = state.fleets.find((fleet) => fleet.id === command.flagshipFleetId);
    const activeCommander = flagship === undefined
      ? undefined
      : selectActiveCommanderShip(command, flagship.id, flagship.ships);
    progress.innerHTML = `
      <article><span>Уровень Адмирала</span><strong>${command.level} / 40</strong></article>
      <article><span>Боевой опыт</span><strong>${command.experience} / ${nextThreshold}</strong><progress max="100" value="${progressPercent}"></progress></article>
      <article><span>Активная способность</span><strong>${activeCommander?.definition.commanderAbility?.name ?? 'не активна'}</strong><small>${activeCommander?.definition.name ?? command.flagshipFleetId ?? 'флагман не назначен'}</small></article>
    `;
    const stationed = state.fleets.filter((fleet) =>
      fleet.empireId === 'player' && fleet.status === 'stationed' && fleet.location.type === 'planet',
    );
    fleetSelect.replaceChildren();
    const none = document.createElement('option');
    none.value = '';
    none.textContent = 'Не назначать';
    fleetSelect.append(none);
    for (const fleet of stationed) {
      const option = document.createElement('option');
      option.value = fleet.id;
      const commander = Object.keys(fleet.ships).find((unitId) =>
        COMPLETE_COMMANDER_SHIP_CATALOG.some((definition) => definition.id === unitId),
      );
      option.textContent = `${commander === undefined ? '' : '✦ '}${fleet.id} · ${Object.values(fleet.ships).reduce((sum, count) => sum + count, 0)} кораблей`;
      fleetSelect.append(option);
    }
    fleetSelect.value = command.flagshipFleetId ?? '';
    flagshipForm.querySelector<HTMLButtonElement>('button[type="submit"]')!.disabled =
      command.level < FLAGSHIP_UNLOCK_LEVEL && command.flagshipFleetId === null;

    commanderCards.replaceChildren(
      ...COMPLETE_COMMANDER_SHIP_CATALOG.map((definition) => {
        const owned = countCommanderShipForEmpire(state, 'player', definition.id) > 0;
        const unlocked = command.level >= (definition.requiredAdmiralLevel ?? 1);
        const active = activeCommander?.unitId === definition.id;
        const card = document.createElement('article');
        card.className = `commander-ship-card${active ? ' is-active' : ''}${owned ? ' is-owned' : ''}${!owned && unlocked ? ' is-available' : ''}${!unlocked ? ' is-locked' : ''}`;
        card.dataset.mechanicalId = definition.id;
        const status = active
          ? 'Активен во флагманском флоте'
          : owned
            ? 'Построен'
            : unlocked
              ? 'Доступен для производства'
              : `Откроется на уровне ${definition.requiredAdmiralLevel}`;
        const art = document.createElement('div');
        art.className = 'commander-ship-card__art';
        art.setAttribute('role', 'img');
        art.setAttribute('aria-label', definition.name);
        const asset = resolveCompleteMechanicalAsset(definition.id).asset;
        if (asset !== undefined) applyMechanicalAssetArtwork(art, asset);
        card.innerHTML = `
          <header><strong>${definition.name}</strong><span>${status}</span></header>
          <p>${definition.description}</p>
          <dl><div><dt>Адмирал</dt><dd>${definition.requiredAdmiralLevel}</dd></div><div><dt>Верфь</dt><dd>${definition.requiredShipyardLevel}</dd></div><div><dt>Способность</dt><dd>${definition.commanderAbility?.name ?? '—'}</dd></div></dl>
          <small>${definition.commanderAbility?.description ?? ''}</small>
        `;
        card.prepend(art);
        return card;
      }),
    );
  };

  doctrineForm.addEventListener('submit', (event) => {
    event.preventDefault();
    if (bridge.execute({
      type: 'SET_COMMAND_DOCTRINE',
      empireId: 'player',
      doctrineId: doctrineSelect.value as CommandDoctrineId,
    }, 'Командная доктрина обновлена')) render();
  });
  flagshipForm.addEventListener('submit', (event) => {
    event.preventDefault();
    if (bridge.execute({
      type: 'ASSIGN_FLAGSHIP',
      empireId: 'player',
      fleetId: fleetSelect.value.length > 0 ? fleetSelect.value : null,
    }, 'Флагманское назначение обновлено')) render();
  });
  button.addEventListener('click', () => {
    render();
    dialog.showModal();
  });
}
----- FILE: src/ui/commandRanking.test.ts (41 lines) -----
import { describe, expect, it } from 'vitest';
import { createInitialGameState } from '../simulation/createInitialGameState';
import { createEmpireRanking, createPlayerCommandProfile } from './commandRanking';

describe('command ranking', () => {
  it('contains every empire exactly once and is deterministically sorted', () => {
    const state = createInitialGameState('ranking-deterministic');
    const first = createEmpireRanking(state);
    const second = createEmpireRanking(state);
    expect(first).toEqual(second);
    expect(first.map((entry) => entry.empireId).sort()).toEqual([...state.empires].sort());
    expect(first.map((entry) => entry.rank)).toEqual([1, 2, 3, 4]);
    for (let index = 1; index < first.length; index += 1) {
      expect(first[index - 1]?.score).toBeGreaterThanOrEqual(first[index]?.score ?? 0);
    }
  });

  it('raises the player rank when its economy is substantially increased', () => {
    const state = createInitialGameState('ranking-player');
    const boosted = {
      ...state,
      planets: state.planets.map((planet) =>
        planet.ownerEmpireId !== 'player'
          ? planet
          : {
              ...planet,
              economy: {
                ...planet.economy,
                resources: Object.fromEntries(
                  Object.entries(planet.economy.resources).map(([resourceId, stock]) => [
                    resourceId,
                    { ...stock, amount: stock.amount + 10_000_000 },
                  ]),
                ) as typeof planet.economy.resources,
              },
            },
      ),
    };
    expect(createPlayerCommandProfile(boosted).rank).toBe(1);
  });
});
----- FILE: src/ui/commandRanking.ts (121 lines) -----
import { FACTION_SHOWCASES } from '../assets/factionShowcase';
import type { FactionId } from '../simulation/planet/types';
import { createUnifiedMissionReports } from '../simulation/reports/missionReports';
import type { GameState } from '../simulation/types';

export interface EmpireRankingEntry {
  readonly rank: number;
  readonly empireId: string;
  readonly factionId: FactionId;
  readonly factionName: string;
  readonly doctrine: string;
  readonly score: number;
  readonly colonies: number;
  readonly resourceStock: number;
  readonly productionPerHour: number;
  readonly buildingLevels: number;
  readonly researchLevels: number;
  readonly units: number;
  readonly fleets: number;
  readonly victories: number;
}

const FACTION_COPY = new Map(
  FACTION_SHOWCASES.map((faction) => [faction.id, faction] as const),
);

function sumRecord(record: Readonly<Record<string, number>>): number {
  return Object.values(record).reduce((total, value) => total + value, 0);
}

function getFactionId(state: GameState, empireId: string): FactionId {
  return state.planets.find((planet) => planet.ownerEmpireId === empireId)?.factionId ?? 'aegis';
}

function countVictories(state: GameState, empireId: string): number {
  return createUnifiedMissionReports(state).filter((report) => {
    if (report.primaryEmpireId === empireId && report.outcome === 'success') return true;
    return (
      report.kind === 'battle' &&
      report.secondaryEmpireId === empireId &&
      report.outcome === 'failure'
    );
  }).length;
}

function createRawEntry(state: GameState, empireId: string): Omit<EmpireRankingEntry, 'rank'> {
  const planets = state.planets.filter((planet) => planet.ownerEmpireId === empireId);
  const factionId = getFactionId(state, empireId);
  const faction = FACTION_COPY.get(factionId);
  const resourceStock = planets.reduce(
    (total, planet) =>
      total +
      Object.values(planet.economy.resources).reduce(
        (resourceTotal, stock) => resourceTotal + stock.amount,
        0,
      ),
    0,
  );
  const productionPerHour = planets.reduce(
    (total, planet) =>
      total +
      Object.values(planet.economy.resources).reduce(
        (resourceTotal, stock) => resourceTotal + stock.productionPerHour,
        0,
      ),
    0,
  );
  const buildingLevels = planets.reduce(
    (total, planet) =>
      total + planet.buildings.reduce((levelTotal, building) => levelTotal + building.level, 0),
    0,
  );
  const researchLevels = sumRecord(
    state.research.find((research) => research.empireId === empireId)?.levels ?? {},
  );
  const units = planets.reduce(
    (total, planet) =>
      total + sumRecord(planet.inventory.ships) + sumRecord(planet.inventory.defenses),
    0,
  );
  const fleets = state.fleets.filter((fleet) => fleet.empireId === empireId).length;
  const victories = countVictories(state, empireId);
  const score =
    planets.length * 1_000 +
    Math.floor(resourceStock / 100) +
    productionPerHour * 2 +
    buildingLevels * 250 +
    researchLevels * 300 +
    units * 40 +
    fleets * 120 +
    victories * 500;

  return {
    empireId,
    factionId,
    factionName: faction?.name ?? factionId.toUpperCase(),
    doctrine: faction?.doctrine ?? 'Неизвестная доктрина',
    score,
    colonies: planets.length,
    resourceStock,
    productionPerHour,
    buildingLevels,
    researchLevels,
    units,
    fleets,
    victories,
  };
}

export function createEmpireRanking(state: GameState): readonly EmpireRankingEntry[] {
  return state.empires
    .map((empireId) => createRawEntry(state, empireId))
    .sort((left, right) => right.score - left.score || left.empireId.localeCompare(right.empireId))
    .map((entry, index) => ({ ...entry, rank: index + 1 }));
}

export function createPlayerCommandProfile(state: GameState): EmpireRankingEntry {
  const player = createEmpireRanking(state).find((entry) => entry.empireId === 'player');
  if (player === undefined) throw new Error('Player ranking entry is missing.');
  return player;
}
----- FILE: src/ui/commandRankingScreen.ts (135 lines) -----
import '../styles/commandRanking.css';
import { getGeneratedFactionIdentityAssets } from '../assets/generatedFactionIdentityAssets';
import type { GameState } from '../simulation/types';
import { createEmpireRanking, createPlayerCommandProfile } from './commandRanking';

export interface CommandRankingScreenOptions {
  readonly getState: () => GameState;
}

const NUMBER_FORMAT = new Intl.NumberFormat('ru-RU');

function createDialog(): HTMLDialogElement {
  const existing = document.querySelector<HTMLDialogElement>('#command-ranking-dialog');
  if (existing !== null) return existing;
  const dialog = document.createElement('dialog');
  dialog.id = 'command-ranking-dialog';
  dialog.className = 'command-ranking-dialog';
  dialog.innerHTML = `
    <header class="command-ranking-header">
      <div><p class="panel-label">Strategic Command</p><h2>Командный профиль и рейтинг</h2></div>
      <button type="button" class="dialog-close" aria-label="Закрыть рейтинг">×</button>
    </header>
    <section class="command-profile"></section>
    <section class="command-ranking-list" aria-label="Локальный рейтинг империй"></section>
  `;
  dialog.querySelector<HTMLButtonElement>('.dialog-close')?.addEventListener('click', () => {
    dialog.close();
  });
  document.body.append(dialog);
  return dialog;
}

function createStat(label: string, value: string): HTMLElement {
  const item = document.createElement('div');
  const term = document.createElement('span');
  term.textContent = label;
  const output = document.createElement('strong');
  output.textContent = value;
  item.append(term, output);
  return item;
}

export function mountCommandRankingScreen(options: CommandRankingScreenOptions): void {
  const button = document.querySelector<HTMLButtonElement>('#nav-rating')
    ?? document.querySelector<HTMLButtonElement>('[aria-label="Рейтинг"]');
  if (button === null) return;
  button.id = 'nav-rating';
  button.disabled = false;
  const dialog = createDialog();
  const profileHost = dialog.querySelector<HTMLElement>('.command-profile');
  const rankingHost = dialog.querySelector<HTMLElement>('.command-ranking-list');
  if (profileHost === null || rankingHost === null) {
    throw new Error('Command ranking containers are missing.');
  }

  const render = (): void => {
    const state = options.getState();
    const profile = createPlayerCommandProfile(state);
    const identity = getGeneratedFactionIdentityAssets(profile.factionId);
    profileHost.style.setProperty('--command-profile-bg', `url("${identity.backgroundUrl}")`);
    profileHost.replaceChildren();

    const hero = document.createElement('img');
    hero.className = 'command-profile-hero';
    hero.src = identity.heroUrl;
    hero.alt = '';
    const body = document.createElement('div');
    body.className = 'command-profile-body';
    const identityRow = document.createElement('div');
    identityRow.className = 'command-profile-identity';
    const emblem = document.createElement('img');
    emblem.src = identity.emblemUrl;
    emblem.alt = '';
    const copy = document.createElement('div');
    const kicker = document.createElement('p');
    kicker.className = 'panel-label';
    kicker.textContent = `Место ${profile.rank} · ${NUMBER_FORMAT.format(profile.score)} очков`;
    const title = document.createElement('h3');
    title.textContent = profile.factionName;
    const doctrine = document.createElement('p');
    doctrine.textContent = profile.doctrine;
    copy.append(kicker, title, doctrine);
    identityRow.append(emblem, copy);
    const stats = document.createElement('div');
    stats.className = 'command-profile-stats';
    stats.append(
      createStat('Колонии', String(profile.colonies)),
      createStat('Запасы', NUMBER_FORMAT.format(profile.resourceStock)),
      createStat('Производство/ч', NUMBER_FORMAT.format(profile.productionPerHour)),
      createStat('Уровни зданий', String(profile.buildingLevels)),
      createStat('Уровни наук', String(profile.researchLevels)),
      createStat('Юниты', String(profile.units)),
      createStat('Флоты', String(profile.fleets)),
      createStat('Победы', String(profile.victories)),
    );
    body.append(identityRow, stats);
    profileHost.append(hero, body);

    rankingHost.replaceChildren();
    const heading = document.createElement('header');
    const titleList = document.createElement('h3');
    titleList.textContent = 'Локальный рейтинг империй';
    const note = document.createElement('p');
    note.textContent = 'Очки вычисляются из текущего состояния партии и не сохраняются отдельным полем.';
    heading.append(titleList, note);
    rankingHost.append(heading);

    for (const entry of createEmpireRanking(state)) {
      const row = document.createElement('article');
      row.className = `command-ranking-entry${entry.empireId === 'player' ? ' is-player' : ''}`;
      const place = document.createElement('strong');
      place.className = 'command-ranking-place';
      place.textContent = `#${entry.rank}`;
      const identityAssets = getGeneratedFactionIdentityAssets(entry.factionId);
      const emblem = document.createElement('img');
      emblem.src = identityAssets.emblemUrl;
      emblem.alt = '';
      const copy = document.createElement('div');
      const name = document.createElement('strong');
      name.textContent = entry.empireId === 'player' ? `${entry.factionName} · игрок` : `${entry.factionName} · ${entry.empireId}`;
      const detail = document.createElement('small');
      detail.textContent = `${entry.colonies} кол. · ${entry.units} юн. · ${entry.victories} побед`;
      copy.append(name, detail);
      const score = document.createElement('b');
      score.textContent = NUMBER_FORMAT.format(entry.score);
      row.append(place, emblem, copy, score);
      rankingHost.append(row);
    }
  };

  button.addEventListener('click', () => {
    render();
    dialog.showModal();
  });
}
----- FILE: src/ui/fleetComposerViewModel.ts (181 lines) -----
import type { ResourceCost } from '../simulation/economy/types';
import { getCommanderFleetEffects } from '../simulation/command/commanderShips';
import { getResearchEffectsForEmpire } from '../simulation/factions/factionResearchEffects';
import {
  calculateFleetComposition,
  getCargoAmount,
  validateShipComposition,
} from '../simulation/fleets/fleetCalculations';
import {
  estimateFlight,
  estimateFlightToGalaxyPlanet,
} from '../simulation/fleets/flightCalculations';
import type { FleetMissionKind, FleetState } from '../simulation/fleets/types';
import type { GameState } from '../simulation/types';
import { getUnitDefinition } from '../simulation/units/catalog';

export interface FleetComposerShipOption {
  readonly unitId: string;
  readonly name: string;
  readonly available: number;
  readonly selected: number;
}

export interface FleetComposerViewModel {
  readonly originPlanetId: string;
  readonly originName: string;
  readonly ships: readonly FleetComposerShipOption[];
  readonly selectedShips: Readonly<Record<string, number>>;
  readonly speed: number;
  readonly shipCount: number;
  readonly cargoCapacity: number;
  readonly cargoAmount: number;
  readonly cargo: ResourceCost;
  readonly errors: readonly string[];
  readonly canCreate: boolean;
}

export interface FleetRoutePreview {
  readonly distance: number;
  readonly durationSeconds: number;
  readonly oneWayFuel: number;
  readonly reservedFuel: number;
  readonly originGas: number;
  readonly hasEnoughFuel: boolean;
}

function normalizeCount(value: number): number {
  return Number.isFinite(value) ? Math.max(0, Math.floor(value)) : 0;
}

function normalizeCargo(cargo: ResourceCost): ResourceCost {
  return {
    metal: normalizeCount(cargo.metal),
    crystal: normalizeCount(cargo.crystal),
    gas: normalizeCount(cargo.gas),
  };
}

export function createFleetComposerViewModel(
  state: GameState,
  empireId: string,
  originPlanetId: string,
  requestedShips: Readonly<Record<string, number>>,
  requestedCargo: ResourceCost,
): FleetComposerViewModel | undefined {
  const origin = state.planets.find(
    (planet) =>
      planet.id === originPlanetId && planet.ownerEmpireId === empireId,
  );
  if (origin === undefined) return undefined;

  const selectedShips: Record<string, number> = {};
  const errors: string[] = [];
  const ships = Object.entries(origin.inventory.ships)
    .filter(([, available]) => available > 0)
    .map(([unitId, available]): FleetComposerShipOption => {
      const selected = normalizeCount(requestedShips[unitId] ?? 0);
      if (selected > available) errors.push(`INSUFFICIENT_SHIPS:${unitId}`);
      if (selected > 0) selectedShips[unitId] = selected;
      return {
        unitId,
        name: getUnitDefinition(unitId)?.name ?? unitId,
        available,
        selected,
      };
    });

  for (const unitId of Object.keys(requestedShips)) {
    if (
      !(unitId in origin.inventory.ships) &&
      normalizeCount(requestedShips[unitId] ?? 0) > 0
    ) {
      errors.push(`SHIP_NOT_AT_ORIGIN:${unitId}`);
    }
  }

  const cargo = normalizeCargo(requestedCargo);
  const compositionErrors = validateShipComposition(selectedShips);
  errors.push(...compositionErrors);
  const composition =
    compositionErrors.length === 0
      ? calculateFleetComposition(selectedShips)
      : { speed: 0, cargoCapacity: 0, shipCount: 0 };
  const cargoAmount = getCargoAmount(cargo);
  if (cargoAmount > composition.cargoCapacity) {
    errors.push('FLEET_CARGO_OVER_CAPACITY');
  }
  for (const resourceId of ['metal', 'crystal', 'gas'] as const) {
    if (cargo[resourceId] > origin.economy.resources[resourceId].amount) {
      errors.push(`INSUFFICIENT_CARGO_RESOURCE:${resourceId}`);
    }
  }

  return {
    originPlanetId: origin.id,
    originName: origin.name,
    ships,
    selectedShips,
    speed: composition.speed,
    shipCount: composition.shipCount,
    cargoCapacity: composition.cargoCapacity,
    cargoAmount,
    cargo,
    errors,
    canCreate: errors.length === 0,
  };
}

function getFleetSpeedBonus(state: GameState, fleet: FleetState): number {
  return getResearchEffectsForEmpire(state, fleet.empireId).fleetSpeedPercent +
    getCommanderFleetEffects(state, fleet).speedBonusPercent;
}

export function createFleetRoutePreview(
  state: GameState,
  fleet: FleetState,
  mission: FleetMissionKind,
  targetPlanetId: string,
): FleetRoutePreview | undefined {
  const location = fleet.location;
  if (fleet.status !== 'stationed' || location.type !== 'planet') {
    return undefined;
  }
  const origin = state.planets.find(
    (planet) => planet.id === location.planetId,
  );
  if (origin === undefined) return undefined;

  let estimate;
  try {
    estimate = mission === 'colonize'
      ? estimateFlightToGalaxyPlanet(
          state.galaxy,
          state.planets,
          fleet,
          targetPlanetId,
          getFleetSpeedBonus(state, fleet),
        )
      : estimateFlight(
          state.galaxy,
          state.planets,
          fleet,
          targetPlanetId,
          getFleetSpeedBonus(state, fleet),
        );
  } catch {
    return undefined;
  }

  const reservedFuel =
    mission === 'colonize' ? estimate.fuelCost : estimate.fuelCost * 2;
  const originGas = origin.economy.resources.gas.amount;
  return {
    distance: estimate.distance,
    durationSeconds: estimate.durationSeconds,
    oneWayFuel: estimate.fuelCost,
    reservedFuel,
    originGas,
    hasEnoughFuel: originGas >= reservedFuel,
  };
}
----- FILE: src/ui/fleetDoctrineScreen.ts (173 lines) -----
import '../styles/fleetDoctrine.css';
import {
  CLASS_SKILLS,
  FLEET_FORMATIONS,
  type FleetFormation,
  type FleetTargetPriority,
} from '../simulation/combat/fleetDoctrine';
import type { GameCommand, GameState } from '../simulation/types';

interface FleetDoctrineBridge {
  readonly getState: () => GameState;
  readonly execute: (command: GameCommand, successMessage: string) => boolean;
}

const PRIORITY_NAMES: Readonly<Record<FleetTargetPriority, string>> = {
  balanced: 'Сбалансированный огонь',
  interceptors: 'Перехватчики и малые цели',
  capitals: 'Крупные корабли',
  installations: 'Планетарные установки',
};

function createNavigationButton(): HTMLButtonElement {
  const existing = document.querySelector<HTMLButtonElement>('#nav-fleet-doctrine');
  if (existing !== null) return existing;
  const upgrades = document.querySelector<HTMLButtonElement>('#nav-ship-upgrades');
  const button = document.createElement('button');
  button.id = 'nav-fleet-doctrine';
  button.className = 'rail-button';
  button.type = 'button';
  button.setAttribute('aria-label', 'Боевые доктрины флота');
  button.innerHTML = '<span class="rail-button__icon">◈</span><small>Доктрина</small>';
  upgrades?.insertAdjacentElement('afterend', button);
  if (upgrades === null) document.querySelector<HTMLElement>('.side-rail')?.append(button);
  return button;
}

function createDialog(): HTMLDialogElement {
  const existing = document.querySelector<HTMLDialogElement>('#fleet-doctrine-dialog');
  if (existing !== null) return existing;
  const dialog = document.createElement('dialog');
  dialog.id = 'fleet-doctrine-dialog';
  dialog.className = 'fleet-doctrine-dialog';
  dialog.innerHTML = `
    <header class="fleet-doctrine-header">
      <div><p class="panel-label">Combat Doctrine</p><h2>Формации и приоритеты</h2><p>Настройка доступна только для флотов, находящихся на планете.</p></div>
      <button type="button" class="dialog-close" aria-label="Закрыть доктрины">×</button>
    </header>
    <form class="fleet-doctrine-form">
      <label>Флот<select name="fleet"></select></label>
      <label>Формация<select name="formation"></select></label>
      <label>Приоритет<select name="priority"></select></label>
      <button type="submit" class="primary-button">Применить</button>
    </form>
    <section class="fleet-doctrine-summary"></section>
    <section class="fleet-doctrine-skills" aria-label="Классовые навыки"></section>
  `;
  dialog.querySelector<HTMLButtonElement>('.dialog-close')?.addEventListener('click', () => dialog.close());
  document.body.append(dialog);
  return dialog;
}

export function mountFleetDoctrineScreen(bridge: FleetDoctrineBridge): void {
  const button = createNavigationButton();
  const dialog = createDialog();
  const form = dialog.querySelector<HTMLFormElement>('.fleet-doctrine-form');
  const fleetSelect = form?.elements.namedItem('fleet');
  const formationSelect = form?.elements.namedItem('formation');
  const prioritySelect = form?.elements.namedItem('priority');
  const summary = dialog.querySelector<HTMLElement>('.fleet-doctrine-summary');
  const skills = dialog.querySelector<HTMLElement>('.fleet-doctrine-skills');
  if (
    form === null ||
    !(fleetSelect instanceof HTMLSelectElement) ||
    !(formationSelect instanceof HTMLSelectElement) ||
    !(prioritySelect instanceof HTMLSelectElement) ||
    summary === null ||
    skills === null
  ) {
    throw new Error('Fleet doctrine controls are missing.');
  }

  formationSelect.replaceChildren(
    ...Object.values(FLEET_FORMATIONS).map((formation) => {
      const option = document.createElement('option');
      option.value = formation.id;
      option.textContent = formation.name;
      return option;
    }),
  );
  prioritySelect.replaceChildren(
    ...Object.entries(PRIORITY_NAMES).map(([priority, name]) => {
      const option = document.createElement('option');
      option.value = priority;
      option.textContent = name;
      return option;
    }),
  );
  skills.replaceChildren(
    ...CLASS_SKILLS.map((skill) => {
      const card = document.createElement('article');
      const title = document.createElement('strong');
      title.textContent = skill.name;
      const description = document.createElement('p');
      description.textContent = skill.description;
      card.append(title, description);
      return card;
    }),
  );

  const renderSummary = (): void => {
    const formation = FLEET_FORMATIONS[formationSelect.value as FleetFormation];
    const priority = prioritySelect.value as FleetTargetPriority;
    summary.innerHTML = `
      <article><span>Формация</span><strong>${formation.name}</strong><p>${formation.description}</p></article>
      <article><span>Модификаторы</span><strong>${formation.weaponBonusPercent >= 0 ? '+' : ''}${formation.weaponBonusPercent}% атака · ${formation.armorBonusPercent >= 0 ? '+' : ''}${formation.armorBonusPercent}% защита</strong></article>
      <article><span>Целевой приоритет</span><strong>${PRIORITY_NAMES[priority]}</strong><p>Перераспределяет урон между классами целей, не создавая гарантированного фокуса.</p></article>
    `;
  };

  const render = (): void => {
    const state = bridge.getState();
    const fleets = state.fleets.filter(
      (fleet) =>
        fleet.empireId === 'player' &&
        fleet.status === 'stationed' &&
        fleet.location.type === 'planet',
    );
    const previous = fleetSelect.value;
    fleetSelect.replaceChildren(
      ...fleets.map((fleet) => {
        const option = document.createElement('option');
        option.value = fleet.id;
        option.textContent = `${fleet.id} · ${Object.values(fleet.ships).reduce((total, count) => total + count, 0)} кораблей`;
        return option;
      }),
    );
    if (previous.length > 0 && fleets.some((fleet) => fleet.id === previous)) {
      fleetSelect.value = previous;
    }
    const selected = fleets.find((fleet) => fleet.id === fleetSelect.value) ?? fleets[0];
    form.querySelector<HTMLButtonElement>('button[type="submit"]')!.disabled = selected === undefined;
    if (selected !== undefined) {
      fleetSelect.value = selected.id;
      formationSelect.value = selected.formation ?? 'line';
      prioritySelect.value = selected.targetPriority ?? 'balanced';
    }
    renderSummary();
  };

  form.addEventListener('submit', (event) => {
    event.preventDefault();
    if (fleetSelect.value.length === 0) return;
    if (
      bridge.execute(
        {
          type: 'SET_FLEET_COMBAT_DOCTRINE',
          empireId: 'player',
          fleetId: fleetSelect.value,
          formation: formationSelect.value as FleetFormation,
          targetPriority: prioritySelect.value as FleetTargetPriority,
        },
        'Боевая доктрина флота обновлена',
      )
    ) render();
  });
  fleetSelect.addEventListener('change', render);
  formationSelect.addEventListener('change', renderSummary);
  prioritySelect.addEventListener('change', renderSummary);
  button.addEventListener('click', () => {
    render();
    dialog.showModal();
  });
}
----- FILE: src/ui/fleetMissionEvents.test.ts (17 lines) -----
import { describe, expect, it } from 'vitest';
import { inferMissionForGalaxyTarget } from './fleetMissionEvents';

describe('galaxy target mission inference', () => {
  it('routes free positions to colonization', () => {
    expect(inferMissionForGalaxyTarget(null, 'unclaimed')).toBe('colonize');
  });

  it('routes owned colonies to transport', () => {
    expect(inferMissionForGalaxyTarget('player', 'owned')).toBe('transport');
  });

  it('routes foreign and unknown contacts to scouting', () => {
    expect(inferMissionForGalaxyTarget('aegis-bot', 'current')).toBe('scout');
    expect(inferMissionForGalaxyTarget(null, 'contact')).toBe('scout');
  });
});
----- FILE: src/ui/fleetMissionEvents.ts (27 lines) -----
import type { FleetMissionKind } from '../simulation/fleets/types';
import type { GalaxyIntelVisibility } from '../simulation/galaxy/intelligenceView';

export const FLEET_MISSION_TARGET_EVENT = 'stellar:fleet-mission-target';

export interface FleetMissionTargetRequest {
  readonly targetId: string;
  readonly label: string;
  readonly mission: FleetMissionKind;
}

export function inferMissionForGalaxyTarget(
  ownerEmpireId: string | null,
  visibility: GalaxyIntelVisibility,
): FleetMissionKind {
  if (visibility === 'unclaimed') return 'colonize';
  if (ownerEmpireId === 'player') return 'transport';
  return 'scout';
}

export function dispatchFleetMissionTarget(detail: FleetMissionTargetRequest): void {
  window.dispatchEvent(
    new CustomEvent<FleetMissionTargetRequest>(FLEET_MISSION_TARGET_EVENT, {
      detail,
    }),
  );
}
----- FILE: src/ui/missionReportsPanel.ts (310 lines) -----
import {
  compareEmpirePvePvp,
  createUnifiedMissionReports,
  filterMissionReports,
  summarizeMissionReports,
  type MissionCombatBreakdownLine,
  type MissionReportKind,
  type MissionReportMode,
  type MissionReportReward,
  type UnifiedMissionReport,
} from '../simulation/reports/missionReports';
import type { GameState } from '../simulation/types';
import { formatGameDuration } from './planetViewModel';

export interface MissionReportsPanelOptions {
  readonly getState: () => GameState;
}

const KIND_LABELS: Readonly<Record<MissionReportKind, string>> = {
  battle: 'Бой',
  expedition: 'Экспедиция',
  'space-object': 'Космический объект',
  'world-event': 'Мировое событие',
};

const MODE_LABELS: Readonly<Record<MissionReportMode, string>> = {
  pve: 'PvE',
  pvp: 'PvP',
  system: 'Системное',
};

const WEAPON_LABELS = {
  kinetic: 'Кинетика',
  plasma: 'Плазма',
  missile: 'Ракеты',
  disruptor: 'Дезинтегратор',
} as const;

const PROTECTION_LABELS = {
  'light-armor': 'Лёгкая броня',
  'heavy-armor': 'Тяжёлая броня',
  'shield-grid': 'Щитовая сеть',
  fortified: 'Укрепление',
} as const;

const SIZE_LABELS = {
  small: 'Малая',
  medium: 'Средняя',
  large: 'Крупная',
  installation: 'Установка',
} as const;

function createNavigationButton(): HTMLButtonElement {
  const existing = document.querySelector<HTMLButtonElement>('#nav-reports');
  if (existing !== null) return existing;
  const candidate = document.querySelector<HTMLButtonElement>('button[aria-label="Отчёты"]');
  if (candidate !== null) {
    candidate.id = 'nav-reports';
    candidate.disabled = false;
    return candidate;
  }
  const button = document.createElement('button');
  button.id = 'nav-reports';
  button.className = 'rail-button';
  button.type = 'button';
  button.setAttribute('aria-label', 'Отчёты');
  button.innerHTML = '<span>▤</span><small>Отчёты</small>';
  document.querySelector<HTMLElement>('.side-rail')?.append(button);
  return button;
}

function createDialog(): HTMLDialogElement {
  const existing = document.querySelector<HTMLDialogElement>('#mission-reports-dialog');
  if (existing !== null) return existing;
  const dialog = document.createElement('dialog');
  dialog.id = 'mission-reports-dialog';
  dialog.className = 'mission-reports-dialog';
  dialog.innerHTML = `
    <header class="mission-reports-header">
      <div><p class="panel-label">Operations Intelligence</p><h2>Единые отчёты</h2></div>
      <button type="button" class="dialog-close" aria-label="Закрыть">×</button>
    </header>
    <section class="mission-reports-summary"></section>
    <section class="mission-reports-controls"></section>
    <section class="mission-reports-comparison"></section>
    <section class="mission-reports-list"></section>
  `;
  dialog.querySelector<HTMLButtonElement>('.dialog-close')?.addEventListener('click', () => {
    dialog.close();
  });
  document.body.append(dialog);
  return dialog;
}

function createSelect(
  labelText: string,
  options: readonly { readonly value: string; readonly label: string }[],
): { readonly label: HTMLLabelElement; readonly select: HTMLSelectElement } {
  const label = document.createElement('label');
  label.className = 'mission-report-control';
  const caption = document.createElement('span');
  caption.textContent = labelText;
  const select = document.createElement('select');
  for (const option of options) {
    const element = document.createElement('option');
    element.value = option.value;
    element.textContent = option.label;
    select.append(element);
  }
  label.append(caption, select);
  return { label, select };
}

function rewardText(reward: MissionReportReward): string {
  return `M ${reward.metal} · C ${reward.crystal} · G ${reward.gas} · X ${reward.exoticMatter}`;
}

function lossesText(losses: Readonly<Record<string, number>>): string {
  const entries = Object.entries(losses);
  return entries.length === 0
    ? 'нет'
    : entries.map(([unitId, count]) => `${unitId} × ${count}`).join(' · ');
}

function weaponText(line: MissionCombatBreakdownLine): string {
  return line.weaponContributions
    .filter((entry) => entry.baseDamage > 0)
    .map(
      (entry) =>
        `${WEAPON_LABELS[entry.weaponType]} ${entry.baseDamage} × ${(entry.modifierPermille / 10).toFixed(0)}%`,
    )
    .join(' + ');
}

function createCombatDetails(
  breakdown: readonly MissionCombatBreakdownLine[],
): HTMLDetailsElement {
  const details = document.createElement('details');
  details.className = 'mission-combat-details';
  const summary = document.createElement('summary');
  summary.textContent = `Расчёт combat v2 · строк ${breakdown.length}`;
  const scroller = document.createElement('div');
  scroller.className = 'mission-combat-table-wrap';
  const table = document.createElement('table');
  table.innerHTML = `
    <thead><tr>
      <th>Раунд</th><th>Сторона</th><th>Цель</th><th>Профиль</th><th>Оружие</th>
      <th>База</th><th>Контр</th><th>Разброс</th><th>Урон</th><th>Накоплено</th><th>Прочность</th><th>Потери</th>
    </tr></thead>
  `;
  const body = document.createElement('tbody');
  for (const line of breakdown) {
    const row = document.createElement('tr');
    const values = [
      String(line.round),
      line.side === 'attacker' ? 'Атака' : 'Защита',
      `${line.targetUnitId} × ${line.targetCount}`,
      `${PROTECTION_LABELS[line.protectionType]} · ${SIZE_LABELS[line.targetSize]}`,
      weaponText(line),
      String(line.allocatedBaseDamage),
      `${(line.weightedModifierPermille / 10).toFixed(0)}%`,
      `${(line.variancePermille / 10).toFixed(0)}%`,
      String(line.effectiveDamage),
      String(line.carriedDamage),
      String(line.durability),
      String(line.losses),
    ];
    for (const value of values) {
      const cell = document.createElement('td');
      cell.textContent = value;
      row.append(cell);
    }
    body.append(row);
  }
  table.append(body);
  scroller.append(table);
  const note = document.createElement('small');
  note.textContent = 'Формула: базовый урон × контр-модификатор × детерминированный разброс. Неполный урон переносится в следующий раунд.';
  details.append(summary, scroller, note);
  return details;
}

function createReportCard(report: UnifiedMissionReport): HTMLElement {
  const card = document.createElement('article');
  card.className = `mission-report-card is-${report.kind} is-${report.mode}`;
  const header = document.createElement('header');
  const title = document.createElement('strong');
  title.textContent = report.title;
  const badges = document.createElement('div');
  const kind = document.createElement('span');
  kind.textContent = KIND_LABELS[report.kind];
  const mode = document.createElement('span');
  mode.textContent = MODE_LABELS[report.mode];
  const outcome = document.createElement('span');
  outcome.textContent = report.outcome;
  badges.append(kind, mode, outcome);
  header.append(title, badges);

  const summary = document.createElement('p');
  summary.textContent = report.summary;
  const target = document.createElement('p');
  target.textContent = `Цель: ${report.targetId} · участники ${report.primaryEmpireId ?? '—'} / ${report.secondaryEmpireId ?? '—'}`;
  const rewards = document.createElement('p');
  rewards.textContent = `Награда ${rewardText(report.reward)}`;
  const losses = document.createElement('p');
  losses.textContent = `Потери: свои ${lossesText(report.primaryLosses)} · противник ${lossesText(report.secondaryLosses)}`;
  const balance = document.createElement('small');
  balance.textContent = `Время ${formatGameDuration(report.resolvedAt)} · угроза ${report.threatMultiplierPermille / 10}% · награда ${report.rewardMultiplierPermille / 10}%`;
  card.append(header, summary, target, rewards, losses, balance);
  if ((report.combatBreakdown?.length ?? 0) > 0) {
    card.append(createCombatDetails(report.combatBreakdown ?? []));
  }
  return card;
}

export function mountMissionReportsPanel(options: MissionReportsPanelOptions): void {
  const button = createNavigationButton();
  const dialog = createDialog();
  const summary = dialog.querySelector<HTMLElement>('.mission-reports-summary');
  const controls = dialog.querySelector<HTMLElement>('.mission-reports-controls');
  const comparison = dialog.querySelector<HTMLElement>('.mission-reports-comparison');
  const list = dialog.querySelector<HTMLElement>('.mission-reports-list');
  if (summary === null || controls === null || comparison === null || list === null) {
    throw new Error('Mission report panel containers are missing.');
  }

  const searchLabel = document.createElement('label');
  searchLabel.className = 'mission-report-control mission-report-search';
  const searchCaption = document.createElement('span');
  searchCaption.textContent = 'Поиск';
  const search = document.createElement('input');
  search.type = 'search';
  search.placeholder = 'Цель, империя или результат';
  searchLabel.append(searchCaption, search);

  const kind = createSelect('Тип', [
    { value: 'all', label: 'Все' },
    ...Object.entries(KIND_LABELS).map(([value, label]) => ({ value, label })),
  ]);
  const mode = createSelect('Режим', [
    { value: 'all', label: 'Все' },
    ...Object.entries(MODE_LABELS).map(([value, label]) => ({ value, label })),
  ]);
  const empire = createSelect('Империя', [{ value: 'all', label: 'Все' }]);
  controls.append(searchLabel, kind.label, mode.label, empire.label);

  const render = (): void => {
    const state = options.getState();
    const currentEmpireValue = empire.select.value;
    empire.select.replaceChildren();
    for (const option of [
      { value: 'all', label: 'Все' },
      ...state.empires.map((empireId) => ({ value: empireId, label: empireId })),
      { value: 'pirate-neutral', label: 'Пираты' },
    ]) {
      const element = document.createElement('option');
      element.value = option.value;
      element.textContent = option.label;
      empire.select.append(element);
    }
    empire.select.value = [...empire.select.options].some((option) => option.value === currentEmpireValue)
      ? currentEmpireValue
      : 'all';

    const all = createUnifiedMissionReports(state);
    const filtered = filterMissionReports(all, {
      search: search.value,
      kind: kind.select.value as MissionReportKind | 'all',
      mode: mode.select.value as MissionReportMode | 'all',
      empireId: empire.select.value,
    });
    const totals = summarizeMissionReports(filtered);
    summary.replaceChildren();
    const summaryTitle = document.createElement('h3');
    summaryTitle.textContent = 'Сводка';
    const summaryText = document.createElement('p');
    summaryText.textContent = `Отчётов ${totals.total} · PvE ${totals.pve} · PvP ${totals.pvp} · системных ${totals.system} · успехов ${totals.successes} · потерь ${totals.losses}`;
    const summaryReward = document.createElement('strong');
    summaryReward.textContent = `Получено ${rewardText(totals.rewards)}`;
    summary.append(summaryTitle, summaryText, summaryReward);

    comparison.replaceChildren();
    const comparisonTitle = document.createElement('h3');
    comparisonTitle.textContent = 'Сравнение империй';
    comparison.append(comparisonTitle);
    for (const row of compareEmpirePvePvp(state)) {
      const article = document.createElement('article');
      const title = document.createElement('strong');
      title.textContent = row.empireId;
      const activity = document.createElement('p');
      activity.textContent = `PvE ${row.pveSuccesses}/${row.pveOperations} · PvP побед ${row.pvpWins}/${row.pvpBattles} · потери ${row.losses}`;
      const rewards = document.createElement('small');
      rewards.textContent = rewardText(row.reward);
      article.append(title, activity, rewards);
      comparison.append(article);
    }

    list.replaceChildren(...filtered.map(createReportCard));
    if (filtered.length === 0) list.textContent = 'По заданным фильтрам отчётов нет.';
  };

  for (const input of [search, kind.select, mode.select, empire.select]) {
    input.addEventListener('input', render);
    input.addEventListener('change', render);
  }
  button.addEventListener('click', () => {
    render();
    dialog.showModal();
  });
}
----- FILE: tests/simulation/botFleetMissionPlanner.test.ts (307 lines) -----
import { describe, expect, it } from 'vitest';
import {
  planAllBotFleetMissions,
  planBotFleetMission,
} from '../../src/simulation/bots/fleetMissionPlanner';
import { createInitialGameState } from '../../src/simulation/createInitialGameState';
import { executeCommand } from '../../src/simulation/reducer';
import type { GameState } from '../../src/simulation/types';

const ZERO_CARGO = { metal: 0, crystal: 0, gas: 0 } as const;

function fillGas(state: GameState, empireId: string): GameState {
  return {
    ...state,
    planets: state.planets.map((planet) =>
      planet.ownerEmpireId === empireId
        ? {
            ...planet,
            economy: {
              ...planet.economy,
              resources: {
                ...planet.economy.resources,
                gas: {
                  ...planet.economy.resources.gas,
                  amount: planet.economy.resources.gas.capacity,
                },
              },
            },
          }
        : planet,
    ),
  };
}

describe('bot fleet and mission planner', () => {
  it('forms a valid scout fleet from owned inventory', () => {
    let state = createInitialGameState('bot-fleet-create');
    const empireId = 'synod-bot';
    const planet = state.planets.find((candidate) => candidate.ownerEmpireId === empireId)!;
    state = {
      ...state,
      planets: state.planets.map((candidate) =>
        candidate.id === planet.id
          ? {
              ...candidate,
              inventory: {
                ...candidate.inventory,
                ships: { 'ship.synod.whisper': 1 },
              },
            }
          : candidate,
      ),
    };

    const plan = planBotFleetMission(state, empireId);
    expect(plan).toMatchObject({
      reasonCode: 'fleet-created',
      command: {
        type: 'CREATE_FLEET',
        empireId,
        planetId: planet.id,
        ships: { 'ship.synod.whisper': 1 },
      },
    });
    if (plan.command !== null) {
      expect(executeCommand(state, plan.command).ok).toBe(true);
    }
  });

  it('refreshes observed foreign intelligence without reading hidden resources', () => {
    let state = fillGas(createInitialGameState('bot-fleet-scout'), 'synod-bot');
    const empireId = 'synod-bot';
    const origin = state.planets.find((planet) => planet.ownerEmpireId === empireId)!;
    const target = state.planets.find((planet) => planet.ownerEmpireId === 'player')!;
    state = {
      ...state,
      fleets: [
        {
          id: 'synod-scout',
          empireId,
          originPlanetId: origin.id,
          location: { type: 'planet' as const, planetId: origin.id },
          status: 'stationed' as const,
          ships: { 'ship.synod.whisper': 1 },
          cargo: ZERO_CARGO,
          speed: 16,
          cargoCapacity: 18,
          mission: null,
        },
      ],
      intelligence: state.intelligence.map((entry) =>
        entry.empireId === empireId
          ? {
              ...entry,
              observations: [
                {
                  id: 'synod-old-player-intel',
                  observerEmpireId: empireId,
                  targetPlanetId: target.id,
                  observedAt: 0,
                  expiresAt: 1,
                  detected: false,
                  snapshot: {
                    planetId: target.id,
                    name: target.name,
                    ownerEmpireId: 'player',
                    factionId: target.factionId,
                    level: 1 as const,
                  },
                },
              ],
            }
          : entry,
      ),
      clock: { ...state.clock, elapsedSeconds: 100 },
    };

    const before = planBotFleetMission(state, empireId);
    const hiddenChanged = {
      ...state,
      planets: state.planets.map((planet) =>
        planet.id === target.id
          ? {
              ...planet,
              economy: {
                ...planet.economy,
                resources: {
                  ...planet.economy.resources,
                  metal: { ...planet.economy.resources.metal, amount: 9_999 },
                },
              },
            }
          : planet,
      ),
    };
    expect(planBotFleetMission(hiddenChanged, empireId)).toEqual(before);
    expect(before.command).toMatchObject({
      type: 'SEND_FLEET',
      fleetId: 'synod-scout',
      targetPlanetId: target.id,
      mission: 'scout',
    });
    if (before.command !== null) {
      expect(executeCommand(state, before.command).ok).toBe(true);
    }
  });

  it('sends loaded cargo to the weakest owned colony', () => {
    let state = fillGas(createInitialGameState('bot-fleet-transport'), 'aegis-bot');
    const empireId = 'aegis-bot';
    const origin = state.planets.find((planet) => planet.ownerEmpireId === empireId)!;
    const second = state.planets.find((planet) => planet.ownerEmpireId === 'player')!;
    state = {
      ...state,
      planets: state.planets.map((planet) => {
        if (planet.id === second.id) {
          return {
            ...planet,
            ownerEmpireId: empireId,
            factionId: origin.factionId,
            economy: {
              ...planet.economy,
              resources: {
                ...planet.economy.resources,
                metal: { ...planet.economy.resources.metal, amount: 0 },
              },
            },
          };
        }
        return planet;
      }),
      fleets: [
        {
          id: 'aegis-transport',
          empireId,
          originPlanetId: origin.id,
          location: { type: 'planet' as const, planetId: origin.id },
          status: 'stationed' as const,
          ships: { 'ship.aegis.cargo': 1 },
          cargo: { metal: 300, crystal: 0, gas: 0 },
          speed: 9,
          cargoCapacity: 1_200,
          mission: null,
        },
      ],
    };

    const plan = planBotFleetMission(state, empireId);
    expect(plan.command).toMatchObject({
      type: 'SEND_FLEET',
      fleetId: 'aegis-transport',
      targetPlanetId: second.id,
      mission: 'transport',
    });
    if (plan.command !== null) {
      expect(executeCommand(state, plan.command).ok).toBe(true);
    }
  });

  it('selects a legal colonization target through normal mission validation', () => {
    let state = fillGas(createInitialGameState('bot-fleet-colonize'), 'veyra-bot');
    const empireId = 'veyra-bot';
    const origin = state.planets.find((planet) => planet.ownerEmpireId === empireId)!;
    state = {
      ...state,
      research: state.research.map((research) =>
        research.empireId === empireId
          ? {
              ...research,
              levels: { ...research.levels, 'technology.veyra.brood-seeding': 1 },
            }
          : research,
      ),
      fleets: [
        {
          id: 'veyra-colonizer',
          empireId,
          originPlanetId: origin.id,
          location: { type: 'planet' as const, planetId: origin.id },
          status: 'stationed' as const,
          ships: { 'ship.veyra.brood-ark': 1 },
          cargo: ZERO_CARGO,
          speed: 6,
          cargoCapacity: 500,
          mission: null,
        },
      ],
    };

    const plan = planBotFleetMission(state, empireId);
    expect(plan.reasonCode).toBe('mission-colonize-selected');
    expect(plan.command).toMatchObject({
      type: 'SEND_FLEET',
      fleetId: 'veyra-colonizer',
      mission: 'colonize',
    });
    if (plan.command !== null) {
      expect(executeCommand(state, plan.command).ok).toBe(true);
    }
  });

  it('attacks only a current level-three target with favorable perceived power', () => {
    let state = fillGas(createInitialGameState('bot-fleet-attack'), 'aegis-bot');
    const empireId = 'aegis-bot';
    const origin = state.planets.find((planet) => planet.ownerEmpireId === empireId)!;
    const target = state.planets.find((planet) => planet.ownerEmpireId === 'player')!;
    state = {
      ...state,
      fleets: [
        {
          id: 'aegis-strike',
          empireId,
          originPlanetId: origin.id,
          location: { type: 'planet' as const, planetId: origin.id },
          status: 'stationed' as const,
          ships: { 'ship.aegis.fighter': 3 },
          cargo: ZERO_CARGO,
          speed: 13,
          cargoCapacity: 90,
          mission: null,
        },
      ],
      intelligence: state.intelligence.map((entry) =>
        entry.empireId === empireId
          ? {
              ...entry,
              observations: [
                {
                  id: 'aegis-target-intel',
                  observerEmpireId: empireId,
                  targetPlanetId: target.id,
                  observedAt: 0,
                  expiresAt: 10_000,
                  detected: false,
                  snapshot: {
                    planetId: target.id,
                    name: target.name,
                    ownerEmpireId: 'player',
                    factionId: target.factionId,
                    level: 3 as const,
                    defenses: {},
                    stationedFleets: [],
                  },
                },
              ],
            }
          : entry,
      ),
    };

    const plan = planBotFleetMission(state, empireId);
    expect(plan).toMatchObject({
      reasonCode: 'mission-attack-selected',
      command: {
        type: 'SEND_FLEET',
        fleetId: 'aegis-strike',
        targetPlanetId: target.id,
        mission: 'attack',
      },
    });
  });

  it('is deterministic for all bot empires', () => {
    const state = createInitialGameState('bot-fleet-determinism');
    expect(planAllBotFleetMissions(state)).toEqual(planAllBotFleetMissions(state));
  });
});
----- FILE: tests/simulation/commandDoctrine.test.ts (84 lines) -----
import { describe, expect, it } from 'vitest';
import {
  addCommandExperience,
  calculateCommandLevel,
  getCommandCombatEffects,
} from '../../src/simulation/command/commandDoctrine';
import { createInitialGameState } from '../../src/simulation/createInitialGameState';
import { executeCommand } from '../../src/simulation/reducer';

describe('command doctrine and flagships', () => {
  it('initializes every empire and calculates deterministic levels', () => {
    const state = createInitialGameState('command-initial');
    expect(state.commanders).toHaveLength(state.empires.length);
    expect(state.commanders.every((entry) => entry.level === 1 && entry.experience === 0)).toBe(true);
    expect(calculateCommandLevel(0)).toBe(1);
    expect(calculateCommandLevel(100)).toBe(2);
    expect(calculateCommandLevel(1_500)).toBe(5);
  });

  it('changes doctrine through the shared reducer and applies its effects', () => {
    const state = createInitialGameState('command-doctrine');
    const result = executeCommand(state, {
      type: 'SET_COMMAND_DOCTRINE',
      empireId: 'player',
      doctrineId: 'vanguard',
    });
    expect(result.ok).toBe(true);
    if (!result.ok) return;
    expect(result.value.commanders.find((entry) => entry.empireId === 'player')?.doctrineId).toBe('vanguard');
    expect(getCommandCombatEffects(result.value.commanders, 'player')).toMatchObject({
      weaponBonusPercent: 8,
      armorBonusPercent: -2,
      isFlagship: false,
    });
  });

  it('requires level two and an armed stationed fleet for a flagship', () => {
    const initial = createInitialGameState('command-flagship');
    const planet = initial.planets.find((candidate) => candidate.ownerEmpireId === 'player');
    if (planet === undefined) throw new Error('Player planet is missing.');
    const fleet = {
      id: 'player-flagship',
      empireId: 'player',
      originPlanetId: planet.id,
      location: { type: 'planet' as const, planetId: planet.id },
      status: 'stationed' as const,
      ships: { 'ship.aegis.fighter': 4 },
      cargo: { metal: 0, crystal: 0, gas: 0 },
      speed: 13,
      cargoCapacity: 120,
      formation: 'wedge' as const,
      targetPriority: 'balanced' as const,
      mission: null,
    };
    const levelOne = { ...initial, fleets: [...initial.fleets, fleet] };
    expect(executeCommand(levelOne, { type: 'ASSIGN_FLAGSHIP', empireId: 'player', fleetId: fleet.id })).toMatchObject({
      ok: false,
      code: 'FLAGSHIP_LEVEL_REQUIRED',
    });
    const levelTwo = {
      ...levelOne,
      commanders: addCommandExperience(levelOne.commanders, 'player', 100),
    };
    const assigned = executeCommand(levelTwo, {
      type: 'ASSIGN_FLAGSHIP',
      empireId: 'player',
      fleetId: fleet.id,
    });
    expect(assigned.ok).toBe(true);
    if (!assigned.ok) return;
    expect(getCommandCombatEffects(assigned.value.commanders, 'player', fleet.id)).toMatchObject({
      isFlagship: true,
      weaponBonusPercent: 11,
      armorBonusPercent: 11,
    });
  });

  it('awards adaptive doctrine experience deterministically', () => {
    const state = createInitialGameState('command-xp');
    const after = addCommandExperience(state.commanders, 'player', 100);
    const player = after.find((entry) => entry.empireId === 'player');
    expect(player).toMatchObject({ experience: 110, level: 2 });
  });
});
----- FILE: tests/simulation/fleetDoctrine.test.ts (109 lines) -----
import { describe, expect, it } from 'vitest';
import {
  getClassSkillBonusMaps,
  getTargetPriorityWeightPermille,
} from '../../src/simulation/combat/fleetDoctrine';
import { resolveBattle } from '../../src/simulation/combat/resolveBattle';
import { createInitialGameState } from '../../src/simulation/createInitialGameState';
import { executeCommand } from '../../src/simulation/reducer';

describe('fleet doctrine', () => {
  it('provides original class skills and target-priority weights', () => {
    expect(
      getClassSkillBonusMaps({ 'ship.aegis.spy-probe': 4 }, 'screen').armor,
    ).toEqual({ 'ship.aegis.spy-probe': 18 });
    expect(
      getClassSkillBonusMaps({ 'ship.aegis.fighter': 4 }, 'wedge').weapon,
    ).toEqual({ 'ship.aegis.fighter': 15 });
    expect(
      getTargetPriorityWeightPermille('interceptors', 'ship.aegis.scout'),
    ).toBeGreaterThan(
      getTargetPriorityWeightPermille('interceptors', 'ship.aegis.frigate'),
    );
  });

  it('updates doctrine only for a stationed owned fleet', () => {
    const initial = createInitialGameState('fleet-doctrine-command');
    const planet = initial.planets.find((candidate) => candidate.ownerEmpireId === 'player');
    if (planet === undefined) throw new Error('Player planet is missing.');
    const state = {
      ...initial,
      fleets: [
        ...initial.fleets,
        {
          id: 'player-doctrine-fleet',
          empireId: 'player',
          originPlanetId: planet.id,
          location: { type: 'planet' as const, planetId: planet.id },
          status: 'stationed' as const,
          ships: { 'ship.aegis.fighter': 5 },
          cargo: { metal: 0, crystal: 0, gas: 0 },
          speed: 12,
          cargoCapacity: 0,
          formation: 'line' as const,
          targetPriority: 'balanced' as const,
          mission: null,
        },
      ],
    };
    const result = executeCommand(state, {
      type: 'SET_FLEET_COMBAT_DOCTRINE',
      empireId: 'player',
      fleetId: 'player-doctrine-fleet',
      formation: 'wedge',
      targetPriority: 'capitals',
    });
    expect(result.ok).toBe(true);
    if (!result.ok) return;
    expect(
      result.value.fleets.find((fleet) => fleet.id === 'player-doctrine-fleet'),
    ).toMatchObject({ formation: 'wedge', targetPriority: 'capitals' });
  });

  it('changes damage and target allocation deterministically', () => {
    const line = resolveBattle(
      73,
      {
        empireId: 'player',
        units: { 'ship.aegis.fighter': 12 },
        weaponBonusPercent: 0,
        armorBonusPercent: 0,
        formation: 'line',
        targetPriority: 'balanced',
      },
      {
        empireId: 'enemy',
        units: { 'ship.aegis.scout': 8, 'ship.aegis.frigate': 4 },
        weaponBonusPercent: 0,
        armorBonusPercent: 0,
      },
    );
    const wedge = resolveBattle(
      73,
      {
        empireId: 'player',
        units: { 'ship.aegis.fighter': 12 },
        weaponBonusPercent: 0,
        armorBonusPercent: 0,
        formation: 'wedge',
        targetPriority: 'interceptors',
      },
      {
        empireId: 'enemy',
        units: { 'ship.aegis.scout': 8, 'ship.aegis.frigate': 4 },
        weaponBonusPercent: 0,
        armorBonusPercent: 0,
      },
    );
    expect(wedge.rounds[0]?.attackerDamage ?? 0).toBeGreaterThan(
      line.rounds[0]?.attackerDamage ?? 0,
    );
    const scoutAllocation = wedge.rounds[0]?.attackerTargetBreakdown.find(
      (target) => target.targetUnitId === 'ship.aegis.scout',
    )?.allocatedBaseDamage ?? 0;
    const frigateAllocation = wedge.rounds[0]?.attackerTargetBreakdown.find(
      (target) => target.targetUnitId === 'ship.aegis.frigate',
    )?.allocatedBaseDamage ?? 0;
    expect(scoutAllocation).toBeGreaterThan(frigateAllocation);
  });
});
----- FILE: tests/simulation/intelligence.test.ts (166 lines) -----
import { describe, expect, it } from 'vitest';
import { createInitialGameState } from '../../src/simulation/createInitialGameState';
import { executeGameCommand } from '../../src/simulation/executeGameCommand';
import { estimateFlight } from '../../src/simulation/fleets/flightCalculations';
import { getCurrentObservations } from '../../src/simulation/intelligence/intelligenceState';
import { resolveScoutArrival } from '../../src/simulation/intelligence/resolveScout';
import type { GameState } from '../../src/simulation/types';

function prepareScoutState(seed: string): GameState {
  const state = createInitialGameState(seed);
  const origin = state.planets.find((planet) => planet.ownerEmpireId === 'player');
  if (origin === undefined) throw new Error('Player planet missing.');
  return {
    ...state,
    planets: state.planets.map((planet) =>
      planet.id === origin.id
        ? {
            ...planet,
            inventory: {
              ...planet.inventory,
              ships: { 'ship.aegis.scout': 2, 'ship.aegis.cargo': 1 },
            },
            economy: {
              ...planet.economy,
              resources: {
                ...planet.economy.resources,
                gas: {
                  ...planet.economy.resources.gas,
                  amount: 100_000,
                  capacity: 100_000,
                },
              },
            },
          }
        : planet,
    ),
    research: state.research.map((research) =>
      research.empireId === 'player'
        ? {
            ...research,
            levels: {
              ...research.levels,
              'technology.aegis.sensors': 4,
            },
          }
        : research,
    ),
  };
}

function createScoutFleet(state: GameState): GameState {
  const origin = state.planets.find((planet) => planet.ownerEmpireId === 'player')!;
  const created = executeGameCommand(state, {
    type: 'CREATE_FLEET',
    empireId: 'player',
    planetId: origin.id,
    ships: { 'ship.aegis.scout': 1 },
    cargo: { metal: 0, crystal: 0, gas: 0 },
  });
  if (!created.ok) throw new Error(created.code);
  return created.value;
}

describe('intelligence and counter-intelligence', () => {
  it('scouts an enemy planet, stores a level-three snapshot and returns', () => {
    const initial = createScoutFleet(prepareScoutState('scout-mission'));
    const fleet = initial.fleets[0]!;
    const target = initial.planets.find(
      (planet) => planet.ownerEmpireId !== 'player',
    )!;
    const estimate = estimateFlight(initial.galaxy, initial.planets, fleet, target.id);
    const sent = executeGameCommand(initial, {
      type: 'SEND_FLEET',
      empireId: 'player',
      fleetId: fleet.id,
      targetPlanetId: target.id,
      mission: 'scout',
    });
    expect(sent.ok).toBe(true);
    if (!sent.ok) return;

    const arrived = executeGameCommand(sent.value, {
      type: 'ADVANCE_TIME',
      seconds: estimate.durationSeconds,
    });
    expect(arrived.ok).toBe(true);
    if (!arrived.ok) return;

    const playerIntel = arrived.value.intelligence.find(
      (entry) => entry.empireId === 'player',
    )!;
    const observation = playerIntel.observations[0];
    expect(observation).toMatchObject({
      targetPlanetId: target.id,
      snapshot: { level: 3, ownerEmpireId: target.ownerEmpireId },
    });
    expect(observation?.snapshot.resources).toBeDefined();
    expect(observation?.snapshot.defenses).toBeDefined();
    expect(arrived.value.fleets[0]?.status).toBe('returning');
  });

  it('rejects scouting without a scout ship', () => {
    const state = prepareScoutState('missing-scout');
    const origin = state.planets.find((planet) => planet.ownerEmpireId === 'player')!;
    const created = executeGameCommand(state, {
      type: 'CREATE_FLEET',
      empireId: 'player',
      planetId: origin.id,
      ships: { 'ship.aegis.cargo': 1 },
      cargo: { metal: 0, crystal: 0, gas: 0 },
    });
    expect(created.ok).toBe(true);
    if (!created.ok) return;
    const target = state.planets.find((planet) => planet.ownerEmpireId !== 'player')!;
    expect(
      executeGameCommand(created.value, {
        type: 'SEND_FLEET',
        empireId: 'player',
        fleetId: created.value.fleets[0]!.id,
        targetPlanetId: target.id,
        mission: 'scout',
      }),
    ).toMatchObject({ ok: false, code: 'SCOUT_SHIP_REQUIRED' });
  });

  it('produces deterministic detection and defender alerts', () => {
    const state = createScoutFleet(prepareScoutState('counter-intel'));
    const fleet = state.fleets[0]!;
    const target = state.planets.find((planet) => planet.ownerEmpireId !== 'player')!;
    let detected: GameState | undefined;

    for (let sequence = 0; sequence < 50; sequence += 1) {
      const result = resolveScoutArrival(state, fleet, target, sequence);
      const observation = result.intelligence
        .find((entry) => entry.empireId === 'player')
        ?.observations[0];
      if (observation?.detected) {
        detected = result;
        break;
      }
    }

    expect(detected).toBeDefined();
    if (detected === undefined) return;
    expect(
      detected.intelligence.find(
        (entry) => entry.empireId === target.ownerEmpireId,
      )?.alerts,
    ).toHaveLength(1);
  });

  it('filters stale observations without mutating saved history', () => {
    const state = createScoutFleet(prepareScoutState('intel-staleness'));
    const fleet = state.fleets[0]!;
    const target = state.planets.find((planet) => planet.ownerEmpireId !== 'player')!;
    const observed = resolveScoutArrival(state, fleet, target, 7);
    const intelligence = observed.intelligence.find(
      (entry) => entry.empireId === 'player',
    )!;
    const expiresAt = intelligence.observations[0]!.expiresAt;

    expect(getCurrentObservations(intelligence, expiresAt - 1)).toHaveLength(1);
    expect(getCurrentObservations(intelligence, expiresAt)).toHaveLength(0);
    expect(intelligence.observations).toHaveLength(1);
  });
});
----- FILE: tests/simulation/pveBalanceAndReports.test.ts (306 lines) -----
import { describe, expect, it } from 'vitest';
import { createInitialGameState } from '../../src/simulation/createInitialGameState';
import type { FleetState } from '../../src/simulation/fleets/types';
import type { PlanetState } from '../../src/simulation/planet/types';
import { PIRATE_EMPIRE_ID } from '../../src/simulation/pve/neutralForces';
import {
  PVE_MAX_THREAT_MULTIPLIER_PERMILLE,
  PVE_MIN_REWARD_MULTIPLIER_PERMILLE,
  applyPvePlunderMultiplier,
  calculatePirateThreatMultiplier,
  calculatePveRewardMultiplier,
  scalePveUnits,
} from '../../src/simulation/pve/pveBalance';
import {
  compareEmpirePvePvp,
  createUnifiedMissionReports,
  filterMissionReports,
  summarizeMissionReports,
} from '../../src/simulation/reports/missionReports';
import type {
  ExecutedGameEvent,
  GameState,
  ScheduledGameEvent,
} from '../../src/simulation/types';

function executedEvent(
  sequence: number,
  executedAt: number,
  payload: ScheduledGameEvent['payload'],
): ExecutedGameEvent {
  return {
    event: {
      id: `event-${sequence}`,
      executeAt: executedAt,
      sequence,
      payload,
    },
    executedAt,
  };
}

function battleEvent(
  sequence: number,
  executedAt: number,
  options: {
    readonly targetId?: string;
    readonly attacker?: string;
    readonly defender?: string;
    readonly winner?: 'attacker' | 'defender' | 'draw';
    readonly mode?: 'pve' | 'pvp';
    readonly rewardMultiplierPermille?: number;
    readonly threatMultiplierPermille?: number;
  } = {},
): ExecutedGameEvent {
  const attacker = options.attacker ?? 'player';
  const defender = options.defender ?? PIRATE_EMPIRE_ID;
  const winner = options.winner ?? 'attacker';
  return executedEvent(sequence, executedAt, {
    type: 'BATTLE_REPORT',
    report: {
      id: `battle-${sequence}`,
      seed: sequence,
      resolvedAt: executedAt,
      targetPlanetId: options.targetId ?? 'pirate-base-test',
      attackerEmpireId: attacker,
      defenderEmpireId: defender,
      winner,
      rounds: [],
      attackerInitial: { 'ship.aegis.fighter': 5 },
      defenderInitial: { 'defense.aegis.gun-battery': 4 },
      attackerRemaining: winner === 'defender' ? {} : { 'ship.aegis.fighter': 3 },
      defenderRemaining: winner === 'attacker' ? {} : { 'defense.aegis.gun-battery': 2 },
      debrisCreated: { metal: 100, crystal: 50 },
      plunderedCargo: { metal: 300, crystal: 200, gas: 100 },
      mode: options.mode ?? (defender === PIRATE_EMPIRE_ID ? 'pve' : 'pvp'),
      rewardMultiplierPermille: options.rewardMultiplierPermille ?? 1_000,
      threatMultiplierPermille: options.threatMultiplierPermille ?? 1_000,
    },
  });
}

function expeditionEvent(
  sequence: number,
  executedAt: number,
  targetId = 'galaxy-target',
): ExecutedGameEvent {
  return executedEvent(sequence, executedAt, {
    type: 'EXPEDITION_RESOLVE',
    report: {
      id: `expedition-${sequence}`,
      empireId: 'player',
      fleetId: 'fleet-expedition',
      originPlanetId: 'player-home',
      targetGalaxyPlanetId: targetId,
      startedAt: executedAt - 600,
      resolvesAt: executedAt,
      outcome: 'salvage',
      reward: { metal: 200, crystal: 100, gas: 40 },
      losses: { 'ship.aegis.scout': 1 },
      narrative: 'Тестовая экспедиция.',
      rewardMultiplierPermille: 750,
    },
  });
}

function spaceObjectEvent(
  sequence: number,
  executedAt: number,
  objectId = 'space-object-test',
): ExecutedGameEvent {
  return executedEvent(sequence, executedAt, {
    type: 'SPACE_OBJECT_MISSION_RESOLVE',
    report: {
      id: `space-object-report-${sequence}`,
      empireId: 'player',
      fleetId: 'fleet-mining',
      originPlanetId: 'player-home',
      objectId,
      startedAt: executedAt - 900,
      resolvesAt: executedAt,
      reward: { metal: 0, crystal: 0, gas: 0, exoticMatter: 2 },
      depletion: 2,
      losses: {},
      controllerUntil: executedAt + 3_600,
      narrative: 'Тестовая аномалия.',
      rewardMultiplierPermille: 500,
    },
  });
}

function withEventLog(
  state: GameState,
  eventLog: readonly ExecutedGameEvent[],
): GameState {
  return {
    ...state,
    clock: { ...state.clock, elapsedSeconds: 10_000 },
    eventLog,
  };
}

function totalPlanetAndFleetResource(
  planet: PlanetState,
  fleet: FleetState,
  resourceId: 'metal' | 'crystal' | 'gas',
): number {
  return planet.economy.resources[resourceId].amount + fleet.cargo[resourceId];
}

describe('PvE balance policy', () => {
  it('applies diminishing returns for repeated target farming and respects the floor', () => {
    const base = createInitialGameState('pve-repeat-policy');
    const targetId = 'repeated-pirate-base';
    const state = withEventLog(base, [
      battleEvent(1, 4_000, { targetId }),
      battleEvent(2, 5_000, { targetId }),
      battleEvent(3, 6_000, { targetId }),
      battleEvent(4, 7_000, { targetId }),
      battleEvent(5, 8_000, { targetId }),
    ]);

    expect(calculatePveRewardMultiplier(base, 'player', 'pirate-raid', targetId, 10_000)).toBe(1_000);
    expect(
      calculatePveRewardMultiplier(
        withEventLog(base, [battleEvent(1, 9_000, { targetId })]),
        'player',
        'pirate-raid',
        targetId,
        10_000,
      ),
    ).toBe(750);
    expect(calculatePveRewardMultiplier(state, 'player', 'pirate-raid', targetId, 10_000)).toBe(
      PVE_MIN_REWARD_MULTIPLIER_PERMILLE,
    );
    expect(calculatePveRewardMultiplier(state, 'player', 'pirate-raid', 'other-target', 10_000)).toBe(1_000);
  });

  it('increases pirate threat with victories and caps the multiplier', () => {
    const base = createInitialGameState('pve-threat-policy');
    const victories = Array.from({ length: 15 }, (_, index) =>
      battleEvent(index, index * 100, { targetId: `pirate-${index}` }),
    );
    const state = withEventLog(base, victories);
    expect(calculatePirateThreatMultiplier(base, 'player')).toBe(1_000);
    expect(calculatePirateThreatMultiplier(state, 'player')).toBe(
      PVE_MAX_THREAT_MULTIPLIER_PERMILLE,
    );
    expect(scalePveUnits({ fighter: 3, frigate: 1 }, 1_500)).toEqual({
      fighter: 5,
      frigate: 2,
    });
  });

  it('reduces plunder without destroying resources or prior cargo', () => {
    const state = createInitialGameState('pve-plunder-conservation');
    const planet = state.planets.find((candidate) => candidate.ownerEmpireId === PIRATE_EMPIRE_ID)!;
    const fleet: FleetState = {
      id: 'test-plunder-fleet',
      empireId: 'player',
      originPlanetId: state.planets.find((candidate) => candidate.ownerEmpireId === 'player')!.id,
      location: { type: 'planet', planetId: planet.id },
      status: 'stationed',
      ships: { 'ship.aegis.cargo': 1 },
      cargo: { metal: 450, crystal: 300, gas: 150 },
      speed: 10,
      cargoCapacity: 1_000,
      mission: null,
    };
    const before = {
      metal: totalPlanetAndFleetResource(planet, fleet, 'metal'),
      crystal: totalPlanetAndFleetResource(planet, fleet, 'crystal'),
      gas: totalPlanetAndFleetResource(planet, fleet, 'gas'),
    };
    const result = applyPvePlunderMultiplier(
      planet,
      fleet,
      { metal: 400, crystal: 200, gas: 100 },
      500,
    );
    expect(result.plundered).toEqual({ metal: 200, crystal: 100, gas: 50 });
    expect(totalPlanetAndFleetResource(result.planet, result.fleet, 'metal')).toBe(before.metal);
    expect(totalPlanetAndFleetResource(result.planet, result.fleet, 'crystal')).toBe(before.crystal);
    expect(totalPlanetAndFleetResource(result.planet, result.fleet, 'gas')).toBe(before.gas);
  });
});

describe('unified mission reports', () => {
  it('combines battle, expedition, space-object and world-event histories once', () => {
    const base = createInitialGameState('unified-reports');
    const state: GameState = {
      ...withEventLog(base, [
        battleEvent(1, 1_000),
        expeditionEvent(2, 2_000),
        spaceObjectEvent(3, 3_000),
      ]),
      worldEvents: {
        ...base.worldEvents,
        history: [{
          id: 'world-event-history',
          definitionId: 'solar-storm',
          targetType: 'system',
          targetId: base.galaxy.systems[0]!.id,
          startedAt: 3_100,
          endsAt: 4_000,
          chainDepth: 0,
          completedAt: 4_000,
          completion: 'completed',
        }],
      },
    };
    const reports = createUnifiedMissionReports(state);
    expect(reports).toHaveLength(4);
    expect(reports.map((report) => report.kind)).toEqual([
      'world-event',
      'space-object',
      'expedition',
      'battle',
    ]);
    expect(new Set(reports.map((report) => report.id)).size).toBe(reports.length);
    expect(filterMissionReports(reports, { mode: 'pve' })).toHaveLength(3);
    expect(filterMissionReports(reports, { search: 'аномалия' })).toHaveLength(1);
  });

  it('summarizes rewards, losses and PvE/PvP activity', () => {
    const base = createInitialGameState('unified-report-summary');
    const state = withEventLog(base, [
      battleEvent(1, 1_000),
      battleEvent(2, 2_000, {
        attacker: 'player',
        defender: 'aegis-bot',
        mode: 'pvp',
        winner: 'defender',
      }),
      expeditionEvent(3, 3_000),
      spaceObjectEvent(4, 4_000),
    ]);
    const reports = createUnifiedMissionReports(state);
    const summary = summarizeMissionReports(reports);
    expect(summary).toMatchObject({
      total: 4,
      pve: 3,
      pvp: 1,
      system: 0,
      successes: 3,
    });
    expect(summary.rewards).toEqual({
      metal: 800,
      crystal: 500,
      gas: 240,
      exoticMatter: 2,
    });
    expect(summary.losses).toBeGreaterThan(0);

    const comparison = compareEmpirePvePvp(state);
    expect(comparison.find((entry) => entry.empireId === 'player')).toMatchObject({
      pveOperations: 3,
      pveSuccesses: 3,
      pvpBattles: 1,
      pvpWins: 0,
    });
    expect(comparison.find((entry) => entry.empireId === 'aegis-bot')).toMatchObject({
      pvpBattles: 1,
      pvpWins: 1,
    });
  });
});
----- FILE: tests/ui/fleetComposerViewModel.test.ts (135 lines) -----
import { describe, expect, it } from 'vitest';
import { createInitialGameState } from '../../src/simulation/createInitialGameState';
import type { GameState } from '../../src/simulation/types';
import {
  createFleetComposerViewModel,
  createFleetRoutePreview,
} from '../../src/ui/fleetComposerViewModel';

function prepareState(): GameState {
  const state = createInitialGameState('fleet-composer');
  const home = state.planets.find(
    (planet) => planet.ownerEmpireId === 'player',
  );
  if (home === undefined) throw new Error('Player planet missing.');
  return {
    ...state,
    planets: state.planets.map((planet) =>
      planet.id === home.id
        ? {
            ...planet,
            inventory: {
              ...planet.inventory,
              ships: {
                'ship.aegis.scout': 3,
                'ship.aegis.cargo': 2,
              },
            },
            economy: {
              ...planet.economy,
              resources: {
                ...planet.economy.resources,
                metal: {
                  ...planet.economy.resources.metal,
                  amount: 2_000,
                },
                crystal: {
                  ...planet.economy.resources.crystal,
                  amount: 2_000,
                },
                gas: {
                  ...planet.economy.resources.gas,
                  amount: 10_000,
                  capacity: 10_000,
                },
              },
            },
          }
        : planet,
    ),
  };
}

describe('fleet composer view model', () => {
  it('calculates speed, cargo capacity and valid cargo from one origin', () => {
    const state = prepareState();
    const home = state.planets.find(
      (planet) => planet.ownerEmpireId === 'player',
    )!;
    const view = createFleetComposerViewModel(
      state,
      'player',
      home.id,
      {
        'ship.aegis.scout': 1,
        'ship.aegis.cargo': 2,
      },
      { metal: 1_000, crystal: 500, gas: 100 },
    );
    expect(view).toMatchObject({
      speed: 9,
      shipCount: 3,
      cargoCapacity: 2_430,
      cargoAmount: 1_600,
      errors: [],
      canCreate: true,
    });
  });

  it('blocks unavailable ships, excessive cargo and missing resources', () => {
    const state = prepareState();
    const home = state.planets.find(
      (planet) => planet.ownerEmpireId === 'player',
    )!;
    const view = createFleetComposerViewModel(
      state,
      'player',
      home.id,
      { 'ship.aegis.scout': 4 },
      { metal: 3_000, crystal: 0, gas: 0 },
    );
    expect(view?.canCreate).toBe(false);
    expect(view?.errors).toEqual(
      expect.arrayContaining([
        'INSUFFICIENT_SHIPS:ship.aegis.scout',
        'FLEET_CARGO_OVER_CAPACITY',
        'INSUFFICIENT_CARGO_RESOURCE:metal',
      ]),
    );
  });

  it('creates deterministic route previews with round-trip fuel', () => {
    const state = prepareState();
    const home = state.planets.find(
      (planet) => planet.ownerEmpireId === 'player',
    )!;
    const target = state.planets.find((planet) => planet.id !== home.id)!;
    const fleet = {
      id: 'preview-fleet',
      empireId: 'player',
      originPlanetId: home.id,
      location: { type: 'planet' as const, planetId: home.id },
      status: 'stationed' as const,
      ships: { 'ship.aegis.scout': 2 },
      cargo: { metal: 0, crystal: 0, gas: 0 },
      speed: 14,
      cargoCapacity: 40,
      mission: null,
    };
    const first = createFleetRoutePreview(
      state,
      fleet,
      'scout',
      target.id,
    );
    const second = createFleetRoutePreview(
      state,
      fleet,
      'scout',
      target.id,
    );
    expect(first).toEqual(second);
    expect(first?.reservedFuel).toBe((first?.oneWayFuel ?? 0) * 2);
    expect(first?.hasEnoughFuel).toBe(true);
  });
});
