# Deferred batch: NEMEXIA-PROTO-SIM-SCALING

The simulation-scaling audit was the active contract at `main` commit `15c846cb05d21e08415ce5f69e3134f8f8ec4b18` before the owner reprioritized the full Nemexia-style UI. Its accepted work item NEM-01 is merged in PR #194; NEM-02 remains authorized but deferred and must not be mixed into the UI batch.

The complete pre-deferral contract remains recoverable from Git history at `docs/audits/current-batch-audit.md` on commit `15c846c`. The next simulation action remains NEM-02 after the UI batch closes, unless the owner reprioritizes again.
